# Goble architecture tracker

Aggregated, ordered work across the whole tree. Each item points to the owning doc; each doc also carries the same `[ ] / [~] / [x]` markers. Reorder/prioritize freely; the point of this file is that the `.agents/` tree is a **live tracker**, not prose.

Legend: `[ ]` pending · `[~]` in progress · `[x]` done.

## 00 — Repository tooling
- [x] **Track `vendor/alacritty_terminal` in git.** It is a path dependency of `goble-terminal`; the tree is tracked (29 files), so a fresh clone builds without running `scripts/fetch-alacritty-terminal.sh --install` first. The release workflow still runs that script when the checkout lacks the tree, and the script re-checks the archive against the vendored bytes by SHA-256 (`terminal-emulator.md`)
- [ ] **Make the workspace `rustfmt`-clean, or drop the gate.** `./scripts/fmt.sh --check` reports roughly 200 hunks across 127 files on committed HEAD, including files untouched by this phase; `cargo fmt --all` is not usable here either, because `--all` also formats path dependencies and would rewrite the vendored emulator. Either run `./scripts/fmt.sh` once as a mechanical commit or remove the gate from `publish-release` (`README.md`)

## 02 — First-run & routing
- [x] Show an "empty LLM key" banner as a modal overlay instead of the canned reply — the canned reply is no longer sent on the no-key path; the banner is a centered `Dialog` overlay over the whole app (`onboarding-no-model-key.md`)
- [x] Wire the banner click to the model-provider overlay dialog (`onboarding-no-model-key.md`)
- [x] After a key is saved, close the dialog and move to the workspace-type prompt (`onboarding-no-model-key.md`)
- [x] Define the `Routing` decision type and attach it to a conversation — `WorkspaceRouting{Local,Remote}` is persisted per conversation on the `chats.workspace_routing` column and restored on load (`router-local-vs-remote.md`)
- [~] Make `local` a valid runtime target — chat turns run on the local harness for `local`/unset routing; the `resolve_worker_for_target` `bail!` for the agent/workflow deploy path is still pending a local agent runner (`router-local-vs-remote.md`)
- [x] Persist the routing choice across restarts (`router-local-vs-remote.md`)
- [ ] Define the "workspace package" to ship to a remote host (`remote-bootstrap.md`)
- [ ] Add the remote self-configuration step (read TOML → resolve providers/models) (`remote-bootstrap.md`)
- [ ] Reuse the existing pairing/SSH/helm path to bring up the remote workspace (`remote-bootstrap.md`)
- [ ] Publish a workspace endpoint the router can point at (`remote-bootstrap.md`)
- [x] **Remote bootstrap R74 — the worker install carries our own harness and nothing else:** `ProvisionConfig` still asked the host for `install_docker`, `install_hermes` and `install_crewai`, and its install script ran Docker's apt-repo dance, a **literal stub** for the Hermes runtime (`echo "Hermes runtime install stub: add actual install command here"`) and `pip install crewai`; both CLI subcommands carried the three as flags. None of it matches the tree: `goblin` already links the harness (`goble-harness-{internal,runtime,types,protocol}`, `goble-daemon`, `goble-workflow`, `goble-sandbox`, `goble-replay`) and the secrets live in the client's `config.toml`, so the host is given no third-party agent runtime, no container runtime and no interpreter — which is what [`remote-bootstrap.md`](02-first-run-and-routing/remote-bootstrap.md) already says the install is. What replaced them is the one heavy piece that *is* ours: `install_remote_desktop`, because computer use needs a desktop on the host. It writes `$INSTALL_PATH/goblin-remote-desktop.sh`, launches it with `nohup … &` **after** `systemctl restart goblin.service`, and logs to `/var/log/goblin-remote-desktop.log`; it installs `xrdp xorgxrdp xfce4 xfce4-terminal dbus-x11` and pins `/etc/xrdp/startwm.sh` to `startxfce4`. The CLI flag is `--remote-desktop` on `worker-provision` and `setup-worker` (`remote-bootstrap.md`)
- [ ] Found while doing R74, not fixed: the install script writes `$INSTALL_PATH/tls/ca-key.pem` under a comment about keeping the CA key on the VPS "for disaster recovery", but the value interpolated is `WorkerBundle.ca_cert_pem` — the CA's **public certificate**, and the bundle carries no CA private key at all. So the file is a misleadingly named copy of a public cert, and the disaster-recovery it claims does not exist (`remote-bootstrap.md`)

## 03 — Workspace model
- [ ] Introduce a `Workspace` type (id, kind, root, config) and persist it (`workspace.md`)
- [ ] Allow a workspace `kind` to be promoted local → remote while keeping the same id (`workspace.md`)
- [ ] Make the provisioned worker bundle a remote workspace record (endpoint, TLS, worker id) (`workspace.md`)
- [ ] Extend `GobleConfig` to cover plugins, workflows, personas, memory, deep-research (`shared-secrets-and-toml.md`)
- [ ] Add a config-is-agent-editable surface (agent reads/patches the TOML, never the vault) (`shared-secrets-and-toml.md`)
- [ ] Add outbound secret-scrubbing on logs/traces/events (`shared-secrets-and-toml.md`)
- [ ] Ship the TOML + encrypted vault blob together in remote bootstrap (`shared-secrets-and-toml.md`)
- [ ] Add `workspace_id`, `persona`, `cwd` + per-agent conversation key to the agent model (`multiple-agents.md`)
- [ ] Persist per-agent state so an agent survives restarts (`multiple-agents.md`)
- [ ] Let agents be created/edited from natural language and bound to workspace config (`multiple-agents.md`)
- [x] Populate a credential from the ask-user card by name and keep the value out of the transcript — card gains name + masked value; harness `credentials` tool lists names and `run_command` expands `{{credential:<name>}}` server-side, so the model never sees the key (`shared-secrets-and-toml.md`)
- [x] Give the machine a hidden home `~/.goble` mirroring `~/.grok`, split into a universal **base** (identity/auth/config/sessions/logs, for every user) and a **workspace payload** (bundled tooling, worktrees, threads, local store — only when the workspace runs locally); remote-only users keep a minimal home (`workspace.md`, `shared-secrets-and-toml.md`)
- [x] Track every principal with access + their grants — `access_grants` table + store methods; harness `principals` tool lists principals and grants; per-principal `principals/<id>/` home dir (`shared-secrets-and-toml.md`)
- [x] Seed a Goble user guide into `~/.goble/docs/user-guide` on first launch (ported from `.grok` and Goble-ified; base-only, so it exists for remote-only users too) and expose a `user_guide` harness tool (list/read topics) + a system-prompt pointer so the model answers product questions from the guide — incl. remote/self-as-worker via Tailscale and mobile access (`workspace.md`, `docs.rs`/`app_home.rs`)

## 04 — Agent runtime (harness)
- [ ] Fix the hardcoded `workspace_root = /var/goblin/workspaces` default in `goblin-worker` so harness tests/dev runs don't hit `Permission denied` on machines without that dir (currently breaks `goblin-worker` runner/scheduler unit tests and `goble-cli`'s `e2e_worker` in a stock dev environment) (`sandbox-and-cwd.md`)
- [ ] Define durable **state** vs compacting **transcript** boundaries (`agent-state-and-compaction.md`)
- [ ] Wire compaction triggers by token/size budget; persist the rolling summary (`agent-state-and-compaction.md`)
- [ ] Key state by `agent_id` and make it survive local↔remote routing (`agent-state-and-compaction.md`)
- [x] **Sub-agents S1 — the `SubAgent` model, budget and lifecycle:** a `SubAgentSpec` / `SubAgentBudget` / `SubAgentStatus` / `SubAgentRecord` in `goble-core` whose budget exhaustion is terminal by construction, with a bounded spawn depth and a per-child CWD subdir (`subagents.md`)
- [x] **Sub-agents S2/S3 — spawn a real child, foreground and background:** a `spawn_subagent` harness tool that runs a child on its own conversation (`parent_chat_id` on `chats`), a bounded loop carrying the child's own budget and tool set, a per-child record registry, and cancellation that reaches every child so a routine never blocks the parent's turn (`subagents.md`) — S2 done: the tool runs a real foreground child (own chats row + messages, budget-terminal loop, depth refusal); S3 done: every child runs on its own task and is registered in the `Harness`'s registry (list + snapshot accessors) before it starts, `run_in_background: true` returns the id with a live record, a foreground await past its budget moves the child to the background, and `Harness::cancel` or `Harness::cancel_subagent` ends a record `Cancelled` with the reason (panic → `Failed`, exhaustion → terminal)
- [x] **Sub-agents S4 — lifecycle on the wire:** `SubAgentSpawned` / `Progress` / `Finished` cross all six hops with `CommandProposed` as the template — `HarnessEvent` → `map_event` → `HarnessServerEvent` → the daemon mapper → `DaemonEvent` → `translate_daemon_event`, which emits `chat:subagent_spawned` / `chat:subagent_progress` / `chat:subagent_finished` with typed payloads beside `CommandProposedEvent` — and they are emitted by S3's registry transitions (`register` spawns, `publish` progresses/finishes, `cancel_entry` finishes), the registry pushing them to a per-turn sink the live turn's stream merges in (`with_subagent_lifecycle`), so a child on its own task reaches the wire while its turn runs; payloads are plain data mirroring the record's own fields, the protocol crates still do not depend on `goble-core` (`subagents.md`)
- [x] **Sub-agents S5 — the transcript row:** a sub-agent is a quiet ruler row in the parent's transcript, not a card — one line with a status bullet (`◐` Running/Accent, `●` Completed/Success, `◆` Failed/Error, `◇` Cancelled/Muted), the record's description and, only while running, the activity label and the elapsed time (`4.5s` / `43s` / `2m05s`, grok-build's `format_duration` shape); it folds with A4's three states (one line folded, the detail expanded), and a click on the row fires `ChatAction::OpenSubAgent(child_id)` — the view it enters is S6, which has landed: the click now shows the child's own conversation in the pane. The data is S4's live records, not the tool's arguments: `SubAgentRow` (`crates/goble-ui/src/elements/chat_content.rs`) is built by the app (`PaneRuntime::apply_subagent_{spawned,progress,finished}` keyed by `subagent_id`, re-keyed for the element by `parent_call_id` in `UiState::sub_agent_rows`) and handed to the bubble through `ChatView::with_sub_agents`. `ToolPresentation::SubAgent` stays the resolution key; the expanded body shows what the record carries and says plainly that it holds no rows of the child's own conversation (`subagents.md`)
- [x] **Sub-agents S6 — the child view:** a pane shows one child's own conversation: the row's click puts the child's card into the pane's block list and filters the pane to the child (A7's `enter_agent_view`, aimed at the child), under a framed title carrying the child's type, description, status and elapsed time — the same affordance as the row's — and no composer, because an input line there would write into a conversation the user is only reading. The body is the rows the store holds under the child's chat id, parsed through the one `MessageParseCache` → `ChatView` path, so a child's tool output is the same `terminal_block` as anywhere else. Esc returns to the conversation the row was clicked in — the shell, or the parent's own agent view when the row was clicked from inside it, which the pane remembers in `returned_to` — and both cards stay in the block list, so neither conversation is stranded; binding the pane to another conversation unmounts the child (`UiState::{open_sub_agent, close_sub_agent_view, sub_agent_views}`); nested children stay enterable — the child view hands its pane's live sub-agent rows to `ChatView::with_sub_agents` and relays the open action for that pane, so a `spawn_subagent` row inside a child view opens that child too (`subagents.md`)
- [x] **Sub-agents S7 — the work overlay — removed 2026-09-12:** the user, looking at the running app, found the panel titled `Work in flight` redundant and asked for it gone, so the overlay no longer exists. Deleted: `crates/goble-ui/src/elements/work_overlay.rs` and its module registration/exports, the `Sheet` wiring and its close route in `app/src/ui/mod.rs`, the `Cmd/Ctrl+Shift+W` binding (`app/src/root_view.rs`) and the palette command that fired the same action, `UiState::{open,close}_work_overlay`, `work_overlay_groups`, `work_overlay_scroll`, the dismissal bookkeeping + auto-open rule that watched the running count (`work_overlay_dismissed`/`work_overlay_running_children`/`note_running_sub_agents`/`running_sub_agent_count`), and `UiState::kill_sub_agent` with `UiActions::on_kill_sub_agent` — checked first: nothing else called them. Also deleted `app/src/integration_testing/work_overlay_flow.rs` and its `[[test]]` registration, and the element's six unit tests. The cues are kept because they still carry what the user needs (how much work is running): C2's footer still-running line and C3's topbar `◆ N` indicator stay drawn but are now non-interactive (`TurnStatusFooter` and `build_live_indicator` take no handler). The two `root_view` tests that had worked around the auto-opened overlay are back to their plain pane-Esc premise (`s.work_overlay_open = false` removed), and the topbar/footer/chat-view/menu tests were rewritten to assert the indicators draw without taking a pointer event. Verified with `cargo test -p goble-ui -p goble-app` (`subagents.md`).
- [ ] Build the tool-registry abstraction with a uniform schema (`tools.md`)
- [ ] Load skills as tool-adjacent docs and inject into context (`tools.md`)
- [ ] Emit tool-call events to renderer + trace (`tools.md`)
- [ ] Drive MCP install/discover from the harness, not only a UI panel (`mcp.md`)
- [ ] Let an agent request an MCP install mid-conversation (`mcp.md`)
- [ ] Surface MCP server status + tool list in the renderer (`mcp.md`)
- [~] Reuse `goble-core::llm` + grok-build sampler for the loop (`llm-and-models.md`)
- [x] Wire the composer's model selector to really change the model (`llm-and-models.md`)
- [~] Stream deltas to renderer + trace in real time — renderer streaming done; per-token trace + reasoning deltas pending (`llm-and-models.md`)
- [ ] Define the workspace message-bus contract (addressing, message kinds) (`inter-agent-communication.md`)
- [ ] Implement the local in-process channel; reuse worker transport for remote (`inter-agent-communication.md`)
- [ ] Enforce loop bounding and show agent↔agent activity (`inter-agent-communication.md`)
- [ ] Give each agent + sub-agent a CWD subdir under the workspace root (`sandbox-and-cwd.md`) - the sub-agent half lands with S1/S2 (`subagents.md`)
- [ ] Tighten isolation (fs/network) per workspace, reusing `xai-grok-sandbox` where portable (`sandbox-and-cwd.md`)
- [ ] Stream remote terminal output to the local renderer (`sandbox-and-cwd.md`)
- [ ] Add a `MemoryNote` store with embedding-based search (`memory.md`)
- [ ] Expose `remember` + a query tool to agents (`memory.md`)
- [ ] Wire retrieval into context assembly (`memory.md`)
- [ ] Define the `Persona` config and store it in the TOML (`personas.md`)
- [ ] Assemble system prompts from persona + config (`personas.md`)
- [ ] Let personas be created/edited from natural language (`personas.md`)
- [ ] Add a `DeepResearch` routine (steps, budget, progress events) (`deep-research.md`)
- [ ] Render deep-research progress + final report (`deep-research.md`)
- [ ] Allow saving a research result to workspace memory (`deep-research.md`)

## 05 — Execution router & targets
- [ ] Add `Local` + `SelfAsWorker` to the runtime-target set; remove the `local` bail (`runtime-targets.md`)
- [ ] Represent each target's connection params (`runtime-targets.md`)
- [ ] Map a workspace `kind` to a default target, allow per-conversation override (`runtime-targets.md`)
- [ ] Implement `resolve()`, honoring worker-pairing status (`execution-router.md`)
- [ ] Cache the resolved target per conversation (`execution-router.md`)
- [ ] Implement local→remote promotion, preserving agent+conversation state (`execution-router.md`)

## 06 — Renderer
- [x] Merge the hot-reloadable UI crate into the app — the UI builder (`build_ui`) + snapshot/action types + per-screen modules now live in `app/src/ui`, built directly in the app crate like warp-new builds its windows in `app`; `goble-ui-hot`, the cdylib/`#[no_mangle]`/ABI boundary and the hot-reload handshake are gone (`renderer-architecture.md`)
- [x] Move `integration_testing/` under `app/src/integration_testing/` and point the `app` test targets there (`renderer-architecture.md`)
- [x] Route chat turns through `app/src/runtime` (`crate::runtime::run_turn`) — local harness today; remote degrades to local with a logged warning (`renderer-architecture.md`)
- [x] Drive the chat from the harness (replace the canned reply with real events) (`renderer-architecture.md`)
- [x] Render tool calls distinctly — `role="tool"` rows render as terminal blocks, and the assistant message shows "⚙ tool" invocation chips (`renderer-architecture.md`)
- [x] Align chat + composer with warp-new's UI model — tool/command blocks render as `surface_2` raised cards (1px border, radius 8); the composer is a floating `surface_2` card (border, radius 8, 80px editor) with `surface_1`→`surface_2` pill buttons for model / profile / attach / stop; `ColorToken` maps to warp-new's `surface_1`/`surface_2`/`outline`/`text_sub` (`design-tokens.md`)
- [~] Warp-new parity, still pending: per-message author/avatar header row, per-status tool icons (running/blocked/success/failed), the status/action footer (thumbs / continue / fork / usage), and live tool-call streaming to the renderer (`design-tokens.md`)
- [ ] Add the missing product pages (agents, executions/traces, logs) to the native shell (`renderer-architecture.md`)
- [~] Wire the model selector + attach/voice to real behavior — model selector drives `run_chat_turn` and the Stop button cancels a turn (`renderer-architecture.md`)
- [x] Vendor the emulator instead of writing one — `vendor/alacritty_terminal` holds `alacritty_terminal` 0.26.0 (Apache-2.0) byte-identical to the published archive (SHA-256 checked, `tests/` and `Cargo.lock` removed); `crates/goble-terminal` drives it through the crate's own `vte` re-export, so parser and handler cannot drift. Provenance and the attribution duties are discharged in `THIRD-PARTY-NOTICES.md`, and `scripts/fetch-alacritty-terminal.sh` re-checks the archive against the vendored tree (`terminal-emulator.md`)
- [x] Build a real VT emulator behind the terminal pane — `crates/goble-terminal` (keys, mouse, hooks, osc, palette, screen, blocks; 106 unit tests + a 27-fixture conformance suite, clippy clean) behind `app/src/emulator.rs`, which feeds the screen from the reader thread; the pane paints it as a cell grid through `goble_ui::elements::TerminalGrid` — per-cell colour, attributes, wide characters and the cursor — instead of the ANSI-stripping line buffer it used to be, and encodes keys and pointer events through the crate (`terminal-emulator.md`)
- [x] **Phase 0 — key encoding:** arrows, Home/End, PageUp/Down, Insert/Delete, F1–F35, Alt/Meta, Shift+Tab, xterm modifier form, asserted by encode-direction tables — `crates/goble-terminal/src/keys.rs`; the pane encodes every key through it, and `goble-ui` now delivers the named keys it used to drop (`terminal-emulator.md`)
- [x] **Phase 1 — parser + grid:** grid, cell model with wide-char spacers and combining marks, cursor, and the renderer seam — provided by the vendored emulator plus `screen.rs`, which hands the renderer owned rows/cells (`Screen::lines`, `Screen::content_lines`, `Screen::cursor`) instead of a borrowed-grid trait (`terminal-emulator.md`)
- [x] **Phase 2 — addressing + editing + SGR:** cursor addressing, deferred wrap, ED/EL/ICH/DCH/ECH/IL/DL/SU/SD/DECSTBM, tab stops, IRM, SGR incl. 256/truecolor and colon form — from the emulator; asserted here for CUP, SGR 16/256/truecolor, underlines and hard reset (`terminal-emulator.md`)
- [x] **Phase 3 — alt screen + modes + replies:** 47/1049 lifecycle, DEC private mode table, DA1/DA2/DSR/XTVERSION, DECSCUSR, title — asserted for the alt-screen swap, no-scrollback and restore, mode reporting (app cursor, bracketed paste), title events, and clipboard/colour/text-area queries with their formatters (`terminal-emulator.md`)
- [x] **Phase 4 — scrollback + resize/reflow** — scrollback with a per-block row cap and one reflow path are implemented and asserted, including that a shrink which reflows into more rows loses nothing; the pane measures itself from the font's cell pitch and calls `TerminalSession::set_size`, which resizes the screen and the pty together, so the 24×96 hard-coding is gone (`terminal-emulator.md`)
- [x] **Phase 5 — terminal surface:** bracketed paste, app-cursor mode, OSC 8 hyperlinks, OSC 52 clipboard queries and window-title/reset-title events are exposed in `screen.rs`; focus events (`ESC [ I`/`ESC [ O` under 1004, driven by the window's focus), mouse reporting (1000/1002/1003 + SGR 1006 with modifiers and the middle button, `mouse.rs`) and the OSC 7/133 observer (`osc.rs`) are in the crate and used by the pane, which falls back to its own scrollback when the program is not listening for the wheel (`terminal-emulator.md`)
- [x] **Phase 6 — conformance harness:** `crates/goble-terminal/tests/conformance.rs` walks a 27-fixture corpus under `crates/goble-terminal/tests/conformance/` (4-file format: recording/size/config/grid, enumerated from disk, `grid.json` denies unknown keys), asserting rows, the full cursor (position, visibility, shape, deferred-wrap flag) and exact cells, with scrollback/alt-screen pinned by `history_size`/`alt_screen`/`content_rows`; `cargo nextest` adopted (`.config/nextest.toml` + `.github/workflows/ci.yml`). Upstream's corpus is **not** fetchable (the published `.crate` holds only `src/`); an external corpus is supplied via `GOBLE_TERMINAL_CONFORMANCE_DIR` (`terminal-emulator.md`)
- [x] **Block terminal B0 — shell integration (bash + zsh):** `precmd`/`preexec` hooks, one DCS hex-JSON hook channel, `PS1` save/suppress/restore, `InitShell` + `Bootstrapped` handshake — `crates/goble-terminal/assets/shell/{bash.sh,zsh.sh,zsh_env.sh}` emit the channel (`InitShell`/`Bootstrapped`, `Preexec`, `CommandFinished` with an exit code, `Precmd` with pwd/git/honor_ps1, `InputBuffer`, `Clear`), save/suppress/restore `PS1`/`PROMPT`, and the pane's PTY spawn points bash at `--rcfile` and zsh at `ZDOTDIR`; `cargo test -p goble-terminal` includes a test that decodes the exact bytes each script emits, and a real interactive bash+zsh pty session reported a `CommandFinished` exit code per command (`terminal-blocks.md`)
- [~] **Block terminal B1 — block model:** `Block` + `BlockList`, lifecycle state machine, per-block screens, freeze-on-finish, feed routing, background promotion, `clear` gaps and block metadata are in `blocks.rs`; the height sum-tree layout, prompt grids and prompt hiding are not (`terminal-blocks.md`)
- [ ] **Block terminal B2 — prompt hiding:** OSC 133 prompt demarcation, hidden prompt grid, `honor_ps1` negotiation + user toggle (`terminal-blocks.md`)
- [ ] **Block terminal B3 — clear + edges:** `clear` interception, in-place clears, per-block output cap + truncation notice, alt-screen takeover, no-integration banner and plain-terminal fallback (`terminal-blocks.md`)
- [ ] **Block terminal B4 — persistence:** block serialization/restore, cross-block selection + search (`terminal-blocks.md`)
- [x] **Agent TUI A1 — block kinds + view visibility:** `BlockKind::{Shell, AgentView{conversation_id, label}}` and a per-block `BlockVisibility`, so one list holds shell blocks and a conversation card; `BlockList::{push_agent_view_block, associate_with_conversation, set_visibility, visible_blocks, visible_lines}` with `BlockView::{Terminal, Agent{conversation_id}}` as the two filters — a conversation block never becomes active, never takes PTY bytes, and its own card is hidden while its agent view is open (`agent-tui.md`)
- [x] **Agent TUI A2 — tool-call status carrier:** write `status`/`result` per call into the `tool_calls` column on start and on finish so a re-read from the store renders terminal states (`agent-tui.md`)
- [x] **Agent TUI A3 — live tool events:** emit a structured `chat:tool` event from `ToolCallStarted/Finished/Error` and hold a per-pane in-flight map, since `chat:updated` only says "re-read the store" (`agent-tui.md`)
- [x] **Agent TUI A4 — the tool-call fold, as grok-build has it:** two of A4's three asks landed with Q6/Q7/A2 (`ToolCall { id, name, arguments, status, result }` carries the status, `tool_status_affordance` gives it a glyph and a colour — ○ Pending/Muted, ◐ Running/Accent, ● Finished/Success, ◆ Error — and the result row is tied to its call by call id). The **fold** now lands too: every tool shape starts folded (`Collapsed`), a header click or `e` advances it `Collapsed -> Truncated -> Expanded -> Collapsed`, and the app-owned `tool_fold` map next to `reasoning_expanded` keeps the fold across the per-frame rebuild. A folded read is one line carrying its path, a folded edit carries a `+N/-M` diffstat, a folded command hides its output; truncated shows first 5 + last 3 (read) / first 2 + last 3 (command). (`agent-tui.md`)
- [x] **Agent TUI A5 — composer proposal mode:** the composer can show a command proposal with candidate variants, edit it in place, approve or reject it, and cycle between candidates from the keyboard (`agent-tui.md`)
- [x] **Agent TUI A6 — harness approval suspension:** suspend before a command tool runs (mirroring `ask_user`), emit `CommandProposed`, and resume on approve/edit/reject, sharing the `auto_approve` bit — today `run_command` executes immediately behind an allow-list only (`agent-tui.md`)
- [x] **Agent TUI A7 — agent view over the block list:** Cmd+Enter pushes the conversation's card into the pane's block list and switches the pane's view filter to that conversation's agent view, Esc returns to the terminal leaving the card behind, and a click on the card reopens the view; the agent view draws the conversation's blocks through the one terminal-block renderer (`agent-tui.md`)
- [x] **Agent TUI A8 — conversation card element:** the card block: conversation title, status, last activity, click handler (`agent-tui.md`)
- [x] **Agent parser Q1 — transcript scrolling + follow the stream:** the transcript is a `Scrollable` with no `ScrollState`, so it has no viewport, no clip, no wheel and no offset and simply overflows; attach the state, clip, and pin to the bottom unless the user scrolled up (`agent-parser.md`)
- [x] **Agent parser Q2 — Markdown fidelity:** enable the GFM extensions and handle the fall-through arms — tables, task lists, horizontal rules and images are dropped today; nested lists are broken by a shared buffer, and blockquotes and list items are flattened to a plain string (`agent-parser.md`)
- [x] **Agent parser Q3 — italic renders:** the parser recognises italic and the renderer discards the flag, so `_x_` shows as plain and `***x***` as bold only (`agent-parser.md`)
- [x] **Agent parser Q4 — code blocks are mono:** a code block rendered through `Text`, i.e. the system font, and the `Code` element that should do it was dead code that drew nothing; `ChatMessageBubble` now renders a fenced block through `Code` in the mono family, unwrapped, with the fence's language label (`agent-parser.md`)
- [x] **Agent parser Q5 — tool-call status:** `ToolCall { id, name, arguments, status, result }` written on start as well as finish (the persisted carrier); the live overlay from A3 remains tracked by A3 (`agent-parser.md`)
- [x] **Agent parser Q6 — inline tool-call rows:** replace the bordered `surface_2` card (a `cpu` icon and the literal label `"tool"`) with inline rows — status glyph, tool, arguments, result — collapsed when there is nothing to show; no tool call is drawn in a box. The card is gone (`build_tool_call_rows` draws no `Container`, no border, no raise); the status glyph/colour is read from the persisted `ToolCall.status`, and the app's substring guess (`tool_terminal_data`'s `body.contains("ERROR:")`) is gone too (`agent-parser.md`)
- [x] **Agent parser Q7 — per-tool presentation:** a presenter keyed by the tool definition the harness dispatches on, rather than by string matching on the tool's name (command vs path vs diff vs search vs sub-agent). `goble_core::harness::presentation` answers `tool_row(name, arguments, result) -> ToolRow { verb, subject, detail, kind }` and `tool_kind_for(name) -> ToolKind { Execute, Read, Edit, Create, List, Search, WebSearch, WebFetch, SearchTools, UseTool, MemorySearch, Skill, SubAgent, Other }`; the renderer consumes that row and never matches `call.name` — a command draws its command line and output, a read its path, an edit its path plus Q9 `Diff` rows, a search its query, a sub-agent its own rows. **Aligned to grok-build name for name:** `tool_kind_for` answers for grok-build's own tools first (`read`, `edit`, `write`, `bash`, `grep`, `ls`, `web_fetch`, `search_tool`, `use_tool`, `memory_search`, `skill`) and then for our harness definitions, and `tool_row` gives the grok-build row — the bold verb, the operand the arguments carry and the dim detail the result states (`Read src/lib.rs`, `Edit src/lib.rs +1/-1`, `Run cargo test`, `Search "fn build" in src`, `Subagent “audit the migration” running · reading · 4.5s`), never the raw tool name or the raw argument JSON. Core's `the_parse_answers_for_every_defined_tool` iterates every defined tool, so the parse cannot drift from the definitions (`agent-parser.md` §12)
- [x] **Agent parser Q8 — terminal segment as a block:** a command the agent runs is drawn by the same terminal block element as terminal mode, from the block the bridge produced, instead of being re-rendered as agent output; depends on P1/P3 (`agent-parser.md`). One renderer builds every block (`crates/goble-ui/src/elements/terminal_block.rs`'s `terminal_block`, over `TerminalData::for_command`); the transcript's command tool call draws that block (command line + output + status, no more `$ cmd` text rows), the terminal-mode fragment draws it, and the pane's executed-command block path (`app/src/ui/terminal.rs::executed_command_block`) is the one the chat transcript reuses (`agent-parser.md`)
- [x] **Agent parser Q9 — diff rendering:** diff rows with line gutters and add/remove colouring, inline and without a box; no diff view exists anywhere today (`agent-parser.md`)
- [x] **Agent parser Q10 — parse once per change:** every assistant delta currently triggers a full re-read of all rows and a full Markdown re-parse on the UI thread during layout; cache fragments per message and re-parse only what changed (`agent-parser.md`)
- [x] **Agent parser Q11 — reasoning surface:** the model's thinking now reaches the app and renders. `HarnessServerEvent`/`DaemonEvent` gained `ReasoningStarted`/`ReasoningDelta`/`ReasoningDone`, `goble-harness-internal` maps them through instead of dropping them, the desktop service emits a `chat:reasoning` event, and the app overlays each step as its own transcript row — recessed (`Muted`) and collapsed to a clickable header until the user expands it (`agent-parser.md`)
- [x] **Agent terminal bridge P1 — block ownership:** `BlockOwner::{User, Agent{conversation_id, call_id}}` on shell blocks, `Block::owner()`, and `BlockList::claim_next_pre_exec(id, owner)` — a claimed `Preexec` takes the owner, an unclaimed one is the user's, and a claim aimed at another block (or an already-running/finished one) is refused (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P2 — the claim protocol:** `TerminalSession` writes the agent's command to the pane's pty, holds the claim, resolves it on the next `Preexec`, and fails it on a bounded timeout instead of attaching it to the wrong block (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P3 — tool-result extraction:** a claimed block's output text and exit code become the tool result — `BlockEvent::ToolResult(ToolResult)` carries the block, conversation, call id, command, output and `ToolOutcome::{Finished{exit_code}, StillRunning}`; produced on `CommandFinished` and on the `Precmd` that promotes a still-running command, so non-zero exit, empty output, Ctrl-C (130) and background promotion all resolve rather than hang (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P4 — route the shell tool through the pane:** agent + terminal panes run the agent's commands in the pane's own shell; panes without a terminal keep `SandboxedCommandRunner` (`agent-terminal-bridge.md`)
- [ ] **Agent terminal bridge P5 — in-band claim id (hardening):** carry the call id in the shell hook payload, which needs B0's shell scripts; only if P2's correlation proves insufficient (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P6 — owner into the views:** feed the owner into A1's `associate_with_conversation` so an agent-run command shows in its conversation and a user-run one does not (`agent-terminal-bridge.md`)
- [x] **Agent UI follow-up R1 — the approval gate must run commands:** A6 suspends every `run_command` when `auto_approve` is off and that is the default (`crates/goble-core/src/harness.rs:515`, `app/src/state.rs:815`, `:1786`); the `CommandProposed` it yields is dropped at `crates/goble-harness-internal/src/lib.rs:255` and `Harness::resume_command` has no caller outside tests, so the command never runs and the turn never closes — a suspension with no path back is a hang, not a gate (`agent-tui.md` §9)
- [x] **Agent UI follow-up R2 — pane routing falls back to the sandbox:** `shell_command` installs the hook scripts only for bash and zsh but `pane_session` (`app/src/terminal.rs:1300-1316`) hands a runner to any pane with a session, so under any other shell the command is written to the pty, no `Preexec` arrives and the claim fails after `CLAIM_TIMEOUT` where it used to run through `SandboxedCommandRunner` (`agent-terminal-bridge.md` §9)
- [x] **Agent UI follow-up R3 — links stay clickable inside a paragraph:** Q2 folds links into the inline flow as `InlineSpan::link` (`chat_content.rs:423`) but `chat_message_bubble.rs:512-515` paints paragraphs as bare `InlineText`, and the `ChatBlock::Action { OpenUrl }` path and its test `link_fragment_becomes_interactive_action` were deleted — styling a link is not making it interactive (`agent-parser.md` §10). Fixed: the inline span carries its click handler to the painted `InlineText` and the test is restored.
- [x] **Agent UI follow-up R4 — one block renderer for a tool-result terminal segment:** `crates/goble-ui/src/elements/group_chat_message.rs:288` rendered `ChatFragmentKind::Terminal` as `Empty` instead of the shared `terminal_block` renderer Q8 built (`agent-parser.md` §10). `build_content_column` now flushes the inline run and draws the fragment through `render_terminal` → the one `terminal_block`, so the same block the transcript's command tool call and terminal mode draw appears here too; the new `GroupChatMessage` test `terminal_fragment_draws_the_shared_block` asserts the fragment's drawn runs equal the shared renderer's (`agent-parser.md` §10)
- [x] **Agent UI follow-up R5 — read the status, do not encode it as a string:** `crates/goble-core/src/reasoning.rs:726`, `:925-928` carry a tool result's status as the literal `ERROR: ` prefix and `app/src/state.rs::tool_terminal_data` renders it as text, while `app/src/ui/terminal.rs:562-566` infers status from the boolean `block.failed`; carry and read the real field (`agent-parser.md` §10, `agent-tui.md` §9). Fixed: the tool-result row is now just `<call_id>\n<body>` (both the immediate and resumed paths); the app resolves the row's status from the call record on the assistant row's `tool_calls` column and passes it into the block, so the text is never matched; `VisibleBlock` carries the block's `BlockState` and `exit_code` instead of a `failed` boolean, and the agent view maps them (a block that is not done is `Running`, never `Success`)
- [x] **Agent UI follow-up R6 — the card-free cards:** A5's proposal was a `Container` with `Border::all(1.0)` and `corner_radius(8)` (`chat_composer.rs:620-629`) and the app drew its own bordered card in `app/src/ui/terminal.rs::build_card` while A8's card-free `ConversationCard` element was exported and never constructed (`agent-tui.md` §9). Fixed: the composer proposal is now a full-width band of rows with no border and no radius, and `build_card` builds A8's `ConversationCard` from the block identity the pane's view carries (`ConversationCard::new`, which `from_block` now delegates to), so the app draws the real element; the composer's proposal test asserts zero borders and `ui::terminal::tests::the_conversation_card_is_card_free` asserts the app's card paints the element's square status rail and no border
- [x] **Agent UI follow-up R7 — the link's click must reach an opener:** R3 made a link a live hit target inside the element, but nothing consumes the action in the app: `grep -rn "with_on_action\|on_action" app/src/` returns nothing, so `ChatView::with_on_action` (`crates/goble-ui/src/views/chat_view.rs`), which A5/A7 never call either, is still the end of the line, and the app has no external-URL opener anywhere (`detect_screen_link` in `app/src/state.rs` only recognises `rdp://`/`goble://desktop` handoffs). Wire the tap in `app/src/ui/chat.rs` and give the app an opener (macOS `open`, with the plain-`Http`/`Https` guard the URL must pass), so a click on a link in the transcript actually opens it (`agent-parser.md` §11). Fixed: `app/src/ui/chat.rs::chat_action_relay` hands a fragment's `ChatAction::OpenUrl` to `UiActions::on_open_url` (`app/src/ui/mod.rs`/`app/src/actions.rs`), which calls `app/src/state.rs::open_external_url`; the guard accepts only `http`/`https` (with a non-empty host) and passes the URL to the platform opener (`open`/`xdg-open`/`explorer`) as a single argv argument, refusing every other scheme before the opener is reached. The `rdp://`/`goble://desktop` handoff path is untouched. Tests: `ui::chat::link_action_tests::{a_link_click_reaches_the_app_opener_with_its_url,a_non_http_scheme_click_is_refused_before_the_opener}`, `state::external_url_tests::{web_scheme_accepts_only_http_and_https,open_external_url_refuses_non_http_schemes}`
- [x] **Agent UI follow-up R8 — the pane's executed-command block reads its status:** `executed_command_block` (`app/src/ui/terminal.rs:90-106`) still ends in `TerminalData::new("terminal", lines).with_status(TerminalStatus::Success)`, so a pane command that is running or that failed is drawn as a finished success; `block_status` (`:113`), which reads `block.state`/`block.exit_code`, is the helper that already does this and is used by the agent view only. Called from `app/src/ui/chat.rs:38` (`agent-parser.md` §11). Fixed: `executed_command_block` takes the newest visible block that carries a command (a finished command leaves an empty prompt block behind it) and maps it through `block_status`, so a command still running draws `Running` and a finished non-zero exit draws `Error`; test `ui::terminal::tests::the_executed_command_block_reads_the_blocks_status`
- [x] **Agent UI follow-up R9 — the four surfaces that are still bordered cards:** `AskUserCard` (`crates/goble-ui/src/elements/ask_user.rs:297`, `Border::all(1.0)` + `corner_radius(8.0)`, drawn inline by `crates/goble-ui/src/views/chat_view.rs:690`) — an agent interaction, while the sibling A5 proposal is a borderless band; the queued-prompt card (`chat_view.rs:755`); and the two handoff cards (`:806`, `:851`). Rebuild each as rows in the pager idiom and flip any test that asserts the border (`agent-tui.md` §10). **Done:** all four now draw as borderless `SurfaceRaised` full-width bands with no corner radius — `AskUserCard`'s root (`crates/goble-ui/src/elements/ask_user.rs`) and the queued-prompt, inline-desktop and screen-link blocks (`crates/goble-ui/src/views/chat_view.rs`) drop `Border::all(1.0)`/`corner_radius(8.0)`, matching A5's proposal band. The absence is asserted, not claimed: `elements::ask_user::tests::the_ask_user_surface_paints_a_band_not_a_bordered_card` checks no `StrokeRect` traces the surface's full bounds (the quick-reply checkboxes still stroke their boxes), and `views::chat_view::tests::chat_view_renders_the_queued_prompt_as_a_borderless_band` plus the extended `chat_view_renders_inline_screen_as_image` / `chat_view_renders_screen_link_card_without_frame` assert `stroke_rect == 0`
- [x] **Agent UI follow-up R10 — the two R1 loose ends:** `resume_command_chat_turn` (`crates/goble-desktop-service/src/state.rs:2733`) issued the resume before subscribing to the bus, a race the R1 verifier could not trigger in five runs but that is real by inspection; and the acceptance clause "a turn containing a command tool never leaves the session busy" was an assertion inside the approve/reject tests rather than a test of its own (`agent-tui.md` §10). **Done:** the listener now subscribes to the daemon bus before `resume_command` is issued, so a broadcast receiver exists before the resumed turn can emit `TraceFinished` and the event cannot be lost; a new named test `state::tests::a_command_turn_frees_the_session_on_every_exit_path` drives the proposal through approved, rejected and approved-but-failing, asserting the daemon holds the session while suspended and drops it once the joined listener sees the terminal event. Verified with `cargo test -p goble-desktop-service` (34 passed; 0 failed)
- [x] **Agent transcript H1 — syntax highlighting infrastructure:** there is no highlighting anywhere in the workspace (`grep -rn "syntect\|tree-sitter\|two-face\|highlight" --include=Cargo.toml .` finds nothing; `crates/goble-ui/Cargo.toml` has `pulldown-cmark` and nothing for code), so `Code` (`crates/goble-ui/src/elements/code.rs`) draws a fenced block in one colour with the language label above it. Add `syntect` (default-features off, pure-Rust regex backend) and `two-face` (bat's grammar set), author **our own** `.tmTheme` in grok-build's visual idiom under a new asset directory — do not copy theme or grammar files out of `~/Projects/grok-build` or `~/Projects/warp-new` — and expose one entry point that turns a text + a language into styled spans; the language comes from a file extension, a markdown fence info string, or the `bash` token for a command. Wire it into markdown fences and the tool-call bodies; keep syntect's foreground colours, drop its backgrounds, and do not regress `Code`'s existing mono/no-wrap contract (`agent-parser.md` §11). **Done:** `crates/goble-ui/Cargo.toml` adds `syntect` 5.3.0 (`default-features = false`, features `parsing`/`dump-load`/`plist-load`/`regex-fancy` — the pure-Rust `fancy-regex` backend, no Oniguruma) and `two-face` 0.4.5 (`syntect-fancy`), so the grammar set is bat's 900 KiB `syntaxes-fancy-newlines.bin`; the theme is **ours**, `crates/goble-ui/assets/themes/goble-night.tmTheme`, loaded from that asset. `crates/goble-ui/src/syntax.rs` is the one entry point, `highlight(text, language) -> Option<Vec<Vec<TextSpan>>>`, reusing the renderer's existing run type and keeping only the foreground plus bold/italic (syntect's background is dropped); `language` resolves a fence info string, a token, an extension or a path, and an unresolved language returns `None` so the plain rendering stays. `Code` carries the runs (`with_highlight`/`with_highlighted_lines`) and keeps its mono family, language label and no-wrap contract; `ChatMessageBubble` wires the fence path (`render_block`) and the read tool-call body (`path_body`, by the path's extension) to it.
- [x] **Agent transcript H2 — a file read is an IDE excerpt, not a text blob:** `path_body` (`crates/goble-ui/src/elements/chat_message_bubble.rs:223-236`) draws the path and then the raw result string as one `Muted` row. A read must draw the file's lines with a right-aligned line-number gutter and H1's highlighting, on its own background band, collapsed to the path alone by default (A4's fold). Depends on A4 and H1 (`agent-parser.md` §11). **Done:** `read_file` is now its own registry shape (`ToolPresentation::Read`; `write_file`/`delete_file` keep `Path`, so a write/delete confirmation is never numbered) and `read_body` draws the path then, while open, an editor excerpt: a right-aligned gutter `format!("{:>w$}  ", n, w = width)` (width from the largest number), each line's content through H1's `syntax::highlight`, every line on a `ColorToken::Bg` band; truncated draws first 5 + `…` + last 3. The base line comes from the call's own data — an explicit `offset`/`start_line` argument, else a consistent `cat -n`-style gutter kept verbatim, else 1 (the harness's `read_file` declares no offset and returns raw content, so the base is 1) — and a failed/empty result, or a gutter that parses only partly, degrades to today's plain `result_rows` rather than wrong numbers (`agent-parser.md` §11)
- [x] **Agent transcript H3 — the diff bands, and the code inside them:** the band exists but is a 10% tint (`crates/goble-ui/src/elements/diff.rs:15` `TINT = 0.10`, painted at `:461` as `surface.mix(&color, TINT)`) and each changed line's text is one flat `Success`/`Error` colour (`:354-358`), where grok-build fills the content area with real insert/delete backgrounds (`diff_insert_bg`, `diff_delete_bg`) and highlights the code inside the band. Saturate our own insert/delete background tokens enough to read as green/red in this dark palette, and run each changed line through H1's highlighter so the code is coloured inside the band. Depends on H1 (`agent-parser.md` §11). **Done:** `theme.rs` gains `ColorScheme.diff_insert_bg`/`diff_delete_bg` and `ColorToken::{DiffInsertBg, DiffDeleteBg}`; dark and midnight are `#0f3d20` (green) and `#4d1116` (red), light `#d6f5e0`/`#fbdada`. `Diff` paints each changed row's full width with the token (context rows band-free) instead of the 10% surface tint, and `Diff::with_language` runs every changed line through `crate::syntax::highlight`, painting the runs inside the band; an absent or unresolved language degrades to the flat `Success`/`Error` colour. Gutters, rail, hunk headers and gap rows are unchanged. `edit_file`'s `diff_body` passes the edited `path` as the language. Tests: `changed_rows_paint_the_theme_band_at_full_width`, `changed_lines_are_highlighted_inside_the_band`, `an_unknown_language_keeps_the_flat_change_colour`, and `chat_message_bubble::tests::edit_call_paints_the_diff_band_and_highlights_the_code`
- [x] **Agent transcript H4 — the command line and its output keep their colours:** `TerminalData::for_command` (`crates/goble-ui/src/elements/terminal_block.rs:151-163`) splits the tool result on newlines into plain `TerminalLine::output` rows and nothing in `goble-ui` parses ANSI (`grep -rn "ansi\|SGR\|escape"` over the terminal-block and chat-content files finds nothing), so an agent's command output loses the colours it actually emitted — while the pane path shows them, because the real emulator draws it. Highlight the command line as shell and reproduce the output's own ANSI colours, reusing the vendored emulator in `crates/goble-terminal` if its parser takes a plain byte string, otherwise a bounded SGR parser. Depends on H1 (`agent-parser.md` §11). **Done:** `crates/goble-terminal/src/ansi.rs` runs the same vendored `vte::ansi::Processor` the screen runs through a line-oriented sink (`render_lines(&[u8]) -> Vec<ScreenLine>`), so the parser is reused, not re-implemented: printable characters keep the colour/weight/emphasis in force, CR/BS/tab are honoured, and every other sequence (cursor addressing, erases, modes, unknown finals) is consumed with no text lost. `TerminalData::for_command` highlights the command line with H1 (`syntax::highlight(command, "bash")`, falling back to plain on an unresolved/multi-line command) and parses the output through that renderer, carrying each line's `runs: Vec<TerminalRun>`; a plain line yields no runs and is still drawn as one `Text` run, and the block paints runs through `InlineText` (colour + bold + italic + underline, default colours left to the theme). `ScreenColor::is_default()` was added so the terminal defaults resolve to the block's theme colour. Tests: `terminal_block::tests::{command_line_is_highlighted_as_shell, ansi_output_keeps_its_colours, an_unrecognised_escape_sequence_loses_no_text}` (`agent-parser.md` §11)
- [x] **Agent UI follow-up R11 — the clear paths must clear the approval state (regression):** `on_clear_transcript` (`app/src/actions.rs:633-644`) clears `rt.messages`, `rt.pending_ask`, `rt.queued_prompt` but not the approval fields A5/R1 added, while `finish_turn` (`app/src/state.rs:1523-1524`) does clear `rt.pending_command`/`rt.command_selection`; `pane_chat_snapshot` reads them back and `app/src/ui/chat.rs:211` renders them, so a stale approval card and its selected candidate survive "Clear transcript". Same omission in `app/src/state.rs:1623-1625` and in `on_stop` (`app/src/actions.rs:645-654`), which only sets `busy = false`. The existing test only asserts the message list is empty, so it misses this (`agent-tui.md` §10). **Done:** all three clear paths now drop `rt.pending_command` and `rt.command_selection` alongside the rest of the pane runtime — `on_clear_transcript` and `on_stop` in `app/src/actions.rs`, and the store-less branch of `bind_active_pane_conversation` in `app/src/state.rs`. Each path has a test that fails if either field survives: the existing `clear_transcript_clears_active_pane_messages` is strengthened, plus `stop_drops_the_pending_approval` and `rebinding_without_a_store_drops_the_pending_approval`. Verified with `cargo test -p goble-app` (176 passed, 0 failed)
- [x] **Agent UI follow-up R12 — the model must be able to see that a call failed (regression R5 introduced):** R5 removed the `ERROR: ` prefix so the UI reads the status from the record, but `crates/goble-core/src/reasoning.rs:728` and `:924-925` now write the tool row as `<call_id>\n<message>`, and `build_history` (`:966-969`) puts that remainder straight into the LLM-facing `Message.content`. The structured replacement does not reach it: the assistant row's `tool_calls` is deserialized into `LlmToolCall` (`crates/goble-core/src/llm.rs:8-12`), which has `id`/`name`/`arguments` and **no status**. So the model, which used to read `ERROR: <message>`, now sees the message alone with no failure signal — the premise in `agent-parser.md` §10 that "nothing reads the result text" is false for the history builder. Either carry the outcome into the LLM-facing model or restore an explicit, machine-readable marker; do not fix it by putting the prefix back for the UI (`agent-parser.md` §11). **Done:** `llm::Message` (the LLM-facing message) carries `tool_status: Option<ToolCallStatus>`, and `reasoning::build_history` resolves each `Role::Tool` message's status from the assistant row's persisted call records (a `call_id -> status` map over the rows' `tool_calls` columns) and stamps it on the message; the persisted tool row is unchanged (`<call_id>\n<body>`, no marker). Since no provider protocol has a status slot on a tool message, the three wire builders (`into_openai_messages`, `into_ollama_messages`, the Anthropic mapping) send a failed tool message as `[tool call failed]\n<body>` and a successful one as its bare body, so the model reads the failure. Tests: `reasoning::tests::test_failed_tool_call_is_failed_in_the_llm_history` (a real failing `run_turn`, then `build_history` yields a `Role::Tool` message with `tool_call_id == tc_bad` and `tool_status == Error`), `llm::tests::{failed_tool_message_carries_a_failure_marker_on_the_wire,a_successful_tool_message_is_sent_as_its_body}`, with R5's `test_failed_tool_result_row_does_not_encode_status_as_text` still green (`agent-parser.md` §11)
- [x] **Agent UI follow-up R13 — the error notice is still a bordered card:** `build_agent_error` (`app/src/ui/chat.rs:408-413`) wraps the agent's error in `Container::with_background(Surface).with_border(ColorToken::Error).with_corner_radius(...)`, painted inline in the agent transcript through `build_agent_chat`'s `with_notice` (`app/src/ui/chat.rs:160` → `crates/goble-ui/src/views/chat_view.rs:216`, drawn at `:694-695`). R9 fixed four surfaces and this is the fifth; the rule has no exception for it (`agent-tui.md` §10). **Done:** `build_agent_error` drops `with_border(ColorToken::Error)` and `with_corner_radius(...)`; the notice is now a full-width `SurfaceRaised` band of rows with padding only — the pager idiom A5's proposal and R9's four bands use. The absence is asserted, not claimed: `ui::chat::error_notice_tests::the_error_notice_is_a_band_not_a_bordered_card` renders the builder headlessly and checks `stroke_rect == 0` and that its band fill carries `corner_radius == 0.0`. After the change `grep -rn 'with_border\|Border::all'` over the agent-surface files (`app/src/ui/chat.rs`, `views/chat_view.rs`, `elements/{chat_message_bubble,chat_composer,chat_content,conversation_card,ask_user,group_chat_message}.rs`) finds no hit; the sole remaining one is the sanctioned terminal block (`elements/terminal_block.rs:672`). Verified with `cargo test -p goble-ui -p goble-app` (goble-app lib 177 passed, 0 failed; goble-ui lib 277 passed, 0 failed; every integration suite ok)
- [x] **Agent UI follow-up R14 — one command, one rendering:** the persisted tool-result path builds its block with `TerminalData::new` + `TerminalLine::output` (`app/src/state.rs:278-301`), while the assistant row's tool call and the pane both use `TerminalData::for_command` (`crates/goble-ui/src/elements/chat_message_bubble.rs:382`, `app/src/ui/terminal.rs:150`). So H4's shell highlighting and ANSI-coloured output reach two of the three paths and not the third, and the same command can appear highlighted and flat in one transcript (`agent-parser.md` §11). **Done:** the result row's command is resolved from the call record on the assistant row's persisted `tool_calls` column (the `command` argument of a command-shaped call, found and parsed by the same `tool_presentation_for`/`arguments` the renderer uses) and `tool_terminal_data` now builds the block through `TerminalData::for_command`, so the command line is shell-highlighted and the output keeps its ANSI colours; a non-command result (or one whose call has no command) has no command line to invent and keeps the plain block. Test `state::pane_session_tests::a_persisted_tool_result_builds_the_same_block_as_the_tool_call` asserts the persisted block equals `TerminalData::for_command(command, output, status)` — the block the tool-call path's `command_body` builds — and that its command line carries shell runs and its output an ANSI colour.
- [x] **Agent UI follow-up R15 — the agent surface is pill-free, locked by tests:** the agent's transcript draws no pill anywhere — not for the agent itself and not for a tool call, which is the row grok-build draws. `crates/goble-ui/src/test_util.rs` owns the predicate (`is_pill`, `pills`, `pill_rects`, `assert_pill_free`, `assert_no_row_border`, `bordered_control`): a rounded fill that wraps a row, and a border with no control under it, are pills; two shapes pass by design — a **bordered control** (a border and a fill over the same rect: the shared terminal command block's card, or a button such as the ask card's option) and a **text highlight** (a rounded fill no taller than the line of text it backs: an inline code span). `chat_message_bubble::tests::pill` sweeps prose and markdown in both roles, a reasoning step collapsed and expanded, every tool family in every fold and in `Pending`/`Running`/`Error`, every sub-agent status, the ask card and the still-running footer; `the_lock_catches_a_row_wrapped_in_a_card` proves the lock is not vacuous (the same row wrapped in a rounded card is reported, unwrapped is not); `views::chat_view::tests::the_agent_transcript_paints_no_pill` paints the whole pane in one frame with a call in flight, an interrupted ask, a command proposal, a queued prompt and the footer; and `ui::chat::agent::agent_pill_tests::the_agent_pane_paints_no_pill_around_its_rows` renders the `RootView` over a real store and asserts no pill covers any row's own run, excluding only the pane's square split frame (window chrome), whose squareness it also asserts. No pixels changed: the pixels were already pill-free. Verified with `cargo test -p goble-core -p goble-ui -p goble-app` (`goble-ui` lib 345 passed, `goble-app` lib 215 passed, `goble-core` lib 259 passed, every integration suite ok) (`agent-parser.md` §12)
- [x] **Agent parser H5 — the markdown boxes are boxes:** a fenced code block and a blockquote are wrapped in a rounded `SurfaceRaised` container (`crates/goble-ui/src/elements/chat_message_bubble.rs:991-1007`, and in the group view `crates/goble-ui/src/elements/group_chat_message.rs:234-243`, `:326-330`, `:404-413`). They carry no border, but the rule names "bordered card or box"; grok-build draws both as full-width bands instead. Convert them to bands so the agent's markdown is rows, not rounded panels (`agent-tui.md` §10). **Done:** `ChatBlock::CodeBlock` is a full-width `SurfaceRaised` band (the code element stretched in a cross-axis-stretched column) and `ChatBlock::BlockQuote` is a new `QuoteRail` element — a muted 2 px rail spanning the content's height with the content indented past it; the group view's `render_code_block` drops its corner radius and `render_block_quote` uses the same `QuoteRail` (`QuoteRail` is `pub(crate)` in `chat_message_bubble.rs`). The fence's language label and the H1 highlight are untouched. Tests: bubble `fenced_code_block_is_a_band_not_a_rounded_box` / `block_quote_is_an_indent_and_a_rail`, group `code_block_draws_a_band_not_a_rounded_box` / `block_quote_draws_an_indent_and_a_rail` — each asserts no rounded `SurfaceRaised` fill and the flat band / square muted rail, plus the surviving fence label and the highlighted fence body. Inline code deliberately keeps its small rounded chip: it matches the bubble's `resolve_span` idiom (a 3 px rounded chip), not a markdown box.
- [x] **Agent UI chrome C1 — drain live agent activity into app state:** `RootView::drain_events` (`app/src/root_view.rs:120-308`) matches a fixed list of names, so the `agent:log` / `agent:started` / `agent:finished` / `agent:state_update` / `agent:tool_result` events the desktop service already emits (`crates/goble-desktop-service/src/state.rs:1553`, `:1584`, `:1613`, `:1623`, `:1705`) are dropped, and nothing exposes what is running as live state (`pane_in_flight_tool_count` is documented test-only, `app/src/state.rs:1549`). Handle those names and add one accessor the chrome reads: the running executions, the pane's in-flight tool calls and busy flag, a pending approval or question, and a turn start time when one can really be observed. This item also delivers the `07 - Observability` drain row below (`agent-tui.md` §11). **Done:** `drain_events` now handles all five `agent:*` names; the one accessor is `UiState::live_work()` returning `LiveWork { executions: Vec<LiveExecution>, tool_calls, turn_busy, pending_approval, pending_question, turn_started_at }`. A worker run is tracked from `agent:started` (which now carries the service's own `started_at` on the event) until `agent:finished`; `agent:state_update` stores the run's `RuntimeState` and `agent:tool_result`/`agent:log` its last activity, so none is dropped. `turn_started_at` is recorded by the new `UiState::begin_turn` where the app issues a turn (it observes its own start) and cleared by `finish_turn`; a turn whose start was not observed reports `None` rather than a guessed time. `pane_in_flight_tool_count` is **deleted** in favour of `live_work()`. Verified: `cargo test -p goble-app` — lib `181 passed; 0 failed` (3 new), every integration suite ok, doc-tests 0; the acceptance test `root_view::tests::drain_events_leaves_the_live_accessor_reporting_what_is_in_flight` scripts `chat:tool`, `chat:turn_finished`, `agent:started`×2, `agent:finished`, `agent:state_update`, `agent:tool_result` and `executions:updated` and asserts the accessor reports exactly one in-flight tool and one running execution and nothing else. `cargo test -p goble-desktop-service` → 34 passed.
- [x] **Agent UI chrome C2 — the live turn-status footer:** no footer exists anywhere; the chat column ends at the divider + composer (`crates/goble-ui/src/views/chat_view.rs:1058-1059`) and `RunningIndicator` is dead code that paints nothing (`crates/goble-ui/src/elements/running_indicator.rs:47-49`). Add a one-row band above the composer drawn in the pager idiom: while busy the activity (a running tool / waiting on approval / waiting on a question / thinking) plus the elapsed time, while idle with work in flight a `{n} {kind} still running` line naming each kind as one hit target, and zero height when nothing is in flight; revive or delete `RunningIndicator`. Depends on C1 (`agent-tui.md` §11). **Done:** the new `TurnStatusFooter` element (`crates/goble-ui/src/elements/turn_status.rs`, `TurnStatus`/`TurnActivity`/`WorkKind`) draws the three states in the pager idiom (borderless full-width row, a `◆` bullet, zero height when nothing is in flight) and is placed in `ChatView` directly above the composer's divider via `with_turn_status`/`with_on_open_work` (added only while in flight, so an idle pane costs neither height nor column spacing). `RunningIndicator` is revived: its `paint` now draws `◐ ◓ ◑ ◒` frames, phase driven by the real observed turn start the app passes (the element layer has no clock or frame counter), and no token count is drawn because no live source carries one. The busy activity is resolved in `app/src/state.rs::pane_turn_status` from the pane's own live runtime (pending approval → `Waiting on approval…`, pending question → `Waiting on a question…`, an in-flight call → its command/name, else `Thinking…`/`Responding…`) and the still-running line counts `WorkKind::{Command, Execution}` for the pane's in-flight tools plus `live_executions` (C1's `live_work` data; `pane_in_flight_tool_count` no longer exists). The cue fires `UiActions::on_open_work` → `UiState::open_work_overlay`, which today only flips `work_overlay_open`: the overlay is S7, so the action has no visible surface yet. The composer keeps the only Stop control (a busy footer is not a hit target). Verified with `cargo test -p goble-ui -p goble-app` (`agent-tui.md` §11)
- [x] **GUI polish R1 — Chat geometry:** the transcript's rows are full-width bands now, not pills. The agent's own reply (`ChatMessageBubble`, `crates/goble-ui/src/elements/chat_message_bubble.rs`) paints no box at all — no background fill, no corner radius, no border — and the width cap is gone (`BUBBLE_MAX_WIDTH_RATIO` deleted, the `ConstrainedBox` around the bubble and the role-based `MainAxisAlignment` row removed, `rebuild` losing its `max_width`), so the reply is laid out at the transcript's own width: the pane's. The user's own message is the one surface — a `SurfaceRaised` band with square corners (`with_corner_radius` gone) — and it spans the pane too. Both keep the transcript's padding at both edges (`EdgeInsets::uniform(spacing_px(SpacingToken::Md))`: 12 px at the default density, 10.2–13.8 px across `Density`), so no line of text touches the pane's border. Everything else is untouched: tool-call folds, reasoning folds, code/diff/terminal blocks, sub-agent rows, the streaming caret, every click target and the markdown rendering; `ChatRole::Tool` (the pane's inline executed-command block) loses only the same redundant bubble fill, because `terminal_block` carries its own band and border. Tests: `elements::chat_message_bubble::tests::transcript_rows_are_full_width_bands_not_pills` (each row measures the pane's full width; the reply paints zero fills and zero strokes; the user's message paints exactly one square band spanning `0..width`; both inset their text by 10–15 px), `views::chat_view::tests::the_transcript_rows_span_the_pane_with_ten_to_fifteen_px_of_padding` (a real `ChatView` transcript: no strokes, one square band across the pane, both rows sharing the pane's left edge inside the padding) and `ui::chat::chat_geometry_tests::the_panes_transcript_paints_full_width_rows` (the app's own pane surface over a real in-memory `DesktopState`: the user's band runs from `SIDEBAR_WIDTH` to the window's right edge with square corners, and the reply's text starts at the same x). Each of the three was confirmed red with the old cap and alignment temporarily restored, then green without. No existing test encoded the old geometry — all 296 `goble-ui` and 199 `goble-app` lib tests passed unchanged, so none was rewritten or weakened. Verified with `cargo test -p goble-ui -p goble-app` → exit 0 (`goble_ui` lib `298 passed; 0 failed`, `goble_app` lib `200 passed; 0 failed`, every integration suite ok, doc-tests 0) (`06-renderer/agent-tui.md` §11, `06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R2 — the topbar tabs:** the workspace/space tabs (`build_workspace_strip` / `build_workspace_chip` plus the `WorkspaceChip` element in `app/src/ui/shell.rs`) were rounded chips inset in the bar; they now read as browser-style tabs. Each tab's own `layout` returns `TOPBAR_HEIGHT` and its `paint` fills that whole rect with `fill_rect` — square corners, no stroke — so a tab runs the full height of the topbar. Adjacent tabs are separated by exactly one `Divider::vertical` child per boundary (`build_workspace_strip` inserts it before every tab after the first, and the row's spacing is `0.0`), so N tabs draw N-1 rules: one line per boundary and no gap that would read as two. The active tab stays distinct with the existing `SurfaceRaised` fill and `Text` label colour; inactive tabs keep no idle background and take a `Hover` fill only under the pointer. The bar itself no longer pads vertically: the strip is pinned to `TOPBAR_HEIGHT` (`ConstrainedBox::with_height` around a `CrossAxisAlignment::Stretch` row), so the shorter controls center against the tabs exactly where they were — the +/▾ control and the Settings icon keep their place and size (asserted at the bar's mid-height). The label still swaps for the inline rename field while `space_rename_editing` is set (now centred in the same full height via `build_rename_tab`, so the bar's height is unchanged while editing), and the trailing "x" still closes that space. Tests: `ui::shell::tests::{a_workspace_tab_fills_the_bar_with_square_corners, only_the_active_tab_is_filled, a_click_on_the_tab_selects_its_space, a_click_on_the_close_control_closes_its_space}` and `ui_render::{workspace_tabs_fill_the_toolbar_and_draw_one_rule_per_boundary, the_active_workspace_tab_becomes_the_rename_field}`. Verified with `cargo test -p goble-ui -p goble-app` → exit 0 (`goble_app` lib `199 passed; 0 failed`, `ui_render` 14 passed incl. both R2 tests, `goble_ui` lib `296 passed; 0 failed`; re-verified 2026-09-12) (`06-renderer/agent-tui.md` §11)
- [x] **GUI polish R5 — Shell agent surface:** two halves, both about the shell/terminal space. **(1) The shared input bar.** The terminal pane had no input bar at all — only the PTY grid — so `app/src/ui/terminal.rs` now pins the chat surface's own composer element to the bottom of the shell pane. `chat::build_terminal_composer` (`app/src/ui/chat.rs`) builds it from the very `ChatComposer` the chat surface (`ChatView`) mounts, wired to the pane's own state: the value is this pane's draft, focus is `active && state.composer_focused`, the model label is this pane's `PaneControls::model`, the path label is this pane's session path, `Stop` shows while this pane's turn is busy, and the change/focus/send/cmd-enter/slash/attach/select-model/stop/profile callbacks, both footer menus and the harness/dir/branch pills all route to this pane's own controls (`state.composer_context.get(&pane_id)`). Enter sends the draft as this pane's agent turn and opens the harness on it; Cmd/Ctrl+Enter starts a new conversation there — the same gestures the chat composer carries. **One control is deliberately omitted: A5's command-proposal card.** A suspended command only exists while the harness owns a pane's input — Esc at a proposal is the proposal's Reject, not the way out — and a pane whose harness is open draws the shared agent view (which contains that card) instead of this bar, so a card here could never be live and would be a dead button. The deleted bespoke editor also treated a `!`-prefixed draft as a shell command; the shared bar's Enter is the agent turn and does not, because the pane's grid directly above the bar is where a shell command is typed and runs (and the app exposes no action that writes a command to a pane's pty from a UI callback). `TerminalView` gained `bar`/`composer_focused`, forwards MouseDown/MouseUp/MouseMove (and KeyDown while the bar holds the keyboard) to its rebuilt subtree before treating the event as the grid's, and the bespoke `❯` input line plus its editor (`handle_harness_key`) are deleted. **(2) One agent view.** The pty-only degraded view (`TerminalView::build_agent_view`, a bare list of `terminal_block`s) is deleted; `terminal::build_terminal` returns `chat::build_agent_chat(...)` for `BlockView::Agent` — the single function both a `PaneKind::Chat` leaf (`app/src/ui/panes.rs::build_leaf`) and an open shell harness call — with its Escape hook wired to close the harness (`on_set_pane_harness_mode(pane_id, false)` plus unfocusing the bar), and `chat::build_agent_chat` threads that hook into `ChatView::with_on_escape` (a chat pane passes `None`), so the shell's harness is the same agent view element Space 1 mounts, not a second copy. Tests (both over a real `RootView` on a single terminal space with a detached session, so the pane's own events, the app's real actions and the per-frame rebuild are what they drive): `ui::terminal::tests::the_shells_bottom_bar_is_the_shared_composer` (the shell pane draws the `ChatComposer` placeholder the chat pane draws, and the bespoke `❯` line is gone) and `ui::terminal::tests::the_shells_bar_sends_an_agent_turn_and_opens_the_shared_agent_view` (the focused bar takes typing and edits the pane's own draft; Enter opens the harness on the pane's bound conversation and the turn is drawn in the shared transcript with the shared composer still at the bottom; Esc returns the pane to the shell with the conversation's card left in its block list; the grid takes the keys back and the bar is not edited; a click on the card reopens the agent view). Two tests were **rewritten, not deleted**, because their premises were the two things R5 removes: `harness_mode_owns_the_keys_and_esc_returns_to_the_shell` and `cmd_enter_esc_and_a_card_click_round_trip_the_agent_view` are replaced by the two tests above, and `the_agent_view_never_draws_a_running_block_as_success` becomes `a_command_block_is_drawn_from_its_lifecycle_not_a_guess`, which keeps every one of its assertions (Executing/Background → Running, DoneWithExecution 0 → Success, non-zero → Error, and the drawn title colour for each) on the surviving `block_status` → `terminal_block` path. No assertion was deleted or narrowed and the lib test count is unchanged (`goble-app` 200). Verified with `cargo test -p goble-ui -p goble-app` → exit 0 (`goble_app` lib `200 passed; 0 failed`, `goble_ui` lib `298 passed; 0 failed`, every integration suite ok, doc-tests 0) (`06-renderer/agent-tui.md` §11)
- [x] **GUI polish R6 — the sidebar is a flat surface:** the left conversation sidebar drew rounded corners on four of its own surfaces, carried two `Divider::horizontal()` rules, and its search field led with a magnifier. It is flat now: no rounded fill anywhere in the band, no separator, no magnifier, and the search field is a few pixels taller. Concretely, `SearchInput` gains `with_icon(bool)` and `with_extra_height(px)` and loses the compact branch's `corner_radius = 6.0` (the sidebar is its only compact caller, so the app keeps exactly one search shape); `HoverButton`, `Button` and `TopbarButton` each gain `with_corner_radius(radius)` as an opt-in whose default is unchanged (the theme's radius, or half of it for `TopbarButton`); `ConversationListItem` drops the radius on its row band and on the environment badge, and passes `0.0` to its 3-dot control and its delete row; `app/src/ui/sidebar.rs` drops both `Divider` children, drops the `+` box's radius, and configures the field `with_compact(true).with_icon(false).with_extra_height(3.0)` (`xs` = 4 px, so the box carries 7 px of vertical padding at the default density instead of 4). Tests: `elements::search_input::tests::the_sidebar_search_is_a_taller_square_field_without_a_magnifier` (the box grows by exactly `2 × extra`; no `search` icon; no rounded fill) with `the_search_field_keeps_its_magnifier_by_default` proving the flag is a real switch, and `ui::sidebar::sidebar_surface_tests::the_sidebar_is_flat_square_and_free_of_separators_and_the_magnifier`, which mounts the real `RootView` over an in-memory store in four hover states (card hovered, "new conversation" hovered, both, neither) and asserts the band left of the workspace draws no rounded fill, no hairline spanning the sidebar, and no `search` icon — while still drawing the field's own border, the placeholder, the create row, a folder header, a card name and `View all (5)`, so the absence is not vacuous. Confirmed red with the card's `with_corner_radius(app.theme.radius_px())` temporarily restored (four 8 px fills reported), then green without; `the_separator_rule_reports_a_rule_and_not_a_field_border` proves the rule discriminates a rule from the field's own edge. Verified with `cargo test --workspace` → exit 0 (every target ok, no failures), `cargo check -p goble-ui -p goble-app` → exit 0, `goble-ui` lib 347 passed, `goble-app` lib 217 passed, `goble-core` lib 259 passed. Not verified: the on-screen look (house rules forbid building or running `goble-app`), so the field's new height and the flat column are proven by the drawn commands, not seen in a window (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R7 — the tasks & workflows overlay:** the app had no way to see the durable tasks, the daemon's execution ledger and the registered workflows while the workspace was up — only the harness pages (`app/src/ui/harness.rs`), which replace the whole main area and are reachable from nowhere (no topbar button, no palette entry, no binding; `on_workflows`/`on_executions` had no UI caller). The new overlay `app/src/ui/task_workflow.rs` is a right-anchored `Sheet` over the workspace: a header (`Tasks & workflows`, the three counts, an ✕) and a scrollable body of three sections — **Tasks**, **Executions**, **Workflows** — each with its count and an honest empty line. The rows are the pages' own builders (`build_task_row`, `build_execution_row`, `build_workflow_row`, now `pub(crate)` in `harness.rs`), so a record is drawn once; `harness::card` lost its corner radius, which also makes all four observability pages square bands like the transcript and the sidebar. It is an overlay, not a page: `AppTab` is untouched, the pane tree stays mounted underneath, and it opens with Cmd/Ctrl+Shift+W (and the palette entry "Tasks & workflows") and closes with the same chord, Escape, its ✕, or a click on the backdrop — state `UiState::task_workflow_open`, actions `on_toggle_task_workflow` / `on_close_task_workflow`, wired through `UiSnapshot`/`UiState`/`build.rs` exactly as the crons drawer is. This is **not** the deleted "Work in flight" panel: that one listed in-flight commands and sub-agents with a kill per row and was removed on the user's instruction; this one lists durable records and carries no kill. Test `ui::task_workflow::tests::the_overlay_lists_the_real_records_and_every_route_closes_it` mounts the real `RootView` over an in-memory store with one workflow seeded through `DesktopState::create_workflow`, asserts the panel is absent while closed, that the palette lists the entry, that the chord opens it, that the drawn panel carries the heading, the counts, the three section names, `task-7`, `exec-3` and `Nightly digest` while the pane's own composer (`Ask anything...`) is still drawn underneath, that no rounded fill wider than a status dot appears in the panel's band — confirmed red by restoring `card`'s radius, which reports three `radius 8` row bands — and then that Escape, the ✕ and the backdrop each close it. Verified with `cargo test --workspace` → exit 0, no failures, no unused-code warnings; `goble-app` lib 218 passed (`goble-ui` lib 347, `goble-core` lib 259). Not verified: the on-screen look (house rules forbid building or running `goble-app`); the page-level routes (`AppTab::{Workflows,Executions}`) stay unreachable from the UI, which this row does not change (`07-observability/executions-and-trace.md`)
- [x] **GUI polish R8 — the rich input's instruction strip:** the rich input gave no clue what its keys do. It now draws one above its editor — the warp-new shortcuts view's shape (a key cap per key of the chord, then the name) carrying grok-build's own instructions. The widget is `crates/goble-ui/src/elements/shortcut_hints.rs` (`ShortcutHint` / `ShortcutHints`: a cap per key, caps 2 px apart, the name 4 px after them in `Muted` at 11 px, square `SurfaceRaised` caps), reached through `ChatComposer::with_hints` and `ChatView::with_composer_hints`; the strip is the first row of the composer's column, above the context pills and the editor, and an empty list is an `Empty` the column skips, so every existing caller's layout is unchanged. The entries are the app's, in `app/src/ui/shortcut_hints.rs`: grok-build's prompt-focused hint set (`views/agent.rs::build_hints`, labelled as grok-build labels it), kept only where this GUI binds the chord — the agent input draws `↵ send` (relabelled `↵ queue` while the pane's turn is in flight, grok-build's own relabel), `⌘↵ new conversation`, `! shell`, `⌘K commands`, `⌘⇧W tasks`; the shell bar draws `↵ run`, `⌘↵ agent`, `⌘K commands`, `⌘⇧W tasks` and no `!`. grok-build's `newline`, `mode`, `cancel`, `yolo`, `todos`, `sessions`, `extensions`, `send to bg`, `multiline` and `stash` are **not** drawn (and `queue` is not a separate entry: it is the relabel of `send` while a turn runs): this app binds none of them, and the strip never promises a key nothing answers. Tests: `elements::shortcut_hints::tests::{the_strip_draws_a_cap_per_key_and_the_name,an_empty_strip_draws_nothing}`, `elements::chat_composer::tests::the_hints_are_drawn_above_the_editor_and_only_when_given` (the name's line is above the editor placeholder's, and a composer handed none draws none), `ui::shortcut_hints::tests::the_hints_are_the_gui_s_own_bindings` and `ui::shortcut_hints::rich_input_tests::the_agent_rich_input_carries_the_instructions_above_its_editor`, which mounts the real `RootView` over an in-memory store, pins the strip's line above both the editor's placeholder and the context pills, and re-renders with the pane's runtime `busy` set to see `send` become `queue`. Confirmed red with the `.with_composer_hints(...)` wiring removed (`idle: "send" is drawn`), then green with it back. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no failures; `goble-ui` lib 350 passed (3 new), `goble-app` lib 220 passed (2 new), `goble-core` lib 259 passed. Not verified: the on-screen look — the app was neither built nor launched (house rule), so the strip is proven by the drawn commands, and the `↵` cap is proven to be *drawn*, not proven to have a glyph in the shipped font (`06-renderer/ui-spec/05-chat-view.md` §The instruction strip)
- [x] **GUI polish R9 — the strip's place, the footer's line, and one key icon:** four corrections after R8, all about where the rich input's pieces sit. **(1) The separator is under the strip, not over it.** The strip was the first child of the `ChatComposer`'s column, i.e. *below* `ChatView`'s `Divider`; it moved out of the composer entirely — `ChatComposer` loses `hints`/`with_hints` — into the view's column in `crates/goble-ui/src/views/chat_view/transcript.rs`, before `Divider::horizontal()`, so the pane's order is transcript -> strip -> separator -> editor -> footer. The separator is the input's own top edge and the strip describes what is under it. **(2) The shell bar draws no strip.** `app/src/ui/chat/composer.rs` no longer passes hints and `app/src/ui/shortcut_hints.rs`'s `shell_rich_input_hints()` (its `↵ run`, `⌘↵ agent`, `⌘K commands`, `⌘⇧W tasks`) is deleted: a shell pane's line is a command already, so there is no second meaning to explain. **(3) One footer, and it wraps.** The context row (harness/dir/branch pills, attach) sat above the editor and the action row (model, stop, vim badge) below it; both became child lists (`context_children` / `action_children`) feeding one `footer_row` under the editor, left to right: pills, attach, model, stop, badge. The row is the new `Wrap::row` (`crates/goble-ui/src/elements/wrap.rs`) rather than a `Flex::row`, so at resize the controls flow onto a second line instead of being drawn past the pane's edge — warp-new's own input footer. `Wrap` measures each child at its **own** size (an unbounded main-axis constraint, warp's `child_constraint_along_axis`) and clamps it to the row, then starts a new run when the child no longer fits; a `Flex::row` gives every child the full constraint, which is why both the strip and the footer used to overflow. The strip is a `Wrap::row` too. **(4) The Return key is drawn as an icon.** `key_icon("↵" | "⏎") -> Some("terminal-input")` in `crates/goble-ui/src/elements/shortcut_hints.rs`; every other key stays its own character — warp-new's `Icon::icon_for_key` likewise resolves exactly one key (`⏎`). Tests: `elements::wrap::tests::{items_that_fit_stay_on_one_run_in_order,an_item_that_does_not_fit_starts_a_new_run,the_first_item_of_a_run_always_stays_on_it,a_run_centers_its_items_on_the_tallest}`; `elements::shortcut_hints::tests::{the_strip_draws_a_cap_per_key_and_the_name,the_strip_wraps_instead_of_overflowing}`; `elements::chat_composer::tests::{composer_puts_every_control_in_the_footer_below_the_editor,the_footer_wraps_instead_of_overflowing_the_input}`; `views::chat_view::tests::the_composer_hints_sit_above_the_separator_and_only_when_given`; and the rewritten `ui::terminal::tests::the_shells_bottom_bar_is_the_shared_composer` (it now asserts the shell bar draws **no** label of either vocabulary) with `ui::shortcut_hints::rich_input_tests::the_agent_rich_input_carries_the_instructions_above_its_editor` (the strip's line, a rule strictly between it and the editor within 40 px, the pills under the rule, and the `terminal-input` icon). Two tests were rewritten, not weakened, because their premise was the old order: `composer_puts_the_context_row_above_the_editor_and_the_model_below_it` -> `composer_puts_every_control_in_the_footer_below_the_editor`, and the composer-level `the_hints_are_drawn_above_the_editor_and_only_when_given` -> the `ChatView`-level `the_composer_hints_sit_above_the_separator_and_only_when_given` (the strip is no longer the composer's). Non-vacuity: swapping the footer's `Wrap` back to a `Flex::row` fails `the_footer_wraps_instead_of_overflowing_the_input` (`the model label moved to a second line: 82.5 then 82.5`). Measured on the real tree at 1024x768: strip at y=604.8 (`↵` as icon, `send`, `⌘↵ new conversation`, `! shell`, `⌘K commands`, `⌘⇧W tasks`), separator at y=631 (300..1024), placeholder `Ask anything...` at y=664, footer at y=727 (harness pill, dir pill `~/Projects/goble/app`, attach, model `gpt-4o`); at 560 px the strip wraps to two lines (y=509.6 and y=532.8) and the footer to three (655/691/726), with nothing drawn past x=560. Verified with `cargo test --workspace` -> exit 0, no failures (`goble-ui` lib 356, `goble-app` lib 220, `goble-core` lib 259). Not verified: the on-screen look — the app was neither built nor launched (house rule), so every claim above is about drawn commands, and the icon is proven *emitted*, not proven to rasterise in a window (`06-renderer/ui-spec/05-chat-view.md` §Composer, §The instruction strip)
- [x] **GUI polish R10 — the wheel's sign:** the scroll direction was inverted in every scrollable surface. It is now the platform's own. `Scrollable::dispatch_event` (`crates/goble-ui/src/elements/scrollable.rs`) moves the region by the delta's **negation** (`state.borrow_mut().scroll_by(-delta)`) and `TerminalView::handle_wheel` (`app/src/ui/terminal/input.rs`) reads a **negative** delta as "the content moves up", i.e. the walk into the scrollback (`let down = delta_y < 0.0`), which is what makes a two-finger swipe up on macOS carry the transcript down. The sign is deliberately **not** platform-conditional, and that is the whole finding: winit's own contract is that the delta says how far the *content* should move and that positive is right/down (`winit::event::MouseScrollDelta`, `winit-0.30.13/src/event.rs:954-976`), and all four backends already land on it — macOS passes AppKit's `scrollingDeltaY` straight through (`src/platform_impl/macos/view.rs:669`), so the OS's natural-scrolling setting is what decides the sign there; X11 turns wheel-up (button 4) into `LineDelta(0.0, 1.0)` and wheel-down (button 5) into `-1.0` (`src/platform_impl/linux/x11/event_processor.rs:1086-1087`, and it negates a raw smooth-scroll delta the same way below at `:1162`); Wayland negates its axis values to fit the same convention (`src/platform_impl/linux/wayland/seat/pointer/mod.rs:198-210`, "NOTE: Wayland sign convention is the inverse of winit"); Windows passes the signed `WM_MOUSEWHEEL` value through unchanged (`src/platform_impl/windows/event_loop.rs:1728-1740`). So the old `scroll_by(delta)` was inverted on macOS **and** on Windows/Linux alike (a wheel-up scrolled the list down on all three), and this one negation is what makes Windows/Linux read as *normal* — a `#[cfg(target_os = "macos")]` branch, which the platform modules would allow (`goble-ui/src/platform/mod.rs`), would re-invert them. Nothing per-platform was needed at the app's single conversion point either: `crates/goble-ui/src/platform/window.rs:305-313` maps `LineDelta` × 20 px and `PixelDelta` ÷ the zoomed scale for every platform, and the one other wheel consumer, the computer-use frame (`app/src/ui/screen.rs:470-476`), forwards the raw delta to its own callback and was left alone. Tests: `elements::scrollable::tests::wheel_over_the_viewport_scrolls_and_is_consumed` (rewritten to assert both signs — `+30.0` leaves the offset at 0, `-30.0` carries it to 30) and `wheel_outside_the_viewport_is_ignored` (now sends `-30.0`, so its `offset == 0.0` is not vacuous); `ui::terminal::tests::the_wheels_sign_scrolls_the_panes_scrollback` (a real `Emulator`/`TerminalSession` fed 120 rows: `+60.0` walks back into the history, `-60.0` walks forward again); `ui::sidebar::sidebar_surface_tests::the_wheels_sign_scrolls_the_sidebar` (the real `RootView` with 40 conversations: the band really overflows, `+40.0` keeps the list at its top, `-40.0` carries the offset on, and the offset survives the per-frame rebuild) — it paints with the cursor over the band on purpose, because a wheel event carries no position and the attribution comes from the last paint, which is why `test_util::render_element` (its `PaintContext` has `cursor_inside = false`) cannot drive it. Non-vacuity: reverting `let down = delta_y < 0.0` to `delta_y > 0.0` fails `the_wheels_sign_scrolls_the_panes_scrollback` with `a positive delta walks back into the scrollback: 0`. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line; `goble-ui` lib 356 passed, `goble-app` lib 222 passed (2 new), `goble-core` lib 259 passed. Not verified: a Windows or Linux run — no such machine here, so those two platforms are argued from winit's sources above, not observed — and the on-screen feel (the app was neither built nor launched, house rule). The convention is written down in `06-renderer/renderer-architecture.md` §Input conventions
- [x] **GUI polish R11 — the workspace menu launches a terminal, and Ctrl+. opens the shortcuts panel:** two additions in one pass. **(1) The "+ ▾" menu's first entry.** The workspace topbar's environment menu (`build_add_workspace_control`, `app/src/ui/shell/topbar.rs`) listed only the environments ("Local", "Remote (xrdp)") and "New environment…", so it offered no way to launch a terminal. It now opens with **"New terminal"** (the `terminal` icon) wired to `UiActions::on_add_space` — a new space whose root pane is a plain PTY, the same workspace "+" opens — then the environments (the current one checked; choosing one **opens a new space that runs on it**, its tab landing at the end of the strip as "+" does) and the trailing "New environment…". Every item index shifts by one and the handler is written against that (`index == 0` → new terminal, `1..=n` → `UiActions::on_add_space_with_medium` with medium `index - 1`, otherwise add a new environment). **(2) The shortcuts panel.** `Ctrl+.` — grok-build's own binding for its cheatsheet — toggles a centered overlay, `app/src/ui/shortcuts_help.rs`, drawn in the panels' idiom (a header with the title and a ✕, a rule, a scrollable body) and closed by the chord again, by Escape, by the ✕ or by a backdrop click. Its content is this app's own binding table in grok-build's section shape (a heading, then a row per gesture), not a copy of grok-build's key list: **Workspace** (the global chords `root_view/element.rs` answers), **Input** (the composer's, the set the instruction strip carries) and **Palette**. Each row draws the strip's own key caps — `ShortcutHint::caps`, new in `crates/goble-ui/src/elements/shortcut_hints.rs` — in a fixed 108 px key column, so the names line up down the panel while a chord looks exactly as it does above the editor. Nothing advertises the chord yet: no palette entry, no hint row, no topbar control, so the panel is reachable only by `Ctrl+.` (the instruction was explicit that we do not tell anyone how to open it for now). Tests: `ui::shortcuts_help::tests::{the_panel_lists_the_workspaces_own_chords_and_every_route_closes_it,every_chord_the_panel_names_is_one_the_app_answers,the_panel_s_rows_are_the_chords_it_draws}`, `menu_flow::topbar_environment_menu_launches_a_terminal`, `menu_flow::topbar_environment_menu_opens_a_space_in_that_environment`, and `elements::shortcut_hints::tests::a_chords_caps_can_be_drawn_without_its_name` in `goble-ui`. The honesty rule is tested rather than asserted: `every_chord_the_panel_names_is_one_the_app_answers` dispatches every Workspace row and checks the effect its label names (⌘K opens the palette, the three split rows add a pane, ⌘W removes one, ⌘⇧W opens the tasks overlay, and both arrow rows move the focus off the active pane — each after a split in the direction that row names, since ⌘Space lays panes side by side and ⌘⇧D stacks them), and it **fails on a label it does not know**, so a new claim has to arrive with its own proof. The caps are display labels (`⌘`, `←`, `Space`, `Esc`) while a handler receives `"ArrowLeft"`, `" "`, `"Escape"` (`crates/goble-ui/src/platform/window.rs::logical_key_string`), and the test maps the two vocabularies in one place. Non-vacuity: with the `Ctrl+.` arm disabled the panel test fails at `the chord is consumed`, with the menu entry renamed the menu test fails at `the menu lists the terminal entry`, and the environment rows' test fails at `the new space runs on the chosen environment` when that row is swapped back to a plain "+"-space (`on_add_space`, which uses the *active* medium) and at `choosing an environment opens a new space` when it opens nothing at all — all confirmed, then reverted. Measured on the real tree at 1024×768: the panel is 560 × 453.4 at (232, 157.3) — centered — with its first row at y=244.5 (`⌘ K command palette`), every row's caps at x=250 and every name at x=360; the window's own height caps the panel at 90 % and the body scrolls, and the test asserts no row is drawn outside the panel's band or past the window's bottom. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` note) and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 357, `goble-app` lib 225, `goble-core` lib 259, `menu_flow` 10). Not verified: the on-screen look — the app was neither built nor launched (house rule) — and the chord arriving from a real key press; `logical_key_string` passes `Ctrl+.` through as the character `.`, which is what the handler matches, but no window was opened here to watch it land. Found while writing this, deliberately not fixed: the `Cmd+K` palette's "New space" entry advertises `Cmd+Shift+N` and nothing binds that chord (`root_view/element.rs` answers ⌘K, ⌘Space, ⌘⇧D, ⌘⇧T, ⌘⇧W, ⌘W and the four arrows) — the palette's label is stale, which is one reason the panel does not carry that row
- [x] **GUI polish R12 — the environment menu's hover tray:** warp-new's new-session dropdown shows an action sidecar beside the item under the pointer; this workspace's "+ ▾" menu offered only what a click on a row did. `PopupMenu` (`crates/goble-ui/src/elements/popup_menu.rs`) now takes a hover tray — `with_hover_tray(width, build)` builds it for the hovered item's index (`None` for an item that carries none) and hangs it from that row's top edge, `TRAY_GAP` = 4 px past the panel's trailing edge, or to the panel's left when the available width says it would leave the window — and routes an event that lands in the tray to it, consuming it so a tray click neither closes the menu nor falls through to the "outside the panel" close. Because the tree is rebuilt every frame, the row under the pointer is app-owned like the open flag: `with_hover_index(Rc<RefCell<Option<usize>>>)`, written at paint time (hover lives in `PaintContext`) and read by the next frame's layout — `UiState::env_selector_hover`, copied into the snapshot in `root_view/rebuild.rs`. `CursorMoved` now requests a frame of its own (`platform/window.rs`), so a hover-only surface is not left waiting for the idle heartbeat. The menu's environment rows carry `build_environment_tray` (`app/src/ui/shell/topbar.rs`): the environment's name and a full-width "Make default" button, which makes that environment the one new spaces start in (`MediaActions::on_select_medium`) without opening a space — clicking the row itself still opens a space on it — and closes the menu, as warp-new does; for the environment that already is the default the button is inert, muted, and carries an "Already the default" tooltip. Tests: `elements::popup_menu::tests::{hovering_a_row_shows_its_tray_beside_it, a_tray_click_fires_its_own_button_and_keeps_the_menu_open, a_tray_that_would_leave_the_window_renders_to_the_left, the_tray_survives_the_per_frame_rebuild_through_the_shared_hover_cell}` and, over the real `RootView`, `menu_flow::{hovering_an_environment_row_offers_make_default_in_a_tray, the_tray_s_make_default_does_nothing_for_the_environment_already_default}`. Non-vacuity, each confirmed and then reverted: a builder that returns `None` fails the app test at `the row and the tray beside it both name the environment` (1 ≠ 2); a forced left placement fails it at `the tray is drawn beside the row, not over it`; and dropping `with_disabled` fails the second test at `an inert button does not close the menu either`. Measured on the real tree at 1024×768 in the default state (one space): the menu's "Remote (xrdp)" row draws at x=258/y=118.5, the tray's band is 220 × 66 at (444, 110) — 4 px past the panel's right edge at 440 — its title at x=452 and the "Make default" label at x=516.5/y=145, all inside the window. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` note) and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 361, `goble-app` lib 225, `goble-core` lib 259, `menu_flow` 12). Not verified: the on-screen look — the app was neither built nor launched (house rule) — and the tray against a live pointer, since hover is read from `PaintContext`'s cursor. The overflow rule needs the menu's own position on screen, which layout does not have and a rebuilt element forgets; in the topbar the available width is unbounded (a shrink-to-content row), so there the tray always opens rightwards, where this control has room at the window widths the app opens at.
- [x] **GUI polish R13 — the working-directory and branch selectors, and the card's working directory:** the composer's directory and branch pills opened nothing. `app/src/ui/pickers.rs` is new: `DirectoryRow`/`BranchRow`, `directory_rows(cwd)` (the pane's own subdirectories plus `.. (Parent Directory)`) and `branch_rows(cwd)` (`git --no-optional-locks -C cwd branch --no-color --sort=-committerdate`, the `*` row hoisted and marked current), `shell_quote`, `change_directory_command(path)` = `cd '<path>'`, `checkout_branch_command(b)` = `git checkout '<b>'` and `PickerCache`, which lists a directory or a repository only while that menu is open and drops the listing when it closes — so a menu costs exactly one read per opening and a frame that opens nothing reads nothing. The cache lives on `RootView` beside the state (`root_view/construct.rs`) for the same reason: the tree is built while the state is borrowed, and a listing is a read of the machine. `root_view/rebuild.rs` fans the two menus out per pane — ids are absolute paths and branch names, the pane's own cwd and current branch are `.selected()` — and `make_actions.rs`'s `on_select_dir`/`on_select_branch` write the command the user would have typed into that pane's own pty (`run_terminal_command`, so the shell's own idea of where it is moves with it) and then move the pane's record: `set_pane_path` re-derives the branch (`current_branch` walks up to the repository, handling both a `.git` directory and a `gitdir:` file), and a branch selection also moves the pill at once. A pane whose shell has not started has nowhere to run `cd`, so the choice is recorded for the shell it will start and nothing is written; a branch selection there is a no-op, leaving the pill naming the branch the repository is really on. The conversation card now draws its working directory under the subject (`ConversationListItem::with_directory`, fed from the card's `entry.directory`). Tests, all over a `CaptureWriter`-backed session so the bytes are read: `terminal::tests::context_pills::{the_directory_pill_runs_cd_in_the_panes_own_shell,a_pane_with_no_shell_only_records_the_directory,the_branch_pill_checks_the_branch_out_in_the_panes_own_shell,a_pane_with_no_shell_does_not_move_its_branch}` (`06-renderer/ui-spec/05-chat-view.md`, `04-conversation-sidebar.md`)
- [x] **GUI polish R14 — the sidebar's three views, Starred, and the collapsible sections:** the sidebar had exactly one view. It now has three behind an icon-only toolbelt at its top: `Agents` (default), `Explorer` and `Search` (`ui::types::SidebarView`, with the toolbelt's tabs drawn by `view_toolbelt` in `app/src/ui/sidebar.rs` as 24×24 `HoverRow`s carrying the view's tooltip and a `Selected` band on the active one). The view is app state (`UiState::sidebar_view`) and entering a view rewinds its own list. **The agents view** is now built from two helpers: `section_header` (a 12 px chevron, the name and the row count — a click toggles it) and `conversation_card` (a card whose menu carries the star toggle). Every folder section is collapsible, `General` — the default folder of a conversation that was never filed — included, and the new `Starred` section sits above them listing the starred conversations of every environment, whichever folder they belong to; a section with nothing in it is not drawn. Collapsed keys live in `UiState::collapsed_sections` (`"starred"`, `format!("folder:{name}")`), the starred ids in `UiState::starred_conversations`, and the card marks a starred conversation with a `star-filled` icon after its subject. **The project explorer** (`app/src/ui/explorer.rs`, new) is the active pane's working directory read one level at a time by `ExplorerCache`: `children(dir)` skips hidden entries and orders directories first, then files, case-insensitively; a row is indented 16 px per level with a 12 px chevron or a 12 px gap, a 14 px folder/file-type icon and the name; a directory click expands it (`UiState::explorer_expanded`) without moving the pane's cwd; a file click opens the file in its own pane of the tab (R21, which supersedes the shell-quoted path this row first shipped). **Global search** (`app/src/ui/global_search.rs`, new) runs the real recursive search — `rg --null --line-number --no-heading --color never --smart-case --max-count 10 --max-columns 400 -e <literal> -- <root>` — only when the query changes, never per frame; exit code 1 is an answer, and ripgrep is preferred but not required (R17: a machine without it falls back to the view's own walk, so the sentence "a missing `rg` is the one error the view reports" that this row first shipped is no longer true), and the rows are grouped a file header then its matched lines, each clickable into the file's own pane. The `search` icon the sidebar draws is the toolbelt's own tab, which is why the flat-surface test's magnifier assertion now counts exactly one. Tests: `ui::sidebar::sidebar_surface_tests::{the_toolbelt_switches_the_sidebar_between_the_tree_and_the_search,a_collapsed_section_keeps_its_header_and_drops_its_cards,a_starred_conversation_is_drawn_in_the_starred_section_above_the_folders,clicking_a_tab_switches_the_view_and_the_view_opens_at_the_top}` and `ui::sidebar::sidebar_action_tests::{selecting_a_view_switches_it,a_section_key_toggles_only_that_section,starring_a_conversation_toggles_it,a_file_click_opens_the_file_in_a_pane_not_in_the_input,a_query_runs_the_search_and_an_empty_one_clears_it}` over the mounted `RootView`, `ui::explorer::tests::{a_closed_explorer_reads_nothing_and_a_root_change_drops_the_listings,a_hidden_entry_is_not_a_row_and_an_unreadable_directory_shows_nothing,expanding_a_directory_draws_its_children_under_it}`, `ui::global_search::tests::{a_query_is_a_literal_string,a_search_groups_matches_under_the_file_they_are_in}`, and in `goble-ui` `elements::conversation_list_item::tests::the_menus_star_row_stars_the_conversation_and_closes_the_menu` (a click on the drawn star row runs the toggle and closes the menu). Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 373, `goble-app` lib 256). Not verified: the on-screen look — the app was neither built nor launched (house rule) (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R15 — the rich input stays at the bottom of the pane, and its instructions lead it:** with a long conversation the input block was carried out of the pane — the pane's composer was measured at y=5964 in a 480 px view, and at y=2049 in the same view after the era's first fix (the regression test below reproduces the second figure). The transcript's outer column claimed only its content (`MainAxisSize::Min`), so the `Expanded` transcript was laid out unbounded, the column reported the whole conversation's height and everything under it landed past the pane. `crates/goble-ui/src/views/chat_view/transcript.rs`: the outer column takes `.with_main_axis_size(MainAxisSize::Max)` (`:285`), and so does the inner column that carries the terminal filter bar over the transcript (`:518`). The instructions are the head of the input, not its foot: `ChatComposer::rebuild` draws `ShortcutHints` as the input column's first child, above the context row and the editor, and `ChatView` now asks for the context pills over the editor too (`ChatComposer::with_context_above_editor(true)`, asked for by the shared bar in `app/src/ui/chat/composer.rs:82` and by the view's own composer at `crates/goble-ui/src/views/chat_view/transcript.rs:565`), so the order in a pane is transcript → [slash menu] → separator → instructions → pills → editor → action row, and switching a pane between the shell and its agent view moves nothing but the text in the editor. Tests: `views::chat_view::tests::the_composer_stays_at_the_bottom_of_the_pane_with_a_long_transcript` (40 messages in a 600×480 view: the editor is drawn in the lower half of the view and nothing is drawn past it; confirmed red by putting the column back to `MainAxisSize::Min` — `the input stays at the bottom of the pane: 2049.0005 of 480`), `views::chat_view::tests::the_composer_hints_lead_the_input_and_only_when_given` (the strip leads the input, just under the pane-wide rule; a view handed no hints draws none), `ui::shortcut_hints::rich_input_tests::the_agent_rich_input_leads_with_the_instructions_over_its_editor` over the real `RootView` (strip above pills above editor, in the shell bar and the agent view alike), `ui::terminal::tests::the_shells_bottom_bar_is_the_shared_composer`. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` note) and `cargo test --workspace` → exit 0, every target ok and no `FAILED` line (`goble-ui` lib 378, `goble-app` lib 265, `goble-core` lib 259). Not verified: the on-screen look — the app was neither built nor launched (house rule), so the pinning is proven by the drawn commands, not seen in a window (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R16 — the dotted lines over and under the text were missing-glyph boxes:** the shortcuts panel, the instruction strip above the rich input and the in-flight cues drew their symbols as characters, and the bundled faces have no glyph for the modifier, arrow and diamond symbols — so `⌘`, `⇧`, `⌥`, `⌃`, `↵`, `⏎`, `⌫`, `⇥`, `↑`, `↓`, `←`, `→` and `◆` each came out as the font's `.notdef` box, which reads as a dotted line above or below the text. Every one is now an icon: eleven new files in `crates/goble-ui/assets/icons/` (`key-command`, `key-shift`, `key-option`, `key-control`, `key-return`, `key-delete`, `key-tab`, `key-arrow-up`, `key-arrow-down`, `key-arrow-left`, `key-arrow-right`) plus `diamond.svg`, registered in `ICON_FILES`; `key_icon(key)` maps a key to its icon and `key_has_icon(key)` says whether a cap draws ink from the icon set or from its own character, and `ShortcutHint`'s caps go through it (`key_cap`, `crates/goble-ui/src/elements/shortcut_hints.rs`). The live cues stopped drawing `◆ N` as text: `build_live_indicator` (the topbar) and `TurnStatusFooter` (the pane footer) draw `Icon::new("diamond")` beside the count. Tests: `elements::shortcut_hints::tests::{a_cap_draws_a_symbol_the_text_font_lacks_as_an_icon, the_strip_draws_a_cap_per_key_and_the_name, a_chords_caps_can_be_drawn_without_its_name}` (the first checks every icon key is answered and every remaining key is one the bundled face really covers, with `font_covers`; the second that each symbolic key is drawn as an icon and never as a character), `elements::turn_status::tests::the_footer_draws_the_activity_and_the_elapsed_time_while_busy` and `ui::shell::tests::the_cue_shows_the_live_count_and_a_spinner` (the count is drawn as text and marked by a `diamond` `DrawIcon`), `root_view::tests::topbar::the_topbar_cue_reads_the_live_count_and_is_inert` (the cue's diamond is the one in the topbar band, and neither it nor the count takes a pointer event), `ui::shortcuts_help::tests::the_panel_lists_the_workspaces_own_chords_and_every_route_closes_it` (the caps are icons: `key-command`, `key-shift`, `key-arrow-*`, `key-return`, and the panel's own key filter is the same `key_has_icon`). Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 378, `goble-app` lib 265). Not verified: the on-screen glyphs — the app was neither built nor launched (house rule) — so what is proven is that no character outside the faces' coverage is drawn and that an icon command is emitted for each of these keys, not that the SVGs rasterise (`06-renderer/agent-tui.md` §11)
- [x] **GUI polish R17 — global search without ripgrep:** the search view answered a machine without ripgrep on `PATH` with `Global search needs ripgrep` and nothing else, so the feature was unreachable on a stock macOS box (`rg` is not installed there) and the message read as a defect rather than as a missing dependency. `app/src/ui/global_search.rs` now prefers ripgrep and never requires it: `search(root, query)` runs the binary when `ripgrep_binary()` finds one and otherwise falls back to `search_in_process(root, query)`, a `walkdir` walk that skips the same `SKIPPED_DIRS` (`.git`, `target`, `node_modules`, …), never reads past `MAX_FILE_BYTES` (1 MB) per file, keeps the same caps (`LINES_PER_FILE` 10 matched lines per file, 200 files), and matches a **plain substring** — case-insensitively unless the query carries an uppercase letter, which is ripgrep's own smart-case rule. The failure the view reports is now only a real one (an unreadable root), and a machine with ripgrep is still the fast path. Tests: `ui::global_search::tests::{the_search_prefers_a_ripgrep_binary_it_can_find, the_in_process_search_is_literal_smart_case_and_skips_the_noise, an_unreadable_root_is_the_reported_failure, a_search_groups_matches_under_the_file_they_are_in, a_query_is_a_literal_string}` — the second seeds a temp tree with a `target/` full of decoys and asserts the literal, case-sensitive-when-cased behaviour and the skipped directories, so the fallback is exercised whether or not `rg` exists. Verified with `cargo test --workspace` → exit 0, no `FAILED` line, and hand-checked on this machine: `which rg` exits 1 (ripgrep is not installed here), which is exactly the machine the in-process walk now serves. Not verified: the on-screen look (the app was neither built nor launched, house rule) and ripgrep's own path on a machine that has it — the preference is asserted against a located binary, not observed from a real `rg` run (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R18 — the explorer's structure, styled as warp-new's:** the project tree drew its rows as bare text, so depth, kind and the expandable state were all invisible. `app/src/ui/explorer.rs` now builds each row the way warp-new's project explorer does: the depth is a 16 px `ConstrainedBox` per level, a directory carries a 12 px `chevron-down`/`chevron-right` in a 16 px slot (a plain file gets an empty box of the same size, so the names stay in one column at every depth), then a 16 px icon — a directory's own `folder`/`folder-closed`, a file's type from `file_icon_name(name)` (`crates/goble-ui/src/elements/file_icon.rs`, backed by the 21 `file_type/*.svg` assets plus `file.svg`/`folder-closed.svg`; an unknown or absent extension is the plain document) — then the name at 14 px, `Text` for a file and `Muted` for a directory, the whole row a `HoverRow` with 8 px of horizontal and 4 px of vertical padding and a tooltip carrying the path below it, over a muted header naming the root (`Icon::new("folder")` + the display path). Nothing about the row is a card: no border, no radius, no separator. Tests: `ui::explorer::tests::{expanding_a_directory_draws_its_children_under_it, a_hidden_entry_is_not_a_row_and_an_unreadable_directory_shows_nothing, a_closed_explorer_reads_nothing_and_a_root_change_drops_the_listings}` and, in `goble-ui`, `elements::file_icon::tests::{a_known_extension_names_its_language, an_unknown_or_absent_extension_is_a_plain_document}` (each asserted name is a registered atlas icon). Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 378, `goble-app` lib 265). Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the rows are proven to be laid out at their indents with their icons and chevrons and to answer a click, not seen against warp-new's window (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R19 — the rich input's caret: the covered letter stays readable, and a click puts the beam where it landed:** two defects in `TextArea` (`crates/goble-ui/src/elements/text_area.rs`). **(1)** The block caret is an accent-`Container` drawn *over* the character it covers, so the letter under it was painted accent-on-accent and vanished; the caret now draws the covered character itself in `ColorToken::Bg` (`caret_shape(vim)` → `Bar`/`Block`/`Underline`, and the renderer draws every rect before any text, so the fill can never hide a glyph it did not paint). **(2)** A press inside the field did not move the insertion point at all. `MouseDown` in bounds now converts the pointer's x to a character index (`index_at_offset`: each character-boundary prefix is measured with `measure_text_family(..., FontFamily::System, false)` and the nearest boundary wins, clamped to the field's ends) and calls `set_caret_index`, and a field can opt into reporting the full width it was given (`with_full_width(true)`, which only the composer's editor asks for) so its hit box covers the line the user sees rather than just the ink. Tests: `elements::text_area::tests::{the_block_caret_draws_the_letter_it_covers` (the covered `h` is drawn in `ColorToken::Bg` over the block's `FillRect`), `a_press_puts_the_beam_where_the_pointer_is`, `a_field_that_does_not_claim_its_line_keeps_its_hit_box_at_the_text}`; and `ui::terminal::tests::an_active_shell_pane_keeps_the_keys_in_its_bar` still drives typing through the same field, so a click on the output above cannot steal the caret. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 378, `goble-app` lib 265). Not verified: the on-screen look — the app was neither built nor launched (house rule) — and a click placed by a real mouse, since the press is dispatched through the tree at a computed offset rather than read from the OS (`06-renderer/ui-spec/05-chat-view.md` §Composer)
- [x] **GUI polish R20 — a split opens a pane you can leave:** `⌘/Ctrl+Space` (split right) and `⌘⇧D` (split down) opened the new pane **in the agent view over no shell**: `split_active_pane` created a `PaneKind::Chat` leaf, which has no terminal behind it, so `Esc` — the gesture that leaves the harness — had nothing to return to and the pane was stuck in the agent view. The split now creates a `PaneKind::Terminal` leaf and opens that pane's harness on top of it: `app/src/actions/pane_ops.rs` gained `open_pane_harness(state, pane_id, desktop)` (sets `harness_mode`, binds the pane a conversation if it has none, `enter_agent_view`), used by `on_split_right`/`on_split_down` and by `on_set_pane_harness_mode` — the same door the badge uses, so the two cannot drift. `⌘⇧T` stays the plain pty with no harness. Test: `actions::pane_independence_tests::a_split_opens_an_agent_pane_that_can_return_to_its_terminal` — a split from the terminal pane asserts the new leaf is a `Terminal` with its harness open and a conversation bound, then dispatches `Escape` and asserts the pane is back on its own shell (and that the leaf is still a terminal, not a chat). Verified with `cargo test --workspace` → exit 0, no `FAILED` line (`goble-app` lib 265). Not verified: the on-screen feel — the app was neither built nor launched (house rule) — so the round trip is proven by dispatched keys and the pane tree, not watched in a window (`06-renderer/renderer-architecture.md` §What a split opens)
- [x] **GUI polish R21 — a file click opens the file, not a path in the input:** clicking a file in the explorer (or a search result) pasted a shell-quoted path into the active pane's draft, which is what a user types to *command* something with the file rather than what they mean by clicking it. A click now opens the file in its own pane of the current tab, which is the variant of the two the user allowed (the other — a new topbar tab — needs a modifier the mouse events do not carry: `DispatchedEvent::{MouseDown,MouseUp}` carry a position and a button and no modifiers, so `HoverRow::with_on_click` cannot tell a ⌘-click from a click). `app/src/ui/file_view.rs` is new: `FileCache::read(&[(pane_id, path)])` re-reads a file only when its length or mtime moved and keeps only the files on screen; `read()` caps a file at 4 MB and 2000 drawn lines (counting the remainder in full), and classifies it as `Lines` / `TooLarge` / `NotText` (a NUL in the first 8 KB or invalid UTF-8) / `Unreadable` (a directory or a missing path), each with its own `detail()`/`message()`; `build_file_view` wears the same topbar as every other pane, then a title row (the file-type icon, the name with the path as its tooltip, the muted detail) and a scrollable monospace body with a padded 5-digit gutter in `Muted`. `PaneKind::File { path }` is a third leaf kind (`app/src/ui/pane.rs`), so it splits, closes, restores and serialises like the others; the scroll offset is per pane (`UiState::file_scroll`, a plain non-following `ScrollState`), and `RootView.file_cache` reads only the files the active space's leaves name (`file_leaves`). Tests: `app/src/integration_testing/ui_render.rs::a_file_view_pane_draws_the_file_it_was_opened_on` (the real `RootView` over an in-memory store: the pane draws the file's `fn main()` line), `ui::sidebar::sidebar_action_tests::{a_file_click_opens_the_file_in_a_pane_not_in_the_input, a_file_click_opens_a_file_pane_and_a_second_click_retargets_it}` (`leaf_kind` is the `File` leaf and the draft stays empty), `ui::file_view::tests::{a_file_is_read_into_its_lines_and_counted, an_unchanged_file_is_not_read_again_and_a_changed_one_is, a_binary_file_a_missing_path_and_a_directory_are_reported_not_drawn, a_long_file_is_drawn_up_to_the_cap_and_counted_in_full}`. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 378, `goble-app` lib 265). Not verified: the on-screen look — the app was neither built nor launched (house rule) — and syntax colouring in the file body: the rows are plain monospace text today, which this row does not claim otherwise (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R22 — the global search runs off the UI thread:** the search view ran `global_search::search(&root, &query)` **synchronously in the query field's own callback** (`app/src/actions/make_actions.rs::run_global_search`), so every keystroke walked the pane's whole working directory on the UI thread: with no `rg` on this machine that is `search_in_process`, which reads every file under the root up to 1 MB, and a query that matches nothing reads the entire tree before it answers — the window froze for as long as the walk took, once per key. `app/src/ui/global_search.rs` now owns a `SearchWorker`: a dedicated `global-search` thread, spawned lazily on the first query (a state that never searches never starts one), fed by an unbounded `Sender<Job>`, which the field reaches through `UiState::global_search_worker`. Typing is a stream of superseded queries, so the worker **keeps only the newest**: it drains `try_recv()` down to the last job, and a walk already running gives up as soon as `latest` (`Arc<AtomicU64>`, bumped per query) no longer matches its generation — the check costs one load every `CANCEL_CHECK_FILES` (32) files. An answer is published only when it is still the newest (`Arc<Mutex<Option<SearchOutcome>>>`), and `UiState::take_global_search_result` — called once per frame right after `RootView::drain_events` — collects it only if its generation is still the field's, so what the band draws always belongs to the query in the field; the 250 ms idle heartbeat is what makes a finished walk visible without extra wake plumbing. Clearing the query cancels the walk in flight and clears rows/searched/error without queuing anything; the previous query's rows stay on screen while the new one walks, with the summary reading "Searching…", so typing never blanks the band. A machine that refuses a thread still searches: the walk then happens in line, as before. Tests: `ui::global_search::tests::{the_worker_answers_the_newest_query, a_cancelled_query_is_never_shown}` (two queries dispatched back to back yield the newest one's rows and nothing else; a cancelled query is never collected) and `ui::sidebar::sidebar_action_tests::a_query_runs_the_search_and_an_empty_one_clears_it`, which now asserts `searched == false` immediately after the keystroke and pumps `take_global_search_result` until the answer lands, so it proves the asynchronous path rather than a synchronous one. Verified with `cargo check -p goble-app` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 380, `goble-app` lib 267, `goble-core` lib 259). Not verified: the felt responsiveness in the window — the fix is proven by the search no longer being on the calling thread and by the generation gate, not by a stopwatch on a keystroke (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R23 — content ends on the input:** in the shell and the agent view the content sat at the **top** of its area with the empty space left between it and the rich input below, which is not what warp-new does: there the newest block and the last message end on the input, and the history grows upwards. `crates/goble-ui/src/elements/scrollable.rs` gained `with_bottom_anchor(bool)`: when the content is shorter than the viewport the region paints it flushed to the end (`slack = viewport − content`, since a zero scroll range cannot express it), and hit-testing follows the paint because element bounds come from it. Both surfaces use it — the agent transcript (`crates/goble-ui/src/views/chat_view/transcript.rs`, next to its `following()` scroll state) and the shell's block list (`app/src/ui/terminal/build.rs`, `with_bottom_anchor(!screen.alt_screen)`), the latter matching the raw cell grid, which already bottom-anchored its rows (`TerminalGrid::with_bottom_anchor`) and now agrees with the block view above it. A full-screen program keeps its own layout: the alternate screen is drawn where the program put its rows. Test: `elements::scrollable::tests::a_bottom_anchored_region_flushes_short_content_to_the_end` (short content in a 100 pt viewport is drawn at its end, not its start). Verified with `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 380, `goble-app` lib 267, `goble-core` lib 259). Not verified: the on-screen look of a short history in a real window (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R24 — the instructions sit over the separator, and the shell's one hint stays inside the input:** the pane's instruction strip was drawn as the **first row inside the rich input** (over the context pills and the editor), so the strip read as part of the input block rather than as the pane's chrome over it. `ChatView` now draws the strip it was handed with `with_composer_hints` between the transcript and the separator — the content's last row, padded `md`/`sm` — and `ChatComposer::with_hints` draws the entries a host keeps inside its input as that input's **last** row, under the action row. The one exception the user named is the shell bar's `⌘↵ new conversation` (`app/src/ui/chat/composer.rs`): it stays inside the input, under the editor, because it belongs to the command line being typed there, and it is the only instruction a shell pane shows — the agent pane's five entries are the strip over its separator. The empty-strip rule is unchanged: a host that passes none draws nothing, so the separator stays the input's top edge. Tests: `views::chat_view::tests::the_composer_hints_sit_over_the_separator_and_only_when_given` (the strip is under the message, over the rule, over the editor, and the rule does not move for it), `elements::chat_composer::tests::the_instructions_close_the_input_under_the_action_row` (the entry is under the editor and the editor does not move; the input grows by exactly the strip's height), `ui::shortcut_hints::rich_input_tests::the_agent_strip_sits_over_the_inputs_separator` (in the real `RootView`: the five entries are over the rule, the context pills are inside the input under it, and a running turn relabels send → queue in the same place) and `ui::terminal::tests::the_shells_bottom_bar_is_the_shared_composer`, extended to assert the shell's hint is drawn **under** the bar's separator. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 380, `goble-app` lib 267, `goble-core` lib 259). Not verified: the on-screen look of the strip over the rule in a real window (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R25 — Fork and the conversation's usage are hover affordances, and a conversation's token total outlives the run:** the footer's two affordances were `Button`s with `ButtonVariant::Ghost`, i.e. controls with a resting box and padding, which is what made Fork — and the usage disclosure beside it — read as physical buttons rather than as the reference tool's footer. **(1) The shape.** Both are `HoverRow`s now (`crates/goble-ui/src/views/chat_view/transcript.rs::build_conversation_footer`): no fill at rest, a band only under the pointer, padded `sm` horizontally and `xs` vertically, with the same icon/label pair inside each (Fork's `branch` glyph in `Text`, the usage's `activity` glyph in `Muted`). **(2) The usage slot always exists.** A conversation whose provider has reported nothing used to draw no usage affordance at all; it now draws `no usage yet` (the `activity` icon at 13 px, 11 px `Muted` text) with the tooltip naming why, and its disclosure does not open — so the footer of a conversation that has run turns reads the same as one that has not, with no number invented to fill it. **(3) The totals are durable.** `chats` gains `usage_input` / `usage_output` / `usage_cached` (`crates/goble-core/src/store/schema.rs`, in both the `CREATE TABLE` and the probe-then-`ALTER TABLE` idiom; `usage_cached` stays nullable because a provider that reports no cache accounting has no cache figure, which is not a zero). `Store::add_chat_usage(id, input, cached, output)` folds one model call into the row — a `CASE WHEN ?4 IS NULL` leaves an existing cached figure alone when the call reports none — and `Store::chat_usage(id)` reads the total back, answering `None` for a conversation nothing was reported for. `Harness::recorded_usage` (`crates/goble-core/src/harness/mod.rs`) wraps the turn's event stream in both `run_turn` and `resume_turn`, folding every `HarnessEvent::TokenUsage` into the conversation's row **before** the event is passed on: the turn's own stream is the one place a conversation's calls and its sub-agents' counts (which carry the parent's chat id) both cross, so one wrapper covers the turn's whole spend. `DesktopState::chat_usage` (`crates/goble-desktop-service/src/state/chats.rs`) exposes it to the app, and `UiState::seed_conversation_usage` (`app/src/state/ui_state/agents.rs`, called from the pane refresh at `app/src/state/ui_state/panes.rs:228`) seeds a pane's footer from the store **once** — a conversation that already holds live `chat:usage` counts keeps them, since the store's figure is that same spend and seeding it on top would count it twice. Tests: `views::chat_view::tests::the_transcript_footer_shows_fork_and_the_reported_token_usage` was rewritten into a rest/hover pair (it lays the real `ChatView` out and paints it twice — with no cursor and with the cursor over each affordance — asserting that at rest both labels are drawn and neither draws a row-height band, that the pointer over Fork draws its band with no band around the usage, that the pointer over the usage draws its own, that a click fires the fork action, and that a conversation with no reported usage draws `no usage yet` and its disclosure never opens — the previous assertion "the usage affordance is not drawn at all" is replaced, not dropped), `state::pane_session_tests::usage::{a_stored_total_reaches_the_pane_that_shows_the_conversation, a_live_total_is_never_re_seeded_from_the_store, a_conversation_that_reported_nothing_gets_no_total}` (new module `app/src/state/pane_session_tests/usage.rs`), `goble_core::store::tests::test_chat_usage_accumulates_and_stays_absent_when_unreported` (two calls fold to `1_500`/`Some(900)`/`100`; a cache-less call leaves the cached figure as it was; an untouched chat and an unknown id both read `None`), `goble_core::reasoning::tests::test_reported_usage_is_persisted_on_the_chat` (a real `run_turn` against a stub provider: once the stream is drained the counts the provider reported are on the chat's own row) and `goble_desktop_service::state::tests::chat_usage_reads_the_persisted_totals`. Verified with `cargo test --workspace` → exit 0, no `FAILED` line (`goble-ui` lib 381, `goble-core` lib 261, `goble-desktop-service` lib 37). Not verified: the on-screen look of either affordance — the app was neither built nor launched (house rule) — so the footer is proven by the drawn commands at rest and under a simulated cursor, not watched in a window; and the counts are the provider's own, which the stub providers report, so a real provider that reports no usage leaves the slot saying so; the footer's `Fork` and `no usage yet` were later seen in a running window, recorded in the R25–R27 row of `RESOLVER.md` (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R26 — no conversation without a model, and a refused prompt's only trace is the notice:** submitting a prompt on a machine with no model configured created a conversation in the sidebar and wrote the user's message into it, then answered it with a canned reply (`NO_MODEL_REPLY` / `MODEL_MISSING_REPLY` / `fallback_reply`) — three things the user asked to be rid of: a sidebar entry, a persisted message of their own, and an invented assistant answer. **(1) The submit path refuses first.** `send_agent_prompt` (`app/src/actions/prompt.rs`) — the one function `on_send_message`, `on_cmd_enter` and `on_terminal_command` share — now opens with `if !state.can_run_agent_turn(pane_id) { state.show_llm_key_banner = true; state.agent_busy = false; return false; }`, which is above the line that clears the draft, so the composer keeps the text the user typed and nothing is written anywhere. The canned replies and every `fallback_reply` branch are deleted: a refused prompt produces no assistant message at all, so no transcript row and no store row can describe one. **(2) What "can run" means.** `UiState::pane_agent_model(pane_id)` resolves the model a turn on that pane would run — the pane's own choice, then the window-global selection, then the configured default — and `can_run_agent_turn(pane_id)` is a non-empty key **and** a provider **and** a resolved model; `llm_notice_heading()` names what is missing: `No API key configured` for an empty key, `No model configured` for a key with no provider/model behind it (`app/src/state/ui_state/panes.rs`). **(3) The band is the reason and the fix.** `UiSnapshot` carries that heading (`app/src/ui/snapshot.rs`, filled once in `root_view/rebuild.rs`, read by `app/src/ui/chat/agent.rs`) into `build_agent_error(app, actions, heading)` (`app/src/ui/chat/overlays.rs`), which is now the heading and the `Edit API keys` button and nothing else — the redundant "Failed to use agent. Configure your API keys before using agent." sentence is gone, so the band the user sees when they submit is exactly the reason. **(4) Nothing else creates a conversation either.** With nothing runnable, `on_create_submit` (`app/src/actions/make_actions.rs`), `on_cmd_enter`, `on_terminal_command` and `open_pane_harness` (`app/src/actions/pane_ops.rs` — a pane that owns no conversation is refused and the banner is set, rather than opening an agent view on a thread nothing could answer) all decline; a shell pane asked for an agent view keeps `view = Terminal`, so it stays on its own shell. **(5) Stale bindings do not survive a restart.** `restore_panes` (`app/src/state/ui_state/spaces.rs`) drops any restored `pane_sessions[*].conversation_id` the store no longer holds, so a pane whose conversation was deleted follows the sidebar selection instead of owning a thread that is gone. **(6) A chat deleted out of band leaves the sidebar without a restart.** `DesktopState::list_chats` (`crates/goble-desktop-service/src/state/chats.rs`) intersects the in-memory cache with the store's live id set, falling back to the cache only when the store cannot be read. **(7) The database was cleaned**, which is what the user asked for and what made the remaining empty conversations disappear: the store held 41 conversations, 39 of them holding no assistant message or only a canned no-model reply from the era this row removes, so those were deleted (backup `~/.goble/goble_store.sqlite.bak-20260914-020029`) leaving **2 conversations / 4 messages** (`145b58f9… Demo · Custom renders`, `d21522c2… Demo · Hot reload`), with `pragma integrity_check` ok, no orphan messages, and `pending_asks`/`pending_commands`/`missions`/`reasoning_steps` empty. Tests: `integration_testing::chat_flow::{a_prompt_with_no_model_is_never_persisted, a_prompt_with_no_model_creates_no_conversation}` (the real `RootView` over an in-memory store: a submit with no model leaves `conversations` empty, writes no `chat_messages` row, keeps the draft, and draws the band), `ui::terminal::tests::a_prompt_with_no_model_creates_no_conversation`, `ui::chat::overlays::error_notice_tests::the_error_notice_is_the_reason_and_the_button` (the band's drawn texts are exactly `["No API key configured", "Edit API keys"]`), `state::pane_session_tests::{a_panes_own_model_wins_over_the_windows_selection, a_turn_runs_only_with_a_key_a_provider_and_a_model}` (the key/provider/model matrix and both headings; a pane's own model wins over a later window pick), `state::pane_session_tests::{a_restored_pane_keeps_its_conversation, a_restored_pane_does_not_point_at_a_conversation_the_store_lost}` and `goble_desktop_service::state::tests::a_chat_deleted_out_of_band_leaves_the_list`. Five tests that encoded the old behaviour were **rewritten, not weakened**, because their premise was that a prompt is persisted whatever the machine's configuration: `actions::pane_independence_tests::{send_routes_to_active_pane_conversation, cmd_enter_appends_to_the_pane_conversation, terminal_command_appends_to_the_pane_conversation, first_agent_turn_in_a_pty_workspace_creates_its_conversation}` now configure a runnable model first (the module gained `configured(&state)` — provider `mock` plus a key, returning a `tokio::runtime::Runtime` the test enters — and `wait_for_messages(desktop, conv, count)`, a bounded poll for the turn's rows on its background task, since `run_chat_turn_with_pane_session` spawns), and `ui::terminal::tests::the_shells_bar_runs_its_draft_as_a_command_and_cmd_enter_opens_the_agent_view` configures the model before Cmd+Enter so the refusal does not eat the draft it asserts about; `integration_testing::first_run_flow` asserts the sidebar stays empty after a refused prompt. Verified with `cargo test --workspace` → exit 0, no `FAILED` line (`goble-app` lib 276, `goble-ui` lib 381, `goble-core` lib 261, `goble-desktop-service` lib 37). Not verified: the on-screen look of the band in a real window (the app was neither built nor launched, house rule) and a real provider's behaviour — the refusal is proven by what the store and the drawn commands contain after a submit, not watched on a screen; the band and the two surviving conversations were later seen in a running window, recorded in the R25–R27 row of `RESOLVER.md` (`06-renderer/ui-spec/05-chat-view.md`, `06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R27 — the files tab draws its directories from the file set:** the explorer's directory rows drew `folder-closed` — the sidebar toolbelt's own folder shape — in `Muted`, while a file drew its `file_type/*` icon in `Text`, so a directory read as a second, greyer family sitting in the same column as the file icons. `crates/goble-ui/assets/icons/folder.svg` is now warp's folder glyph (16×14, one shape for both states; the row's own chevron carries open/closed, which is why the closed variant is not needed in the tree) and `app/src/ui/explorer.rs` draws it at the row's own colour — `ColorToken::Text` for a directory exactly as for a file, the `Muted` special case removed — with each row's hover band rounded (`ROW_RADIUS = 4.0`, through the new `HoverRow::with_corner_radius` in `crates/goble-ui/src/elements/hover_row.rs`, which paints with `fill_rounded_rect` only when the radius is non-zero, so every other `HoverRow` in the app keeps the square band it had; the element's own lock is `elements::hover_row::tests::a_row_with_a_corner_radius_paints_a_rounded_highlight`, which asserts the rounded command for a radiused row and the square one for a row without). `folder-closed` still names the toolbelt's Explorer tab (`app/src/ui/sidebar.rs`), which is the one place a closed-folder shape means something. Test: `app/src/integration_testing/ui_render.rs::the_files_tab_draws_directories_from_the_file_icon_set` — the real `RootView` over an in-memory store, the active pane's session path pointed at a temp directory holding `src/` and `main.rs`, rendered headless with `sidebar_view = SidebarView::Explorer`: it filters the frame's 16 px `DrawIcon` commands and asserts both the directory's `folder` and the file's `file-rust` are drawn **and in the same colour**, so the two families cannot drift apart again. Verified with `cargo test --workspace` → exit 0, no `FAILED` line (`goble-app` `ui_render` 16). Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the rows are proven drawn from the file icon set at one colour, not seen against warp-new's window; the tree was later seen in a running window, recorded in the R25–R27 row of `RESOLVER.md` (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R28 — the files tab's rows take the reference tree's one colour, and its columns finally line up:** this **supersedes R27's colour rule** — that row settled which glyph a directory draws but left every row's name and icon in `ColorToken::Text` and its chevron in `Muted`, so a row was two colours and hover changed nothing. warp-new gives a tree item **one** colour for the whole row: `ItemHighlightState::text_and_icon_color` (`app/src/ui_components/item_highlight.rs`) is the theme's `foreground()` when selected, else `blended_colors::text_main(theme, background)` — the font colour at 90%, `crates/octomus_core/src/ui/theme/color.rs:467-474` — while the pointer is over the row, else `text_sub` (60%) at rest, and `render_item_with_hover` (`app/src/code/file_tree/view.rs:1771-1905`) paints the chevron, the icon **and** the label with it, inside a container padded 4 top/bottom and 8 left/right whose highlight is `background_color` (`internal_colors::fg_overlay_2` hovered, `fg_overlay_4` selected, none at rest) with `corner_radius` 4. `app/src/ui/explorer.rs` now draws all three parts of a row in `ColorToken::Muted` at rest and `ColorToken::Text` while the pointer is over that row — our theme's ~60% and 90% font colours, the token mapping otherwise unchanged — and the list has no spacing between rows (`.with_spacing(0.0)`, the reference's `UniformList` in `render_file_tree`, `view.rs:2622-2690`). **Hover is the previous frame's**, because the tree is rebuilt every frame and those colours are decided in `layout`, before the `paint` where hover is known: `HoverRow` gained `with_hover_key(cell: Rc<RefCell<Option<String>>>, key)` (`crates/goble-ui/src/elements/hover_row.rs`), the app passes `UiState::explorer_hover` — one app-owned cell, the same paint-time shape as `env_selector_hover`, cloned into `UiSnapshot` — plus its path per row, a row under the pointer writes its path there, and a row the pointer has left **clears its own key**, so the cell always names the row the pointer is on and never keeps one highlighted after the pointer is gone. The one-frame lag is invisible: the move that reaches a row already asks for the frame that applies it. **The columns were never actually drawn**, which is what made the one-colour rule look wrong in the first place: the indent, the gaps and a file's empty chevron slot were `ConstrainedBox`es around `Empty`, and a `ConstrainedBox` reports its *child's* size — an `Empty` is 0×0 — so the tree never indented (a file one level in drew its name at the root's x, measured) and a file's icon sat a chevron's width (12 pt, measured) left of a directory's at the same depth. A blank column is now an `Empty` sized explicitly (`explorer::fixed`), a directory's chevron is inset to the middle of its 16 pt slot, and the drawn row is the reference's recipe: 16 pt of indent per level, a 16 pt chevron slot + 4 gap, a 16 pt icon slot + 8 gap, padding 4/8, a 14 pt name. Tests: `app/src/integration_testing/ui_render.rs::the_files_tab_draws_directories_from_the_file_icon_set` — reworked, not weakened (its old assertion, "a directory's icon is drawn in the same colour as a file's", is exactly the rule this row replaces) — the real `RootView` over an in-memory store, the active pane's session path pointed at a temp tree holding `src/lib.rs` and a root `main.rs`, `src` open, rendered headless over five frames — one at rest, then the report-and-draw pair for each hovered row: with no pointer every row's chevron, icon and name is `Muted` and no row draws a band; the cursor moved onto the file (a frame to report it, a frame to draw it) turns **that row's name and icon** `Text` and gives it a band in the hover colour with radius 4, while both sibling rows stay `Muted` and band-less; the cursor then moved onto the directory turns its chevron, folder glyph and name `Text` and puts the file row back to `Muted`, so the cell is proven to hold one row rather than an accumulation; and the geometry is pinned — one level is 16 pt, a file's icon is in a directory's column, the name starts 24 pt (icon slot + gap) after the icon, and the hovered row's band is the icon slot plus its 4 pt padding either side, grown by the label's line box, spanning the tree's whole width rather than hugging the row's content. `elements::hover_row::tests::a_row_reports_its_hover_through_the_app_cell` locks the element: a row the pointer is away from names nothing, the row under it names itself in the app's cell and paints the rounded band, and it clears its own key once the pointer leaves. Both tests were shown to fail with the cell write disabled, so neither passes for an unrelated reason. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note) and `cargo test -p goble-app --lib` → `276 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`, `cargo test -p goble-app --test ui_render` → `16 passed; 0 failed`, `cargo test -p goble-ui --lib` → `387 passed; 0 failed` (the whole `cargo test -p goble-app` run is green too). Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the colour rule and the geometry are proven by the frame's drawn commands at rest and under a simulated cursor, not seen in a window; the drawn row is 24.8 pt tall rather than the recipe's 24, because the label's own line box is 14 pt at the framework's 1.2 line-height ratio (16.8), a fraction taller than the 16 pt icon slot — warp-new's inline text uses the same 1.2 ratio, so its rows are this tall as well, and the label's line height was deliberately left alone; and ignored (gitignored) entries are still drawn like any other row — the reference italicises them at light weight from `render_state.is_ignored` (`app/src/code/file_tree/view.rs::render_item_with_hover`), and our tree has no ignore data at all, so that stays a known difference rather than a rule invented here. The same bug shape this row fixed was then found and fixed in the search view: `app/src/ui/global_search.rs` indented a matched line's number under its file row with a 14 pt `ConstrainedBox` around an `Empty`, which is the 0-width box this row explains, so every matched line's number sat under the file's *icon*; it is now an `Empty::new().with_size(vec2f(14.0, 0.0))`, the column the file row's own icon occupies, so the numbers line up under the file name (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R29 — the focused text field's caret is blue:** every focused text field drew its insertion point in `ColorToken::Accent` — the bar, block and underline shapes all (`crates/goble-ui/src/elements/caret.rs`) — and `TextInput` / `SearchInput` drew their focused ring in that same token (`crates/goble-ui/src/elements/text_input.rs`, `search_input.rs`), but the app's accent is deliberately neutral: `AccentColor::Blue => ColorU::hex(0x9a9a9a)` with the comment that the UI is intentionally grey (`crates/goble-ui/src/theme.rs:242-243`). So the caret and the focused ring were **grey**, where the reference apps' input cursor is blue. **What the reference's colour is, and where it comes from.** warp-new's default theme kind is `ThemeKind::Dark` (`app/src/themes/theme.rs:50`, `#[default]`), whose `dark_theme()` sets the accent to `#19aad8` (`app/src/themes/default_themes.rs:262-273`), and the colour its editor draws the cursor in **is that same accent**: `default_cursor_colors` (`app/src/editor/view/mod.rs:1595-1601`) returns `theme.cursor().on_background(theme.background(), MinimumAllowedContrast::Text)`, and `WarpTheme::cursor()` (`crates/octomus_core/src/ui/theme/color.rs:131-133`) is `self.cursor.unwrap_or(self.accent())` — the theme's `cursor` field (`crates/octomus_core/src/ui/theme/mod.rs:592`) is `None` for every bundled theme, and the only call sites that set one are the iTerm2 and Alacritty importers, which carry the user's own cursor colour (`app/src/settings/import/iterm_parser.rs:149`, `alacritty_parser.rs:297`) — so on a bundled theme the fallback is the accent. Against the dark theme's `#000000` background that accent's contrast already clears the `Text` minimum, so `on_background` hands it back unchanged. The reference's focused input ring is the same accent (`app/src/code/editor/element.rs:49`, `app/src/workspace/view.rs:20075`), so one value covers both. **What changed.** `crates/goble-ui/src/theme.rs` gains one token, `ColorToken::Focus`, backed by a new `ColorScheme::focus` field, documented where the value comes from: dark `#19aad8`, midnight `#19aad8` (same neutral-dark family), light `#00c2ff` (the reference's `light_theme()` accent, `app/src/themes/default_themes.rs:275-286`). `caret.rs` paints all three shapes in `ColorToken::Focus`, and because every field is built on `caret`/`caret_beam`, the blue reaches `text_input.rs`, `text_area.rs`, `search_input.rs` and the `chat_composer`'s editor at once; the focused ring in `text_input.rs` and `search_input.rs` is the token too, and the unfocused ring stays `ColorToken::Border`. **The app's neutral accent was deliberately left alone**: `AccentColor::Blue` is still `#9a9a9a` and every button, badge, running-indicator, proposal row and tab that reads `ColorToken::Accent` is byte-for-byte unchanged — the user asked for the input cursor, not a re-themed UI. No caret blinks (the app has no frame timer for one) and a blurred field still draws neither the caret nor the ring. One file outside the assigned list, reported as allowed: the `Cmd+K` palette's own query box (`app/src/ui/palette.rs:381`) hand-builds its field box with the accent ring and is focused for as long as the palette is open, so its ring moved to `ColorToken::Focus` with the others. **Tests.** `elements::text_input::tests::the_focused_field_paints_its_caret_and_ring_in_the_focus_blue`, `elements::search_input::tests::the_focused_field_paints_its_caret_and_ring_in_the_focus_blue` and `elements::text_area::tests::the_focused_field_paints_its_beam_in_the_focus_blue` all paint-and-inspect `RenderCommand`s in the crate's existing style: the focused field's caret is a `FillRect` in `app.theme.color(ColorToken::Focus)` at the caret's own `CARET_WIDTH` × `CARET_HEIGHT`, its ring is a `StrokeRect` in the same colour, and the blurred field has neither with its ring still `ColorToken::Border`; `elements::chat_composer::tests::the_focused_editor_shows_the_focus_blue_caret` asserts the same for the rich input's editor through `paint_composer`, and `theme::tests::focus_is_the_reference_blue_and_not_the_accent` pins the value and asserts it is not the accent. They are asserted against the token, not the hex, so they do not depend on the chosen value — and they were shown **red on the old colour**: with the three usages temporarily put back to `ColorToken::Accent` the run was `383 passed; 4 failed; 0 ignored` on those four tests (the `theme` test stays green, as it should — it locks the token), and green again with them restored. No existing test asserted `Accent` for a caret or a focus ring, so none was rewritten, weakened or deleted. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note), `cargo test -p goble-ui --lib` → `387 passed; 0 failed`, `cargo test -p goble-app --lib` → `276 passed; 0 failed`. Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the blue is proven by the drawn render commands and never seen in a window, and the value is read from the reference's own source (`#19aad8`) rather than sampled from its screen, so a reference build that carried a theme with its own `cursor` override would differ (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R30 — the file panel is coloured by the file, and "Edit API keys" opens `config.toml`:** three corrections in the file pane's neighbourhood. **(1) The notice band's button.** `build_agent_error` (`app/src/ui/chat/overlays.rs`) still called `UiActions::on_config_llm_key`, so the band that says "no API key configured" opened the model-provider overlay — a form — although the keys and the models live in the home's `config.toml` (`crates/goble-core/src/app_home.rs::GobleHome::config_path()`, the same file `DesktopState::open_default` loads and the model form saves to, and the file the Settings→Models page names at `app/src/ui/settings.rs:523`). The band's button now asks for that path and opens it in a file pane. **(2) The pane drew every line as plain text.** R21 left this unclaimed in so many words ("syntax colouring in the file body: the rows are plain monospace text today"), so a `config.toml` opened from the explorer read as one flat block. **(3) The explorer's module doc** still claimed a file row "puts its path in the active pane's input, quoted", which R21 had already replaced with a pane. **The config path, and how it reaches the band:** one new action, `UiActions::on_open_config_file` (`app/src/ui/actions.rs`), implemented in `make_actions` (`app/src/actions/make_actions.rs`): it resolves the app's own home through `goble_core::app_home::GobleHome::locate()` and hands `home.config_path()` to `open_file_pane` (`app/src/actions/pane_ops.rs`) — the very route the explorer's `on_explorer_file_click` takes, so the file opens **beside** the pane that asked (`SplitDir::Horizontal`), focused, and a pane already showing a file takes the new path over instead of splitting again. The band clones that action; a machine whose home cannot be resolved logs and opens nothing rather than guessing a path. **What happened to `on_config_llm_key`:** it is untouched, implementation and all, and `on_close_llm_dialog`/`on_save_llm` with it — but the band was its only surface, and nothing else in the tree opened the model form (`grep -rn on_config_llm_key app/src` now finds it only in `first_run_flow`'s three tests), so **the model-provider overlay has no opener in the UI any more**; it is reachable only through the action. That is the honest reading of the request, and it is written into the action's own doc comment rather than left to be discovered. **The highlighting:** `FileCache` (`app/src/ui/file_view.rs`) now caches a `FileContent { body, runs }` — the raw lines **and** the runs — under the one size + mtime stamp it already keyed on, so a frame that changes nothing re-reads and **re-highlights** nothing; the runs live beside the body rather than inside it only because a run carries a click callback and so is neither `PartialEq` nor `Eq`, which `FileBody` is. The lines are highlighted by the file's own path through the one shared call every other surface uses, `goble_ui::syntax::highlight(&shown.join("\n"), path)` — `.toml`, `.rs`, `.md`, `.json`, `.sh` all resolve through it and an unresolvable type returns `None`, which the pane reads as "draw the text plain" — and only the lines the pane actually shows are handed over, so `MAX_LINES` (2 000) bounds the highlighting exactly as it bounds the body, with `MAX_FILE_BYTES` (4 MB) bounding the read before it. Painting reuses the app's one text model rather than a second one: a line's runs go through `Code::with_highlighted_lines` (the element the chat's code blocks and read excerpts already paint runs with, mono and unwrapped), while a line with no runs keeps the plain `Text` row it had, and the 5-digit gutter, the per-pane scroll and the title row are untouched. The explorer's stale paragraph now says what a file row does. **Tests.** `ui_render::a_file_pane_draws_a_toml_files_lines_as_highlighted_runs` (the real `RootView` over an in-memory store, the active pane pointed at a temp `config.toml`): the line `api_key = "sk-test"` is drawn as **more than one command** whose concatenation is exactly the line, in more than one colour, while the same text in a `notes.zzz` draws as **exactly one** command holding the whole line. `ui_render::the_notice_bands_edit_api_keys_opens_the_config_file_in_a_pane` mounts the real tree with the banner on, finds the button's own drawn origin, clicks it (down/up), and asserts the active pane's kind is `PaneKind::File { path }` on `GobleHome::locate().config_path()` **and** that `llm_dialog_open` is still false. `ui_render::an_explorer_row_click_opens_the_file_in_a_pane_on_the_right_headed_by_its_name` drives the path #3 asked about end to end — a click on the explorer's **row label** (the 14 pt "main.rs" the tree draws), not on the action: the pane opens as the right half of a `SplitDir::Horizontal` split of the pane that was active, it is the focused one, and its own header row draws the file's name at 13 pt with the `file-rust` icon in the pane, right of the sidebar. `ui::file_view::tests::a_files_lines_carry_the_runs_of_its_own_type_or_none_at_all` locks the cache: `config.toml`, `main.rs`, `notes.md`, `data.json` and `deploy.sh` each carry one run set per shown line (with `config.toml`/`main.rs` colour-multi), `notes.zzz` carries none; `an_unchanged_file_is_not_read_again_and_a_changed_one_is` now also asserts the **same `Rc`** comes back on an unchanged read and a different one after the file is rewritten, which is what "highlighting is not per frame" means. `crates/goble-ui/src/syntax.rs::tests::a_file_path_resolves_by_its_extension` extends its direct assertion to `~/.goble/config.toml`. Non-vacuity, all confirmed then reverted: with the pane's `highlight` call stubbed to `None`, `a_file_pane_draws_a_toml_files_lines_as_highlighted_runs` fails at "a TOML line is drawn as its runs" (`[("api_key = \"sk-test\"", …)]`) and both cache tests fail; with the band's button put back on `on_config_llm_key`, the band test fails at "the model-provider overlay must not open"; with `open_file_pane`'s `SplitDir::Horizontal` flipped to `Vertical`, the explorer test fails at "the file view opens beside the pane, not over it" (`left: Vertical`). Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note), `cargo test -p goble-app --lib` → `277 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (+1), `cargo test -p goble-app --test ui_render` → `19 passed; 0 failed` (+3), `cargo test -p goble-ui --lib` → `387 passed; 0 failed` (its one extended test), and `cargo test -p goble-app --test first_run_flow` → `10 passed; 0 failed` for the untouched model-dialog action. `rustfmt --edition 2021 --check` reports no diff inside any new hunk (each touched file's pre-existing formatting debt is left alone). Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the colours are proven by the frame's own draw commands (run boundaries and colours) and never seen in a window, and there is no assertion that a given token is *this* hex; "the pane's header names the file with its type icon" is the file view's **title row** (icon + name + size, the path on hover), drawn under the pane's shared topbar — the topbar itself is the chrome every pane wears (the 3-dots tray and the close button) and was deliberately not given a second copy of the name, so a reader who means "the topbar" should read this as a known difference rather than a fix; the model-provider overlay now has no UI opener, as reported above; and the highlight is syntect's, so what a `.toml` looks like in the theme is that grammar's colouring applied to our theme's foregrounds, with no side-by-side against the reference tool (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R31 — the settings panel answers the arrow keys in the content pane, not only in the rail:** the settings overlay was two halves that disagreed about the keyboard. `build_nav`'s rail was wrapped in a `KeyHandler` that swallowed `Escape` (close) and `ArrowUp`/`ArrowDown` (step the category through `on_settings_category_step`, wrapping) and let modified arrows through, but the content pane answered **nothing**: every switch, button, text field, theme swatch and the `ColorWheel` could only be reached with the mouse, and because the pane's controls were built ad hoc there was also no list to notice when a control was added and silently left unreachable. **The model** (documented in `app/src/ui/settings.rs`'s module comment): there are **two focus regions**, the rail and the pane, and `UiState::settings_focus` says which holds the keyboard; `UiState::settings_pane_focus` is the pane's slot and `UiState::settings_pane_field_active` says whether the focused control is a text field the caret has been put into. All three live in `UiState`, are copied into [`UiSnapshot`] once per frame (`app/src/ui/snapshot.rs`, filled in `app/src/root_view/rebuild.rs`) and survive the rebuild. Opening the overlay starts in the rail (`on_settings_open`); changing the category resets the pane's slot to `0` (`settings_select_category`). The key map: `Up`/`Down` move inside whichever region has the keyboard (the rail steps the category and wraps, exactly as before; the pane steps its slot and clamps), `Right` moves the rail into the pane onto its first control, `Enter`/`Tab`/`Space` do the same from the rail and inside the pane **activate** the focused control, `Left` walks back to the rail from a control with no discrete value and steps one back through a control that has one (the stepper, the theme-channel swatches) as `Right` steps forward, `Escape` closes the overlay from the rail and otherwise steps out one level at a time. Modified arrows (`ctrl`/`command`/`alt`) are still never the overlay's — they are the app's pane navigation — so the existing passthrough test stays true. **Text fields.** A field only holds the caret once `Enter`/`Space` has put it there (until then it is just the focused row, drawn with the ring); while it holds the caret the overlay reserves **`Escape` alone** — letters, `Space` and `Backspace` must reach the field — and `Escape` then takes the caret out (the ring stays), a second one leaves the pane and a third closes the overlay, so a field the user typed into is never a trap. Which keys are deliberately the overlay's is a comment at the `KeyHandler` (`settings.rs`, `KeyHandler::new(panel, …)`). **One focus order, built from what the pane draws:** `SettingsControl` (new, in `app/src/ui/types.rs`) names every interactive row; `pane_controls(category, groups, open_group)` is the one list both sides agree on — the keyboard resolves the focused slot through it and each pane walks it while building its rows through `FocusOrder`, whose `take` asserts the row being drawn is the control the order holds at that slot and whose `finish` asserts the pane drew them all, so a control added to a pane without joining the order fails a test rather than being unreachable. Activation goes through the actions that already own each behaviour (`on_toggle_dark_mode`, `on_toggle_vim_mode`, `on_toggle_auto_approve`, `on_choose_workspace`, `on_reload_model_config`, `on_set_scroll_speed`, `on_set_font_size` — the last one also drives the shared `ui_zoom` cell — and `on_set_theme_target`), so a click and the `Enter` key are one implementation; a text field taking the caret and the Environment rows are `UiState::{settings_activate_control, settings_adjust_control}`. The focused control wears a `ColorToken::Focus` ring (`focus_ring`, the same blue the focused field's caret and ring already use); the rail's selected category is ringed in it only while the rail holds the keyboard. `ColorWheel` is a focus stop but holds no discrete value (no hue math at the action layer, which has no theme or `AppContext`), so `Left` there returns to the rail and `Enter` does nothing — stated in the module comment and in `SettingsControl::has_discrete_values`. Tests, all in `app/src/ui/settings_category_tests.rs` and driven through the real `RootView` (`press` lays out, paints and dispatches one key): `the_arrows_move_the_region_that_holds_the_keyboard` (Down/Up on the rail step the category; `Right` enters the pane on its first control; Down steps the pane's own order and leaves the category alone; Down at the last control clamps; `Left` on the switch — no discrete value — returns to the rail with the overlay open; `Tab` goes back in; a category change resets the slot), `enter_flips_a_switch_and_the_setting_is_persisted` (`Enter` on Vim mode flips it, `DesktopState::get_vim_mode()` reads the flip back **from the service/store**, and a second `Enter` flips it back), `left_and_right_adjust_the_focused_stepper` (`Right`/`Left` step `settings_scroll_speed` and keep the keyboard in the pane; `Right` on the switch above does not toggle it) and `a_pane_text_field_takes_the_caret_and_escape_steps_back_out` (`Enter` puts the caret in the group-name field, `p`,`r`,`o`,`d` and `Space` reach the draft, and `Escape` gives the caret up, then leaves the pane, then closes the overlay). `categories_match_grok_build_labels` (5 → 7, in order) and the two original navigation tests (`the_arrow_keys_move_the_category_and_escape_closes`, `a_modified_arrow_does_not_move_the_category`) are unchanged and still true. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note), `cargo test -p goble-app --lib` → `283 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (+6, all of them the tests above; the 12 tests in `ui::settings_category_tests` are green), `cargo test -p goble-core --lib` → `262 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`, `cargo test -p goble-desktop-service --lib` → `38 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`, `cargo test -p goble-ui --lib` → `387 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the focus ring, the rail's ring and the caret are proven by the state the dispatched keys leave behind and by the drawn tree, not seen in a window; the wheel's hue cannot be nudged from the keyboard (a deliberate stop, above); and `FocusOrder`'s assertions are `debug_assert!`s, so a pane that dropped a row is caught in a test build and not in a release one (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R32 — Settings → Environment: groups of secrets, persisted and readable, and transmitted nowhere:** the user asked for a settings menu named "Environment" holding **groups of secrets** to be sent to the server when working remotely. Nothing of the sort existed: the `credentials` table is a flat name/value list (and the encrypted store is `vault_secrets` behind `CredentialVault`), and a group is its own thing — a name, a membership and a lifecycle — so it did not become a `credentials` row under a prefix. **Placement:** `SettingsCategory::Environment` (label `Environment`) sits directly after `Agent & Approval` and before `Models` — that page is where the user picks Local or Remote, so the secrets belong next to the choice that makes them matter, ahead of the model and machine-config pages — and `SettingsCategory::ALL` (always-read order, asserted by `categories_match_grok_build_labels`) is now the seven: Appearance, Mouse, Editor & Input, Agent & Approval, Environment, Models, Advanced. **Store.** Two `STRICT` tables in the existing `CREATE TABLE` block (`crates/goble-core/src/store/schema.rs`): `secret_groups(id, name UNIQUE, created_at, updated_at)` and `secret_group_entries(id, group_id, name, value, created_at, updated_at, UNIQUE(group_id, name))` with an index on `group_id`; values are stored the way `credentials` stores them — **plaintext**, said so in the module comment of `crates/goble-core/src/store/secret_groups.rs` rather than implying encryption, and neither the `credentials` table nor the vault sheet (`app/src/ui/vault.rs`) was touched. The API, in that module and in the store's own insert style (caller-supplied id and timestamp): `create_secret_group`, `list_secret_groups` (groups ordered by name, each with its entries ordered by name, a group with none carrying an empty `Vec` rather than being absent), `get_secret_group`, `delete_secret_group` (entries removed explicitly, no cascade, so the two tables cannot disagree; returns whether a group was there), `upsert_secret_entry` (the `(group_id, name)` pair is unique, so adding a name twice edits the entry's value), `delete_secret_entry` (by id) and `delete_secret_entry_named` (by name, used when an edit renames an entry). **Service.** `crates/goble-desktop-service/src/state/environment.rs` adds `DesktopState::{environment_groups, environment_group, create_environment_group, delete_environment_group, save_environment_secret, delete_environment_secret}` (ids via `uuid::Uuid::new_v4()`, timestamps via `Utc::now().to_rfc3339()`, the file's neighbours' conventions; `create_environment_group` refuses a blank name). **Pane.** `build_environment` (`app/src/ui/settings.rs`) creates a group by name, lists the groups with their secret count, opens one (its row highlighted through `HoverRow::with_selected`), adds a secret from a name field and a value field, loads an entry back into those fields to edit it (activating its row) and removes an entry or a whole group on its own trash control; the values are never drawn (the row shows `••••••`). Every one of those rows is a slot in the pane's focus order, so the whole surface is usable from the keyboard through R31's model (the two fields take the caret on `Enter`, and the tests type into them with the keys). The pane's state lives in `UiState` (`settings_environment_{groups,open_group,group_draft,secret_name,secret_value,editing}`) with `refresh_environment_groups` / `environment_{create,open,delete}_group` / `environment_{save,delete}_secret` / `environment_edit_secret`, reloaded from the store by `refresh_settings` (`app/src/state/ui_state/settings.rs`) and carried to the view in `UiSnapshot`. **The transmission — the honest part: nothing sends these groups anywhere, and the pane says so.** The only remote construction site in the tree is `DaemonModel::run_turn`'s `Route::Remote` arm (`app/src/daemon/state.rs`), which calls `build_remote_turn` and hands the `HarnessTurn` to a `DaemonClient`; but no client is ever attached in production — `app/src/runtime.rs` builds `DaemonModel::new(Arc::clone(desktop)).with_pane_session(...)` and `with_remote` appears only in one test — so a remote-routed turn fails with "no remote daemon client is configured" before the turn is built, and there is no remote-server protocol in this tree. The groups were therefore **not** wired into a wire type (a dead field on `HarnessTurn` would have implied a transport that does not exist); they are persisted, exposed on the accessor the remote path would read (`DesktopState::environment_groups`), and stop there, which is written in that accessor's own doc comment and in `environment.rs`'s module comment: "No server side exists yet … these groups are read by the settings pane and stop there." Tests: `goble_core::store::tests::test_secret_groups_roundtrip_and_lifecycle` (empty at first; two groups listed by name; three entries upserted across them; `upsert` on an existing `(group_id, name)` edits the value rather than duplicating; `delete_secret_entry_named` reports the first removal and `false` for the second; deleting a group takes its entries with it and the second delete reports `false`; the entries and the `name` uniqueness are read back), `goble_desktop_service::state::tests::environment_groups_are_persisted_and_lifecycle_is_complete` (the service's create/list/get/delete and its refusal of a blank name, asserted against the store's own rows through `state.store.lock().get_secret_group(..)`), and in `app/src/ui/settings_category_tests.rs` `the_environment_pane_creates_adds_and_deletes_through_the_store` — the real `RootView` over an in-memory `DesktopState`: `Tab` into the pane, type `prod`, `ArrowDown` to `Create group`, `Enter`, and the assertion is `desktop.environment_groups()` holding one group named `prod` with the pane on it; then `ArrowDown` past the group's row and its delete control to the secret-name field, `Enter`, type `API_KEY`; `ArrowDown`, `Enter`, type `sk-1`; `ArrowDown` to `Add secret`, `Enter`, and the store now holds one entry `API_KEY` = `sk-1` with both fields cleared; activating the entry's row loads `API_KEY`/`sk-1` back into the fields for editing; its delete control empties the group while the group stays; the group's own delete control leaves `environment_groups()` empty and no open group. `the_environment_rows_answer_the_mouse_as_well_as_the_keys` clicks the group's drawn label and asserts the same open the key performs, so the two paths are one implementation. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note), `cargo test -p goble-app --lib` → `283 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (2 of them this pane's), `cargo test -p goble-core --lib` → `262 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (+1), `cargo test -p goble-desktop-service --lib` → `38 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (+1), `cargo test -p goble-ui --lib` → `387 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. **Transmission, in one line: no — the groups are persisted locally in plaintext and read back by the pane and by `DesktopState::environment_groups()`, and nothing in this tree sends them to a server**, because the remote route errors before a turn is built and no remote client is attached anywhere. Not verified: the on-screen look — the app was neither built nor launched (house rule) — so the pane's rows, highlights and rings are proven by the drawn tree and the store's rows, not seen in a window; the values are stored unencrypted by design (the vault is untouched and still the encrypted store); and no remote server was contacted, for the reason above (`crates/goble-desktop-service/src/state/environment.rs`)
- [x] **GUI polish R33 — a hover chip is drawn over whatever covers it:** a `Tooltip` painted itself **inside its child's own paint pass**, so any sibling painted later drew over it: the sidebar's tab chip was covered by the pane to its right, and a pane header's chip by the panes and the sheets that follow it in the root `Stack`. There was no deferred draw in `PaintContext` (only `renderer`, `cursor_position`, `cursor_inside`) and no cross-tree z-index, so no element could paint after the tree. `crates/goble-ui/src/elements/hover_chip.rs` is new: `HoverChipRegistry::push(origin, element)` queues a chip, `HoverChipLayer` is an `Element` whose layout takes no room and whose `paint` **takes the whole vec before painting anything** (`mem::take`), so an empty registry costs one `RefCell` borrow and no allocation, and nothing can survive into the next frame. The registry hangs off `AppContext` (`hover_chips`, shared through its `Rc`, so the clone the window hands each frame reaches the same one), and `RootView` owns a `HoverChipLayer` field painted **last** — after `self.element`, i.e. after the panes, the topbar and every sheet/dialog (`app/src/root_view/{mod.rs,construct.rs,element.rs}`). `Tooltip`'s paint now computes the same panel it always did (centred, above/below with the same 6 pt gap, clamped to ≥ 0) and queues it instead of drawing; none of its four app-side call sites changed. Tests: `elements::hover_chip::tests::{a_hovered_chip_is_painted_after_the_sibling_that_covers_it,a_frame_with_nothing_hovered_queues_nothing,a_hovered_chip_is_drawn_once_and_not_into_the_next_frame}`, `elements::tooltip::tests::{tooltip_queues_its_chip_only_while_hovered,tooltip_never_draws_its_panel_in_its_own_paint_pass,tooltip_chip_keeps_its_place_below_the_child}` (the last two replace the test that asserted the inline draw, which is the behaviour this item removes) and the app-level `root_view::tests::hover_chip::a_hovered_sidebar_tab_chip_is_the_windows_last_paint`, which hovers a real sidebar tab in a real `RootView` and asserts **command order** — the chip's box and text come after the last thing the sidebar and the pane beside it paint — rather than a text snapshot. Non-vacuity was checked by reverting each half and watching exactly those tests go red. Verified with `cargo check --workspace --all-targets` → exit 0, `cargo test -p goble-ui --lib` → `394 passed; 0 failed`, `cargo test -p goble-app --lib` → `288 passed; 0 failed`. Not verified: the on-screen result — the app was neither built nor launched (house rule) — and only the sidebar-tab path has an app-level order test; the topbar's, the file view's, the composer's and the explorer's chips ride the same mechanism and the same geometry, but no per-surface assertion was added (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R34 — the rich input's controls are cards with a grey border, and no drop-down arrows:** the footer controls of the rich input (the working-directory pill, the harness pill, the branch pill, attach `+`, the model button and stop) were **bare** — no resting outline, a fill only under the pointer — and three of them carried a `chevron-down` glyph. `ComposerButton` (`crates/goble-ui/src/elements/composer_button.rs`) is the one place all six are boxed, so the change is there and nowhere else: at rest a 1 px outline in `ColorToken::Border` at the theme's own radius (`app.theme.radius_px()`, 8.0 on the default theme — the same radius `Button` and the composer's card use), with **no fill of any kind**; under the pointer the same rect fills with `ColorToken::Hover` and keeps its outline, so the fill sits inside the card. The hover fill's radius was a hardcoded `6.0`; it is the theme radius now, so the border and the fill share one shape. Height (28 pt) and horizontal padding (10 pt) are unchanged, and every control still dispatches on its whole card rect, so the trays open from a click anywhere on the card. The `chevron-down` is gone from the working-directory pill, the model button and `ContextPill` (`context_pill.rs`), which removes it from the harness and branch pills; the control's own glyph stays. One deliberate opt-out: the proposal card's candidate rows (`chat_composer/proposal.rs`) keep `.with_outline(false)` — they are full-width list rows inside the proposal's own card, not footer controls. Tests: `elements::chat_composer::tests::{a_rich_input_control_is_a_card_that_fills_on_hover,the_rich_input_controls_draw_no_drop_down_chevron}` (the first asserts the border colour, width, theme radius and the absence of any `Hover` fill in a resting frame, then the fill over exactly the card's rect in a hovered one; the second asserts the frame's drawn icon names are exactly `["agentmode", "folder", "git-branch", "plus", "sparkle"]`), and `composer_puts_every_control_in_the_footer_below_the_editor` / `composer_renders_harness_dir_branch_pills` were rewritten from `strokes == 0` to assert four and three cards with those three properties. Verified with `cargo check --workspace --all-targets` → exit 0, `cargo test -p goble-ui --lib` → `394 passed; 0 failed`, `cargo test -p goble-app --lib` → `288 passed; 0 failed`. Not verified: the pixels — the GUI was not built or launched — so the card and its hover are proven by the drawn commands, not seen; and the stale comment in `app/src/integration_testing/menu_flow.rs:84` about the composer pills reusing `chevron-down` was corrected later, in R41 (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R35 — a tab is named for what it holds, and its close ✕ appears only under the pointer:** every tab was created as `Space {count}`, a name that says nothing about the pane inside it. `Space` (`app/src/ui/pane.rs`) gained `pub named: bool` **beside** `name`, and `UiState::space_label` (`app/src/state/ui_state/spaces.rs`, whose doc comment is the written rule) derives the label a frame keeps current through `refresh_space_labels()` (called from `app/src/root_view/rebuild.rs`): a tab the user renamed by hand keeps the typed name — `rename_active_space` is the only thing that sets `named`, and the derivation never takes it back; a terminal-only tab reads the focused pane's working directory written the way `~` means it (the composer's `display_path`), so a tab opened on a path reads that path; an agent tab reads the conversation's subject (the sidebar card's title) or **`New Agent`** while that conversation has none; a file-view tab reads the file's path; a tab holding both kinds reads its **focused** pane. Construction is explicit — `Space::new` marks named (fixtures), `Space::unnamed` does not (every workspace the app opens, `+`, the environment menu's, and the fallback when the last tab closes) — and `#[serde(default)]` reads older layouts as never-named, which is honest: a name typed before this change cannot be told from a derived one, so it is re-derived. Nothing in `topbar.rs` changed: `Space::name` **is** the label the chip draws. Along the way a real defect fell out: the double-click timestamp was a local of `make_actions`, rebuilt every frame, so the second click always saw `None` and the inline rename could never open — it is `space_click_at` in app state now, and clicking two different tabs quickly is no longer a double click. The second half of the ask: `WorkspaceChip` (`app/src/ui/shell/topbar.rs`) paints and offers its close control **only while the pointer is over the tab** (`ctx.hovered` at paint; the event is forwarded to the close only when its position is inside the tab), while `layout` keeps the close's slot reserved, so the strip's tabs do not change width under the pointer and the drop targets stay put. Tests: four in `ui::shell::tests` (terminal `~` → `~/Projects/goble` → `/tmp/elsewhere`; agent `New Agent` → `Demo · Hot reload`; a split tab reading its focused pane; a rename driven end to end through the real double-click, `Backspace`, typing and `Return` with a frame between events), each asserting the **drawn** label in the topbar band so the composer's identical path pill cannot satisfy it, and `a_click_on_the_close_control_closes_its_space` was rewritten as `the_close_control_appears_on_hover_and_closes_its_space` (unhovered: no `x-close` icon in the frame; hovered: it is drawn, and a click at its centre closes that space and does not select it). Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line. Not verified: the on-screen look, and how an over-long path reads in a tab — the label is not clamped (`06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R36 — a folder row in the explorer no longer spells its own path on hover:** the files tree put a path tooltip on **every** row, so hovering a directory drew a chip repeating what the row already says — and, before R33, that chip was covered by the pane beside it, which is how the user saw it. `app/src/ui/explorer.rs` now builds the tooltip only for a file (`if !is_dir { row = Tooltip::new(row, file_hint)… }`), with the path cloned before the click closure takes it; a directory's tooltip is gone and its row is otherwise untouched. Verified with `cargo check -p goble-app --all-targets` → exit 0 and the workspace run above. Not verified: the on-screen result (the app was not launched).
- [x] **GUI polish R37 — a file opened in a pane is editable:** a file pane was strictly a reader: `FileCache` read the file when its size or mtime moved, `build_file_view` drew a gutter and one `Code` row per line, and there was no way to type into it. The ask: "files opened in a panel must be editable". `crates/goble-ui/src/elements/text_area.rs` now carries a multi-line mono buffer as well as the single-line field the composer uses (`with_multiline`, `with_line_numbers`, `with_line_height`, `with_line_runs` over a new `LineRuns`, `with_anchor`; `multiline_key`/`move_caret`/`vertical_index`/`replace_selection`/`index_at_point`/`gutter_width`, plus `slice_runs`/`runs_code` so the highlighted runs survive editing) — single-line behaviour is untouched, `multiline` defaults false, and a command/control chord returns `false` so a shortcut is never typed as a character. `app/src/ui/file_view.rs` gains the state machine: `FileBuffer { text, saved, read_only, caret, selection, status, note, stamp }`, `read_only_reason(content)` (`total > shown` = capped at `MAX_LINES` → "too long to edit", `TooLarge` → "too large to edit", `NotText`, `Unreadable`), a dirty rule of `text != saved` (so a keystroke typed and typed back out is not unsaved), and `save()` which **refuses** a read-only buffer with a message rather than writing a truncation — the rule lives in the state machine, not in whether an editor was drawn, and a capped file's buffer holds no text at all, so a partial buffer cannot exist. Save is `fs::write(path, text.as_bytes())` under the shared `FileKeys` wrapper (Cmd/Ctrl+S); success clears the marker and says `saved`, a failure says why and changes nothing. `FileCache::settle` runs per frame per pane: a buffer that just wrote drops its cache entry; equal stamps change nothing (text, caret, selection kept); a stamp that moved while the buffer is **dirty** keeps the buffer and says "changed on disk — your unsaved changes are kept"; a clean one reloads and clamps the caret; one that can no longer be edited replaces itself with a fresh read-only buffer, so the pane falls back to the read body. Buffers for panes that no longer show a file are dropped. The field takes keys only while its pane is active (`active_pane_id == pane_id && buffer.focused()`, the composer's own mechanism), so a file pane split beside a terminal takes no key from it, and the R29 blue beam/focus ring come from the shared elements. `UiSnapshot` carries `file_buffers` (shared `Rc<RefCell<FileBuffer>>` handles, so text, caret and markers survive the per-frame rebuild). Tests: `ui::file_view` 11 (`a_file_is_read_into_its_lines_and_counted` and the cap/missing/binary tests kept; the new ones cover insert/delete/newline, dirty set and cleared, a save writing the exact bytes, a capped file refusing to save with nothing on disk, and an external change while dirty keeping the buffer), `elements::text_area` 16 (5 new), and `ui_render::{a_file_pane_is_typed_into_and_saved, a_file_too_long_to_edit_says_so_and_saves_nothing}` driving the real `RootView`. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line. **Not implemented, stated plainly:** undo/redo (the shared element has no undo outside vim mode and a per-keystroke snapshot of up to 4 MiB is not cheap — Cmd+Z is inert, never typed), Cmd+Left/Right (claimed by pane navigation in `root_view/element.rs` before the tree), no confirm-on-close for a dirty buffer, and no clipboard keys. Not verified: the on-screen feel — the app was neither built nor launched — so typing, saving and the markers are proven through the real tree, real key events and real temp files, not seen in a window (`06-renderer/ui-spec/06-file-view.md`)
- [x] **GUI polish R38 — a pane dragged onto the tab strip becomes a tab:** the strip could reorder its tabs, but a pane had no way out of the grid. `app/src/state/ui_state/pane_move.rs` is new: `PaneDrag { pane_id, source_space, title, origin, position, drop_index }`, `PaneMove`, the pure reshape `move_pane_to_new_space`, and `UiState::{begin_pane_lift, drag_pane_to, drop_pane}`, with the drag state mirrored into `UiSnapshot` (the tree is rebuilt every frame, so nothing lives on an element). A press lifts a pane only when it lands in the top `TOPBAR_HEIGHT` band of the pane, **only when the content did not handle it** (so the 3-dots tray and the ✕ win) and only when no divider drag and no other lift is in flight; the body is never a handle, which keeps text selection and the terminal's own mouse reporting intact. `SpaceDropStrip`/`PaneDragGhost`/`TabRect`/`insertion_index` in `app/src/ui/shell/topbar.rs` draw the ghost (a `SurfaceRaised` card with an `Accent` border, radius 6, capped at 200 pt, 12/6 offset from the pointer, drawn only after 4 px of travel so a plain header click never flashes it), highlight the strip (`Selected`) and draw a 2 pt `Accent` insertion bar at the index the pointer names — the stop counting drawn tab centres left of the pointer, so the drop follows what the user can see. Releasing over the strip moves the pane into its own tab there; releasing anywhere else cancels, changing nothing. The reshape: out of a split → the split collapses and the survivor takes the space; out of a single-pane space → that space is consumed; out of the **last** space → that space resets in place to a fresh empty chat pane, exactly the path `on_close_space` uses, so the program never exits; the moved pane becomes the focused pane of its new tab. **Identity is preserved by construction**: the move relocates the very same `Pane::Leaf { id, kind }`, so `pane_sessions` (conversation id, draft, path), `pane_runtime`, `pane_chat_scroll`, `file_buffers` and the `TerminalRegistry` PTY keyed by pane id all follow. Contested presses are settled deterministically: the topbar strip wins over the body (it is a later child of the root `Stack`) and a split divider wins over a lift (`SplitNode` consumes the press). Tests: 9 unit tests in `state::ui_state::pane_move` (the four reshape cases, the index left/between/right, the cancel, and a terminal pane keeping its kind and its path across the move), `ui::shell::tests::the_drop_index_counts_the_drawn_tab_centers_left_of_the_pointer`, and `ui_render::{a_pane_dragged_onto_the_tab_strip_becomes_its_own_tab, a_pane_lifted_onto_the_strip_is_dropped_off_it_with_no_change}`, which drive real `MouseDown`/`MouseMove`/`MouseUp` through the real `RootView`. Not implemented: no other drop target (onto a pane, the sidebar or the body cancels), no live preview inside the grid, no cross-window drag; and a release over the strip's own painted rect only — a release over `+ ▾` or the settings icon cancels rather than appending a tab. Not verified: the visual result on a GPU and a live PTY surviving the move at runtime — identity is structural (the id and its registry entry are never re-created) and asserted, but nothing was opened in a window (`06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R39 — settings is a smaller sheet in Grok Build's flat style, built to be walked from the keyboard:** the overlay was "the window minus an inset" (nearly the whole surface), its rows were card-like, and `Enter` did not mean "modify". `app/src/ui/settings.rs` now opens a **640 × 520** sheet (`PANEL_WIDTH`/`PANEL_HEIGHT`, clamped by the non-inset `Dialog` to the viewport and by `ConstrainedBox::with_max_height` to `min(520, 0.9 × viewport)`): `NAV_WIDTH` 148 + a 1 pt `RULE_WIDTH` rule + 2 × 12 pt padding + a 467 pt content column, so 640 = 148 + 1 + 24 + 467. 148 fits the longest label ("Agent & Approval") on one 12 px line; 467 holds the longest row unwrapped; 520 sits above the 360 a short window allows, so a short window shrinks the sheet instead of clipping it and the backdrop stays clickable on every side. Measured, then asserted: at 520 pt the scroll region is 406.8 pt — a 61 pt header band and a 52.2 pt footer band plus two 1 pt rules are the fixed furniture and the region takes exactly what they leave, and those bands are constant across the four tested sizes, with `clip.height == panel.height − bands`. A real defect surfaced while measuring: the body `Flex::row` defaulted to `MainAxisSize::Min`, so the page was laid out with an **infinite** max width, the scrollable's clip came out `inf` wide and every `Spacer` collapsed — meaning label-left/value-right rows did not actually put the value on the right; it is `MainAxisSize::Max` now. The interior follows Grok Build's list idiom rather than Warp's: flat rows split by `Divider` rules, a muted heading per group, no cards, the focused row's only ring in `ColorToken::Focus`, keys drawn as `ShortcutHint` caps, and a **footer built from the same `footer_hints` list the tests read**, so the drawn promise and the handler cannot drift. The keyboard: `KeyHandler` claims keys only with no modifier held (so pane navigation and the window chords are untouched); in the rail `↑↓` page (wrapping), `↵` opens the page, `Esc` closes; in the pane `↑↓` move (clamped), `→`/`←` step a discrete row, `↵` **activates** — a toggle flips, a stepper advances, a choice row selects its value, a list row opens, a text field takes the caret and `↵` again commits, `Esc` cancels the edit and **keeps the sheet open**, a button row runs — and `Esc` from the pane returns to the rail, so the ladder is field → pane → rail → closed. New actions `on_settings_commit_field`/`on_settings_cancel_field` and `settings_field_edit_start` on `UiState`. Tests: `settings_category_tests` 18 (`the_sheet_is_a_compact_sheet_whose_width_adds_up`, `a_long_page_scrolls_inside_the_sheet` with 14 groups overflowing, `the_focus_order_walks_down_the_rows_the_page_draws`, `the_footer_says_what_the_focused_row_answers`, `enter_advances_a_stepper_and_selects_a_choice`, `the_window_chords_still_answer_while_the_sheet_is_open`, `the_sheet_still_answers_the_mouse`, and the field caret/cancel test rewritten) plus the ui_render wheel test rewritten for the new bands. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line. Not verified: the on-screen look (not launched), the Models page specifically (every page shares one `Scrollable`, but overflow was exercised through Environment only) and density variants (geometry asserted at the default density) (`06-renderer/ui-spec/07-settings.md`)
- [x] **GUI polish R40 — the `Ctrl+.` shortcuts panel can be searched:** the panel (`app/src/ui/shortcuts_help.rs`) was a fixed list of sections with no filter, no highlight and no empty state. It now has a filter line under the title (the input's look: a `Surface` fill, a `ColorToken::Focus` ring, a muted `Filter shortcuts` placeholder and the blue caret beam) and `filtered_sections(filter)` **wraps `sections()`**, so the table stays the single source of truth and the panel still never promises a chord nothing answers. The matching rule: the filter is lower-cased and split on whitespace, every term must match at least one of the row's haystacks (multi-word is AND, and the terms may land in different haystacks of the same row), where the haystacks are the row's name, each cap as drawn, each symbol cap's **words** (`⌘`→"cmd command", `⇧`→"shift", `↵`→"return enter", arrows→their names, …), the caps joined (`⌘⇧t`) and the same chord joined in words (`cmd shift t`) — so `split`, `palette`, `esc`, `k`, `⌘⇧t` and `cmd k` all narrow the list sensibly. A section heading is drawn only while it still has a matching row, and nothing matching draws `No shortcuts match` rather than an empty box. Keys: `Ctrl/Cmd+/` clears the filter and the highlight, a printable character appends, `Backspace` deletes, `↑`/`↓` step the highlight (clamped to the visible rows, `PageUp`/`PageDown` by 10), `Esc` clears a non-empty filter first and closes only when it is already empty, `Enter` is a consumed no-op (there is no row action to invent, and consuming keeps it out of the composer); `Ctrl+.` still toggles the panel exactly as before, and reopening clears the filter, the highlight and the scroll. A new app-local element `ShortcutList` measures the items it lays out so the highlighted row is scrolled into view inside the shared `Scrollable`, whose viewport is capped at `LIST_MAX_HEIGHT = 360.0` because a content-sized panel hands its list an unbounded height. The footer names the keys with `ShortcutHint` caps (`⌘/` clear the filter, `↑↓` move, `Esc` clear / close). State lives in `UiState` (`shortcuts_help_{filter,index,scroll}`) and is copied per frame, so a burst of key events between two frames cannot be lost. One deliberate difference from Grok Build, said here because it is a deviation: there `Ctrl+/` toggles a search **mode**; this panel's filter is the focused field whenever the panel is open, so the chord means "clear the filter" (`app/src/ui/shortcuts_help.rs`). Tests: `ui::shortcuts_help` 10 (7 new: typing narrows; a cap, a name and a chord all match; a heading with no match is dropped and the empty state appears; `Esc` clears then closes; `Ctrl+.` toggles and reopening resets; `Backspace` edits; the highlight stays in range and the list scrolls it into view). Verified with `cargo check -p goble-app --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line. Not verified: the on-screen look, `Cmd+/` on a real window (the tests press Ctrl+/), and the wheel scroll (`06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R41 — the pane topbar drops its two drawers and gains the `◆ N` work chip:** the pane's 3-dot menu carried `Scheduled tasks` (the crons sheet) and `Toggle panel` (the right-hand side panel) — two entries that opened drawers instead of acting on the pane. Both are gone from `menu_items` and from `with_on_select` (`app/src/ui/chat/header.rs`), the remaining arms renumbered `0 Copy, 1 Restart, 2 Rename, 3 Clear transcript, 4 Fullscreen`, and the stale item list in the doc comment rewritten. Neither surface is stranded, which was checked before removing anything: the crons sheet is on the ⌘K row `Open scheduled tasks` and `/scheduled` (and the right panel's own add), and the right panel is on the macOS menubar's **View → Toggle Right Sidebar (⌘B)**, the ⌘K row `Toggle right sidebar` and `/sidebar`. In their place, left of the dots, sits `[ rule ] [ ◆ N ]` built by `build_work_chip`: a 32 pt `Divider::vertical()` (the header's inner row is the bar minus its 2 pt padding, so a 36 pt rule would push the header past `TOPBAR_HEIGHT`), an 8 pt `diamond` and the count at 12 pt, both in `ColorToken::Accent`, spaced like the app-level `build_live_indicator` — the shape the user asked for by name ("a diamond and the number of tasks/workflows/sub-agents, in the accent colour, exactly as in Grok Build"). It is information only: it carries no handler, so a press passes through to the header and lifts the pane. At zero it draws a 0×0 `Empty` — no rule, no diamond, no count, no hit target — so an idle pane's bar is byte-for-byte what it was. **What the number counts, and what it cannot:** `UiState::pane_live_work(pane_id)` (`app/src/state/ui_state/agents.rs`) reads that pane's `pane_runtime` and returns one item per in-flight tool call (`kind: task`, the call's name) and per **running** sub-agent child (`kind: sub-agent`, `type · description`; a finished child stays in `sub_agents` for the transcript but is filtered out), each with a `Tooltip` line, so the number is never a mystery. Deliberately **not** counted: `LiveWork::executions` (app-wide worker executions keyed by trace id carry no pane — folding them in would be a lie, and the app-level `◆ N` still shows them), durable tasks/workflows/crons (registered app-wide, nothing attributes them to a pane, so there is no dead `Workflow` variant), and the pane's own busy turn, queued prompt or pending approval (that is the pane's foreground, already on the turn-status footer and the composer). Tests: `ui::chat::header::tests::{the_tray_carries_the_panes_own_actions_and_no_drawer,the_shifted_rows_still_run_the_action_they_draw,the_work_chip_draws_the_count_and_vanishes_at_zero,the_work_chip_counts_and_names_the_panes_own_work,each_pane_counts_only_its_own_work,the_work_chip_takes_no_press_of_its_own}`, driven through the real tree and asserted on drawn commands (the second catches index drift by clicking the drawn `Clear transcript` row; the fifth gives a two-pane split `1` and `2` in their own halves). Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test --workspace` → exit 0, no `FAILED` line. Not verified: the on-screen look (not launched); a background space's panes are not mounted so they draw no chip, unchanged from before (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R42 — the sidebar card's dots stay inside the sidebar, and `View all` wears the text colour:** the conversation card's 3-dot control was already hover-gated (`hover || menu_open` over the whole card rect), but it was a flex child at the end of the row, so a long title reported an oversized text column and pushed the dots off the card — and the user read that as "the dots do not appear on hover and they stick out of the sidebar". `crates/goble-ui/src/elements/conversation_list_item.rs` now places the dots as a **fixed 16 × 16 box** (`DOTS_SIZE`) laid out `SizeConstraint::tight(16, 16)` and positioned explicitly in `paint` at the card's trailing edge, so no title or menu can move it; the card's single root became `row` (avatar + text, painted under a clip), `menu` (painted below the row, clipped only by the card so Delete still reaches its edge) and `dots`, with the band fill moved into `paint` because the row is clipped at the gutter while the band is not, and the two nested clips pushed and popped in balance. The geometry rule, with its arithmetic written in place: the card's trailing edge sits on the sidebar's own `SpacingToken::Xs` padding (4.0 at the default scale), so `app/src/ui/sidebar.rs` passes `dots_inset = CARD_DOTS_INSET − padding = 5.0 − 4.0 = 1.0` and `dots.max_x() == sidebar_width − 5` at **every** sidebar width and in both states; the row clip ends at `card_right − (1 + 16 + 6) = sidebar_width − 27`, so the title is cut before the dots. The `View all (N)` / `Show less` button now draws its label **and** its border in `ColorToken::Text` (new `Button::with_border_color`, whose `Default` arm falls back to `ColorToken::Border`, so Primary and Ghost are untouched), keeping its variant, square corners, height, padding and click. Tests: `ui::sidebar::{the_cards_dots_follow_the_pointer_on_and_off_the_card,the_cards_dots_stay_five_pt_inside_the_sidebar_at_every_width_and_state,the_view_all_button_draws_its_label_and_border_in_the_text_colour}` (widths 200/300/480 × collapsed/expanded × hovered/menu-open, asserting the measured right edge rather than a snapshot) and `elements::conversation_list_item::the_dots_are_pinned_to_the_trailing_edge_clear_of_the_title`; two regression probes (removing the inset, restoring the flex-cursor placement) were run and reverted to prove the tests bite. One honest note: `~/Projects/warp-new` has **no** conversation-card element (its `octomusui_core` has no `conversation_*`), so this surface follows the repo's own idioms and the reference could not be consulted. Also worth recording: the hover behaviour the user saw as broken was a test-harness gap — `seeded_view` wrote `state.conversations` without registering the per-conversation `AgentCardUi` cells that `refresh_conversations` registers in the app, so every rebuilt card lost the pointer; the new app test registers them first. Verified with `cargo check --workspace --all-targets` → exit 0, `cargo test -p goble-ui --lib` → `400 passed; 0 failed`, `cargo test -p goble-app` → `412 passed; 0 failed`. Not verified: the visual result — no GUI was built or launched — so the 5 pt is proven against painted `RenderCommand`s, not seen; and the 5 pt is derived from the default spacing scale only (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R43 — the rich input's inset matches the pane's content margins, and its cards are less round:** the composer carried a gutter of its own. `ChatComposer::rebuild` wrapped its card in `Padding::new(card, EdgeInsets::new(md, sm, md, md))` and gave the card `EdgeInsets::new(md, md, md, sm)` — 12 pt outside on three sides and 8 on the right, plus 12/12/12/8 inside — so the input's own content sat ~20–24 pt in from the pane's edge while the conversation beside it starts at 16 (a transcript row's `xs` margin plus a message bubble's `uniform(md)` padding), and the right and left gutters did not even match each other. The ask: the rich input's inset should fit the chat/terminal area's margins, i.e. be much smaller, and the controls should be less rounded. Now the card is `EdgeInsets::uniform(md)` and the block's own margin is `EdgeInsets::uniform(xs)` (`crates/goble-ui/src/elements/chat_composer/composer.rs`), so the editor's text column starts exactly where a message's text does — 4 pt from the pane's edge plus 12 pt of card padding on both sides — and the block's vertical gutter is 4 pt instead of 12. The roundness: `crates/goble-ui/src/elements/composer_button.rs` gains `pub const COMPOSER_CONTROL_RADIUS: f32 = 4.0`, used by the card's own outline and hover fill and re-exported from `elements.rs` for the composer's card, in place of the theme's `radius_px()` (8.0) — at 28 pt tall the controls read as cards rather than as pills. The composer tests that asserted the theme radius now assert `COMPOSER_CONTROL_RADIUS` (`elements::chat_composer::tests::{composer_puts_every_control_in_the_footer_below_the_editor, composer_renders_harness_dir_branch_pills, a_rich_input_control_is_a_card_that_fills_on_hover}`), which keeps them biting: the assertions are on the drawn radius, so a control that went back to the theme's radius fails them. Verified with `cargo test -p goble-ui -p goble-app --no-fail-fast` → **exit 0, 812 passed / 0 failed** (`goble-ui` lib 400, `goble-app` lib 326 plus its 13 integration binaries), and seen in the running window: the input's cards now sit at the pane's margin, the two context cards and the editor line up under the conversation's text, and the cards' corners are the smaller radius. Not verified: the same geometry at a non-default spacing scale (the change is expressed in `spacing_px` tokens, but only the default scale was exercised) (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R44 — a starred conversation is listed once, and a card's actions open in a tray anchored to the 3-dot control:** two asks. (1) A conversation pinned to `Starred` was also listed again under its folder, so the same card appeared twice. `agents_view` (`app/src/ui/sidebar.rs`) now reads the pinned ids first and drops them from the environment-filtered list the folders group (`visible`), so each conversation is drawn once, in the section the user put it in; the Starred section still spans every environment, as before. The empty state moved with it: it is drawn only when `visible` **and** `starred` are both empty, so a list that is entirely pinned no longer prints `No conversations for this environment.` while its cards are on screen, and `View all (N)` counts the folder rows it actually reveals. (2) The card's 3-dot control opened its actions as rows **inside** the card (`layout` added the menu's height to the card, so opening it also pushed the cards below it down). It now opens the same card the rich input's controls open beside themselves: `ConversationListItem`'s `menu` became `Rc<RefCell<Box<dyn Element>>>` — a `Container` of `SurfaceRaised` + the `Border` hairline + radius 6 + `uniform(8)` padding (the `PopupMenu` card) around a column of the star and delete rows — laid out loose at `TRAY_MAX_WIDTH` (168) with its measured size kept in `menu_size`; `layout` no longer adds it to the card's own size, so opening a tray moves nothing. `paint` places it through the new `menu_bounds()` — its trailing edge on the control's, its top `TRAY_GAP` (4) under the card's bottom edge so it covers none of the card's own text, clamped to the card's edges — and queues the same handle into the frame's hover-chip layer, so the cards painted after it cannot cover it: `HoverChipRegistry::push_shared` is new and `ChipPanel::{Owned,Shared}` lets the layer draw a panel its owner keeps (a `Tooltip` still queues `push`'s owned box). `dispatch_event` routes the events that land on the tray — and on the 4 pt strip between it and the card, so a pointer walking down to the tray is not read as leaving the card — into the tray's own rows, and a pointer there still counts as on the card: without that the tray put itself away before the pointer arrived, and a press on it fell through to the card's own select. Verified with `cargo test -p goble-ui -p goble-app --no-fail-fast` → **exit 0, 817 passed / 0 failed** (`goble-ui` lib 402, `goble-app` lib 329 plus its 12 integration binaries), including the new `ui::sidebar::sidebar_surface_tests::{a_starred_conversation_is_listed_under_starred_only, the_cards_tray_hangs_from_its_control_above_the_cards_after_it}` (the second drives a real `RootView`: pointer onto the card, release on the dots, then asserts the tray is a rounded `SurfaceRaised` card under the dots and trailing-edge-aligned with them, that its runs come after the next card's, that the pointer on the tray and on the strip keeps it open, and that its star row stars the conversation) and `elements::conversation_list_item::tests::hover_renders_dots_and_menu_renders_delete` (extended: the tray is drawn outside the card's own band and inside its width). `the_sidebar_is_flat_square_and_free_of_separators_and_the_magnifier` was **rewritten, not weakened**: the band's one allowed rounded card is the open tray (a rounded fill that holds one of the tray's own rows, required to be drawn while a tray is open), and every other rounded fill in the band still fails it. Not verified: the on-screen look — the session was told to stop driving the app (see [`RESOLVER.md`](RESOLVER.md) §Blockers), so the tray's placement is proven by the drawn commands only (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R45 — the rich input hugs its rows, its shortcut strip sits lower, and the environment pill is replaced by the cloud chord:** the second half of the same punch list. The block's vertical gutter is gone: `ChatComposer::rebuild` now gives its card `EdgeInsets::new(md, xs, md, xs)` — the `md` that lines the editor's text up with the conversation's text column stays on the sides, the vertical is `xs` — and the block's own margin is `EdgeInsets::new(xs, 0.0, xs, 0.0)`, so the first row is 4 pt under the block's top edge and the last is 4 pt above its bottom, where it was 16 (`crates/goble-ui/src/elements/chat_composer/composer.rs`). The strip of shortcut caps the agent pane draws (the view's own, `crates/goble-ui/src/views/chat_view/transcript.rs` — not the composer's internal one, which only the shell bar uses) gained 4 pt of its own above it (`EdgeInsets::new(md, 0.0, md, sm)` → `EdgeInsets::new(md, 4.0, md, sm)`), so the caps sit a few points lower, under the transcript they belong to. The instructions the strip shows are now grok-build's `↵ send` / `queue` and `⌘⇧W tasks` alone (`app/src/ui/shortcut_hints.rs`): `⌘↵ new conversation`, `!` shell and `⌘K commands` are gone from the agent input. In the `⌘K` entry's place is the reference's cloud gesture — `⌘⌥↵ send to cloud`, warp-new's `ENTER_CLOUD_AGENT_VIEW_NEW_CONVERSATION_KEYSTROKE` (cmd+alt+enter on macOS, worded "submit to cloud agent" in `crates/onboarding/src/callout/view.rs`) — and it is **bound**, because this repo keeps a hint only where it binds the chord it names: `ChatComposer::handle_send_to_cloud_key` takes the chord before the editor sees it and reports it to the host through the new `on_send_to_cloud` (`ChatComposer::with_on_send_to_cloud` → `ChatView::with_composer_on_send_to_cloud` → `UiActions::on_send_to_cloud`), whose body (`app/src/actions/make_actions.rs`) routes the conversation to the cloud medium — `WorkspaceRouting::Remote` in state and `set_chat_workspace_routing(.., "remote")` on the conversation — and then submits the draft through the ordinary `send_agent_prompt`. Nothing claims a remote run: there is no remote transport in this tree (`crates/goble-desktop-service/src/state/environment.rs` says so), so the routing choice plus the local submit is the whole gesture, and an empty draft is a no-op like `⌘↵`'s. The environment control went the other way: `ChatComposer::context_children` no longer draws the harness pill (the one whose tooltip read `Select environment`), so the input names where the draft runs through the working directory and the branch alone; `with_harness_label`/`with_harness_menu` stay for the hosts that still pass them, and the pill simply is not drawn. Tests were rewritten to the new truth, not dropped: `elements::chat_composer::tests::{composer_renders_dir_and_branch_pills_without_the_environment_pill, the_rich_input_keeps_only_xs_above_and_below_its_rows, the_cloud_chord_submits_to_the_cloud_and_plain_enter_still_sends}`, `ui::shortcut_hints::{tests::the_hints_are_the_gui_s_own_bindings, rich_input_tests::the_agent_strip_sits_over_the_inputs_separator}` (the strip still has to sit over the input's separator, and a running turn still turns `send` into `queue`), `ui::terminal::tests::the_shells_bottom_bar_is_the_shared_composer`, `integration_testing::menu_flow::the_composer_harness_pill_is_gone`, and `actions::pane_independence_tests::send_to_cloud_routes_the_conversation_remote` (the app half: the chord's action leaves `workspace_routing == Remote` and `"remote"` persisted on the pane's conversation). Verified with the same run as R44 → **exit 0, 817 passed / 0 failed**. Not verified: the on-screen look and the chord's feel in a real window (the session was told to stop driving the app); the geometry is proven by the composer's own test (first row ≈ `xs` under the top edge, last row ≈ `xs` above the bottom) (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R47–R56 — the fourth punch list: the text formatter, the rich input's model band and caret, the file pane, the explorer icon, tabs, the cwd pill and the workflow overlay:** ten asks in one batch, each implemented and then re-verified by a separate adversarial agent that read the diff, checked the new tests actually fail on the old behaviour, and re-ran the suite. **R47/R56 — the text formatter (the batch's priority), spaces and re-wrap and the speckles:** `markdown.rs`'s `take_pending` no longer `.trim()`ed interior whitespace and `append_text` no longer dropped whitespace-only runs, so `foo **bar** baz`, `**a** **b**` and ``one `code` two`` keep their spaces (fragments are turned verbatim into spans); `inline_text.rs` budgets words and space runs by the new `advance_width` (the sum of per-glyph `ceil(advance)`, exactly the quantity fontdue breaks on) instead of the ink extent `measure_text_family` reports, and gives a placed run the room left on its line, so a run is no longer re-broken by the renderer inside its own quad (measured before: the line run at `max_width = 201` drew 2 lines, height 33, inside a 14.4 px box); `text.rs`/`code.rs` paint at the `wrap_width` the box was measured at instead of `size.x + 1.0`, so resize re-wraps (31 of 120 swept widths disagreed before); the atlas keys on the exact `f32` bits rather than rounded values, keeps its 4-texel gutter on all four sides, commits the packing cursor only once a region fits, and wipes **and re-uploads** the texture on reset — the old code left the previous pass's ink under the new runs' edges, which is the speckle. **R48 — the model control:** the `sparkle` icon is gone, an unset label reads `not configured` (`MODEL_NOT_CONFIGURED`, defined once in `chat_composer/composer.rs` and re-exported by the app), and a press opens a full-width band listing the models — the slash menu's own presentation, extracted as `MenuBand` and shared by `SlashMenu` and the new `ModelMenu`, with the highlight in app state (`PaneControls::model_index`) so it survives the per-frame rebuild; the two bands are mutually exclusive and the editor keeps the caret. **R49 — the caret:** `CARET_WIDTH` 3 → 4 and the bar is a `CaretBeam` that reports **zero advance** and paints its own rect, so the text no longer shifts as the caret moves; the covered letter stays legible because the renderer closes a batch on every kind change and draws each batch's rects before its text (`wgpu_render_engine/frame.rs`), so the glyph lands on the focus fill; `text_area.rs` measures each side of the cut at the pen rather than the ink. **R50 — the file pane:** the lag was the **per-frame rebuild of one element per line** (measured in debug: a 500-line `.rs` 3.96 s/frame, a 2000-line one 16–21 s/frame), not a per-frame read — the read/highlight cache was already sound (`stat` only on a frame that changes nothing); the read-only body now draws only its viewport's lines with height fillers for the rest, and the **editable** body is windowed inside `TextArea` (`with_scroll_state`/`drawn_rows`/`reveal_caret`, caret revealed when it moves), taking a 1900-line editor from 16.7 s to ~1.0 s/frame and stopping the cost growing with the file. It did not save because the field's focus gate was `buffer.focused()` — set only by a press **inside** the field — while the pane's Cmd/Ctrl+S gate was the active pane, so a freshly opened pane drew an editor that took no keys at all; both now read `active_pane_id == pane_id`. A file pane is now a tab of the topbar in its own frame (`build_file_tabs`/`build_file_tab`: file-type icon, path tooltip, always-drawn close), pinned to `TOPBAR_HEIGHT` and placed outside `SpaceDropStrip`, so a lifted pane's drop index still counts workspace tabs only; one row pitch (`LINE_BOX`) drives both the drawn row and the filler. **R51 — the explorer icon:** `assets/icons/folder-closed.svg` was a 24×24 red (`#FF0000`) folder-with-a-prohibition-slash — literally the circle with a bar through it the user described — and is now a monochrome 16×14 filled folder, same filename and atlas registration. **R52 — New conversation and Cmd+Enter:** the sidebar row opened no tab and refused outright when no model resolved; it now appends a `Space` with one terminal leaf, binds a conversation and opens the harness, so the tab is terminal + agent, and `open_pane_harness` no longer bails **before** setting `harness_mode` — the switch always happens and the existing notice band reports the missing key or model. **R53 — Edit API keys:** `Button::with_corner_radius(0.0)` on that one control only. **R54 — `cd ..`:** the pane's shell-integration channel (`InitShell`/`Precmd` hooks and OSC 7) already carried the cwd and nothing consumed it; the pump records it now, `UiState::adopt_reported_cwds` adopts a report exactly once, and an app-written path retires the reports that preceded it (so a directory chosen from the pill's menu is not reverted while its `cd` runs) while a shell's own **start** report is never a move. **R55 — Cmd+Shift+W:** a right-anchored full-height `Sheet` became `Dialog::with_inset(32)`, and the content was rebuilt on grok-build's workflow shape (run list beside a detail, phases rail, agent roster from real executions, honest empty states) from goble's own records; elapsed time, `{done}/{total}` progress and the agent budget are omitted because the daemon keeps no run ledger — the tasks and executions it used to list are still drawn by `harness::build_executions_page`. Tests were added or rewritten (never weakened) across `elements::{markdown,inline_text,text,code,text_area,chat_composer,slash_menu,caret,button}`, `platform::text_atlas`, `ui::{file_view,shell::topbar,task_workflow,chat::overlays,terminal::tests}`, `actions::pane_independence_tests`, `state::pane_session_tests`, `terminal::tests::{context_pills,session}` and `integration_testing::{chat_flow,menu_flow}`. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note) and `cargo test -p goble-ui -p goble-app --no-fail-fast` → **exit 0, 866 passed / 0 failed** (`goble-ui` lib 432, `goble-app` lib 348, its 12 integration binaries 86). Not verified: the pixels — the GUI binary was never built or launched, on the user's instruction — so the speckles, the caret's look, the topbar frame and the overlay's margins are proven by drawn commands, metrics and atlas pixels rather than seen; the 4-texel gutter's sufficiency at high display scales and the linear filter's edge behaviour can only be settled on a GPU; and every frame cost above is an unoptimized debug number (`06-renderer/ui-spec/05-chat-view.md`, `06-renderer/ui-spec/04-conversation-sidebar.md`, `06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R57 — a conversation the agent has not answered is not listed in the sidebar:** starting a conversation creates it before anyone has answered it, so the list showed threads with nothing in them. `refresh_conversations` (`app/src/state/ui_state/refresh.rs`) already reads each conversation's messages once for the card preview, so the new fact costs no extra query: `messages.iter().any(|m| m.role == "assistant" || m.role == "tool")` — the store's own role strings, so `system` is neither, and a `tool` row counts because the agent produced it. It lands on `ConversationEntry::has_agent_reply` (`crates/goble-ui/src/elements/conversation_sidebar.rs`, builder `with_has_agent_reply`), which **defaults to `true`** so a caller that has not read a conversation's messages cannot silently hide it. `agents_view` (`app/src/ui/sidebar.rs`) applies the one rule to both lists, so neither Starred nor the folders can list what the other hides, and `View all (N)` counts what is drawn; the empty state keeps its two readings — an unanswered conversation leaves `No conversations yet. Create one to begin.` because nothing worth listing exists, where the old test for that string was "the app has no conversations at all" and so read an unanswered one as content belonging to another environment. `UiState.conversations` stays **complete**, so tab labels (`conversation_name`), `selected_id` and the transcript keep resolving; the sidebar is the only surface that hides them, and an unanswered conversation stays reachable through its own topbar tab. Tests: `ui::sidebar::sidebar_surface_tests::{a_conversation_the_agent_has_not_answered_is_not_listed,an_unanswered_conversation_leaves_the_first_empty_state}` (a real `DesktopState`, a `user`-only conversation beside an `assistant`-answered one and a `tool`-only one, asserting on the runs the sidebar's own list drew rather than on the whole band, because the topbar's tab names the active conversation at the same x) and `elements::conversation_sidebar::tests::a_conversation_entry_is_listed_until_a_caller_says_it_was_not_answered`; both sidebar tests were checked to fail with the filter reverted. Verified with `cargo check -p goble-ui -p goble-app --all-targets` → exit 0 and `cargo test -p goble-ui -p goble-app --no-fail-fast` → **exit 0, 869 passed / 0 failed** (`goble-ui` lib 433, `goble-app` lib 350, its 12 integration binaries 86). Not verified: the pixels — the GUI binary was never built or launched, on the user's instruction — so the list is proven by the drawn commands rather than seen; and a prompt that has been sent but not yet answered is briefly absent from the sidebar, because the rule flips on the first streamed delta or tool result the store receives (`06-renderer/ui-spec/04-conversation-sidebar.md`)
- [x] **GUI polish R58 — the fifth punch list (twenty-two asks): the terminal's real filters, Settings as its own tab, and the files behind the settings:** each ask implemented and then re-verified by a separate adversarial agent that read the diff and re-ran the suite. **The terminal's filters (three asks in one mechanism):** `TerminalFilter` (`crates/goble-ui/src/elements/terminal_block/filter.rs`, `Clone + Debug` over shared `Rc<RefCell<..>>` cells, so a clone *is* the same filter — the per-frame element tree can share one without losing its query) carries `bar_open`, `query`, `focused` and `hover`; the block's own ⏷ control raises a bar over **that** block's output, `Cmd+F` raises the general one over the pane's whole output and `Cmd+Shift+F` the block under the pointer. A bar narrows only the blocks it is handed (`terminal_filter_bar(filter, placeholder, matched, total, app)`), so one pane's query leaves another pane's output alone — `a_general_filter_in_one_pane_leaves_another_panes_output_alone`, `a_query_in_one_block_leaves_another_block_alone` — and the bar stays down until it is raised (`the_transcript_filter_bar_stays_down_until_it_is_raised`). Both bars are real text fields, not labels: `the_filter_button_opens_a_bar_with_a_real_text_field`, `the_general_filter_bar_is_a_real_field_with_the_counts`, `typing_in_the_bar_narrows_the_block_and_escape_restores_it`, `cmd_f_opens_the_panes_whole_output_filter_and_its_query_narrows_the_output`, `cmd_shift_f_opens_the_filter_of_the_block_under_the_pointer`, and the pane's chord is routed at the root view (`app/src/root_view/tests/filter_chord.rs`, new: `cmd_f_raises_the_transcript_filter_of_an_agent_pane_and_leaves_a_shell_pane_its_own`). Output is formatted at the section level, one prompt row above its command (`a_section_header_draws_one_prompt_row_above_the_command`), and the block's controls draw only under the pointer (`the_blocks_controls_show_only_while_the_pointer_is_over_the_block`). **The rich input (four asks):** `Cmd+Enter` opens a **new** conversation rather than adopting the selected one (`cmd_enter_opens_a_new_conversation_instead_of_adopting_the_selected_one`, `repeated_new_conversation_opens_a_tab_each_time`) and a prompt with no model still opens the agent view without persisting a message (`a_prompt_with_no_model_opens_the_agent_view_and_persists_no_message`); the shortcut strip sits lower over the input (`the_strip_sits_lower_than_its_own_spacing_over_the_input`); the model control reads `not configured` while nothing is configured (`the_model_control_reads_not_configured_when_nothing_is_configured`) and opens the host's own models in the **same band** the slash list uses, not a popup — the two bands are mutually exclusive and take the slot from each other (`the_model_band_opens_over_the_strip_like_the_command_list`, `the_model_menu_lists_the_models_in_the_same_band`, `opening_the_model_band_puts_the_command_list_away`, `typing_a_command_takes_the_slot_from_the_model_band`, `a_model_menu_without_models_says_so`); attach opens the OS file picker and writes no message of its own (`the_attach_control_opens_the_file_picker_and_writes_no_message`, `pick_file_returns_none_off_the_main_thread`, `the_attachments_draw_as_chips_over_the_editor`). **The caret over the ghost text:** the beam is placed at the end of a field's own text, not the placeholder's, in the conversation search, the in-files search and the project field (`a_field_with_a_value_keeps_its_beam_at_the_end_of_the_text`, `an_empty_focused_field_puts_its_beam_on_the_placeholder`). **The topbar (two asks):** a tab is at least the minimum width the window affords its count, and a narrow window still holds every control (`a_tab_is_at_least_the_minimum_width`, `many_tabs_share_the_bar_and_keep_the_controls_inside_it`, `a_narrow_window_still_holds_every_control`). **Settings as its own tab, exactly once (five asks, spec [`06-renderer/ui-spec/10-settings-tab.md`](06-renderer/ui-spec/10-settings-tab.md), new):** the settings surface is a topbar tab drawn as a space of panels — a rail beside the page — not an overlay (`a_settings_leaf_draws_the_rail_and_the_page`, `the_settings_body_fills_the_pane`, `the_settings_tab_still_answers_the_mouse`); pressing the button again focuses the tab it already opened instead of adding a second (`pressing_settings_twice_keeps_one_tab`, `a_settings_tab_in_a_background_space_is_brought_to_the_front`); a restored tab reopens on the page it was on (`a_restored_settings_tab_reopens_on_the_page_it_was_on`); `Ctrl+Tab`/`Ctrl+Shift+Tab` walk the strip and wrap at both ends (`ctrl_tab_walks_the_tab_strip_and_wraps_at_the_last_tab`, `ctrl_shift_tab_walks_the_tab_strip_backwards_and_wraps_at_the_first_tab`, `ctrl_tab_does_not_move_a_one_tab_workspace`, `ctrl_tab_onto_the_settings_tab_puts_the_keyboard_in_its_rail`); the window's chords still answer while the tab is open (`the_window_chords_still_answer_while_the_settings_tab_is_open`). **Keyboard-only editing (two asks):** a row takes the field on `Enter` and `Esc` puts the previous value back (`a_pane_text_field_takes_the_caret_and_escape_puts_the_value_back`, `the_arrow_keys_move_the_page_and_escape_leaves_the_tab_open`, `the_connection_rows_are_focusable_in_order_and_enter_selects`), and switches draw square in every state while still toggling (`switch_draws_square_corners_in_every_state`, `switch_draws_the_same_square_commands_when_hovered_or_pressed`, `switch_still_toggles_and_moves_its_squared_thumb_on_click`, `the_edit_api_keys_button_is_square`). **The files behind the settings (two asks):** `config.toml` is read and written in grok's own shape (`crates/goble-core/src/config/`, a directory replacing the old `config.rs`) — one `[model.<slug>]` table per model, `[models]` for the default, `[theme]` — with the legacy `[llm]` file absorbed on read and written out in the new shape once (`to_toml_emits_the_grok_layout`, `grok_shaped_document_parses_into_the_model_map`, `the_old_goble_file_loads_with_its_models_and_theme_intact`, `the_next_save_writes_the_migrated_shape`, `migration_is_idempotent`, `a_disabled_legacy_model_migrates_to_hidden`, `migrated_slugs_stay_unique_and_round_trip`, `a_grok_file_round_trips_with_its_unmodelled_sections`, `a_bad_section_costs_its_section_and_not_the_file`, `an_unmodelled_top_level_scalar_is_not_kept`); environment variables move out of the store into `~/.goble/environment.toml`, a file that opens with a commented header documenting its own shape (`crates/goble-core/src/environment.rs`, `header_documents_the_file`, `empty_document_is_the_header_alone`, `save_writes_groups_in_a_stable_order`, `a_saved_file_is_private`, `a_bad_group_is_dropped_with_one_message_and_the_others_load`, `a_duplicate_key_is_an_error_not_a_merge`, and the store's groups migrated once: `the_store_s_groups_are_migrated_into_the_file_once`, `a_malformed_file_keeps_the_last_read_and_refuses_writes`); and the new **Connections** page reads `~/.ssh` (`crates/goble-core/src/ssh_hosts.rs`) — `SettingsCategory::Connections` sits after `Environment` as the eighth page (`connections_sits_after_environment`), one row per concrete `Host` with its target and identity file (`the_connections_page_draws_a_row_per_host_with_its_target_and_key`), wildcard/negated/`Match`/`Include` blocks are not hosts (`wildcard_negated_match_and_include_blocks_draw_no_rows`, `wildcard_star_block_is_not_a_host`, `negated_block_is_not_a_host`, `wildcard_suffix_block_is_not_a_host`, `match_block_is_ignored`, `include_is_not_followed`), a missing `~/.ssh` or a config without a concrete host reports a finding rather than an empty list (`missing_ssh_dir_reports_a_finding`, `ssh_dir_without_config_reports_a_finding`, `config_with_no_concrete_host_reports_a_finding`), hashed `known_hosts` entries are reported as a count and never as names (`hashed_known_hosts_is_reported_as_a_count_not_names`), and the reload row re-reads the directory (`the_reload_row_re_reads_the_directory`). **The explorers and the bands (three asks):** hidden dot entries are rows like any other and expand to their children (`a_hidden_directory_and_a_hidden_file_are_rows_like_any_other`, `expanding_a_hidden_directory_draws_its_children_under_it`, `a_hidden_name_draws_a_registered_icon`); a long result set draws only the rows the band shows and scrolls under the wheel (`the_search_results_scroll_under_the_wheel`, `the_results_of_a_typed_query_scroll_under_the_wheel`, `a_long_result_set_draws_only_the_rows_the_band_shows`, `the_window_keeps_the_whole_results_scroll_range`, `an_unreadable_directory_shows_nothing`); and the directory tray is capped and clips the rows it cannot show, scrolling under the wheel (`the_directory_tray_is_capped_and_clips_the_rows_it_cannot_show`, `a_directory_with_more_rows_than_the_cap_draws_a_window_of_the_cap`, `a_wheel_over_the_tray_scrolls_its_rows`). **macOS chrome (one ask):** the app menu reads `Goble`, from the bundle's `CFBundleName` when there is one and the constant otherwise (`app_name_constant_is_goble`, `a_bundle_name_is_used_as_is`, `a_missing_bundle_name_falls_back_to_the_constant`), no placeholder `NSMenuItem` titles are set, and a square dock-icon placeholder ships for the artwork to replace (`placeholder_dock_icon_is_a_square_png`, `crates/goble-ui/assets/icons/app-icon-placeholder.png`; `objc2-app-kit` gained only the `NSOpenPanel`/`NSSavePanel`/`NSPanel`/`NSImage` features, so `Cargo.lock` is untouched). **The isolation defect this batch exposed and fixed:** `DesktopState::save_config` and `environment_path()` resolved `GobleHome::locate()` — the developer's real home — unconditionally, so a test built over a temp store wrote the real `~/.goble/config.toml` and `~/.goble/environment.toml`; both now `bail!` when no path was pinned, and `open_default` (the only production path, `app/src/main.rs`, `goble-desktop-native/src/app.rs`) pins the located home. Verified with `cargo check --workspace --all-targets` → exit 0 and `cargo test -p goble-app -p goble-ui -p goble-core -p goble-desktop-service --no-fail-fast` → **exit 0, 1297 passed / 0 failed**, with `~/.goble/config.toml`'s mtime unchanged and `~/.goble/environment.toml` left absent across the run; a per-crate sweep of all six members (`goble-app --lib`, `goble-ui`, `goble-core`, `goble-desktop-service`, `goble-desktop-native`, `goble-cli`) reports every one clean of the real config file, and the CI gate's own crate passes locally (`cargo test -p goble-terminal` → 128 + 1 + 2 passed, and it is untouched by this batch). Not verified: the pixels — the GUI binary was neither rebuilt nor launched here, on the user's instruction — so the bars, the band, the square switches and the settings rail are proven by drawn commands and metrics rather than seen, and the dock icon's placement can only be settled by a launch (`06-renderer/ui-spec/10-settings-tab.md`, `06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R59 — the pane bar's 3-dots tray becomes the pane's own expand control:** the ask was one control instead of the tray: an expand icon that draws the pane over the whole panes space, which then draws itself as the way back. `build_agent_header` (`app/src/ui/chat/header.rs`) no longer builds a `PopupMenu` at all — the tray and its five rows (Copy, Restart, Rename, Clear transcript, Fullscreen) are gone from the pane bar, and `UiActions::{on_restart, on_rename_agent}` went with them, because three of the five were log-only stubs and the two that act stay reachable elsewhere: `/clear` and `/fullscreen` in the ⌘K palette (`ui::palette`), and `on_copy`, which is the macOS Edit ▸ Copy command registered in `rebuild.rs` and not a tray-only action. In their place the bar draws a `TopbarButton` (`Icon::new(if maximized {"minimize"} else {"maximize"})`, muted, 16 pt) inside a `Tooltip` that reads "Expand this pane" or "Retract this pane", wired to the new `on_toggle_pane_maximized(pane_id)`; the close X is unchanged beside it. **The state it needs:** `UiState.maximized_pane: Option<u64>` (`state/ui_state/mod.rs`, new) beside `fullscreen`, carried once per frame into `UiSnapshot`; the tray's own `agent_header_menus: HashMap<u64, Rc<RefCell<bool>>>` and its whole plumbing (`UiState::agent_menu_open`, `UiState::ensure_agent_menu_flags` and its call at the top of `root_view/rebuild.rs`) are removed with the tray that was their only reader. The toggle lives in `actions/make_actions.rs`: same pane again clears it, any other pane takes it, so one pane at a time is expanded, and closing the expanded pane clears the flag with the pane. **The layout is not mutated — that is the whole trick:** `build_active_space` (`ui/panes.rs`) draws `build_leaf` for that one id instead of the tree, so the split tree, its ratios and the pane's place in it are untouched and retracting restores the exact layout rather than recomputing one; the guard is `leaf_kind_of(&space.root, id)` (a new local walk) plus `Pane::contains_leaf`, so an id that names no leaf of the space on screen — because the pane was closed, or because the flag was set in another space — falls through to the tree, which is what makes the expansion per-space without storing it per-space. **Tests:** `ui::chat::header::tests::{the_header_draws_the_expand_control_and_no_tray, the_expand_control_expands_and_retracts_the_pane_it_belongs_to, an_expansion_in_one_space_leaves_another_space_its_own_layout, expanding_a_pane_draws_it_over_the_whole_panes_space}` — with the new `split_root`, `pane_bars` and `pane_bar_y` helpers, the last one measuring that the split's two bars become one bar that reaches past where the two ended, and that retracting restores the pair exactly — plus `ui::terminal::tests::the_panes_bar_draws_the_expand_control_and_no_tray` and `integration_testing::menu_flow::the_pane_headers_expand_control_expands_the_pane`; the three tests that asserted the tray (`the_tray_carries_the_panes_own_actions_and_no_drawer`, `the_shifted_rows_still_run_the_action_they_draw`, `the_panes_tray_paints_over_the_output_and_stays_inside_the_window`) were rewritten to assert the new contract rather than deleted, and the two pane-drag tests in `ui_render.rs` now locate the pane header by the expand control. Each new test was checked to fail with its change reverted: with the expansion disabled, the layout test reports two bars where it wants one; with the toggle stubbed out, the control test reports `None` where it wants `Some(1)`. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note) and `cargo test -p goble-app -p goble-ui -p goble-core -p goble-desktop-service --no-fail-fast` → **exit 0, 1299 passed / 0 failed**, with the real `~/.goble/config.toml` untouched and `~/.goble/environment.toml` still absent. Not verified: the pixels — the binary was neither rebuilt nor launched, on the user's instruction — so the icon swap, the tooltip and the geometry of an expanded pane are proven by drawn commands and measured rects rather than seen; and whether the expanded pane should stay expanded across a restart is left as it is (the flag is runtime state, not persisted) (`06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R60 — the pane expansion takes warp-new's own rules, and its toggle becomes a command:** the ask was to do what the reference does, so the reference was read first (`~/Projects/warp-new/app/src/pane_group/`). **The rendering question is settled and unchanged:** `PaneGroup::render` (`mod.rs:8841`) substitutes the focused pane's element for the whole split-tree element while maximized — the tree is *not* drawn with the maximized pane's slot left black, and the pane is *not* an overlay — so the tree survives only as state (`SplitPaneState::InSplitPane(PaneState::Maximized)`, read by `is_focused`/`is_maximized` and never by the layout), which is exactly what R59's `build_active_space` already does. Three things were missing. **(1) The focus rule:** `PaneFocusState::set_focused_pane` (`pane_group/focus_state.rs`) clears `is_focused_pane_maximized` whenever the focused pane changes, so a pane that gives up the focus cannot keep the whole space. The new `UiState::focus_pane` (`state/ui_state/session.rs`) is now the path every in-space focus move takes — `on_pane_activate` (a click in a pane), `on_pane_navigate` (`Ctrl/Cmd+Arrow`), `split_active_pane` and `open_file_pane` (`actions/pane_ops.rs`) — so `/split`, `/terminal` or the arrow keys bring the tree back with the new pane on screen, where before the new pane was created under an expanded pane and stayed invisible; `focus_pane` leaves the expansion standing when the focus lands on the pane that already has it, which is what keeps the retract control a toggle. Space switches still leave the flag alone, and that is what keeps the expansion per-space (R59's `an_expansion_in_one_space_leaves_another_space_its_own_layout` still holds). **(2) The toggle needs a split:** `toggle_maximize_pane` refuses while `pane_count() == 1` (`pane_group/mod.rs:6595`), because a pane its space holds alone already is that space. `Pane::leaf_count` (`ui/pane.rs`, new) is the same test here: the action refuses to *expand* without it, while taking an expansion back is never refused (so a pane left expanded by an edit that removed its neighbour still has a way out), and `build_agent_header` draws the control `with_disabled` in that case, its tooltip reading "Split this pane to expand it" — a live-looking button that does nothing and says nothing was worse than the split-tree it cannot use. **(3) The command:** the reference carries this as the command "Toggle Maximize Active Pane" with no default chord of its own, so goble gains the ⌘K row `Toggle maximize active pane` (`ui/build.rs`, applied to the active pane) and `/maximize` in the slash list (`ui/palette.rs`) — the same two surfaces R59 pointed the tray's two real actions at. An expansion also takes the focus with it, so the pane drawn over the space is the pane the app works in. **Tests:** `state::pane_session_tests::sessions::focusing_another_pane_ends_the_expansion`; `ui::chat::header::tests::{moving_the_focus_to_another_pane_ends_the_expansion (the real `Ctrl+Arrow` chord through the root), a_pane_its_space_holds_alone_has_nothing_to_expand_over (the press changes no state and the tooltip is drawn), the_panes_input_lists_the_expand_command}`; `integration_testing::menu_flow::the_palette_toggles_the_active_panes_expansion` (the ⌘K row clicked through the real frame, expanding the active pane and closing the palette). R59's two expand tests were rewritten to the split shape rather than weakened — a one-pane space can no longer expand — and the new `rightmost_icon` and `split_right` helpers find the right-hand pane and split through the real `Cmd+Space` chord. Each rule was checked by reverting it: with the clearing line removed from `focus_pane`, both focus tests fail; with `expandable` forced true, the one-pane test expands where it must not. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note) and `cargo test --workspace` → **exit 0, 1842 passed / 0 failed**, plus `cargo test -p goble-app` after the revert-restore → 393 lib + every integration binary ok. Not verified: the pixels — the binary was neither rebuilt nor launched — so the inert control, its tooltip and the focus following an expansion are proven by drawn commands and the state the dispatched events leave. Left as it is, deliberately: returning to a space whose expansion names a pane other than that space's first leaf puts the focus on the first leaf while the other pane is still drawn expanded — pre-existing, self-healing on the next focus move, and outside "do what the reference does" (`06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R61 — the focused pane carries an accent mark in its own top-left corner:** the ask was the reference's own active-pane indicator. Warp builds it in `add_active_pane_indicator_to_stack` (`~/Projects/warp-new/app/src/pane_group/pane/view/header/mod.rs:966`, called at `:789` while `show_active_pane_indicator`): an `Icon` of `bundled/svg/upper-left-triangle.svg` in `appearance.theme().accent()`, a 16×16 `ConstrainedBox` positioned from the parent's top-left corner with both anchors `TopLeft`. **The visibility rule was read, not assumed:** the flag comes from `PaneConfiguration::show_active_pane_indicator` (`pane/mod.rs:690`, default `false`, set at `:741`), which `refresh_pane_header` (`app/src/terminal/view/pane_impl.rs:112`) sets from `is_active_session(ctx)` → `focus_handle.is_active_session` → `focus_state.rs:196`'s `focus_state.active_session_id() == Some(terminal_id)`. So the mark follows the **focus**, not the split: it is drawn for the one pane the keyboard is in, and a single-pane space carries it too. **What goble gained:** `FOCUS_MARKER_SIZE` (`app/src/ui/panes.rs`, 16.0) is the mark's size *and* the room a bar leaves for it; `build_leaf` wraps the focused pane's element in `Stack::new().with_children(vec![pane]).with_overlay(marker, vec2f(0.0, 0.0))` — an accent `Icon::new("upper-left-triangle").with_size(16.0)` over the pane's own corner, for every pane kind (chat, terminal, file, settings), and the inactive panes are returned untouched. The mark is painted over the bar, so the bar starts past it: `build_agent_header` (`ui/chat/header.rs`) and `build_settings_pane` (`ui/settings.rs`, which also gained the `pane_id` its siblings already took) pad their left edge by `FOCUS_MARKER_SIZE` when the pane is the active one, which is what keeps the harness's "esc for terminal" hint at the front of the bar from sitting under the mark. **The asset is goble's own hand-written 16×16 SVG** (`crates/goble-ui/assets/icons/upper-left-triangle.svg`, `M0 0H16L0 16V0Z`, registered in `ICON_FILES`), sharing the reference's geometry — a right triangle filling the top-left corner, full top and left edges — but not its bytes: the tracker's open licensing item counts 42 icon files byte-identical to the AGPL side, and this must not become the 43rd. **Tests, each shown red with its change reverted:** `ui::chat::header::tests::the_focused_pane_carries_the_accent_mark_in_its_own_corner` (a two-pane split via `split_root`: exactly one accent mark, in the focused pane's own corner — left of and above its bar's close X — then the real `Ctrl+ArrowRight` chord dispatched through the root, the focus on the right pane, and the one mark moved right with it; with the overlay removed it fails `left: 0, right: 1`); `ui::terminal::tests::the_harness_hint_starts_past_the_focus_mark` (the harness opened the way a user opens it, by typing and `Cmd+Enter`: the drawn `esc` starts at or past the mark's right edge; with the bar's inset removed it reports `esc at (308.0, 49.0), mark at (300.0, 37.0) sized 16`, i.e. the hint under the mark); `platform::icon_atlas::tests::the_focus_marker_fills_its_box_and_leaves_the_opposite_corner_empty` (the glyph rasterizes to the full 64×64 cell with ink at the top-left, top-right and bottom-left pixels and none at the bottom-right — a full-bleed corner, not a boxed or floating glyph; with the path changed to a full square it fails `left: 255, right: 0`). Not verified: the pixels — the GUI binary was neither rebuilt nor launched, on the user's instruction — so the mark's colour, size and corner are proven by the drawn commands and the atlas's own pixels rather than seen (`06-renderer/ui-spec/03-topbar.md`)

- [x] **GUI polish R62 — a key set by hand in `config.toml` is a configured key, and every write of that file carries its header:** two asks, and the first had two causes. **(1) The window read the wrong source for the key.** `UiState::refresh_settings` (`app/src/state/ui_state/settings.rs`) filled `settings_llm_provider` / `settings_llm_model` / `settings_llm_api_key` from `desktop.get_llm_setting("openai")` — the **store's** per-provider row alone — while `resolve_llm_provider` (`crates/goble-desktop-service/src/state/llm.rs`) resolves a turn through `config_model_provider` first, i.e. through the `[model.<slug>]` entry's own inline `api_key`. A user who set the key in the file therefore had a runnable model and a window that said `No API key configured` with an `Edit API keys` button pointing at the very file carrying the key. New `DesktopState::effective_llm_setting(provider, model)` is that one resolution in a form the window can read: the `[model.<slug>]` entry for the model in force when it has a key, else the store's setting (and `None` when the row has no key, so an empty row cannot read as configured). `refresh_settings` now resolves the window's model first (`refresh_llm_models`), then the setting, in that order. **(2) The window started on a model the config does not declare.** `default_model` returned `[models] default` unconditionally, so the file below — which is the shape `~/.goble/config.toml` had — selected `gpt-4o`, an id with no `[model.gpt-4o]` table, no key and no row in the tray `available_models` builds from the declared catalog (`["deepseek-flash"]`): the composer's label named a model nothing could run. `default_model` now answers with a model the app **offers** — `[models] default` while the file declares it, else the store's model while the tray lists it, else the catalog's first entry — so the label is always a row of its own tray. A declared default still wins. Measured against the developer's real file (a throwaway probe, since removed; read-only): before, `default_model` was `gpt-4o` and the resolved key was `None`; after, `default_model` is `deepseek-flash` and the key is the entry's own, with its `base_url`. **(3) `config.toml` had no header** because `CONFIG_HEADER` (`crates/goble-core/src/app_home.rs`) was used by the seeder alone — `seed_config_if_missing` prefixed it, and every later `save_config` → `GobleConfig::to_toml` wrote the bare body, so the first model or theme change stripped the file's only documentation. The header moved into the config module as `pub const CONFIG_HEADER` and now comes out of `to_toml` itself — the one writer for the file — so the seeded file and every save carry it, and the seeder is just `GobleConfig::default().to_toml()`. The header documents the three sections, the shape of a model entry, the `[models] default`, an MCP server, `[theme]`, and that unmodelled sections survive; it deliberately contains neither `[llm]` nor `version`, which two tests assert the emitted file never carries. **Tests, each shown red with its change reverted:** `state::llm::tests::{the_key_a_hand_edited_config_carries_is_the_setting_the_app_reads, the_stores_setting_answers_when_no_entry_declares_the_model, the_default_model_is_one_the_configured_catalog_offers, a_declared_default_still_wins_over_the_rest_of_the_catalog}` (`crates/goble-desktop-service/src/state/llm.rs`, over the new `HAND_EDITED` fixture — the developer's own file shape); `integration_testing::first_run_flow::a_key_set_by_hand_in_the_config_is_a_configured_key` (a real `DesktopState` whose `config_path` is a temp file holding that shape: the key, the `base_url`, the model, the tray row and `can_run_agent_turn(1)`); `config::tests::{every_write_opens_with_the_header, the_header_documents_the_file}` (`crates/goble-core/src/config/tests.rs`). Reverting `refresh_settings` to the store-only lookup fails the first-run test with `left: ""` against `"sk-hand-edited"` — the reported symptom exactly; reverting `default_model` fails with `left: "gpt-4o"` against `"deepseek-flash"`; reverting the header out of `to_toml` fails the header test with the bare body. Not verified: the pixels and the window — the standing instruction is that the session does not drive the app, so the band's absence is proven by the gate it is raised on rather than seen; the app **was** rebuilt and launched for the user's own inspection, and that instance has been up with no output since. Left as it is, deliberately: `~/.goble/config.toml` is read at startup (`DesktopState::open_default` → `reload_config`) and has no hot reload, so a hand edit needs a restart; the file's own `[models] default = "gpt-4o"` still names an undeclared model, which the new rule tolerates by falling back to the declared catalog rather than by rewriting the user's file (`crates/goble-core/src/config/mod.rs`)
- [x] **GUI polish R63 — the empty state is centered inside a frame, and the app is assumed to ship no data at all:** two asks, and the first turned out to be a premise rather than a change. **(1) No mock data on the default path — verified, not assumed.** `UiState::from_desktop` (`app/src/state/ui_state/refresh.rs:19`) builds every list empty (`conversations`, `chat_messages`, `messages`, `crons`, `workflows`, `executions`, `tasks`, `timeline`, `costs`, `models`) and fills them from `DesktopState` alone; `RootView::new` (`app/src/root_view/construct.rs:30`) takes only the `from_desktop` constructors, `main` surfaces a store error rather than substituting data (`app/src/main.rs:38`), and the only files a first run writes are `~/.goble/config.toml` / `environment.toml` / the docs — structure, not content. Every `mock()` fixture (`UiState`, `MediaState`, `ScreenState`, `AiState`, `ProjectsState`) lives in a `#[cfg(test)]` module or a test-only file, so a brand-new app has no conversation and no transcript, which is what the new app-level test asserts. **(2) The empty state now sits centered in a frame, following the reference welcome box.** The reference is grok-build's hero box (`crates/codegen/xai-grok-pager/src/views/welcome/hero_box.rs`): a `Block::borders(Borders::ALL)` of `BorderType::Rounded` whose colour is `blend_color(bg_base, gray_dim, 0.45)` — dimmed toward the background "for a softer, dimmer gray" (`:317`) — a width of `content_area.width - 6` capped at 120 columns (`:116`), horizontally centered by `Layout::horizontal([Min(0), Length(box_width), Min(0)]).flex(Flex::Center)` (`:186`), and a top pad of a **third** of the slack (`:146`), not dead center. `build_empty_state` (`crates/goble-ui/src/views/chat_view/transcript.rs`) is now exactly that shape: border `theme.color(Bg).mix(theme.color(Border), 0.45)`, radius `theme.radius_px()`, `SpacingToken::Lg` of padding, two texts center-aligned to each other inside it; the frame's width comes from a `MainAxisSize::Max` row *inside* a centering row (the same `Min(0)/Length/Min(0)` shape), inset `SpacingToken::Xl` (24 px) a side and capped at the new `EMPTY_FRAME_MAX_WIDTH` (720 px); the vertical placement is two `Spacer`s at 1:2, which is the reference's third of the slack with the rest left below. The `Scrollable` the empty state used to be wrapped in is gone: its child constraint is `max.y = INFINITY`, so a `Max` flex cannot fill inside it and vertical centring was impossible there — the new flex claims the height it is handed (`MainAxisSize::Max` + `CrossAxisAlignment::Stretch`) so the composer stays pinned. **Tests, each shown red with its change reverted:** `views::chat_view::tests::the_empty_state_is_centered_in_a_frame` — the frame spans the pane minus 24 px a side in a 600 px pane; the title's measured middle is the frame's middle (within 1 px); a 1200 px pane caps the frame at 720 and centers it (`min_x == (1200-720)/2`); and 600 px more pane moves the frame 200 px, where a box centered in the slack would move 300. `integration_testing::ui_render::a_fresh_app_shows_the_empty_state_inside_its_frame` — the real `RootView` over an empty store: `desktop.list_chats()` is empty, and the invitation and the title both lie inside a rounded border that starts right of the sidebar. Reverting `build_empty_state` to the old top-left container fails both (`the empty state's text sits inside a rounded frame`; `the pane frames its empty state`); making the two spacers 1:1 fails the moved-distance assertion; dropping the placement row's `MainAxisAlignment::Center` fails with `the capped frame is centered in the pane, got Rect(720.0x70.0 at (24.0, 216.33333))` — the frame hugging the margin. Measured in the real app frame with a throwaway probe (since removed): at 1024×768 the frame is `Rect(676×70 at (324.0, 225.3))`, 24 px inside a pane that starts at x=300, its middle 662 px matching the title's own middle, and its top 165 px below the empty area's top against 335 px above the composer. Not verified: the pixels — the standing instruction is that the session does not drive the app, so the box is proven by the frame's draw commands and never seen. Left as it is, deliberately: the frame sizes to its content, so a pane shorter than it (roughly 70 px of transcript area) would paint its frame over the composer — the removed `Scrollable` was the only thing that clipped there, and no element in the tree clips its own overflow (`Clipped` does not, per its own note in `crates/goble-ui/notes/octomusui-port-map.md`) — rather than growing this batch into a clipping element (`06-renderer/ui-spec/03-topbar.md`)
- [x] **GUI polish R64–R68 — the fifth punch list (five asks): an agent's tool call reads continuously in the reply and takes grok-build's own row shape, the footer keeps its place under the last message, the frame stops re-measuring text and glides instead of jumping, and the sidebar names a conversation after what was said in it:** five asks in one batch; three of them (the footer, the frame clock, the title) were implemented by a subagent, and every new test was shown non-vacuous by reverting its rule and watching it fail. **R64 — a call the agent made is not a terminal block; a command the user ran still is.** `command_body` (`crates/goble-ui/src/elements/chat_message_bubble/tool_body.rs`) no longer returns the block `TerminalBlockPlumbing` drew: it reads `TerminalData::for_command(..)` for the terminal's own ANSI resolution, drops line 0 — the command the row above already names — and draws the rest as rows, elided with `… +{hidden} lines` under `COMMAND_TRUNCATION` (first 2 / last 3) while the call is truncated. `TerminalBlockPlumbing` is gone from this path, out of `build_tool_call_rows`, `build_tool_call_body`, `command_body` and the `ChatMessageBubble::rebuild` call site, so an agent call draws no block header, no copy button and no filter. The new `output_spans` turns the runs the terminal resolved into `TextSpan`s (mono, the fallback colour a failed call's own, bold and italic kept, underline only as the SGR attribute the output itself carried) and `tool_body_span_row` draws them on the `Bg` band at the row's own 12 px, line-height 1.4. Segmentation is the user's alone now: the pane's executed-command block and `conversation_command_rows` (the `!` lines typed inside a conversation) are the only `ChatFragment::terminal` producers left, and the comments in `app/src/ui/chat/agent.rs` say so. On the app side a tool *result* row folds into the call it belongs to — `app/src/state/transcript.rs` pairs a persisted or live result with the `call_sites` id it carries — instead of becoming a second message. Tests: `elements::chat_message_bubble::tests::tool_call::{an_agents_command_is_not_a_terminal_block, a_users_command_is_still_the_terminal_block}`, the rewritten `tests::sub_agent::tool_result_block_still_renders_for_a_user_command`, and the rewritten `ui::terminal::tests::a_command_block_renders_identically_in_the_transcript_and_the_pane` — which keeps the user half (a command the user ran is still one block in both places) and pins the agent half (the row names `echo hi`, its output `hi` is drawn under it, no `❯ ` prompt line appears, and every run is 12 px) — plus `integration_testing::harness_flow::refresh_messages_folds_tool_rows_into_their_call`, `state::pane_session_tests::transcript::{a_persisted_tool_result_folds_into_the_call_it_belongs_to, an_orphan_tool_result_row_is_not_a_terminal_block, a_folded_result_never_overwrites_the_calls_own_result}`, and the rewritten `live_events::a_tool_result_reads_its_status_from_the_call_record` (the status is read off the call's own row — `ToolCallStatus::Error` / `Finished`, not a prefix in the result text — and the folded body is the result's text verbatim). Revert-proofs: forcing `command_body` back to the block fails both tool tests (`the row and its body are one size, got [… 13.0 …]`; `an agent's call draws no block prompt`), and disabling the fold in `transcript.rs` turns the result row back into a message of its own and fails the four app tests with `left: 2`. **R65 — the row takes grok-build's shape.** One mark, `◆`, for every state — the state is the mark's colour, never a swapped glyph and never a spinner — with the rail `┃` drawn before it only while the call is open and in the mark's own colour. A collapsed call's mark is blended 50% toward the background (`ColorU::mix`), which is the reference's own dimming of a folded entry; the verb is bold and reads `Muted` while folded; the operand keeps its family's colour (`Warning` for a path, a command or a URL, `Success` for a search pattern or glob, `Text` otherwise); the parenthetical detail is `Muted`; and nothing on a tool row is underlined at any weight. `SubAgentRowStatus::affordance` (`crates/goble-ui/src/elements/chat_content/sub_agent.rs`) and `build_sub_agent_child_view`'s live-record fallback (`app/src/ui/chat/sub_agent.rs`) both drop their old `◐` / `●` / `◇` for the one diamond, so a child's row and the call row that spawned it carry the same mark, and the open-child action sits 2 px off its header. The row and its body are `TOOL_ROW_FONT_SIZE` 12.0; the only 10 px element is `TOOL_ACTION_FONT_SIZE`, the one action a row owns (`open child`) while the row is retracted, and it grows to 12.0 once the row is open. Tests: `tests::tool_call::{the_rail_is_drawn_only_while_the_call_is_open, the_mark_is_dimmed_while_the_call_is_folded, the_row_is_twelve_px_and_a_retracted_action_is_ten, tool_rows_underline_nothing_they_did_not_receive}`, run red with each rule reverted: the rail drawn while folded; `COLLAPSED_DIM` 0.0; `TOOL_ACTION_FONT_SIZE` 12.0; and, for the underline test, both halves — the plain output line underlined, and an SGR underline the output emitted dropped. Stated plainly: grok-build has no per-row actions row and no 10 px font anywhere (its hints live in the bottom shortcuts bar), so the 10 px action is the user's ask met on goble's own row rather than a match of the reference, and the reference's group-header glyph `◈` is not implemented. **R66 — the footer keeps its place under the last message.** `build_conversation_footer` (`crates/goble-ui/src/views/chat_view/transcript.rs`) now ends in `Padding::new(column, EdgeInsets::new(0.0, 0.0, 0.0, md))`: the 12 px the transcript keeps under its own closing row belongs to that row, because it is what the anchored content ends on, and without it the Fork/usage band was pressed onto the composer's separator (measured in a 600×520 pane: band `Rect(59.0×21.2 at (0.0, 417.8))` inside viewport `Rect(600.0×439.0 at (0.0, 0.0))`). It had never been a flex sibling of the message area — it has always been the closing child of the message column inside the same `Scrollable` (with 40 messages in a 600×480 pane `max_offset()` is 1638.2 with the footer against 1605.0 without, so it scrolls away with the content) — so what the ask needed was the flush, not the placement. Five geometry tests read the drawn commands, in the new `views::chat_view::tests` module `the_footers_place`: `{the_footer_is_the_closing_row_of_the_scrolled_content, the_footer_keeps_its_gap_under_the_last_message_when_the_content_is_short, scrolling_the_transcript_carries_the_footer_with_the_last_message, the_footer_stays_under_the_last_message_while_it_streams, an_empty_transcript_draws_no_footer}`. Revert-proofs: the old `Some(column.finish())` fails two (`got 0, want 12`; the band against the viewport rect), and taking the footer out of the message column fails four of the five (`the transcript's room under its closing row at 520: got -33.200012, want 12`). `an_empty_transcript_draws_no_footer` is placement-independent by design and was not revert-proofed; the empty state itself is untouched. **R67 — the frame stops re-measuring text, and the follow glides.** A thread-local measurement cache (`crates/goble-ui/src/platform/text_atlas/cache.rs`, new) keys on the text, the `f32` bits of size / line height / wrap width, weight, family and italic, plus a size-only advance key, with a `static GENERATION` part of every key and `MAX_ENTRIES = 4096` (overflow clears); `measure_text_family` and `advance_width` look up and store, their bodies moved to the `*_uncached` functions the tests compare against, and `TextAtlas::prepare` clears the cache when the scale changes. Measured on a fixed 60-message transcript at 700×700 (throwaway probe, since removed): *the first frame ran 77 fontdue passes in 20.1 ms; the second was asked for 1981 runs, ran 0 passes in 1.29 ms*, against *1981 passes in 951.0 ms* and *1981 passes in 796.7 ms* with the cache bypassed. The follow now glides: `ScrollState` gained `painted_offset()`, `is_animating()` and `advance(dt: f32)` (with `FOLLOW_EASE_SPEED` / `_MIN` / `_MAX`), aiming a time-based ease-out-cubic at the pinned offset and travelling `from → to` over `clamp(distance/3000, 0.07 s, 0.2 s)`, re-aimed when the target moves, monotone, clamped to the end, snapped on a region's first layout, and cancelled instantly by a wheel; `Scrollable` also remembers the pointer from `MouseMove` so a wheel that arrives before the next frame is honoured; and `RootView::wants_animation` (`app/src/root_view/element.rs`) asks the pane scroll states for frames, with a 1 s stall guard. Tests: `platform::text_atlas::tests::{a_cached_measurement_equals_a_fresh_one_for_every_key, a_second_frame_of_the_same_transcript_measures_no_text}`, `elements::scrollable::tests::{a_follow_glide_reaches_the_end_without_overshooting, a_wheel_cancels_a_follow_glide, a_wheel_after_the_pointer_moved_in_scrolls_the_region}` and `root_view::element::tests::a_transcript_glide_keeps_the_frame_clock_at_the_active_rate`, each revert-proofed (cache bypassed → the second frame re-measures 1981 runs; size dropped from the key; the ease removed; `pointer_over` reverted; the glide branch removed from `wants_animation`). Left as it is, deliberately: the RootView dirty flag, which would need invalidation calls across `state/ui_state`, `ui` and `event` and could not be proven because the tree reads `Rc<RefCell<…>>` state the root cannot observe; the parse caches; the raw-pixel held offset, because `set_metrics` sees only the height along the scroll axis and so cannot tell a re-wrap from appended content, and the only heuristic would move a scrollback position two existing tests lock in; and `TextAtlas::prepare`'s own invalidation call, which no test covers because it needs a real wgpu device — the function it calls is tested directly. **R68 — the sidebar names a conversation after its own subject.** New `app/src/state/ui_state/title.rs` derives it deterministically with no model call, so a workspace with no provider key still gets named conversations: `agent_subject(stored_title, messages) -> Option<String>` pairs each assistant reply with the prompt it answered, takes the newest exchange that names something (`GREETING_SUBJECT = "Initial user onboarding"`, `MAX_WORDS = 6`, `MAX_CHARS = 48`, an `OPENERS` stop-list and a `GREETINGS` list), and answers `None` — never a rename — for the placeholder, for a subject equal to the stored one, and for a title the user typed, so a name the user gave is never taken back. `Store::set_chat_title` / `Store::get_chat_title` (`crates/goble-core/src/store/chats.rs`) and `DesktopState::set_chat_title` (`crates/goble-desktop-service/src/state/chats.rs`: store, then the in-memory list, then `chats:updated`, the path `set_chat_model` takes) carry it; `UiState::refresh_conversations` (`app/src/state/ui_state/refresh.rs`) derives it, writes it back only when it actually changed — so the `chats:updated` cannot feed the call into itself — and draws the derived subject in the same pass. `"Hello"` → `Initial user onboarding`; `"Can you add a title to the sidebar?"` → `Add a title to the sidebar`; `"Thanks!"` names nothing. Tests: the ten in `state::ui_state::title::tests`, `crates/goble-core/src/store/tests::test_chat_title_roundtrip`, `crates/goble-desktop-service/src/state/tests::service::setting_a_chat_title_reaches_the_store_the_list_and_the_bus`, and `ui::sidebar::sidebar_surface_tests::{a_conversation_the_agent_answered_reads_its_own_subject, a_later_turn_renames_the_conversation}`. Revert-proofs: `agent_subject` always `None` fails both sidebar tests on `"New conversation"` against `"Initial user onboarding"`; a freeze-after-the-first-title policy fails only the rename test; `is_greeting` → false fails the greeting tests; `Store::set_chat_title` → no-op fails the store and service tests; dropping the cache update and the emit fails on the list the sidebar is built from, and dropping the emit alone fails on `chats:updated`. The create path is unchanged and still pinned by `chat_flow::create_chat_persists_and_selects` / `blank_title_creates_default_agent` — a typed subject wins and a blank one lands on the placeholder, which the agent now replaces once it has answered. Verified with `cargo check --workspace --all-targets` → exit 0 (only the pre-existing `block v0.1.6` future-incompat note) and `cargo test --workspace --no-fail-fast` → **exit 0, 1886 passed / 0 failed / 8 ignored**, of which `goble-ui` lib **478 passed / 0 failed** (R63's 463 plus 15), `goble-app` lib **410 passed / 0 failed** (R63's 395 plus 15), `goble-core` lib **312 passed / 0 failed** and `goble-desktop-service` lib **55 passed / 0 failed**. Not verified anywhere in this batch: the pixels — the standing instruction is that the session does not drive the app, so every rule above is proven by the draw commands, the geometry and the state the dispatched events leave, and the GUI binary was neither rebuilt nor launched, so the instance still running from R62 predates all of it. Also not done, and said so: the title derivation lives in the app (the frontend that owns the sidebar) rather than in the service's turn loop, so `goble-desktop-native` gains `set_chat_title` without calling it and derives no titles; and the cost of deriving a subject during a long streaming turn was not measured (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R69–R71 — the sixth punch list (three asks): consecutive collapsed tool rows touch, no tool row prints an underline or a JSON payload, and the footer carries the reference row's own states:** three asks, one of which (the footer's states) was implemented partly by a subagent; every rule below was shown non-vacuous by reverting it and watching its test fail. **R69 — two folded tool rows touch.** `build_tool_call_rows` (`crates/goble-ui/src/elements/chat_message_bubble/tool_call.rs`) drops the column's uniform spacing and builds the gap as an explicit zero-width `Empty` child placed before each call, omitted exactly when the previous call and this one are both `Collapsed` — grok-build's own rule (`xai-grok-pager/src/scrollback/state/layout.rs:1541`, `gap_after` = 0 only when both entries are groupable and both collapsed, 1 blank row otherwise, and every `ToolCallBlock` is groupable). The column is `CrossAxisAlignment::Stretch` with no `with_spacing`, and each call keeps its own 2 px between its header and its body, so an open call still reads as a block. Test `tests::tool_call::two_collapsed_tool_rows_touch_and_an_open_one_keeps_its_gap` (bubble heights, not baselines, because an open row's rail shifts a header's origin by 2 px): two folded rows sit one row-height apart while the single-call bubble is 46.4 px and the two-call bubble 68.8 px — a difference of exactly the 22.4 px row — and opening the second adds exactly `SpacingToken::Sm`. Both halves revert-proofed: the condition forced to always-gap fails the collapsed pair, forced to never-gap fails the open neighbour (`the headers stand 22.400002 apart against 22.400002 + 8`). **R70 — no underline, and no JSON.** `output_spans` (`tool_body.rs`) ends every span with `with_underline(false)`, so a row of a command's output stays plain even when the command printed SGR 4; the pane's own executed-command block keeps the attribute, because that block reproduces a command's output and an agent row is a reply read as prose (grok-build has no decorative underline in `scrollback/blocks/`, and the only underlines that reach its tool bodies are data — ANSI SGR 4 in shell output and syntect's in file content). The legacy test `tool_rows_underline_nothing_they_did_not_receive` was rewritten rather than weakened: it asserted the emitted SGR underline was kept and now asserts a plain line, and a line whose run asked for it, are both drawn without one. The payload: `argument_rows` prints one `key: value` row per named argument (name `Muted`, value `Text`, both mono, 12 px, line height 1.4) for the integration family, and `other_body` prints a tool no family claims as its result alone — the row's own header already names it. A nested value now reads as its count (`1 field`, `3 items`, `argument_value` + `count_of`) instead of a payload; reverting that one line to `serde_json::to_string` printed `input: {"title":"Bug"}` and failed `an_integration_tools_arguments_are_named_not_printed`, and the same revert is what the reference forbids (its unknown-tool fallback prints `<Name>` plus the first string value, never the payload). Tests: `an_integration_tools_arguments_are_named_not_printed` (names `save_issue`, counts the nested input, asserts no row carries a brace or a bracket, and that the result closes the body) and the rewritten `expanded_generic_tool_call_draws_its_result_and_never_prints_json` (`["┃ ◆ list_entities agent", "2 entities"]`, no braces anywhere). **R71 — the footer's states.** `TurnActivity` (`crates/goble-ui/src/elements/turn_status.rs`) gained the two states the pane's live data can genuinely source. (1) `WaitingForResponse` — `"Waiting for response…"`, byte-identical to grok-build's `WaitingReason::Model` label — resolved by `pane_turn_status` (`app/src/state/transcript.rs`) when the turn is busy with no pending approval, no pending question, no in-flight call, and no reasoning row yet; the old arm read an empty reasoning list as `Thinking`, which claimed reasoning the model was not doing. (2) `WaitingOnSubAgent { description, activity }` — a spawn call is *awaited* (`harness/dispatch.rs` awaits `spawn_subagent`), so while it is in flight the footer names the child it is blocked on: `<description>: <activity>…` when the pane's live record carries the child (`SubAgentRow::activity_segment`, `initializing` before the child has reported), the reference's plain `"Waiting on subagent…"` otherwise. The tool row itself is now the row the tool's own family gives it — `tool_row` from `goble_core::harness::presentation`, the same parse the transcript's header uses — so a running call reads `Run cargo test -p goble-ui`, `Search "fn main"`, `Fetch https://example.com`, and a tool no family claims reads its name; the hand-rolled `Run {command}` prefix and the now-dead `command_of` are gone. `SubAgentRow`'s children are counted by `UiState::running_sub_agent_count` (only `is_running()` records), so the still-running line is `2 commands still running · 1 execution still running · 1 sub-agent still running`. Tests, each revert-proofed: `elements::turn_status::tests::{a_running_tools_row_is_the_row_its_family_gives_it, the_waiting_states_are_named, the_footer_draws_the_activity_and_the_elapsed_time_while_busy}` (the label emptied → `a command is Run and the command`; the detail form collapsed to the plain wait → `left: "Waiting on subagent…"` against `"fix flaky test: reading the schema…"`), and `state::pane_session_tests::live_events::{the_footer_waits_for_the_response_until_the_model_streams, the_footer_waits_on_the_child_the_spawn_call_is_awaiting, the_footer_reads_thinking_and_responding_from_the_reasoning_rows, the_footer_names_a_running_sub_agent_and_ignores_an_ended_one}`, rendered through the mounted `TurnStatusFooter` rather than read off the enum alone (the spawn arm forced to `Tool` → `left: Tool { name: "create_agent", … }` against `WaitingOnSubAgent`; the label reverted → `"Waiting…"`; the ordering swapped three ways; the child filter dropped → `count: 2` against `1`). **The states the reference row has that goble does not, and why** — recorded in `TurnStatusFooter`'s own doc so the gap is stated rather than silently missing: `Cancelling…` (a stop ends the pane's turn synchronously in `on_stop`, so no cancelling window is ever observed), `Verifying…` (no goal-mode verification), `Compacting…` and `Retrying (attempt N)…` (no live event — the pane's live surface is `chat:{tool,reasoning,usage,updated,ask_user,command_proposed,mission,turn_finished,subagent_*}`), `Sleeping…`, `Waiting on task output…` and `Waiting on tasks…` (the only awaitable work a pane spawns is a sub-agent child, which the sub-agent state names), `Running…` (every pane turn is an inference turn), `Starting session…` (no MCP init progress reaches the pane), the fork/worktree/restore command labels (no such command), and the right-hand token count `⇣Nk` (`chat:usage` reports what a conversation has *spent*, not the context the next request carries). Two deviations stated rather than hidden: the still-running line keeps goble's per-kind phrasing (`… still running` per kind) instead of the reference's counts-first `1 command · 2 monitors still running`, because that phrasing is an earlier workstream's own shape with a test pinning it; and the activity is one `Text`, so a tool's label is not split into a gray prefix and a coloured operand. Verified with `cargo check -p goble-ui -p goble-app --all-targets` → exit 0 (no new warnings; only the pre-existing `block v0.1.6` future-incompat note) and `cargo test --workspace --no-fail-fast` → **exit 0, 1893 passed / 0 failed / 8 ignored**, of which `goble-ui` lib **481 passed / 0 failed** (one-for-one with the test R70 rewrote) and `goble-app` lib **414 passed / 0 failed** (413 at the footer subagent's own end, plus the sub-agent-wait test), every app integration target green. **Not verified: the pixels.** The standing instruction is that the session does not drive the application, so the row spacing, the footer's states and the argument rows are proven by drawn commands, measured geometry and the state dispatched events leave — nothing was launched, screenshotted or clicked, and the GUI binary was neither rebuilt nor launched, so any instance still running predates this batch (`06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R72 — a tab is renamed by a double click again, and the gesture is counted on the press:** the report was that the tabs could no longer be renamed by double click. **The cause:** the gesture was timed from the *release*. `on_workspace_click` (`app/src/actions/make_actions.rs`) is reached by a `MouseUp` and compared its own `Instant::now()` against the instant the **previous release** had written, so the window ran release-to-release — the button's own hold time pushed the pair apart — and the release was the only thing that could start the rename, so a pair the timing missed renamed nothing. **The rule now in force is warp-new's own** (`crates/octomusui/src/windowing/winit/event_loop/mod.rs:45,199-221`: `MULTI_CLICK_INTERVAL = 400 ms`, counted **press-to-press**). `WorkspaceChip::dispatch_event` (`app/src/ui/shell/topbar.rs`) reports the primary press through the new `on_press` *before* offering the release to `handle_mouse_event`, and `on_space_press` keeps the run in app state — `UiState::space_press_run` (`SpacePressRun { index, pressed_at, count }` and `MULTI_CLICK_INTERVAL_MS` in `app/src/state/ui_state/mod.rs`) — so the count becomes `count + 1` only when the previous **press** named the same tab and landed inside the window, and a press on another tab, or one after the window, starts a run of its own. `on_workspace_click` reads the count of the press it closes and, at 2, calls the new `UiState::begin_space_rename(index)` — **one entry point** for both paths that start a rename (the double click here, the menu's "Rename tab" in R73), which makes the tab the active one first, because the field is drawn in the *active* tab's slot (`build_workspace_strip`) and renaming a tab while another is on screen would otherwise open a field the user cannot see. `handle_mouse_event` (`crates/goble-ui/src/elements/interactive.rs`) now ignores every non-primary button on both press and release, so a right press is no longer a press of the control it lands on; `BUTTON_PRIMARY` / `BUTTON_SECONDARY` (`crates/goble-ui/src/event.rs`) name the two the platform maps from winit. **Tests, each shown red with its rule reverted:** `a_double_click_on_a_tab_opens_its_rename_field` (the second click opens the field, it is still open on the next frame, its caret is drawn inside the tab's own rect, and a key typed while it is open reaches the draft), `a_double_click_renames_a_tab_that_is_not_the_active_one` (a two-tab strip with the pointer never moving: the first click selects the tab under it, the second opens *its* field, focused), and `a_held_second_press_still_renames_the_tab` — the distinguishing case: the second press lands 200 ms after the first and is held 300 ms, so the **presses** are inside the 400 ms window while the **releases** stand 500 ms apart, which the test asserts itself. Reverting the press branch out of `WorkspaceChip::dispatch_event` fails `a_double_click`; writing `count` where the press wrote `count + 1` fails it too; and re-timing the gesture the old way (a previous-release instant compared against the release) fails `a_held_second_press` at `the second press opens the field, whatever the release that follows it does`, its span assertion still reporting the releases past the window. **Not verified: the pixels** — the session does not drive the app, so nothing was launched, clicked or screenshotted; the tab strip has no owning doc (the drawn strip is described only in `06-renderer/ui-spec/10-settings-tab.md`'s file table)
- [x] **GUI polish R73 — a right click on a tab opens its menu at the pointer, over the window:** warp-new's own tray (its `app/src/tab.rs` opens the panel on `on_right_click` with `TabContextMenuAnchor::Pointer(position)`, the panel an overlay at the pointer), with the rows that were asked for. **The panel hangs from the pointer**, which the app's `PopupMenu` cannot do — it anchors its panel to its trigger's own rectangle — so a new `PointerMenu` (`crates/goble-ui/src/elements/pointer_menu.rs`) holds a menu whose trigger is nothing: it lays the menu out with the window's constraint, takes no room of its own (`size()` is zero), and computes `placed()` at **paint**, where the panel's size is known and where the menu records the origin it hit-tests against, so placement and hit-testing agree by construction; the anchor is clamped so the whole panel stays `EDGE_MARGIN` (6 px) inside the window, and an unbounded frame — a tree laid out loose, as an offline test lays one out — leaves the anchor where the pointer was. **The menu is new `app/src/ui/tab_menu.rs`:** `build_tab_menu(tab, at, on_action, on_close)` wraps the app's own `PopupMenu` over an `Empty` trigger, so a tab menu is the same surface, the same rows, the same hover and the same close-on-a-press-outside as every other menu of the shell. **Its rows are warp-new's, cut to what goble has something to do with** (`app/src/tab.rs::modify_tab_menu_items` :409-463 and `close_tab_menu_items` :496-532): "Rename tab" always; "Reset tab name" only for a name the user typed (`named`); "Move Tab Left" only with a slot to move into; "Close tab" always; "Close other tabs" only with a second tab to close. One ordered list of `(label, action)` pairs, so a drawn row and what it does cannot drift apart. **The colours are its last row**, drawn as the reference draws them (`dot_color_option_menu_items` :576-640 with `app/src/ui_components/color_dot.rs`): a "Default (no color)" slashed circle first, then the six presets of `TabColor::ALL` (`crates/goble-ui/src/theme.rs`) — each a 16 pt dot in a 2 pt ring, a 20 pt box so a ringed dot and a plain one are the same size — laid out `SpaceEvenly` with a 10 pt floor, each its own click target under a tooltip drawn above it, the tab's own colour the ringed one; a dot toggles (`SetColor(None)` clears), so the tab's own dot is how a colour is taken back. **A coloured tab fills with its colour:** `WorkspaceChip` paints through `TabColor::tint_over(base, tint)` — the colour mixed over the fill that state would have had anyway, at 60 % active / 40 % hovered / 20 % resting, the share the reference's own tint uses (`render_tab_container_internal` :1378-1385) — and a tab with no colour paints exactly the three fills it painted before colours existed. **Where it lives:** the menu is the third child of the bar's `Stack`, after the bar and the pane-drag ghost, so it is drawn over the bar and over the pane below it and is dispatched first; `build_space_menu` returns a zero-sized `Empty` while none is open. **Who acts:** `WorkspaceChip` reports a secondary press with the pointer position; `on_space_menu` opens the menu, or closes it when the same tab is right-clicked again (the reference's toggle); `on_space_menu_close` drops it when the panel closed itself; `on_space_menu_action` closes it first — choosing a row closes a menu whether or not the row does anything — then Rename → `begin_space_rename` (R72's one entry point), ResetName → `named = false` (the label is derived again the next time the tree is rebuilt, so a terminal tab reads its directory once more and an agent tab its conversation's subject; the explicit `refresh_space_labels()` the arm first carried was dropped as redundant with `root_view/rebuild.rs:48`), MoveLeft → `reorder_space(index, index - 1)`, Close → `close_space_at`, CloseOthers → every other tab from the far end inwards so the index of the tab being kept never shifts, SetColor → the tab's colour; each of the four that changes what the spaces are saves them through `save_panes`. **`PopupMenu` gained the three things this needed, each general:** `with_footer_row`, a row below the capped item list and outside its viewport; `with_on_close`, fired when the menu closes itself on a press outside; and two rules — a press on the panel's own surface is swallowed rather than handed to whatever it covers, and only the **primary** button dismisses a menu, so a second right press toggles the menu instead of reopening it. **Tests:** `a_right_click_hangs_the_tab_menu_from_the_pointer_over_the_window` (the panel's near corner is the point the click landed on, the panel is drawn over the pane below the bar, and the rows follow in order), `the_tab_menus_rows_are_the_ones_that_apply_to_that_tab`, the five row tests (`the_tab_menus_rename_row_opens_the_inline_field`, `the_tab_menus_reset_row_gives_the_typed_name_back`, `the_tab_menus_move_left_row_swaps_the_tab_with_its_neighbour`, `the_tab_menus_close_row_closes_that_tab`, `the_tab_menus_close_others_row_leaves_only_that_tab`), `a_colour_dot_tints_the_tab_and_the_same_dot_clears_it`, the three panel rules (`a_press_on_the_menus_own_surface_never_reaches_the_tab_under_it`, `a_press_outside_the_tab_menu_closes_it`, `a_second_right_click_on_the_same_tab_closes_its_menu`), `ui::shell::tests::a_coloured_tab_tints_the_fill_its_state_gives_it`, `tab_menu::tests::{the_rows_are_the_ones_that_apply, each_row_carries_its_own_action, the_colour_row_is_the_no_colour_dot_then_every_preset}` and `elements::pointer_menu::tests::{the_panel_opens_at_the_pointer_and_takes_no_room, a_panel_at_the_windows_corner_is_pulled_back_inside_it, an_unbounded_frame_leaves_the_anchor_where_the_pointer_was}`. **Every one of the sixteen rules behind them was reverted and watched to fail**, in five scripts: the right-press branch out of `WorkspaceChip`; `placed()` forced back to the anchor; the panel's swallow rule dropped; the dismissal widened to any button; `on_close` removed; the tab tint dropped; the dot's toggle removed; the "Reset tab name" gate widened; the Rename / Reset / Move Left / Close / Close others arms stubbed; the drawn row list replaced by one repeated label; the colour write dropped; and the menu removed from the bar's `Stack` — each failing its own test, with the tree checked byte-identical afterwards (by hash of every touched file and by `git diff --stat`). **Left out on purpose, from the menu's own module doc:** warp-new's tray also carries "Move Tab Right", "Close Tabs to the Right", the tab-group rows, session sharing, "Save as new config" and the pane-name rows — goble has no tab groups, no shared sessions and no tab configs, and the remaining move/close directions were not asked for. **Not verified: the pixels** — the session does not drive the app, so the panel's raised surface, the dots' colours, the tooltips and the tint's share are proven by drawn commands and the geometry they carry, never seen

- [x] **CI — the release workflow is valid again, and the terminal gate has its zsh:** two independent failures, both found by reading the runs rather than guessing. (1) `.github/workflows/release.yml` read a secret in a step's `if` — `jobs.build-app`'s `Import Apple signing certificate` step carried `if: matrix.platform == 'macos' && secrets.APPLE_CERTIFICATE_P12 != ''`. GitHub rejects that expression, and because it rejects the expression it rejects the whole file, so every push since `3b83a3c` (2026-09-10) produced a run with **zero jobs** annotated `Invalid workflow file: .github/workflows/release.yml#L1 — (Line: 205, Col: 13): Unrecognized named-value: 'secrets'. Located at position 31 within expression: matrix.platform == 'macos' && secrets.APPLE_CERTIFICATE_P12 != ''`; the release path, a `workflow_dispatch` workflow whose file is invalid, could not start at all. The certificate's *presence* is now a job-level flag on `build-app`, `APPLE_SIGNING_CONFIGURED: ${{ secrets.APPLE_CERTIFICATE_P12 != '' }}`, tested as `env.APPLE_SIGNING_CONFIGURED == 'true'` — `env` is a context a step's `if` may read and `secrets` is not — and every other use of the secret stays where it was always legal (step `env:` values). `origin/main` carries the same construct, so this branch fix is what repairs the repository. (2) `ci.yml`'s terminal gate: `crates/goble-terminal/tests/shell_integration.rs` runs one test per shell, and on a runner without zsh `find_shell("zsh")` returns `None`, so `zsh_script_emits_the_hook_channel` panicked `zsh is required to check its integration script` (deliberately loud, so a missing shell cannot pass for a working one) — the ubuntu image ships bash only. The job gained an `Install zsh` step (`sudo apt-get update && sudo apt-get install -y zsh`) ahead of the suite. **Evidence:** `actionlint` 1.7.12 (installed through Homebrew) reports no validity error on either file; what it still raises are two pre-existing shellcheck notes, `SC2046` at `release.yml:214` (the intentionally word-split `security list-keychains` expansion) and `SC2086` at `release.yml:419`, neither of which invalidates a workflow. After pushing `219bda3` the gate is green: push run `34797468045` and PR run `34797472531` both `completed/success`, job `terminal / conformance` with `Install zsh: success` then `Run the terminal emulator conformance suite: success`, `Summary 131 tests run: 131 passed, 0 failed`, and `zsh_script_emits_the_hook_channel` and `bash_script_emits_the_hook_channel` both `PASS`; no `release.yml` workflow-file run was created for that push, where every push since 2026-09-10 created one. **Not verified:** the release path has never executed a build — it is dispatch-only and its one run (2026-07-26) was cancelled — so validity is proven and a real signed release is not; and nothing was built or run locally, because `target/` had just been cleaned, so the evidence is the runner's own log.
- [x] **Agent UI chrome C3 — the topbar live indicator:** the app's `build_topbar` (`app/src/ui/shell.rs:56-123`) is a two-child `SpaceBetween` row with no slot for status. Add a live indicator between the workspace strip and the settings button - a spinner plus `* N` from C1's state when work is in flight, inert at zero - without changing `TOPBAR_HEIGHT` or the workspace controls, and without switching the app to the unused `goble-ui::Topbar`. Depends on C1 (`agent-tui.md` §11). **Done:** `build_topbar` gains a third child between `left` (the workspace controls) and the settings icon — `build_live_indicator` (`app/src/ui/shell.rs`) draws `RunningIndicator` + `◆ N` where N is C1's `LiveWork::work_count()` = the active pane's in-flight tool calls plus every running execution; it is a `LiveWorkCue` hover/click hit target whose click fires `UiActions::on_open_work` → `UiState::open_work_overlay`. At zero it is a zero-size `Empty` that draws nothing and cannot be clicked, so the bar keeps its current look. `UiSnapshot` gains `live_work_count`/`live_work_phase`, read once from `live_work()` in `RootView::rebuild`; `TOPBAR_HEIGHT` (36/40) and the workspace chips and `+ ▾` control are untouched, and the app keeps its own topbar. No context/token indicator is drawn: no live event carries a token count. The work overlay is S7, so the click currently fires an action with **no visible surface yet** — stated in code and here. Verified by `cargo test -p goble-app` (`ui::shell::tests::{the_cue_shows_the_live_count_and_a_spinner,the_cue_is_inert_at_zero,the_cue_click_fires_the_work_overlay_action,the_topbar_height_is_unchanged}`, `root_view::tests::the_topbar_cue_reads_the_live_count_and_opens_the_work_overlay`)
- [ ] Replace or re-license the icon assets in `crates/goble-ui/assets/icons/` that are byte-identical to files in the AGPL side of `~/Projects/warp-new` (`terminal-emulator.md`). Counted at 42 before R14; R14 adds the project explorer's set (`file.svg`, `folder-closed.svg`, `star.svg`, `star-filled.svg` and the 21 `file_type/*.svg` drawn from the same side) on the user's instruction that the file icons "nu sunt protejate de licenta" — so the instruction is that this set is claimed not to need the replacement, and that claim is the one to check when this item is worked.
- [ ] Fix the `POST /pair` hash check (it stores any hash and always answers `paired: true`) and set the pairing hash at provision time (`remote-terminal-renderer.md`)
- [ ] Set `GOBLIN_TLS_BUNDLE` in the Helm chart and `install-goblin.sh` — the chart mounts the bundle but never points the worker at it, so deployed workers start plaintext (`remote-terminal-renderer.md`)
- [ ] Define the remote terminal session + stream protocol (metadata, frames, input, resize) (`remote-terminal-renderer.md`)
- [ ] Render the **session chip** (host badge) above the composer, per warp-new (`remote-terminal-renderer.md`)
- [ ] Render remote PTY output in the local chat (`remote-terminal-renderer.md`)
- [ ] Add a `RemoteTransport`-style trait + bounded reconnect with backoff; keep mTLS over WebSocket rather than switching to SSH (`remote-terminal-renderer.md`)
- [ ] (Later) optional SSH transport backend and optional renderer-on-remote frame streaming (`remote-terminal-renderer.md`)
- [~] Add overlay + form primitives to `goble-ui` — `Dialog` (backdrop + centered panel) in; `FormField`/`SecretField`/`Toggle` pending (`form-components.md`)
- [ ] Port the provider/API-endpoint connector logic from grok-build model config (`form-components.md`)
- [~] Build the provider-settings overlay form (`app/src/ui/model_form`) done; remote-bootstrap composer pending (`form-components.md`)
- [ ] Let skills/plugins request structured input through the overlay (`form-components.md`)
- [x] **Agent UI follow-up R3 — remove the sidebar footers:** the left conversation sidebar's two bottom rows are gone — the `Plugins` label with its `plus` `TopbarButton` (which opened the MCP connectors panel) and the `Screen` label with its `monitor` `TopbarButton` (which opened the screen sheet), plus the trailing `Divider` that opened the footer block. `app/src/ui/sidebar.rs` dropped the `TopbarButton`/`AiActions`/`ScreenActions` imports and `build_sidebar`'s `ai_actions`/`screen_actions` parameters (its one caller, `build_ui`, was updated); the search field, New conversation row, conversation list, `View all`/`Show less` control and the `Scrollable` list state are unchanged. Both panels stay reachable from the `Cmd+K` palette (`Open connectors`, `Open screen`) — those entries are untouched. No test asserted either row, so none was removed. Verified with `cargo test -p goble-app` (lib `195 passed; 0 failed`) (`06-renderer/ui-spec/04-conversation-sidebar.md`; that doc's "Plugins footer" section and `09-testing-checklist.md`'s "Plugins footer" visual line still describe the removed row and are stale)

## 07 — Observability
- [x] Drain `executions:updated`/`agent:*` events into app state - **delivered by the chrome item C1** (`agent-tui.md` §11), which is what marks this row: `drain_events` handles the five `agent:*` names into `UiState`'s live execution records and `executions:updated` still refreshes the pages; the accessor is `UiState::live_work()` (`executions-and-trace.md`)
- [ ] Add an executions list + per-execution trace view in the renderer (`executions-and-trace.md`)
- [ ] Add an **agent-level** observability view (live status, sub-agent tree, trace) (`07-observability/README.md`)
- [ ] Make local harness runs feed the same trace path as remote (`executions-and-trace.md`)
- [ ] Add a logs view (filter by execution/worker/level) (`logs.md`)
- [ ] Route the `agent:log` events into app state (`logs.md`)
- [ ] Run all log lines through the secret sanitizer before display/emit (`logs.md`)
- [~] Build our own crash reporting with local-always capture and consent-gated upload — `crates/goble-telemetry` is written and tested (config, redaction, breadcrumbs, fingerprint, durable queue, upload worker, panic hook); no collector is deployed and the endpoint is `https://telemetry.goble.dev` (`crash-reporting-and-updates.md`)
- [x] Honour `DO_NOT_TRACK` — precedence is `DO_NOT_TRACK` > `GOBLE_TELEMETRY` > the stored user choice > the channel default; the reference implementation has no `DO_NOT_TRACK` support at all (`crash-reporting-and-updates.md`)
- [~] Stand up the collector — `crates/goble-telemetry-server` serves `/health`, `POST /v1/crashes`, `POST /v1/events`, `GET /v1/summary` with an optional shared token and a bounded body; it has never been deployed (`crash-reporting-and-updates.md`)
- [~] Deliver updates from a signed manifest — `crates/goble-update` validates the manifest, verifies an ed25519 signature over a fixed text payload, checks the sha256 while downloading, and produces pure install plans; no release signing key exists yet (`crash-reporting-and-updates.md`)
- [ ] Surface an available update in the UI and wire `plan_install().apply()` + the relaunch handoff (`crash-reporting-and-updates.md`)
- [ ] Add a first-run telemetry notice (`TelemetryState.notice_shown`) and a settings toggle writing `~/.goble/telemetry.json` (`crash-reporting-and-updates.md`)
- [ ] Add minidump capture on Linux and Windows — a Rust panic hook catches panics only (`crash-reporting-and-updates.md`)
- [ ] Split debug info + upload symbols per release so CI backtraces resolve (`crash-reporting-and-updates.md`)
- [~] Ship installers: macOS DMG with a custom background image, Linux AppImage + `.deb` + tarball, Windows per-user Inno Setup with an EULA page — scripts exist and are shell/YAML-checked, but none has been executed (`crash-reporting-and-updates.md`)
- [ ] Generate the release signing key pair and make `--manifest` sign in CI (`crash-reporting-and-updates.md`)
- [x] Make official builds signed instead of signed-if-convenient — `GOBLE_REQUIRE_SIGNING=1` turns a missing credential into a build failure, `scripts/release.sh` sets it for every channel except `dev`, and `--allow-unsigned` is the explicit local escape hatch (`crash-reporting-and-updates.md`)
- [x] Complete the macOS signing path — hardened runtime with a deliberately empty `packaging/macos/entitlements.plist` that says why, no `--deep`, `codesign --verify --strict` after signing, and `spctl --assess` on the DMG and the app after notarisation, which is the check that answers whether Gatekeeper accepts the download (`crash-reporting-and-updates.md`)
- [x] Sign the Windows application executable, not just the installer — the exe is signed from a staged copy before packaging (never `target/release/` in place) and both it and the setup exe are checked with `signtool verify /pa` (`crash-reporting-and-updates.md`)
- [ ] **Obtain the certificates** — Apple Developer Program + Developer ID Application (99 USD/year), and a Windows OV/EV code-signing certificate (~200–500 USD/year) — and add them as repository secrets; no release can be produced until then by design (`crash-reporting-and-updates.md`)
- [ ] Run one release with the real certificates and confirm the notarisation round trip, `spctl`, and `signtool verify` on a real artifact (`crash-reporting-and-updates.md`)
- [ ] Extend packaging to RPM, Flatpak, Homebrew cask and winget, and add arm64 Linux/Windows runners (`crash-reporting-and-updates.md`)
- [x] Set a Windows version floor in the installer — `MinVersion=10.0.18362` in `packaging/windows/goble.iss`, because portable-pty is ConPTY on Windows and Inno's default would install on Windows 7, where the terminal cannot start at all (`crash-reporting-and-updates.md`)
- [ ] Generate `THIRD-PARTY-NOTICES.md` from the dependency graph (`cargo about` + template, as the reference does) instead of maintaining the table by hand (`crash-reporting-and-updates.md`)
- [ ] Decide whether to ship our own ConPTY (`conpty.dll` + `OpenConsole.exe`) so Windows 10 1809 is supported (`crash-reporting-and-updates.md`)

## 08 — Threads (deferred)
- [ ] Define the thread-messaging server hosted on the worker (participants = humans + agents) (`../08-threads-deferred/README.md`)
- [ ] Reuse `goble-core::thread` types + existing thread store (`../08-threads-deferred/README.md`)
- [ ] Add the threads UI to the native shell (`../08-threads-deferred/README.md`)

## 09 — Mobile (deferred)
- [ ] Wire the local machine as a Tailscale-exposed worker (self-as-worker) (`../09-mobile-deferred/README.md`)
- [ ] Confirm client/workspace + harness event contracts are mobile-friendly (`../09-mobile-deferred/README.md`)
- [ ] Scaffold the Kotlin client against the same workspace/routing contract (`../09-mobile-deferred/README.md`)

## 10 — Platform & performance
- [ ] Confirm all three wgpu backends + DPI resize are covered (`wgpu-renderer-platform.md`)
- [ ] Profile the atlas construction (text/icon) path (`wgpu-renderer-platform.md`)
- [ ] Add a frame-time debug overlay + histogram (`performance.md`)
- [ ] Virtualize long message lists / transcripts (`performance.md`)
- [ ] Move streaming decode off the main thread; confirm frame budget holds (`performance.md`)
