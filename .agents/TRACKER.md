# Goble architecture tracker

Aggregated, ordered work across the whole tree. Each item points to the owning doc; each doc also carries the same `[ ] / [~] / [x]` markers. Reorder/prioritize freely; the point of this file is that the `.agents/` tree is a **live tracker**, not prose.

Legend: `[ ]` pending · `[~]` in progress · `[x]` done.

## 02 — First-run & routing
- [x] Show an "empty LLM key" banner as a modal overlay instead of the canned reply — the canned reply is no longer sent on the no-key path; the banner is a centered `Dialog` overlay over the whole app (`onboarding-no-model-key.md`)
- [x] Wire the banner click to the model-provider overlay dialog (`onboarding-no-model-key.md`)
- [x] After a key is saved, close the dialog and move to the workspace-type prompt (`onboarding-no-model-key.md`)
- [x] Define the `Routing` decision type and attach it to a conversation — `WorkspaceRouting{Local,Remote}` is persisted per conversation on the `chats.workspace_routing` column and restored on load (`router-local-vs-remote.md`)
- [~] Make `local` a valid runtime target — chat turns run on the local harness for `local`/unset routing; the `resolve_worker_for_target` `bail!` for the agent/workflow deploy path is still pending a local agent runner (`router-local-vs-remote.md`)
- [x] Persist the routing choice across restarts (`router-local-vs-remote.md`)
- [ ] Define the "workspace package" to ship to a remote host (`remote-bootstrap.md`)
- [ ] Add the remote self-configuration step (read TOML → resolve providers/models) (`remote-bootstrap.md`)
- [ ] Reuse the existing pairing/SSH/helm path to bring up the remote workspace (`remote-bootstrap.md`)
- [ ] Publish a workspace endpoint the router can point at (`remote-bootstrap.md`)

## 03 — Workspace model
- [ ] Introduce a `Workspace` type (id, kind, root, config) and persist it (`workspace.md`)
- [ ] Allow a workspace `kind` to be promoted local → remote while keeping the same id (`workspace.md`)
- [ ] Make the provisioned worker bundle a remote workspace record (endpoint, TLS, worker id) (`workspace.md`)
- [ ] Extend `GobleConfig` to cover plugins, workflows, personas, memory, deep-research (`shared-secrets-and-toml.md`)
- [ ] Add a config-is-agent-editable surface (agent reads/patches the TOML, never the vault) (`shared-secrets-and-toml.md`)
- [ ] Add outbound secret-scrubbing on logs/traces/events (`shared-secrets-and-toml.md`)
- [ ] Ship the TOML + encrypted vault blob together in remote bootstrap (`shared-secrets-and-toml.md`)
- [ ] Add `workspace_id`, `persona`, `cwd` + per-agent conversation key to the agent model (`multiple-agents.md`)
- [ ] Persist per-agent state so an agent survives restarts (`multiple-agents.md`)
- [ ] Let agents be created/edited from natural language and bound to workspace config (`multiple-agents.md`)
- [x] Populate a credential from the ask-user card by name and keep the value out of the transcript — card gains name + masked value; harness `credentials` tool lists names and `run_command` expands `{{credential:<name>}}` server-side, so the model never sees the key (`shared-secrets-and-toml.md`)
- [x] Give the machine a hidden home `~/.goble` mirroring `~/.grok`, split into a universal **base** (identity/auth/config/sessions/logs, for every user) and a **workspace payload** (bundled tooling, worktrees, threads, local store — only when the workspace runs locally); remote-only users keep a minimal home (`workspace.md`, `shared-secrets-and-toml.md`)
- [x] Track every principal with access + their grants — `access_grants` table + store methods; harness `principals` tool lists principals and grants; per-principal `principals/<id>/` home dir (`shared-secrets-and-toml.md`)
- [x] Seed a Goble user guide into `~/.goble/docs/user-guide` on first launch (ported from `.grok` and Goble-ified; base-only, so it exists for remote-only users too) and expose a `user_guide` harness tool (list/read topics) + a system-prompt pointer so the model answers product questions from the guide — incl. remote/self-as-worker via Tailscale and mobile access (`workspace.md`, `docs.rs`/`app_home.rs`)

## 04 — Agent runtime (harness)
- [x] Fix the hardcoded `workspace_root = /var/goblin/workspaces` default in `goblin-worker` so harness tests/dev runs don't hit `Permission denied` on machines without that dir (currently breaks `goblin-worker` runner/scheduler unit tests and `goble-cli`'s `e2e_worker` in a stock dev environment) (`sandbox-and-cwd.md`)
- [ ] Define durable **state** vs compacting **transcript** boundaries (`agent-state-and-compaction.md`)
- [ ] Wire compaction triggers by token/size budget; persist the rolling summary (`agent-state-and-compaction.md`)
- [ ] Key state by `agent_id` and make it survive local↔remote routing (`agent-state-and-compaction.md`)
- [ ] Add a `SubAgent` type with bounded budget + lifecycle (`subagents.md`)
- [ ] Spawn sub-agents so the main chat loop stays responsive (`subagents.md`)
- [ ] Surface routine progress/results in the renderer (`subagents.md`)
- [ ] Build the tool-registry abstraction with a uniform schema (`tools.md`)
- [ ] Load skills as tool-adjacent docs and inject into context (`tools.md`)
- [ ] Emit tool-call events to renderer + trace (`tools.md`)
- [ ] Drive MCP install/discover from the harness, not only a UI panel (`mcp.md`)
- [ ] Let an agent request an MCP install mid-conversation (`mcp.md`)
- [ ] Surface MCP server status + tool list in the renderer (`mcp.md`)
- [~] Reuse `goble-core::llm` + grok-build sampler for the loop (`llm-and-models.md`)
- [x] Wire the composer's model selector to really change the model (`llm-and-models.md`)
- [~] Stream deltas to renderer + trace in real time — renderer streaming done; per-token trace + reasoning deltas pending (`llm-and-models.md`)
- [ ] Define the workspace message-bus contract (addressing, message kinds) (`inter-agent-communication.md`)
- [ ] Implement the local in-process channel; reuse worker transport for remote (`inter-agent-communication.md`)
- [ ] Enforce loop bounding and show agent↔agent activity (`inter-agent-communication.md`)
- [ ] Give each agent + sub-agent a CWD subdir under the workspace root (`sandbox-and-cwd.md`)
- [ ] Tighten isolation (fs/network) per workspace, reusing `xai-grok-sandbox` where portable (`sandbox-and-cwd.md`)
- [ ] Stream remote terminal output to the local renderer (`sandbox-and-cwd.md`)
- [ ] Add a `MemoryNote` store with embedding-based search (`memory.md`)
- [ ] Expose `remember` + a query tool to agents (`memory.md`)
- [ ] Wire retrieval into context assembly (`memory.md`)
- [ ] Define the `Persona` config and store it in the TOML (`personas.md`)
- [ ] Assemble system prompts from persona + config (`personas.md`)
- [ ] Let personas be created/edited from natural language (`personas.md`)
- [ ] Add a `DeepResearch` routine (steps, budget, progress events) (`deep-research.md`)
- [ ] Render deep-research progress + final report (`deep-research.md`)
- [ ] Allow saving a research result to workspace memory (`deep-research.md`)

## 05 — Execution router & targets
- [ ] Add `Local` + `SelfAsWorker` to the runtime-target set; remove the `local` bail (`runtime-targets.md`)
- [ ] Represent each target's connection params (`runtime-targets.md`)
- [ ] Map a workspace `kind` to a default target, allow per-conversation override (`runtime-targets.md`)
- [ ] Implement `resolve()`, honoring worker-pairing status (`execution-router.md`)
- [ ] Cache the resolved target per conversation (`execution-router.md`)
- [ ] Implement local→remote promotion, preserving agent+conversation state (`execution-router.md`)

## 06 — Renderer
- [x] Merge the hot-reloadable UI crate into the app — the UI builder (`build_ui`) + snapshot/action types + per-screen modules now live in `app/src/ui`, built directly in the app crate like warp-new builds its windows in `app`; `goble-ui-hot`, the cdylib/`#[no_mangle]`/ABI boundary and the hot-reload handshake are gone (`renderer-architecture.md`)
- [x] Move `integration_testing/` under `app/src/integration_testing/` and point the `app` test targets there (`renderer-architecture.md`)
- [x] Route chat turns through `app/src/runtime` (`crate::runtime::run_turn`) — local harness today; remote degrades to local with a logged warning (`renderer-architecture.md`)
- [x] Drive the chat from the harness (replace the canned reply with real events) (`renderer-architecture.md`)
- [x] Render tool calls distinctly — `role="tool"` rows render as terminal blocks, and the assistant message shows "⚙ tool" invocation chips (`renderer-architecture.md`)
- [x] Align chat + composer with warp-new's UI model — tool/command blocks render as `surface_2` raised cards (1px border, radius 8); the composer is a floating `surface_2` card (border, radius 8, 80px editor) with `surface_1`→`surface_2` pill buttons for model / profile / attach / stop; `ColorToken` maps to warp-new's `surface_1`/`surface_2`/`outline`/`text_sub` (`design-tokens.md`)
- [~] Warp-new parity, still pending: per-message author/avatar header row, per-status tool icons (running/blocked/success/failed), the status/action footer (thumbs / continue / fork / usage), and live tool-call streaming to the renderer (`design-tokens.md`)
- [ ] Add the missing product pages (agents, executions/traces, logs) to the native shell (`renderer-architecture.md`)
- [~] Wire the model selector + attach/voice to real behavior — model selector drives `run_chat_turn` and the Stop button cancels a turn (`renderer-architecture.md`)
- [ ] Define the remote terminal session + stream protocol (metadata, frames, input, resize) (`remote-terminal-renderer.md`)
- [ ] Render the **session chip** (host badge) above the composer, per warp-new (`remote-terminal-renderer.md`)
- [ ] Render remote PTY output in the local chat (`remote-terminal-renderer.md`)
- [ ] (Later) optional renderer-on-remote frame streaming (`remote-terminal-renderer.md`)
- [~] Add overlay + form primitives to `goble-ui` — `Dialog` (backdrop + centered panel) in; `FormField`/`SecretField`/`Toggle` pending (`form-components.md`)
- [ ] Port the provider/API-endpoint connector logic from grok-build model config (`form-components.md`)
- [~] Build the provider-settings overlay form (`app/src/ui/model_form`) done; remote-bootstrap composer pending (`form-components.md`)
- [ ] Let skills/plugins request structured input through the overlay (`form-components.md`)

## 07 — Observability
- [ ] Drain `executions:updated`/`agent:*` events into app state (`executions-and-trace.md`)
- [ ] Add an executions list + per-execution trace view in the renderer (`executions-and-trace.md`)
- [ ] Add an **agent-level** observability view (live status, sub-agent tree, trace) (`07-observability/README.md`)
- [ ] Make local harness runs feed the same trace path as remote (`executions-and-trace.md`)
- [ ] Add a logs view (filter by execution/worker/level) (`logs.md`)
- [ ] Route the `agent:log` events into app state (`logs.md`)
- [ ] Run all log lines through the secret sanitizer before display/emit (`logs.md`)

## 08 — Threads (deferred)
- [ ] Define the thread-messaging server hosted on the worker (participants = humans + agents) (`../08-threads-deferred/README.md`)
- [ ] Reuse `goble-core::thread` types + existing thread store (`../08-threads-deferred/README.md`)
- [ ] Add the threads UI to the native shell (`../08-threads-deferred/README.md`)

## 09 — Mobile (deferred)
- [ ] Wire the local machine as a Tailscale-exposed worker (self-as-worker) (`../09-mobile-deferred/README.md`)
- [ ] Confirm client/workspace + harness event contracts are mobile-friendly (`../09-mobile-deferred/README.md`)
- [ ] Scaffold the Kotlin client against the same workspace/routing contract (`../09-mobile-deferred/README.md`)

## 10 — Platform & performance
- [ ] Confirm all three wgpu backends + DPI resize are covered (`wgpu-renderer-platform.md`)
- [ ] Profile the atlas construction (text/icon) path (`wgpu-renderer-platform.md`)
- [ ] Add a frame-time debug overlay + histogram (`performance.md`)
- [ ] Virtualize long message lists / transcripts (`performance.md`)
- [ ] Move streaming decode off the main thread; confirm frame budget holds (`performance.md`)
