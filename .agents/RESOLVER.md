# Resolver — live work registry

**Status:** `[x]` mechanism adopted
**Owns:** the single item being resolved **right now**, and its proof.
**Depends on:** [`TRACKER.md`](TRACKER.md) (the full backlog), [`GUIDE.md`](GUIDE.md) (how to execute).

`TRACKER.md` is the complete backlog. **This file is the living side of the plan**: it records exactly one item in flight, its plan, and the verification that proves it is done. Its whole purpose is that neither we nor the model ever lose sight of what is being resolved — so nothing gets silently dropped and nothing is marked done without evidence.

## How the model uses this (each turn)

1. Pick **one** item from `TRACKER.md` — the smallest fully-defined, unblocked item.
2. Copy it into **Active work** below; fill in plan, files, and definition of done.
3. Implement it. Then **verify** with a real command (cargo / tsc / npm) or, for UI, the browser. Record the exact verification.
4. Flip the item to `[x]` in `TRACKER.md`, move it to **Recently resolved** here, clear **Active work**, and repeat.
5. If an item is too big to implement + verify in one turn, split it into smaller items **in its owning doc first** — do not attempt "the whole system".

## Active work

- **Item:** — *(id + link to the owning doc)*
- **Status:** `[~]` in progress
- **Definition of done:** — *(what observable behavior proves this is complete)*
- **Files touched:** — *(concrete paths)*
- **Verification run:** — *(exact command / browser steps + result)*
- **Blockers:** none

## Recently resolved

| Date | Item | Verification (what passed) |
| --- | --- | --- |
| 2026-09-10 | **Agent TUI A8 — conversation card element** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md)) | `cargo test -p goble-ui` → lib `246 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.26s` (was 239; 7 new in `elements::conversation_card::tests`: `the_card_reads_the_block_it_stands_for`, `an_unnamed_conversation_falls_back_to_its_id`, `a_shell_block_has_no_card`, `the_card_draws_a_rail_not_a_bordered_widget`, `the_card_shows_title_status_and_last_activity`, `clicking_the_card_enters_its_conversation`, `hovering_the_card_tints_the_row`), doc-tests 0. `cargo check -p goble-ui --all-targets` clean. `cargo clippy -p goble-ui --all-targets --no-deps` shows the crate's pre-existing lints and none referencing the new file (`grep -c conversation_card` → 0); `rustfmt --edition 2021 --check crates/goble-ui/src/elements/conversation_card.rs` clean (the repo's pre-existing formatting debt elsewhere, including `elements.rs`, is untouched). Implementation: new `crates/goble-ui/src/elements/conversation_card.rs` — `ConversationCard`, built by `ConversationCard::from_block(&Block)`, which reads the identity straight off the agent-view block A7 pushes into the pane's list (`Block::conversation_id`/`Block::label`, falling back to the id for an unnamed conversation, and `None` for a shell block); `with_status(ConversationCardStatus)` (the live half — `Idle`/`Running`/`Waiting`, `Waiting` being the `ask_user`/command-proposal suspension), `with_last_activity(impl Into<String>)` (display-ready, the app owns the clock) and `with_on_click`. Drawn in the pager idiom, matching `diff.rs` and the tool-call rows: a `Flex` row of mono `▸` + title + a `Spacer` + status label + last activity, inset past a 2px rail that is painted in `paint` via `renderer.fill_rect` in the status colour, with a `Hover` tint on hover — no `Container` border, no corner radius (the tests assert zero `StrokeRect` and a square rail). Re-exported from `elements.rs` (`pub mod conversation_card;` + `pub use conversation_card::{ConversationCard, ConversationCardStatus};`). Not verified: the app-side wiring that would pass a real conversation's status/last-activity into the element — A7 still draws its own minimal label row in `app/src/ui/terminal.rs` (`build_card`), and this item's scope is the element plus its tests; and the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent TUI A7 — agent view over the block list** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md)) | `cargo test -p goble-app` → lib `162 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (was 161; 1 new in `ui::terminal::tests`: `cmd_enter_esc_and_a_card_click_round_trip_the_agent_view`), every integration suite ok (chat 8, connector 4, cron 4, first_run 10, harness 9, harness_pages 1, menu 7, projects 3, screen 8, ui_render 12, vault 3), doc-tests 0. `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs`, the focus-test closure); `cargo clippy -p goble-app --all-targets --no-deps` shows the crate's pre-existing lints and no new one (the warning set and counts are unchanged; `TerminalView::new` keeps its `#[allow(clippy::too_many_arguments)]`, and the new `on_open_agent_view` field does not join the `type_complexity` set); `rustfmt --edition 2021 --check` leaves no diff inside any new hunk — the changed files' pre-existing formatting debt (`app/src/emulator.rs`'s `resize`, `app/src/terminal.rs`'s claim/submit hunks, `app/src/actions.rs`'s and `app/src/state.rs`'s older hunks) is untouched. Implementation: `PaneControls` gains `view: BlockView` — the pane's filter, `Terminal` or `Agent{conversation_id}`. `app/src/state.rs` gains `pane_view` (a pane's filter), `enter_agent_view(pane_id, conversation_id, label)` — point the filter at the conversation and push its card into the pane's block list — `leave_agent_view(pane_id)` — filter back to `Terminal`, the card left in place — and `conversation_name` (the card's label, falling back to the id). `app/src/emulator.rs`'s `VisibleBlock` now carries the block's `output`/`failed` and, for a conversation card, its `AgentViewCard { conversation_id, label }`; a new `push_agent_view_block` is idempotent, so re-entering a conversation reuses its one card instead of stacking another. `app/src/terminal.rs` forwards it through `TerminalSession`/`TerminalRegistry::push_agent_view_block`. `app/src/ui/terminal.rs`: the pane reads its filter from the snapshot, draws the conversation's blocks (through the Q8 `terminal_block`) instead of the grid in an agent view, draws the clickable cards under the grid in the terminal view, and a `MouseDown` on a card (recorded by a paint probe, checked before the grid cell) fires the new `on_open_agent_view`; Cmd+Enter and Esc keep going through `on_set_pane_harness_mode`. `app/src/actions.rs`'s harness handler now, on activation, binds a conversation if the pane has none, pushes its card and enters the view, and on deactivation leaves it; the new `on_open_agent_view` handler enters a specific conversation (bringing `harness_mode` with it, as Cmd+Enter does). The acceptance test drives the pane's real events and the app's real `make_actions` as a round trip: Cmd+Enter → `pane_view == Agent{c1}` with the card in `pane_terminal_view` and `pane_agent_view` empty; Esc → `Terminal` with the card still in the list; then a paint records the card's rect and a click at its centre → `Agent{c1}` again. Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`); the card is a minimal label row built in the app this turn, and A8 builds the conversation-card element proper (title, status, last activity). |
| 2026-09-10 | **Agent TUI A5 — composer proposal mode** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md)) | `cargo test -p goble-ui` → lib `239 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (was 232 after Q8; 7 new in `elements::chat_composer::tests`: `proposal_card_shows_the_candidates_and_selects_the_first`, `arrow_keys_cycle_the_candidates_and_follow_the_draft`, `a_host_owned_selection_survives_the_rebuild`, `editing_a_candidate_submits_the_edit`, `enter_approves_the_selected_candidate`, `escape_rejects_the_proposal`, `proposal_keys_are_only_taken_with_focus_and_a_proposal`; the module runs `12 passed; 0 failed`), doc-tests 0. `cargo check -p goble-ui --all-targets` clean; `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs`); `cargo clippy -p goble-ui --all-targets --no-deps` shows only the crate's pre-existing lints (the new `on_decision` field joins the struct's existing `Option<Rc<RefCell<dyn FnMut(..)>>>` callback fields under `type_complexity`, and the `too_many_arguments` on `context_pill` is untouched); `rustfmt --edition 2021 --check` leaves no diff inside any new hunk — the file's pre-existing formatting debt (the two `ComposerButton` one-liners and the old tests' `assert!` one-liners) is untouched. Implementation: `crates/goble-ui/src/elements/chat_composer.rs` gains `CommandProposalUi { id, candidates, cwd }` — the renderable mirror of A6's `CommandProposed`, so candidate generation stays a harness model call and the renderer only draws what it is handed — with the builders `with_proposal(proposal)`, `with_proposal_selection(Rc<RefCell<usize>>)` and `with_on_decision<F: FnMut(String, CommandDecision)>` plus the accessors `proposal()`/`selected_candidate()`. While a proposal is set, `rebuild` draws a raised `SurfaceRaised` 1px-border card above the textarea: an "Agent proposes a command" header with the cwd, one clickable row per candidate (the selected row marked `▸` and accented, the rest `Text`, every command in mono), a keyboard hint, and `Approve` (Primary) / `Reject` (Ghost) buttons. Rows, buttons and keys all act through one `ProposalHandles` value whose `Rc`s are the selected index (app-owned, like the footer menus' `open` flags, because the composer element is rebuilt every frame), the composer's draft and the callbacks, so a click and a key produce the same payload. An empty draft is seeded from the selected candidate during `rebuild`, making the editor below the card the in-place edit surface, and cycling reports the new text through `on_change` so the host's own draft keeps the selection across a rebuild. `dispatch_event` takes `ArrowUp`/`ArrowDown` (wrapping), `Enter` and `Escape` before the tree — only while the composer is focused with a proposal on screen — so ordinary typing still reaches the `TextArea`. The payload is A6's own `CommandDecision`, paired with the proposal id: an untouched candidate is `Approve(candidate)` verbatim, a draft that was edited is `Edit(text)`, and Esc/Reject is `Reject(String::new())`. Not verified: the app-side wiring and any end-to-end run — `crates/goble-harness-internal/src/lib.rs` still maps `CommandProposed` to `Vec::new()` (A6 left it dropped), so no proposal reaches the app yet and `views/chat_view.rs`/`app/src/ui/chat.rs` were deliberately left untouched this turn; and the on-screen look of the card in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent TUI A6 — harness approval suspension** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md)) | `cargo test -p goble-core` → lib `207 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (was 203; 4 new in `reasoning::tests`: `test_command_suspends_before_it_runs`, `test_approve_runs_chosen_command`, `test_edit_runs_edited_command`, `test_reject_errors`), plus every integration suite ok (`deepseek_e2e` 1 passed/3 ignored, `user_guide_harness` 1, `mcp_harness_mock` 3, `harness_deepseek_e2e` 0 passed/3 ignored, `mcp_e2e` 1, `ollama_e2e` 0 passed/1 ignored), doc-tests 0. `cargo check -p goble-harness-internal -p goble-daemon -p goblin-worker -p goble-desktop-service` clean; `rustfmt --edition 2021 --check` shows no diff inside any new hunk (the three files' pre-existing formatting debt is untouched — the repo is not rustfmt-clean, per the open 00 item); `cargo clippy -p goble-core --all-targets` reports only pre-existing lints plus the `too_many_arguments` on the new `Store::insert_pending_command`, which mirrors the existing `insert_pending_ask`. Implementation: `crates/goble-core/src/harness.rs` gains the `CommandProposed { id, candidates, cwd }` `HarnessEvent` variant, the `CommandDecision { Approve(String), Edit(String), Reject(String) }` type, `COMMAND_TOOL = "run_command"`, `command_candidates(call)` (the shell line `shell_line` would run, with `{{credential:<name>}}` left unexpanded so a secret is never shown) and `run_approved_command(store, runner, line)` (expands credentials at execution time, then `runner.run(line, &[])`), and `Harness::resume_command(chat_id, decision)`. `crates/goble-core/src/reasoning.rs`'s execution loop now suspends **before** executing a `run_command` call when `!auto_approve`: it persists the proposal (`Store::insert_pending_command` with the call id, the assistant row holding its tool-call record, the candidates JSON and the workspace cwd), emits `CommandProposed`, and returns; `resume_command_turn` loads the proposal, resolves it, and runs `Approve`/`Edit` verbatim (a runner failure is a `ToolCallError`) while `Reject` becomes `ToolCallError("rejected by user[: reason]")` without running anything, recording the outcome as the `role="tool"` row and the call's `Finished`/`Error` status+result. `crates/goble-core/src/store.rs` adds the `pending_commands` table (with `get_pending_command`/`resolve_pending_command`, and it joins `SNAPSHOT_TABLES`). `crates/goble-harness-internal/src/lib.rs` maps the new variant to `Vec::new()` (the wire/composer half is A5), matching how `ThinkingModeChanged` is dropped. Existing `run_command` turn tests were given `.with_auto_approve(true)` because the immediate path is now the auto-approved one. `auto_approve` remains the single switch for both `ask_user` and command approval. Not verified: the wire event and the composer that would drive approve/edit/reject end-to-end (A5) — the harness event is dropped at the adapter until then, and no GUI run was made (house rules forbid building/running `goble-app`); the approved text is run as one shell line (the pane's native form), so the sandbox route's argv allow-list sees the whole line and refuses it. |
| 2026-09-10 | **Agent parser Q8 — terminal segment as a block** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `232 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (was 231 after Q7; 1 new: `chat_message_bubble::tests::a_command_segment_is_the_same_block_terminal_mode_draws`; Q7's `command_call_shows_the_command_and_its_output` was updated because a command is now the block rather than a `$ <cmd>` row), doc-tests 0. `cargo test -p goble-app` → lib `161 passed; 0 failed` (was 160 after P6; 1 new: `ui::terminal::tests::a_command_block_renders_identically_in_the_transcript_and_the_pane`) and every integration suite ok (chat 8, connector 4, cron 4, first_run 10, harness 9, harness_pages 1, menu 7, projects 3, screen 8, ui_render 12, vault 3), doc-tests 0. `cargo check --workspace --all-targets` clean; `cargo clippy -p goble-ui -p goble-app --all-targets --no-deps` shows only pre-existing lints (no `type_complexity`/`or_insert_with` on the new code; the remaining `LayoutContext::default()` warnings are the crate-wide style lint every element test already triggered); `rustfmt --edition 2021 --check` leaves no diff inside any new hunk — each file's pre-existing formatting debt (`with_on_copy_terminal`, `render_block(s)`, `render_table_row`, `terminal_block.rs`'s old hunks) is untouched. Implementation: `crates/goble-ui/src/elements/terminal_block.rs` gains the one block shape and the one renderer — `TerminalData::for_command(command, output, status)` (the command line, then the output, with the state as the block's status) and `terminal_block(data, filter, global_filter, on_copy)` over a `TerminalCopyHandler` alias, with `TerminalBlock` as the element both draw, re-exported from `elements.rs`. `crates/goble-ui/src/elements/chat_message_bubble.rs`'s `TerminalBlockPlumbing` (per-block filter map + global filter + copy handler) is now the single construction path: `ChatBlock::Terminal` and a command tool call's body both go through it, and `command_body` builds `TerminalData::for_command(command, result, command_status(call.status))` and draws the block — the `$ <command>` + muted-result rows are gone, and `command_status` reads the persisted state (`Pending`/`Running` → `Running`, `Finished` → `Success`, `Error` → `Error`). The Q6 header row and the other Q7 tool shapes are unchanged. `app/src/ui/terminal.rs` gains `executed_command_block(session)` — the pane's own executed-command block built from the session snapshot — and `app/src/ui/chat.rs`'s `with_inline_terminal` now reuses it instead of rebuilding the block inline, so the transcript draws the pane's block. Both acceptance tests compare draw runs (text + colour + family + size, layout-independent): the element test asserts a command tool call's segment equals the block drawn on its own after the tool-call header, and the app test asserts the pane's block (built from a real `Emulator` session) renders identically in the transcript's terminal fragment and that a `run_command` call is that same block. Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`). Stated limitation: the terminal pane still paints its screen as a `TerminalGrid`; "the block the pane draws" here is the pane's executed-command block data (`executed_command_block`) drawn with the same `terminal_block` element the transcript uses, not a per-block rewrite of the pane's grid (that is A7's block-backed view). |
| 2026-09-10 | **Agent parser Q7 — per-tool presentation** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `231 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (was 225 after Q6; 6 new tests, one per tool shape plus the registry check: `command_call_shows_the_command_and_its_output`, `read_call_shows_the_path`, `edit_call_shows_diff_rows`, `web_search_call_shows_the_query_and_its_sources`, `sub_agent_call_shows_its_own_rows`, `tool_shapes_are_read_from_the_harness_definitions`), doc-tests 0; the `chat_message_bubble` module runs `17 passed; 0 failed`. `cargo test -p goble-core` → lib `203 passed; 0 failed` (was 202; the new `harness::tests::tool_presentation_is_read_from_the_definition`). `cargo check --workspace --all-targets` clean; `cargo clippy -p goble-ui -p goble-core --all-targets --no-deps` shows only pre-existing lints (none on the new functions); `rustfmt --edition 2021 --check` on the new hunks is clean — each file's pre-existing formatting debt is untouched. Implementation: `crates/goble-core/src/harness.rs` declares `ToolPresentation { Command, Path, Diff, Search, SubAgent, Generic }` next to `harness_tool_definitions()` (the registry `execute_tool_call` dispatches on), `tool_presentation(&ToolDefinition)` (the shape a definition declares) and `tool_presentation_for(name)` (find the definition, then read its shape; a name the harness does not define — an MCP tool — is `Generic`). `crates/goble-ui/src/elements/chat_message_bubble.rs`'s `build_tool_call_body` reads that shape and never matches `call.name`: a command draws `$ <command>` then its output; a read (and a write/delete) draws its path then its result; an edit draws its path then the replaced/replacement text as Q9 `Diff` rows built by `edit_hunks` (removed old_text lines, added new_text lines, one hunk); a search draws its query then the `URL:` sources its result names (falling back to the raw result when there is none, so an error stays visible); a sub-agent draws its id, its input and its output; every other tool keeps the Q6 argument/result rows. A call whose body is empty still collapses to the status-glyph + name header row (Q6). Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`); the command's output is still ordinary mono rows rather than the shared terminal block, which is Q8's item, and the per-tool mapping is declared by definition name inside `harness.rs` because `ToolDefinition` carries no shape field to read. |
| 2026-09-10 | **Agent parser Q6 — inline tool-call rows** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `225 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (was 221 after Q11; the old `tool_bubble_renders_card_and_result_block` was replaced by 5 tests: `tool_call_draws_no_border_or_card`, `collapsed_tool_call_renders_in_one_row`, `expanded_generic_tool_call_draws_arguments_and_result_in_place`, `status_affordance_reflects_the_persisted_status`, `tool_result_block_still_renders_for_a_tool_row`), doc-tests 0; the `chat_message_bubble` module runs `11 passed; 0 failed`. `cargo test -p goble-app` → lib `160 passed; 0 failed` and every integration suite ok (chat 8, connector 4, cron 4, first_run 10, harness 9, harness_pages 1, menu 7, projects 3, screen 8, ui_render 12, vault 3), doc-tests 0. `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`); `cargo clippy -p goble-ui -p goble-app --all-targets --no-deps` shows only pre-existing lints (none on the new code; the `LayoutContext::default()` warning matches every other element test); `rustfmt --edition 2021 --check` on the new code is clean — the file's pre-existing formatting debt (`with_on_copy_terminal`, `render_block`, `render_blocks`, `render_table_row`, the test import order) is untouched. Implementation: `crates/goble-ui/src/elements/chat_message_bubble.rs` replaces `build_tool_call_cards` with `build_tool_call_rows` — a per-call column of inline rows with **no `Container`, no border, no raised `surface_2` card and no `cpu` icon / literal `"tool"` label**: row one is a status glyph plus the tool name (both mono, glyph coloured), then the arguments row (only when non-empty and not `{}`) and the result row (only when `result.is_some()`). `tool_status_affordance` maps the persisted `ToolCallStatus` to `○`/Muted (pending), `◐`/Accent (running), `●`/Success (finished) and `◆`/Error (error), so the status is read, never guessed. The element tests paint a real `ChatMessageBubble` and assert zero `StrokeRect`, zero `SurfaceRaised` `FillRect`, exactly one text baseline for a collapsed call (glyph + name only), three baselines when arguments and result are present, and the glyph/colour per status. `app/src/state.rs`'s `tool_terminal_data` no longer infers `has_error` from `body.contains("ERROR:")`: the tool-result block renders its lines as plain output and carries no `TerminalStatus`, because the call's real status is the persisted field now drawn on the tool-call row. Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`); the collapsible-body variant and grouping the `role="tool"` result row under its call remain A4's scope, not this item. |
| 2026-09-10 | **Agent terminal bridge P6 — owner into the views** ([`06-renderer/agent-terminal-bridge.md`](06-renderer/agent-terminal-bridge.md)) | `cargo test -p goble-app` → lib `160 passed; 0 failed` (was 158; 2 new: `terminal::tests::the_two_views_show_the_right_blocks_after_a_mixed_sequence`, `state::pane_session_tests::the_pane_views_start_as_the_shells_own_history`), every integration suite ok (chat 8, connector 4, cron 4, first_run 10, harness 9, harness_pages 1, menu 7, projects 3, screen 8, ui_render 12, vault 3), doc-tests 0. `cargo check --workspace --all-targets` clean; `cargo clippy -p goble-app --all-targets --no-deps` shows only pre-existing lints (none on the changed files); `rustfmt --edition 2021 --check` on the three changed files shows no diff inside the new hunks (the files' pre-existing formatting debt is untouched). Implementation: `crates/goble-terminal/src/blocks.rs` is **not** changed — `BlockList::associate_with_conversation` (A1) is called for the first time. `app/src/emulator.rs` now calls it at the moment the owner is decided: in `feed`, each `BlockEvent::BlockStarted { id }` produced by a `Preexec` goes through `associate_owner(id)`, which reads the block's owner and, when it is `BlockOwner::Agent { conversation_id, .. }`, associates the block with that conversation and returns for a `User`-owned block, so an agent's command joins its conversation's view while a user's command stays out. It also gains `VisibleBlock { id, owner, command }` and `Emulator::visible_blocks(&BlockView)`, projecting the block list through the terminal/agent filter. `app/src/terminal.rs` gains `TerminalSession::visible_blocks(&BlockView)` and `TerminalRegistry::visible_blocks(pane_id, &BlockView)` (empty for a pane with no session); `with_emulator` is now `pub(crate)` so app tests can build a pty-less session. `app/src/state.rs` gains `UiState::pane_terminal_view(pane_id)` and `UiState::pane_agent_view(pane_id, conversation_id)` — the app-level entry A7's block-backed view will read. The acceptance test drives the pane's **real** hook path (a detached session in a `TerminalRegistry`, encoded hooks through the emulator, the P4 `submit` route for the agent's command): user `ls` → agent `echo hi` (claimed, tool result `Ok("hi")` asserted) → user `pwd`; the terminal view is `["ls", "echo hi", "pwd"]` and the conversation's agent view is exactly `["echo hi"]` with owner `Agent { conversation_id: "conv-1", call_id: "call-1" }`, so the two views agree on who ran what. Not verified: the on-screen rendering of either view (no GUI run — house rules forbid building/running `goble-app`); P6 is the handoff to A7, which builds the views. Known limitation carried from P4, unchanged by P6 and left to P5: the block list's claim is aimed at the active block, so if a user command's `Preexec` arrives before the agent's, the block list can attach the agent owner to that (user) block; the P2 command-aware claim still fails the tool call, but view ownership in that race is P5's in-band hardening. |
| 2026-09-10 | **Agent terminal bridge P4 — route the shell tool through the pane** ([`06-renderer/agent-terminal-bridge.md`](06-renderer/agent-terminal-bridge.md)) | `cargo test -p goble-core` → lib `202 passed; 0 failed` (was 198; 4 new in `harness::tests`: `a_pane_session_runs_the_agents_command_and_returns_its_output`, `a_pane_session_is_not_gated_by_the_sandbox_allow_list`, `the_sandbox_runs_the_tool_when_the_pane_has_no_session`, `a_pane_line_composes_the_argv_and_quotes_what_the_shell_would_change`), plus all integration suites and doc-tests 0. `cargo test -p goble-app` → lib `155 passed; 0 failed` (2 new in `terminal::tests`: `an_agent_command_runs_in_the_pane_and_returns_its_output`, `a_command_the_shell_never_ran_fails_the_agent_call`, plus `a_pane_without_a_terminal_has_no_pane_session`) and every integration suite ok; `cargo test -p goble-desktop-service -p goble-harness-internal` → `30`/`6` passed; `cargo check --workspace --all-targets` clean; clippy on the changed crates shows only pre-existing lints (none pointing at the new code). Implementation: `crates/goble-core/src/harness.rs` gains the `PaneSession` trait (`async fn run_in_pane(&self, line)`), `RoutedCommandRunner { pane: Option<Arc<dyn PaneSession>>, sandbox: Arc<dyn CommandRunner> }` (installed via the existing `Harness::with_runner`; `run` calls the pane when one is attached and the sandbox otherwise) and `shell_line`/`quote_arg` (the argv is composed into one shell line with POSIX single-quoting). `crates/goble-desktop-service/src/state.rs` adds `run_chat_turn_with_pane_session` (the old `run_chat_turn` delegates with `None`), builds the router around the sandbox and passes the pane session through. `app/src/daemon/state.rs` (`DaemonModel::with_pane_session`) and `app/src/runtime.rs` carry it; `app/src/actions.rs` (both send paths) and `app/src/root_view.rs` (queued-prompt auto-submit) obtain it from `app/src/state.rs::UiState::pane_session`, which returns a session only when `TerminalRegistry::pane_session` finds a live pane session. `app/src/terminal.rs` implements `PaneSession` as `PaneCommandRunner` over a thread-safe bridge (`PaneCommandRequest`/`PaneCommands`, `tokio::sync::oneshot`, 35 s `PANE_TOOL_TIMEOUT`); `TerminalSession::pump` services it — arms the block claim, calls the existing P2 `claim_command`, and answers with P3's `ToolResult` (`tool_result_text`: non-zero exit is an error, `StillRunning` is reported, not hung). `app/src/emulator.rs` now owns a `goble_terminal::BlockList` fed the same pty bytes and hooks so a claimed block's output is available; app Cargo.toml gains `async-trait` and tokio `sync`/`time`. The goble-core tests drive the real `run_turn` + `run_command` tool: a pane double's output is returned verbatim, `rm` (refused by the sandbox) runs through a pane (the security trade, asserted), and with no pane the sandbox still refuses it. The app tests drive the pane's own hook path (`Bootstrapped` → submit → `Preexec` → output bytes → `CommandFinished`) and assert the composed line hits the pty, `hi` comes back as the result, and a claim refused by a user command fails the call. **Stated trade**: in an agent + terminal pane the agent's command now runs in the user's own shell with the user's environment and credentials, is no longer allow-listed or sandboxed, and is visible in the terminal — this is warp's and grok-build's shape, gated by the choice of pane and (A6) per-command approval; a credential expanded from `{{credential:...}}` is written into that shell's command line, so it is echoed on screen and lands in the block, unlike the sandbox path. Not verified: a real interactive shell end-to-end (no pty in the unit tests; the app tests drive the hook path with the crate's own encoded hooks), and the on-screen result in a real window (no GUI run — house rules forbid building/running `goble-app`). Known limitation deferred to P5: the block claim is armed on the active block at claim time, so if a user command's `Preexec` arrives first the block list may carry the agent owner even though P2's command-aware claim refuses it — the tool call still fails, but the view ownership is P5's in-band hardening, not this item. |
| 2026-09-10 | **Agent terminal bridge P3 — tool-result extraction** ([`06-renderer/agent-terminal-bridge.md`](06-renderer/agent-terminal-bridge.md)) | `cargo test -p goble-terminal` → lib `121 passed; 0 failed` (was 114 at P1; 7 new in `blocks.rs::tests`: `a_claimed_command_hands_back_its_output_and_exit_code`, `a_claimed_command_that_fails_hands_back_its_exit_code`, `a_claimed_command_with_no_output_hands_back_an_empty_result`, `an_interrupted_claimed_command_fails_instead_of_hanging`, `a_promoted_claimed_command_hands_back_a_still_running_result`, `a_users_command_produces_no_tool_result`, `a_tool_result_carries_output_that_scrolled_past_the_screen`), plus conformance `1 passed` and shell-integration `2 passed`, doc-tests 0. Implementation: `crates/goble-terminal/src/blocks.rs` gains `ToolOutcome::{Finished{exit_code}, StillRunning}` and `ToolResult { block, conversation_id, call_id, command, output, outcome }` with `exit_code()`, `is_failure()` and `is_still_running()`, plus `Block::output_text()` (the output screen's whole content — `output_lines()`, so output that scrolled past the screen is kept — deliberately not `Block::text()`, which also carries the echoed command line) and `BlockList::tool_result(index, outcome)`, which returns `None` unless the block's owner is `BlockOwner::Agent`. `apply` now hands the result back as the new `BlockEvent::ToolResult(ToolResult)` (caller-facing; the two types are re-exported from `crates/goble-terminal/src/lib.rs`): `on_command_finished` appends it after `BlockFinished` with `Finished { exit_code }`, and `on_precmd` emits `StillRunning` with the output that had arrived at the moment it promotes a still-`Executing` claimed block to `Background` (the block stops being active, so no `CommandFinished` is ever coming — a promoted block's later finish belongs to the next block and produces no second result, which the test asserts). Covered: exit 0 with output, exit 2, empty output, exit 130 (Ctrl-C → `exit_code() == Some(130)` and `is_failure()`, no hang), background promotion (`outcome == StillRunning`, `exit_code() == None`, partial output) and the control case that a user-owned block yields no tool result. `cargo clippy -p goble-terminal --all-targets --no-deps -- -D warnings` clean; `rustfmt --edition 2021 --check` clean on both changed files; `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`). Not verified: the app-side handoff — `TerminalSession` does not own a `BlockList`, so nothing in `app/src/terminal.rs` consumes `ToolResult` yet; putting the block list in the pane session and routing the harness's shell tool through it is P4's routing, not this item. The cases are driven through the block list's real `apply` path with the crate's own `Preexec`/`CommandFinished`/`Precmd` hook values, not by poking block internals. |
| 2026-09-10 | **Agent terminal bridge P2 — the claim protocol** ([`06-renderer/agent-terminal-bridge.md`](06-renderer/agent-terminal-bridge.md)) | `cargo test -p goble-app` → lib `155 passed; 0 failed` (was 148; 7 new in `app/src/terminal.rs::tests`: `a_claim_writes_the_command_to_the_pty`, `a_claimed_command_resolves_on_its_own_preexec`, `a_claim_that_never_gets_a_preexec_times_out`, `a_claim_is_refused_when_a_user_command_arrives_first`, `a_preexec_without_a_command_refuses_the_claim`, `a_second_claim_while_one_is_pending_is_refused`, `an_unclaimed_preexec_leaves_no_outcome`), and every integration suite ok (`chat_flow` 8, `connector_flow` 4, `cron_flow` 4, `first_run_flow` 10, `harness_flow` 9, `harness_pages_flow` 1, `menu_flow` 7, `projects_flow` 3, `screen_flow` 8, `ui_render` 12, `vault_flow` 3), doc-tests 0. The three acceptance sequences drive the session's **real** hook path — `encode_hook` bytes into the pane's own `Emulator`/`HookTap`, then `pump()` — not a hand-fed hook list: claim → resolve (`Preexec` for the same command line), claim → timeout (zero bounded wait, expires on the next `pump`), claim → a user command arrives first (`Preexec` for `ls -la` refuses the claim, and the agent's command running afterwards does not revive it). Implementation: `app/src/terminal.rs` gains `CLAIM_TIMEOUT` (30 s), `PendingCommandClaim { owner: BlockOwner, command, deadline }`, `ClaimError::{EmptyCommand, AlreadyPending}`, `ClaimOutcome::{Resolved, Refused, TimedOut}` (with `owner()`/`is_resolved()`), and on `TerminalSession` the fields `pending_claim`/`claim_outcomes`/`claim_timeout` plus `claim_command(command, owner)`, `pending_claim()`, `claim_outcomes()` and the private `observe_preexec`/`expire_claim`/`push_claim_outcome`. `claim_command` trims the command, refuses an empty line and a second claim while one is pending, writes `<command>\r` to the pty (asserted byte-for-byte via a capturing writer) and holds the claim with a deadline. `pump()` resolves it against each `Preexec` it lifts out of the pty stream and then expires an unanswered claim, so the bounded wait is enforced on the frame the pane already runs; a `Preexec` for a different command — including one that reports no command text — refuses the claim rather than attaching it to the wrong block (rule three). `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`); `cargo clippy -p goble-app --all-targets` shows no lint pointing at `app/src/terminal.rs`; `rustfmt --edition 2021 --check app/src/terminal.rs` clean. Not verified: the claim against a real interactive shell (there is no pty in the unit tests — a detached session has no writer, so the tests drive the hook tap directly); and the wiring that turns a `ClaimOutcome` into a block owner / failed tool call is P3/P4, not this item. |
| 2026-09-10 | **Agent terminal bridge P1 — block ownership** ([`06-renderer/agent-terminal-bridge.md`](06-renderer/agent-terminal-bridge.md)) | `cargo test -p goble-terminal` → lib `114 passed; 0 failed` (was 110; 4 new: `blocks::tests::a_claimed_preexec_takes_the_owner`, `an_unclaimed_preexec_belongs_to_the_user`, `a_claim_aimed_at_the_wrong_block_is_refused`, `a_claim_that_lands_on_another_block_is_refused`), plus conformance `1 passed` and shell-integration `2 passed`, doc-tests 0. Implementation: `crates/goble-terminal/src/blocks.rs` gains `BlockOwner::{User, Agent { conversation_id, call_id }}` (default `User`), a `pub struct PendingClaim { pub block: BlockId, pub owner: BlockOwner }`, a private `Block.owner` field set to `User` at construction with `Block::owner()` and a private `set_owner`, and `BlockList::{pending_claim, claim_next_pre_exec}`. `claim_next_pre_exec(id, owner)` refuses (returns false) unless `id` is the active block and still `BeforeExecution`, so a finished/other/unknown block is never claimed; `on_preexec` consumes the claim and attaches the owner only when the claim's block is the one the `Preexec` lands on, otherwise the command stays `BlockOwner::User` — the claim is dropped, never silently attached to the wrong block. `BlockOwner`/`PendingClaim` are re-exported from `crates/goble-terminal/src/lib.rs`. `cargo clippy -p goble-terminal --all-targets --no-deps -- -D warnings` clean; `rustfmt --edition 2021 --check` clean on both changed files. Not verified: the app-side claim protocol (`TerminalSession::claim_command`, P2) that writes the command to the pty and drives these hooks — no PTY session exercises this end-to-end yet; P1 is verified through scripted hook sequences only. |
| 2026-09-10 | **Agent parser Q11 — reasoning surface** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-harness-internal` → `test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (1 new: `tests::reasoning_events_are_carried_to_the_wire`, which drives `map_event` for `ReasoningStarted`/`ReasoningDelta`/`ReasoningDone` and asserts each becomes the matching `HarnessServerEvent`, and that `ThinkingModeChanged` is the only remaining drop). `cargo test -p goble-app` → lib `148 passed; 0 failed` (1 new: `state::pane_session_tests::reasoning_events_reach_the_transcript_and_render_recessed_rows`), every integration suite ok. `cargo test -p goble-ui` → lib `221 passed; 0 failed` (3 new: `chat_content::tests::reasoning_fragment_is_its_own_block`, `chat_message_bubble::tests::reasoning_row_is_recessed_and_collapsed_until_expanded`, `chat_message_bubble::tests::reasoning_header_click_toggles_the_row`). `cargo test -p goble-daemon-protocol` → 9 passed; `cargo test -p goble-desktop-service` → 30 passed (1 new: `state::tests::reasoning_events_are_emitted_as_chat_reasoning`); `cargo test -p goble-daemon` → 31 passed; `cargo test -p goble-harness-protocol` → 9 passed. Implementation: `crates/goble-harness-protocol/src/lib.rs` and `crates/goble-daemon-protocol/src/lib.rs` gain `ReasoningStarted { step, mode }`, `ReasoningDelta { delta }` and `ReasoningDone { step, mode, content, decision }` (the core's `ReasoningDelta` carries no step, so the wire mirrors that; deltas apply to the step opened by `ReasoningStarted`). `crates/goble-harness-internal/src/lib.rs` `map_event` now maps those three through instead of returning `Vec::new()` (only `ThinkingModeChanged` is still dropped), and `crates/goble-daemon/src/state.rs` `map_event` forwards them; `crates/goblin-worker/src/state.rs` drops them explicitly in its `map_daemon_event` (worker messages have no reasoning kind). `crates/goble-desktop-service/src/state.rs` gains the exported `ReasoningEvent`/`ReasoningPhase`, adds the variants to `daemon_session_id`, and `translate_daemon_event` emits a structured `chat:reasoning` event per transition. `app/src/root_view.rs` handles `chat:reasoning`; `app/src/state.rs` holds `UiState.reasoning_expanded: Rc<RefCell<HashMap<String, bool>>>` and `PaneRuntime.reasoning: Vec<ReasoningRow>`, folds started/delta/done into a step (`PaneRuntime::apply_reasoning`, clearing at step 0 of a new turn), and `overlay_reasoning` splices the steps as their own fragments ahead of the trailing assistant message's prose (idempotent across refreshes); the map is threaded through `UiSnapshot` → `app/src/ui/chat.rs` → `ChatView::with_reasoning_expanded` → `ChatMessageBubble`. `crates/goble-ui/src/elements/chat_content.rs` gains `ChatFragmentKind`/`ChatBlock::Reasoning { key, mode, text, done }`, and `chat_message_bubble.rs` renders it recessed (all text in `ColorToken::Muted`, no card): a clickable `▸/▾ Thinking · <mode>` header, body hidden until the app-owned flag for `key` is set. The app test drives the real event handler, asserts the step lands on the transcript, and paints a `ChatView` to prove the body is not drawn while collapsed, the header paints `Muted`, and expanding the flag draws the body. `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`); clippy on the changed crates shows no new lint pointing at this code; rustfmt clean on the new code (the repo's pre-existing formatting debt is unchanged). Known limitation: a pane's reasoning rows are only replaced when the next reasoning turn starts (step 0), so if a later turn runs with reasoning disabled the previous turn's rows still overlay until then. Not verified: the on-screen look and feel of an expanded thinking row in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent TUI A3 — live tool events to the app** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md)) | `cargo test -p goble-desktop-service` → `29 passed; 0 failed` (1 new: `state::tests::tool_events_fill_and_clear_the_in_flight_map`). `cargo test -p goble-app` → lib `147 passed; 0 failed` (1 new: `state::pane_session_tests::live_tool_event_fills_and_clears_the_pane_in_flight_map`), every integration suite ok. Implementation: `crates/goble-desktop-service/src/state.rs` gains `ToolCallEvent { chat_id, id, name, arguments, status, result }` and a per-chat `tool_in_flight: HashMap<chat_id, HashMap<call_id, ToolCallEvent>>`; the translator body moved into `translate_daemon_event` (so it is drivable in tests without a tokio task) and now matches `ToolCallStarted`/`ToolCallFinished`/`ToolCallError`, calling `record_tool_call`, which inserts the running call on start and removes it on finish/error (the finished wire event carries no name/arguments, so they are read back from the entry being cleared) and emits the structured `chat:tool` event. `app/src/root_view.rs::drain_events` handles `chat:tool` by deserializing the payload into `ToolCallEvent` and calling the new `UiState::apply_tool_event`; `app/src/state.rs` `PaneRuntime` gains `in_flight_tools: HashMap<String, ToolCall>` (keyed by call id), a running call is overlaid onto the transcript by `overlay_in_flight` (updating an already-persisted call or attaching an unpersisted one to the trailing assistant message) both right after the `chat:tool` event and after every `refresh_pane` store read, and a finish/error removes its entry so the persisted terminal state shows. The desktop-service test drives two starts (both entries held, `Running`, one `chat:tool` per transition), then a finish and an error (each clears its entry, leaving the map empty). The app test proves a start gains one entry for the owning pane and overlays it as `Running` without a store re-read, a second call is tracked independently, and finish/error clear their entries. `cargo check -p goble-desktop-service -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`); clippy on both crates shows no warning pointing at the new code. Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent TUI A2 + Agent parser Q5 — tool-call status carrier, persisted** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md), [`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-core` → lib `198 passed; 0 failed` (3 new). `cargo test -p goble-app` → lib `146 passed; 0 failed`, `harness_flow` `9 passed; 0 failed` (1 new), every other integration suite ok. `cargo test -p goble-ui` → lib `218 passed; 0 failed` (2 new). Implementation: `crates/goble-core/src/harness.rs` gains `ToolCallStatus { pending, running, finished, error }` and `ChatToolCall` (id, name, arguments, status, result) with `#[serde(default)]` on the new fields, so a row written by an older build still parses. `crates/goble-core/src/reasoning.rs` now keeps the per-call records and rewrites the assistant row's `tool_calls` column (`persist_tool_call_records`) at three points: the planned write moved below the loop's break checks (it previously wrote a duplicate tool-call row on the terminal no-op iteration), then `running` immediately before the `ToolCallStarted` yield, then `finished`+result / `error`+message before the matching terminal event. New core test `test_tool_call_is_running_in_store_before_turn_ends` drives `run_turn` only up to the start event and reads the store while the turn is still in flight: exactly one record, `status == Running`, `result == None`; draining the turn leaves the same record `Finished` with a result. `test_failed_tool_call_is_persisted_as_error` asserts `Error` + the message; `test_tool_call_row_from_older_build_still_parses` parses a legacy row into `Pending`/`None`. `crates/goble-ui/src/elements/chat_content.rs`'s `ToolCall` is now `{ id, name, arguments, status, result }` and `from_llm_json` defaults the new fields, with element tests `tool_call_carries_status_and_result` and `tool_call_row_from_older_build_still_parses`; `app/src/integration_testing/harness_flow.rs::refresh_messages_surfaces_tool_call_status` proves the app's re-read surfaces `Running`, `Finished`+result and a legacy row's `Pending`. `cargo check -p goble-core -p goble-ui -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`); clippy on the changed crates shows only pre-existing lints. Scope note: the live overlay that makes a **running** call appear without a store re-read is A3's `chat:tool` wire event, which remains open and is not part of this item; this change is the persisted carrier (A2) plus the row written on start (Q5's persisted half). Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent parser Q9 — diff rendering** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `216 passed; 0 failed` (was 207 after Q4), doc-tests 0. Nine new tests in `elements::diff`: `parses_unified_diff_into_hunks` (a two-hunk diff parses with the exact `@@ -1,3 +1,4 @@ fn main() {` / `@@ -10,3 +11,2 @@ fn other() {` headers), `gutter_columns_encode_the_change_type` (context → both columns, removed → old only, added → new only), `hunk_and_diff_line_counts` (`Hunk::added/removed/context` = 2/1/2 and 0/1/2; `Diff::stats()` = +2/−2), `skipped_context_between_hunks_is_a_gap_row` (the single gap row reports 6 skipped lines and sits immediately before the second hunk's header; 11 display rows for 2 headers + 8 body lines + 1 gap), `renders_gutters_markers_and_theme_change_colours` (the `+`/`-` markers and the changed body text paint in `ColorToken::Success`/`Error`, context text in `Text`, gutter numbers in `Muted`), `draws_the_hunk_header_and_the_skipped_context_separator` (both headers plus `... 6 unchanged lines skipped` are drawn), `is_inline_and_draws_no_box` (zero `StrokeRect`; every fill is square, no rounded card), `empty_diff_lays_out_to_nothing`, `hiding_line_numbers_drops_the_gutters`. Implementation: new `crates/goble-ui/src/elements/diff.rs` — `parse_unified_diff` returns `Vec<Hunk>` and `Diff::new(Vec<Hunk>)` is the only constructor, so a caller never hands the element a raw string; display rows are `HunkHeader` / `Line` / `Gap`, drawn in the mono family with right-aligned old/new line-number gutters (which column is populated encodes the change kind), a 2px left rail and a 10% tint mixed from the theme's `Success`/`Error` tokens on changed rows, and no border, no whole-element fill and no rounded corner. `cargo check -p goble-app --all-targets` clean (only the pre-existing `unused_mut` at `app/src/ui/terminal.rs:845`); `cargo clippy -p goble-ui --all-targets` reports zero lints for `diff.rs`; `rustfmt --check` on the new file clean. Not verified: the on-screen look in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent parser Q4 — code blocks are mono** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `207 passed; 0 failed` (was 203 at Q3); doc-tests 0. Four new tests: `elements::code::tests::code_draws_in_the_mono_family_and_keeps_indentation` paints a `Code` block and asserts every `DrawText` run is `FontFamily::Mono`, the body is drawn verbatim with its leading indentation, and the `rust` fence label is drawn; `code_does_not_wrap_but_can_opt_in` asserts the unwrapped height is identical at 1000px and 40px (`wide.y == narrow.y`) while `with_wrap(true)` grows the height at 40px; `inline_code_has_no_label`; and `elements::chat_message_bubble::tests::fenced_code_block_paints_in_the_mono_family` paints real Markdown through `ChatMessageBubble` and asserts the fenced body and its language label are both mono. Implementation: `crates/goble-ui/src/elements/code.rs` was dead code — `paint` only stored its origin and it measured through `text::measure_text` (system family); it now measures and draws via `measure_text_family`/`draw_text_with_font` with `FontFamily::Mono`, measures unbounded (`f32::INFINITY`) unless the caller opts into `with_wrap(true)`, so a long line overflows instead of folding mid-expression, and carries the fence's info string (`with_language`/`language()`) which it draws above the body in mono. `crates/goble-ui/src/elements/chat_message_bubble.rs`'s `ChatBlock::CodeBlock` now builds that element instead of a `Text` column. **No syntax-highlighting dependency was added**: `crates/goble-ui/Cargo.toml` has `pulldown-cmark` and nothing else in this area, so the body is plain mono. `cargo check -p goble-app --all-targets` clean; `cargo clippy -p goble-ui --all-targets` shows only the crate's pre-existing lints (the new tests use `LayoutContext::default()` as every other element test does). Not verified: the on-screen look of a long code line in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent parser Q3 — italic renders** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `203 passed; 0 failed` (was 201 at Q2), doc-tests 0. New element test `elements::chat_message_bubble::tests::italic_is_not_bold_and_bold_italic_is_both` paints real Markdown through `ChatMessageBubble` and asserts the drawn Runs: `_x_` → `FontWeight::Regular` + italic, `***x***` → `FontWeight::Bold` + italic, `**x**` → bold, not italic. `resolve_span` no longer drops the flag (`let _ = italic;` gone): `TextSpan` carries `italic`, so the layout measurement and `draw_text_with_font` use the oblique face. Threaded through the renderer: `RenderCommand::DrawText { font_italic }` → `TextAtlas` `TextKey { italic }` → `FontSet`, which now loads the already-vendored `Roboto-Italic`/`Roboto-BoldItalic`/`Hack-Italic`/`Hack-BoldItalic` faces; `platform::text_atlas::tests::italic_rasterizes_a_distinct_face` proves the italic run rasterizes different ink from the regular run, and `semibold_uses_static_distinct_font` now also asserts the italic faces are distinct files. `cargo check -p goble-ui --all-targets`, `cargo check -p goble-app -p goble-desktop-native --all-targets` clean. `cargo clippy -p goble-ui --all-targets` shows the pre-existing lints plus one new style-only lint, `entry_with_family` now 8/7 arguments (18 `too_many_arguments` lints already existed in the crate). Not verified: the on-screen look of italic in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent parser Q2 — Markdown fidelity** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → lib `201 passed; 0 failed` (was 191 at Q1); doc-tests 0. Ten new parser tests, one per construct, cover exactly the cases that had none: `separates_paragraphs`, `keeps_a_mid_sentence_link_in_the_paragraph`, `parses_image`, `ordered_list_keeps_its_start_number`, `parses_nested_list`, `parses_task_list`, `blockquote_keeps_its_paragraphs`, `parses_table`, `parses_horizontal_rule`, `strips_raw_html`; the existing list/blockquote/heading tests were updated to the structured model. `crates/goble-ui/src/elements/markdown.rs` now parses with `Options::ENABLE_TABLES \| Options::ENABLE_TASKLISTS` and is a recursive event parser instead of a flag fold, so `Event::Rule` and `Tag::Image` are handled, raw HTML is stripped (not shown verbatim), nested lists nest (`ListItem { checked, content }`), blockquotes hold nested fragments, ordered lists keep `Some(start)`, and GFM tables/task lists tokenise. `chat_content.rs` folds `Link` into the paragraph as an inline span (a mid-sentence link no longer becomes its own line) and adds `ChatBlock::{Table, Rule, Image}` plus `BlockQuote(Vec<ChatBlock>)`; `chat_message_bubble.rs` renders them recursively, including nested list/quote structure. `cargo check -p goble-ui --all-targets`, `cargo check -p goble-app -p goble-desktop-native --all-targets` clean; `cargo clippy -p goble-ui --all-targets` shows only pre-existing lints (`LayoutContext::default` in examples/tests, `or_insert_with(TerminalFilter::default)`), none introduced by this change. Not verified: the actual on-screen look of a table or nested list in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Agent parser Q10 — parse once per change** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-app` → lib `146 passed; 0 failed` (was 145) plus every integration suite ok; new unit test `state::pane_session_tests::refresh_reparses_only_the_changed_message` drives a real `DesktopState` (sqlite + temp thread store) with a scripted two-row transcript: first `refresh_messages` parses both rows (`parse_count == 2`), an unchanged second refresh parses nothing (`== 2`), then one `append_chat_message_content("m2", "delta")` re-parses only that row (`== 3`) while the delta shows in the transcript. The cache is `app/src/state.rs::MessageParseCache` (`PaneRuntime::parse_cache`): entries are keyed by store row id and hold the role + content + tool_calls they were parsed from, so a streaming delta or an edit invalidates only its own message and a frame with no new delta does no parsing. `cargo clippy -p goble-app --all-targets` clean of new warnings. Not verified: the wire-level `chat:updated` fan-out itself is unchanged (one event per delta still drives one `list_chat_messages` read per pane); the read remains on the event-drain path, but parsing is now per-change, so the delta cost is one parse instead of N. |
| 2026-09-10 | **Agent parser Q1 — transcript scrolling + follow the stream** ([`06-renderer/agent-parser.md`](06-renderer/agent-parser.md)) | `cargo test -p goble-ui` → `191 passed; 0 failed` (6 new tests: the 5 `elements::scrollable` clamp/pin cases — `a_following_region_opens_at_the_end_of_its_content`, `new_content_keeps_a_following_region_at_the_end`, `scrolling_up_holds_the_position_against_new_content`, `scrolling_back_to_the_end_resumes_following`, `a_plain_state_never_follows_growing_content` — plus `chat_view_transcript_clips_to_the_viewport_and_follows_the_stream`, which asserts `clip_rect`/`pop_clip` are emitted); `cargo test -p goble-app` → `145 passed; 0 failed` lib + every integration suite ok (3 new tests: `a_new_transcript_opens_pinned_to_the_latest_message`, `new_content_does_not_move_a_scrollback_position_the_user_chose`, `returning_to_the_bottom_resumes_following_the_stream`); `cargo test --workspace` → no failures. A required prerequisite landed with it: `Flex` with `CrossAxisAlignment::Stretch` under an unbounded cross axis returned `inf`, so every message bubble (a stretch row) measured as infinite height and a pinned offset would have painted the transcript off-screen; it now sizes to content (`crates/goble-ui/src/elements/flex.rs`). Not verified: the feel of following a live stream in a real window (no GUI run — house rules forbid building/running `goble-app`). |
| 2026-09-10 | **Block terminal B0 — shell integration (bash + zsh)** ([`06-renderer/terminal-blocks.md`](06-renderer/terminal-blocks.md)) | `cargo test -p goble-terminal` → 110 lib + 1 conformance + 2 shell-integration passed, 0 failed; the two new tests feed the exact bytes each script emits through `HookTap` and assert every `HookEvent` variant (incl. `CommandFinished` exit code 7); `bash -n` and `zsh -n` pass on `bash.sh`, `zsh.sh` and `zsh_env.sh`; `cargo check -p goble-app` clean; a real interactive bash+zsh pty session emitted `CommandFinished` exit 0 for `true` and exit 1 for `false` |
| 2026-09-10 | **Agent TUI A1 — block kinds + view visibility** ([`06-renderer/agent-tui.md`](06-renderer/agent-tui.md)) | `cargo test -p goble-terminal` → 110 passed, 0 failed (the 4 new tests: `a_conversation_block_sits_between_shell_blocks`, `a_conversation_block_never_takes_pty_bytes`, `each_view_sees_only_its_own_blocks`, `associating_a_block_hides_it_from_the_terminal_when_asked`) plus the 27-fixture conformance test; `cargo clippy -p goble-terminal --all-targets --no-deps -- -D warnings` clean; `cargo test --workspace` → 1126 passed, 0 failed |

## Rule: no `[x]` without proof

An item is only `[x]` in `TRACKER.md` when this file records the command / test / browser check that actually passed. If something could not be verified, it stays `[~]` and the reason is written here — never silently dropped.

## Anti-laziness contract (also in [`GUIDE.md`](GUIDE.md))

- One small item per turn; stop when it is verified.
- Read the owning doc + the real code before writing.
- No placeholder comments; implement fully or don't touch.
- State plainly what was **not** verified and why.
