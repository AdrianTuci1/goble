# Goble architecture tracker

Aggregated, ordered work across the whole tree. Each item points to the owning doc; each doc also carries the same `[ ] / [~] / [x]` markers. Reorder/prioritize freely; the point of this file is that the `.agents/` tree is a **live tracker**, not prose.

Legend: `[ ]` pending · `[~]` in progress · `[x]` done.

## 00 — Repository tooling
- [x] **Track `vendor/alacritty_terminal` in git.** It is a path dependency of `goble-terminal`; the tree is tracked (29 files), so a fresh clone builds without running `scripts/fetch-alacritty-terminal.sh --install` first. The release workflow still runs that script when the checkout lacks the tree, and the script re-checks the archive against the vendored bytes by SHA-256 (`terminal-emulator.md`)
- [ ] **Make the workspace `rustfmt`-clean, or drop the gate.** `./scripts/fmt.sh --check` reports roughly 200 hunks across 127 files on committed HEAD, including files untouched by this phase; `cargo fmt --all` is not usable here either, because `--all` also formats path dependencies and would rewrite the vendored emulator. Either run `./scripts/fmt.sh` once as a mechanical commit or remove the gate from `publish-release` (`README.md`)

## 02 — First-run & routing
- [x] Show an "empty LLM key" banner as a modal overlay instead of the canned reply — the canned reply is no longer sent on the no-key path; the banner is a centered `Dialog` overlay over the whole app (`onboarding-no-model-key.md`)
- [x] Wire the banner click to the model-provider overlay dialog (`onboarding-no-model-key.md`)
- [x] After a key is saved, close the dialog and move to the workspace-type prompt (`onboarding-no-model-key.md`)
- [x] Define the `Routing` decision type and attach it to a conversation — `WorkspaceRouting{Local,Remote}` is persisted per conversation on the `chats.workspace_routing` column and restored on load (`router-local-vs-remote.md`)
- [~] Make `local` a valid runtime target — chat turns run on the local harness for `local`/unset routing; the `resolve_worker_for_target` `bail!` for the agent/workflow deploy path is still pending a local agent runner (`router-local-vs-remote.md`)
- [x] Persist the routing choice across restarts (`router-local-vs-remote.md`)
- [ ] Define the "workspace package" to ship to a remote host (`remote-bootstrap.md`)
- [ ] Add the remote self-configuration step (read TOML → resolve providers/models) (`remote-bootstrap.md`)
- [ ] Reuse the existing pairing/SSH/helm path to bring up the remote workspace (`remote-bootstrap.md`)
- [ ] Publish a workspace endpoint the router can point at (`remote-bootstrap.md`)

## 03 — Workspace model
- [ ] Introduce a `Workspace` type (id, kind, root, config) and persist it (`workspace.md`)
- [ ] Allow a workspace `kind` to be promoted local → remote while keeping the same id (`workspace.md`)
- [ ] Make the provisioned worker bundle a remote workspace record (endpoint, TLS, worker id) (`workspace.md`)
- [ ] Extend `GobleConfig` to cover plugins, workflows, personas, memory, deep-research (`shared-secrets-and-toml.md`)
- [ ] Add a config-is-agent-editable surface (agent reads/patches the TOML, never the vault) (`shared-secrets-and-toml.md`)
- [ ] Add outbound secret-scrubbing on logs/traces/events (`shared-secrets-and-toml.md`)
- [ ] Ship the TOML + encrypted vault blob together in remote bootstrap (`shared-secrets-and-toml.md`)
- [ ] Add `workspace_id`, `persona`, `cwd` + per-agent conversation key to the agent model (`multiple-agents.md`)
- [ ] Persist per-agent state so an agent survives restarts (`multiple-agents.md`)
- [ ] Let agents be created/edited from natural language and bound to workspace config (`multiple-agents.md`)
- [x] Populate a credential from the ask-user card by name and keep the value out of the transcript — card gains name + masked value; harness `credentials` tool lists names and `run_command` expands `{{credential:<name>}}` server-side, so the model never sees the key (`shared-secrets-and-toml.md`)
- [x] Give the machine a hidden home `~/.goble` mirroring `~/.grok`, split into a universal **base** (identity/auth/config/sessions/logs, for every user) and a **workspace payload** (bundled tooling, worktrees, threads, local store — only when the workspace runs locally); remote-only users keep a minimal home (`workspace.md`, `shared-secrets-and-toml.md`)
- [x] Track every principal with access + their grants — `access_grants` table + store methods; harness `principals` tool lists principals and grants; per-principal `principals/<id>/` home dir (`shared-secrets-and-toml.md`)
- [x] Seed a Goble user guide into `~/.goble/docs/user-guide` on first launch (ported from `.grok` and Goble-ified; base-only, so it exists for remote-only users too) and expose a `user_guide` harness tool (list/read topics) + a system-prompt pointer so the model answers product questions from the guide — incl. remote/self-as-worker via Tailscale and mobile access (`workspace.md`, `docs.rs`/`app_home.rs`)

## 04 — Agent runtime (harness)
- [ ] Fix the hardcoded `workspace_root = /var/goblin/workspaces` default in `goblin-worker` so harness tests/dev runs don't hit `Permission denied` on machines without that dir (currently breaks `goblin-worker` runner/scheduler unit tests and `goble-cli`'s `e2e_worker` in a stock dev environment) (`sandbox-and-cwd.md`)
- [ ] Define durable **state** vs compacting **transcript** boundaries (`agent-state-and-compaction.md`)
- [ ] Wire compaction triggers by token/size budget; persist the rolling summary (`agent-state-and-compaction.md`)
- [ ] Key state by `agent_id` and make it survive local↔remote routing (`agent-state-and-compaction.md`)
- [x] **Sub-agents S1 — the `SubAgent` model, budget and lifecycle:** a `SubAgentSpec` / `SubAgentBudget` / `SubAgentStatus` / `SubAgentRecord` in `goble-core` whose budget exhaustion is terminal by construction, with a bounded spawn depth and a per-child CWD subdir (`subagents.md`)
- [x] **Sub-agents S2/S3 — spawn a real child, foreground and background:** a `spawn_subagent` harness tool that runs a child on its own conversation (`parent_chat_id` on `chats`), a bounded loop carrying the child's own budget and tool set, a per-child record registry, and cancellation that reaches every child so a routine never blocks the parent's turn (`subagents.md`) — S2 done: the tool runs a real foreground child (own chats row + messages, budget-terminal loop, depth refusal); S3 done: every child runs on its own task and is registered in the `Harness`'s registry (list + snapshot accessors) before it starts, `run_in_background: true` returns the id with a live record, a foreground await past its budget moves the child to the background, and `Harness::cancel` or `Harness::cancel_subagent` ends a record `Cancelled` with the reason (panic → `Failed`, exhaustion → terminal)
- [x] **Sub-agents S4 — lifecycle on the wire:** `SubAgentSpawned` / `Progress` / `Finished` cross all six hops with `CommandProposed` as the template — `HarnessEvent` → `map_event` → `HarnessServerEvent` → the daemon mapper → `DaemonEvent` → `translate_daemon_event`, which emits `chat:subagent_spawned` / `chat:subagent_progress` / `chat:subagent_finished` with typed payloads beside `CommandProposedEvent` — and they are emitted by S3's registry transitions (`register` spawns, `publish` progresses/finishes, `cancel_entry` finishes), the registry pushing them to a per-turn sink the live turn's stream merges in (`with_subagent_lifecycle`), so a child on its own task reaches the wire while its turn runs; payloads are plain data mirroring the record's own fields, the protocol crates still do not depend on `goble-core` (`subagents.md`)
- [x] **Sub-agents S5 — the transcript row:** a sub-agent is a quiet ruler row in the parent's transcript, not a card — one line with a status bullet (`◐` Running/Accent, `●` Completed/Success, `◆` Failed/Error, `◇` Cancelled/Muted), the record's description and, only while running, the activity label and the elapsed time (`4.5s` / `43s` / `2m05s`, grok-build's `format_duration` shape); it folds with A4's three states (one line folded, the detail expanded), and a click on the row fires `ChatAction::OpenSubAgent(child_id)` — the view it enters is S6, which has landed: the click now shows the child's own conversation in the pane. The data is S4's live records, not the tool's arguments: `SubAgentRow` (`crates/goble-ui/src/elements/chat_content.rs`) is built by the app (`PaneRuntime::apply_subagent_{spawned,progress,finished}` keyed by `subagent_id`, re-keyed for the element by `parent_call_id` in `UiState::sub_agent_rows`) and handed to the bubble through `ChatView::with_sub_agents`. `ToolPresentation::SubAgent` stays the resolution key; the expanded body shows what the record carries and says plainly that it holds no rows of the child's own conversation (`subagents.md`)
- [x] **Sub-agents S6 — the child view:** a pane shows one child's own conversation: the row's click puts the child's card into the pane's block list and filters the pane to the child (A7's `enter_agent_view`, aimed at the child), under a framed title carrying the child's type, description, status and elapsed time — the same affordance as the row's — and no composer, because an input line there would write into a conversation the user is only reading. The body is the rows the store holds under the child's chat id, parsed through the one `MessageParseCache` → `ChatView` path, so a child's tool output is the same `terminal_block` as anywhere else. Esc returns to the conversation the row was clicked in — the shell, or the parent's own agent view when the row was clicked from inside it, which the pane remembers in `returned_to` — and both cards stay in the block list, so neither conversation is stranded; binding the pane to another conversation unmounts the child (`UiState::{open_sub_agent, close_sub_agent_view, sub_agent_views}`); nested children stay enterable — the child view hands its pane's live sub-agent rows to `ChatView::with_sub_agents` and relays the open action for that pane, so a `spawn_subagent` row inside a child view opens that child too (`subagents.md`)
- [x] **Sub-agents S7 — the work overlay — removed 2026-09-12:** the user, looking at the running app, found the panel titled `Work in flight` redundant and asked for it gone, so the overlay no longer exists. Deleted: `crates/goble-ui/src/elements/work_overlay.rs` and its module registration/exports, the `Sheet` wiring and its close route in `app/src/ui/mod.rs`, the `Cmd/Ctrl+Shift+W` binding (`app/src/root_view.rs`) and the palette command that fired the same action, `UiState::{open,close}_work_overlay`, `work_overlay_groups`, `work_overlay_scroll`, the dismissal bookkeeping + auto-open rule that watched the running count (`work_overlay_dismissed`/`work_overlay_running_children`/`note_running_sub_agents`/`running_sub_agent_count`), and `UiState::kill_sub_agent` with `UiActions::on_kill_sub_agent` — checked first: nothing else called them. Also deleted `app/src/integration_testing/work_overlay_flow.rs` and its `[[test]]` registration, and the element's six unit tests. The cues are kept because they still carry what the user needs (how much work is running): C2's footer still-running line and C3's topbar `◆ N` indicator stay drawn but are now non-interactive (`TurnStatusFooter` and `build_live_indicator` take no handler). The two `root_view` tests that had worked around the auto-opened overlay are back to their plain pane-Esc premise (`s.work_overlay_open = false` removed), and the topbar/footer/chat-view/menu tests were rewritten to assert the indicators draw without taking a pointer event. Verified with `cargo test -p goble-ui -p goble-app` (`subagents.md`).
- [ ] Build the tool-registry abstraction with a uniform schema (`tools.md`)
- [ ] Load skills as tool-adjacent docs and inject into context (`tools.md`)
- [ ] Emit tool-call events to renderer + trace (`tools.md`)
- [ ] Drive MCP install/discover from the harness, not only a UI panel (`mcp.md`)
- [ ] Let an agent request an MCP install mid-conversation (`mcp.md`)
- [ ] Surface MCP server status + tool list in the renderer (`mcp.md`)
- [~] Reuse `goble-core::llm` + grok-build sampler for the loop (`llm-and-models.md`)
- [x] Wire the composer's model selector to really change the model (`llm-and-models.md`)
- [~] Stream deltas to renderer + trace in real time — renderer streaming done; per-token trace + reasoning deltas pending (`llm-and-models.md`)
- [ ] Define the workspace message-bus contract (addressing, message kinds) (`inter-agent-communication.md`)
- [ ] Implement the local in-process channel; reuse worker transport for remote (`inter-agent-communication.md`)
- [ ] Enforce loop bounding and show agent↔agent activity (`inter-agent-communication.md`)
- [ ] Give each agent + sub-agent a CWD subdir under the workspace root (`sandbox-and-cwd.md`) - the sub-agent half lands with S1/S2 (`subagents.md`)
- [ ] Tighten isolation (fs/network) per workspace, reusing `xai-grok-sandbox` where portable (`sandbox-and-cwd.md`)
- [ ] Stream remote terminal output to the local renderer (`sandbox-and-cwd.md`)
- [ ] Add a `MemoryNote` store with embedding-based search (`memory.md`)
- [ ] Expose `remember` + a query tool to agents (`memory.md`)
- [ ] Wire retrieval into context assembly (`memory.md`)
- [ ] Define the `Persona` config and store it in the TOML (`personas.md`)
- [ ] Assemble system prompts from persona + config (`personas.md`)
- [ ] Let personas be created/edited from natural language (`personas.md`)
- [ ] Add a `DeepResearch` routine (steps, budget, progress events) (`deep-research.md`)
- [ ] Render deep-research progress + final report (`deep-research.md`)
- [ ] Allow saving a research result to workspace memory (`deep-research.md`)

## 05 — Execution router & targets
- [ ] Add `Local` + `SelfAsWorker` to the runtime-target set; remove the `local` bail (`runtime-targets.md`)
- [ ] Represent each target's connection params (`runtime-targets.md`)
- [ ] Map a workspace `kind` to a default target, allow per-conversation override (`runtime-targets.md`)
- [ ] Implement `resolve()`, honoring worker-pairing status (`execution-router.md`)
- [ ] Cache the resolved target per conversation (`execution-router.md`)
- [ ] Implement local→remote promotion, preserving agent+conversation state (`execution-router.md`)

## 06 — Renderer
- [x] Merge the hot-reloadable UI crate into the app — the UI builder (`build_ui`) + snapshot/action types + per-screen modules now live in `app/src/ui`, built directly in the app crate like warp-new builds its windows in `app`; `goble-ui-hot`, the cdylib/`#[no_mangle]`/ABI boundary and the hot-reload handshake are gone (`renderer-architecture.md`)
- [x] Move `integration_testing/` under `app/src/integration_testing/` and point the `app` test targets there (`renderer-architecture.md`)
- [x] Route chat turns through `app/src/runtime` (`crate::runtime::run_turn`) — local harness today; remote degrades to local with a logged warning (`renderer-architecture.md`)
- [x] Drive the chat from the harness (replace the canned reply with real events) (`renderer-architecture.md`)
- [x] Render tool calls distinctly — `role="tool"` rows render as terminal blocks, and the assistant message shows "⚙ tool" invocation chips (`renderer-architecture.md`)
- [x] Align chat + composer with warp-new's UI model — tool/command blocks render as `surface_2` raised cards (1px border, radius 8); the composer is a floating `surface_2` card (border, radius 8, 80px editor) with `surface_1`→`surface_2` pill buttons for model / profile / attach / stop; `ColorToken` maps to warp-new's `surface_1`/`surface_2`/`outline`/`text_sub` (`design-tokens.md`)
- [~] Warp-new parity, still pending: per-message author/avatar header row, per-status tool icons (running/blocked/success/failed), the status/action footer (thumbs / continue / fork / usage), and live tool-call streaming to the renderer (`design-tokens.md`)
- [ ] Add the missing product pages (agents, executions/traces, logs) to the native shell (`renderer-architecture.md`)
- [~] Wire the model selector + attach/voice to real behavior — model selector drives `run_chat_turn` and the Stop button cancels a turn (`renderer-architecture.md`)
- [x] Vendor the emulator instead of writing one — `vendor/alacritty_terminal` holds `alacritty_terminal` 0.26.0 (Apache-2.0) byte-identical to the published archive (SHA-256 checked, `tests/` and `Cargo.lock` removed); `crates/goble-terminal` drives it through the crate's own `vte` re-export, so parser and handler cannot drift. Provenance and the attribution duties are discharged in `THIRD-PARTY-NOTICES.md`, and `scripts/fetch-alacritty-terminal.sh` re-checks the archive against the vendored tree (`terminal-emulator.md`)
- [x] Build a real VT emulator behind the terminal pane — `crates/goble-terminal` (keys, mouse, hooks, osc, palette, screen, blocks; 106 unit tests + a 27-fixture conformance suite, clippy clean) behind `app/src/emulator.rs`, which feeds the screen from the reader thread; the pane paints it as a cell grid through `goble_ui::elements::TerminalGrid` — per-cell colour, attributes, wide characters and the cursor — instead of the ANSI-stripping line buffer it used to be, and encodes keys and pointer events through the crate (`terminal-emulator.md`)
- [x] **Phase 0 — key encoding:** arrows, Home/End, PageUp/Down, Insert/Delete, F1–F35, Alt/Meta, Shift+Tab, xterm modifier form, asserted by encode-direction tables — `crates/goble-terminal/src/keys.rs`; the pane encodes every key through it, and `goble-ui` now delivers the named keys it used to drop (`terminal-emulator.md`)
- [x] **Phase 1 — parser + grid:** grid, cell model with wide-char spacers and combining marks, cursor, and the renderer seam — provided by the vendored emulator plus `screen.rs`, which hands the renderer owned rows/cells (`Screen::lines`, `Screen::content_lines`, `Screen::cursor`) instead of a borrowed-grid trait (`terminal-emulator.md`)
- [x] **Phase 2 — addressing + editing + SGR:** cursor addressing, deferred wrap, ED/EL/ICH/DCH/ECH/IL/DL/SU/SD/DECSTBM, tab stops, IRM, SGR incl. 256/truecolor and colon form — from the emulator; asserted here for CUP, SGR 16/256/truecolor, underlines and hard reset (`terminal-emulator.md`)
- [x] **Phase 3 — alt screen + modes + replies:** 47/1049 lifecycle, DEC private mode table, DA1/DA2/DSR/XTVERSION, DECSCUSR, title — asserted for the alt-screen swap, no-scrollback and restore, mode reporting (app cursor, bracketed paste), title events, and clipboard/colour/text-area queries with their formatters (`terminal-emulator.md`)
- [x] **Phase 4 — scrollback + resize/reflow** — scrollback with a per-block row cap and one reflow path are implemented and asserted, including that a shrink which reflows into more rows loses nothing; the pane measures itself from the font's cell pitch and calls `TerminalSession::set_size`, which resizes the screen and the pty together, so the 24×96 hard-coding is gone (`terminal-emulator.md`)
- [x] **Phase 5 — terminal surface:** bracketed paste, app-cursor mode, OSC 8 hyperlinks, OSC 52 clipboard queries and window-title/reset-title events are exposed in `screen.rs`; focus events (`ESC [ I`/`ESC [ O` under 1004, driven by the window's focus), mouse reporting (1000/1002/1003 + SGR 1006 with modifiers and the middle button, `mouse.rs`) and the OSC 7/133 observer (`osc.rs`) are in the crate and used by the pane, which falls back to its own scrollback when the program is not listening for the wheel (`terminal-emulator.md`)
- [x] **Phase 6 — conformance harness:** `crates/goble-terminal/tests/conformance.rs` walks a 27-fixture corpus under `crates/goble-terminal/tests/conformance/` (4-file format: recording/size/config/grid, enumerated from disk, `grid.json` denies unknown keys), asserting rows, the full cursor (position, visibility, shape, deferred-wrap flag) and exact cells, with scrollback/alt-screen pinned by `history_size`/`alt_screen`/`content_rows`; `cargo nextest` adopted (`.config/nextest.toml` + `.github/workflows/ci.yml`). Upstream's corpus is **not** fetchable (the published `.crate` holds only `src/`); an external corpus is supplied via `GOBLE_TERMINAL_CONFORMANCE_DIR` (`terminal-emulator.md`)
- [x] **Block terminal B0 — shell integration (bash + zsh):** `precmd`/`preexec` hooks, one DCS hex-JSON hook channel, `PS1` save/suppress/restore, `InitShell` + `Bootstrapped` handshake — `crates/goble-terminal/assets/shell/{bash.sh,zsh.sh,zsh_env.sh}` emit the channel (`InitShell`/`Bootstrapped`, `Preexec`, `CommandFinished` with an exit code, `Precmd` with pwd/git/honor_ps1, `InputBuffer`, `Clear`), save/suppress/restore `PS1`/`PROMPT`, and the pane's PTY spawn points bash at `--rcfile` and zsh at `ZDOTDIR`; `cargo test -p goble-terminal` includes a test that decodes the exact bytes each script emits, and a real interactive bash+zsh pty session reported a `CommandFinished` exit code per command (`terminal-blocks.md`)
- [~] **Block terminal B1 — block model:** `Block` + `BlockList`, lifecycle state machine, per-block screens, freeze-on-finish, feed routing, background promotion, `clear` gaps and block metadata are in `blocks.rs`; the height sum-tree layout, prompt grids and prompt hiding are not (`terminal-blocks.md`)
- [ ] **Block terminal B2 — prompt hiding:** OSC 133 prompt demarcation, hidden prompt grid, `honor_ps1` negotiation + user toggle (`terminal-blocks.md`)
- [ ] **Block terminal B3 — clear + edges:** `clear` interception, in-place clears, per-block output cap + truncation notice, alt-screen takeover, no-integration banner and plain-terminal fallback (`terminal-blocks.md`)
- [ ] **Block terminal B4 — persistence:** block serialization/restore, cross-block selection + search (`terminal-blocks.md`)
- [x] **Agent TUI A1 — block kinds + view visibility:** `BlockKind::{Shell, AgentView{conversation_id, label}}` and a per-block `BlockVisibility`, so one list holds shell blocks and a conversation card; `BlockList::{push_agent_view_block, associate_with_conversation, set_visibility, visible_blocks, visible_lines}` with `BlockView::{Terminal, Agent{conversation_id}}` as the two filters — a conversation block never becomes active, never takes PTY bytes, and its own card is hidden while its agent view is open (`agent-tui.md`)
- [x] **Agent TUI A2 — tool-call status carrier:** write `status`/`result` per call into the `tool_calls` column on start and on finish so a re-read from the store renders terminal states (`agent-tui.md`)
- [x] **Agent TUI A3 — live tool events:** emit a structured `chat:tool` event from `ToolCallStarted/Finished/Error` and hold a per-pane in-flight map, since `chat:updated` only says "re-read the store" (`agent-tui.md`)
- [x] **Agent TUI A4 — the tool-call fold, as grok-build has it:** two of A4's three asks landed with Q6/Q7/A2 (`ToolCall { id, name, arguments, status, result }` carries the status, `tool_status_affordance` gives it a glyph and a colour — ○ Pending/Muted, ◐ Running/Accent, ● Finished/Success, ◆ Error — and the result row is tied to its call by call id). The **fold** now lands too: every tool shape starts folded (`Collapsed`), a header click or `e` advances it `Collapsed -> Truncated -> Expanded -> Collapsed`, and the app-owned `tool_fold` map next to `reasoning_expanded` keeps the fold across the per-frame rebuild. A folded read is one line carrying its path, a folded edit carries a `+N/-M` diffstat, a folded command hides its output; truncated shows first 5 + last 3 (read) / first 2 + last 3 (command). (`agent-tui.md`)
- [x] **Agent TUI A5 — composer proposal mode:** the composer can show a command proposal with candidate variants, edit it in place, approve or reject it, and cycle between candidates from the keyboard (`agent-tui.md`)
- [x] **Agent TUI A6 — harness approval suspension:** suspend before a command tool runs (mirroring `ask_user`), emit `CommandProposed`, and resume on approve/edit/reject, sharing the `auto_approve` bit — today `run_command` executes immediately behind an allow-list only (`agent-tui.md`)
- [x] **Agent TUI A7 — agent view over the block list:** Cmd+Enter pushes the conversation's card into the pane's block list and switches the pane's view filter to that conversation's agent view, Esc returns to the terminal leaving the card behind, and a click on the card reopens the view; the agent view draws the conversation's blocks through the one terminal-block renderer (`agent-tui.md`)
- [x] **Agent TUI A8 — conversation card element:** the card block: conversation title, status, last activity, click handler (`agent-tui.md`)
- [x] **Agent parser Q1 — transcript scrolling + follow the stream:** the transcript is a `Scrollable` with no `ScrollState`, so it has no viewport, no clip, no wheel and no offset and simply overflows; attach the state, clip, and pin to the bottom unless the user scrolled up (`agent-parser.md`)
- [x] **Agent parser Q2 — Markdown fidelity:** enable the GFM extensions and handle the fall-through arms — tables, task lists, horizontal rules and images are dropped today; nested lists are broken by a shared buffer, and blockquotes and list items are flattened to a plain string (`agent-parser.md`)
- [x] **Agent parser Q3 — italic renders:** the parser recognises italic and the renderer discards the flag, so `_x_` shows as plain and `***x***` as bold only (`agent-parser.md`)
- [x] **Agent parser Q4 — code blocks are mono:** a code block rendered through `Text`, i.e. the system font, and the `Code` element that should do it was dead code that drew nothing; `ChatMessageBubble` now renders a fenced block through `Code` in the mono family, unwrapped, with the fence's language label (`agent-parser.md`)
- [x] **Agent parser Q5 — tool-call status:** `ToolCall { id, name, arguments, status, result }` written on start as well as finish (the persisted carrier); the live overlay from A3 remains tracked by A3 (`agent-parser.md`)
- [x] **Agent parser Q6 — inline tool-call rows:** replace the bordered `surface_2` card (a `cpu` icon and the literal label `"tool"`) with inline rows — status glyph, tool, arguments, result — collapsed when there is nothing to show; no tool call is drawn in a box. The card is gone (`build_tool_call_rows` draws no `Container`, no border, no raise); the status glyph/colour is read from the persisted `ToolCall.status`, and the app's substring guess (`tool_terminal_data`'s `body.contains("ERROR:")`) is gone too (`agent-parser.md`)
- [x] **Agent parser Q7 — per-tool presentation:** a presenter keyed by the tool definition the harness dispatches on, rather than by string matching on the tool's name (command vs path vs diff vs search vs sub-agent). `goble_core::harness::presentation` answers `tool_row(name, arguments, result) -> ToolRow { verb, subject, detail, kind }` and `tool_kind_for(name) -> ToolKind { Execute, Read, Edit, Create, List, Search, WebSearch, WebFetch, SearchTools, UseTool, MemorySearch, Skill, SubAgent, Other }`; the renderer consumes that row and never matches `call.name` — a command draws its command line and output, a read its path, an edit its path plus Q9 `Diff` rows, a search its query, a sub-agent its own rows. **Aligned to grok-build name for name:** `tool_kind_for` answers for grok-build's own tools first (`read`, `edit`, `write`, `bash`, `grep`, `ls`, `web_fetch`, `search_tool`, `use_tool`, `memory_search`, `skill`) and then for our harness definitions, and `tool_row` gives the grok-build row — the bold verb, the operand the arguments carry and the dim detail the result states (`Read src/lib.rs`, `Edit src/lib.rs +1/-1`, `Run cargo test`, `Search "fn build" in src`, `Subagent “audit the migration” running · reading · 4.5s`), never the raw tool name or the raw argument JSON. Core's `the_parse_answers_for_every_defined_tool` iterates every defined tool, so the parse cannot drift from the definitions (`agent-parser.md` §12)
- [x] **Agent parser Q8 — terminal segment as a block:** a command the agent runs is drawn by the same terminal block element as terminal mode, from the block the bridge produced, instead of being re-rendered as agent output; depends on P1/P3 (`agent-parser.md`). One renderer builds every block (`crates/goble-ui/src/elements/terminal_block.rs`'s `terminal_block`, over `TerminalData::for_command`); the transcript's command tool call draws that block (command line + output + status, no more `$ cmd` text rows), the terminal-mode fragment draws it, and the pane's executed-command block path (`app/src/ui/terminal.rs::executed_command_block`) is the one the chat transcript reuses (`agent-parser.md`)
- [x] **Agent parser Q9 — diff rendering:** diff rows with line gutters and add/remove colouring, inline and without a box; no diff view exists anywhere today (`agent-parser.md`)
- [x] **Agent parser Q10 — parse once per change:** every assistant delta currently triggers a full re-read of all rows and a full Markdown re-parse on the UI thread during layout; cache fragments per message and re-parse only what changed (`agent-parser.md`)
- [x] **Agent parser Q11 — reasoning surface:** the model's thinking now reaches the app and renders. `HarnessServerEvent`/`DaemonEvent` gained `ReasoningStarted`/`ReasoningDelta`/`ReasoningDone`, `goble-harness-internal` maps them through instead of dropping them, the desktop service emits a `chat:reasoning` event, and the app overlays each step as its own transcript row — recessed (`Muted`) and collapsed to a clickable header until the user expands it (`agent-parser.md`)
- [x] **Agent terminal bridge P1 — block ownership:** `BlockOwner::{User, Agent{conversation_id, call_id}}` on shell blocks, `Block::owner()`, and `BlockList::claim_next_pre_exec(id, owner)` — a claimed `Preexec` takes the owner, an unclaimed one is the user's, and a claim aimed at another block (or an already-running/finished one) is refused (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P2 — the claim protocol:** `TerminalSession` writes the agent's command to the pane's pty, holds the claim, resolves it on the next `Preexec`, and fails it on a bounded timeout instead of attaching it to the wrong block (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P3 — tool-result extraction:** a claimed block's output text and exit code become the tool result — `BlockEvent::ToolResult(ToolResult)` carries the block, conversation, call id, command, output and `ToolOutcome::{Finished{exit_code}, StillRunning}`; produced on `CommandFinished` and on the `Precmd` that promotes a still-running command, so non-zero exit, empty output, Ctrl-C (130) and background promotion all resolve rather than hang (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P4 — route the shell tool through the pane:** agent + terminal panes run the agent's commands in the pane's own shell; panes without a terminal keep `SandboxedCommandRunner` (`agent-terminal-bridge.md`)
- [ ] **Agent terminal bridge P5 — in-band claim id (hardening):** carry the call id in the shell hook payload, which needs B0's shell scripts; only if P2's correlation proves insufficient (`agent-terminal-bridge.md`)
- [x] **Agent terminal bridge P6 — owner into the views:** feed the owner into A1's `associate_with_conversation` so an agent-run command shows in its conversation and a user-run one does not (`agent-terminal-bridge.md`)
- [x] **Agent UI follow-up R1 — the approval gate must run commands:** A6 suspends every `run_command` when `auto_approve` is off and that is the default (`crates/goble-core/src/harness.rs:515`, `app/src/state.rs:815`, `:1786`); the `CommandProposed` it yields is dropped at `crates/goble-harness-internal/src/lib.rs:255` and `Harness::resume_command` has no caller outside tests, so the command never runs and the turn never closes — a suspension with no path back is a hang, not a gate (`agent-tui.md` §9)
- [x] **Agent UI follow-up R2 — pane routing falls back to the sandbox:** `shell_command` installs the hook scripts only for bash and zsh but `pane_session` (`app/src/terminal.rs:1300-1316`) hands a runner to any pane with a session, so under any other shell the command is written to the pty, no `Preexec` arrives and the claim fails after `CLAIM_TIMEOUT` where it used to run through `SandboxedCommandRunner` (`agent-terminal-bridge.md` §9)
- [x] **Agent UI follow-up R3 — links stay clickable inside a paragraph:** Q2 folds links into the inline flow as `InlineSpan::link` (`chat_content.rs:423`) but `chat_message_bubble.rs:512-515` paints paragraphs as bare `InlineText`, and the `ChatBlock::Action { OpenUrl }` path and its test `link_fragment_becomes_interactive_action` were deleted — styling a link is not making it interactive (`agent-parser.md` §10). Fixed: the inline span carries its click handler to the painted `InlineText` and the test is restored.
- [x] **Agent UI follow-up R4 — one block renderer for a tool-result terminal segment:** `crates/goble-ui/src/elements/group_chat_message.rs:288` rendered `ChatFragmentKind::Terminal` as `Empty` instead of the shared `terminal_block` renderer Q8 built (`agent-parser.md` §10). `build_content_column` now flushes the inline run and draws the fragment through `render_terminal` → the one `terminal_block`, so the same block the transcript's command tool call and terminal mode draw appears here too; the new `GroupChatMessage` test `terminal_fragment_draws_the_shared_block` asserts the fragment's drawn runs equal the shared renderer's (`agent-parser.md` §10)
- [x] **Agent UI follow-up R5 — read the status, do not encode it as a string:** `crates/goble-core/src/reasoning.rs:726`, `:925-928` carry a tool result's status as the literal `ERROR: ` prefix and `app/src/state.rs::tool_terminal_data` renders it as text, while `app/src/ui/terminal.rs:562-566` infers status from the boolean `block.failed`; carry and read the real field (`agent-parser.md` §10, `agent-tui.md` §9). Fixed: the tool-result row is now just `<call_id>\n<body>` (both the immediate and resumed paths); the app resolves the row's status from the call record on the assistant row's `tool_calls` column and passes it into the block, so the text is never matched; `VisibleBlock` carries the block's `BlockState` and `exit_code` instead of a `failed` boolean, and the agent view maps them (a block that is not done is `Running`, never `Success`)
- [x] **Agent UI follow-up R6 — the card-free cards:** A5's proposal was a `Container` with `Border::all(1.0)` and `corner_radius(8)` (`chat_composer.rs:620-629`) and the app drew its own bordered card in `app/src/ui/terminal.rs::build_card` while A8's card-free `ConversationCard` element was exported and never constructed (`agent-tui.md` §9). Fixed: the composer proposal is now a full-width band of rows with no border and no radius, and `build_card` builds A8's `ConversationCard` from the block identity the pane's view carries (`ConversationCard::new`, which `from_block` now delegates to), so the app draws the real element; the composer's proposal test asserts zero borders and `ui::terminal::tests::the_conversation_card_is_card_free` asserts the app's card paints the element's square status rail and no border
- [x] **Agent UI follow-up R7 — the link's click must reach an opener:** R3 made a link a live hit target inside the element, but nothing consumes the action in the app: `grep -rn "with_on_action\|on_action" app/src/` returns nothing, so `ChatView::with_on_action` (`crates/goble-ui/src/views/chat_view.rs`), which A5/A7 never call either, is still the end of the line, and the app has no external-URL opener anywhere (`detect_screen_link` in `app/src/state.rs` only recognises `rdp://`/`goble://desktop` handoffs). Wire the tap in `app/src/ui/chat.rs` and give the app an opener (macOS `open`, with the plain-`Http`/`Https` guard the URL must pass), so a click on a link in the transcript actually opens it (`agent-parser.md` §11). Fixed: `app/src/ui/chat.rs::chat_action_relay` hands a fragment's `ChatAction::OpenUrl` to `UiActions::on_open_url` (`app/src/ui/mod.rs`/`app/src/actions.rs`), which calls `app/src/state.rs::open_external_url`; the guard accepts only `http`/`https` (with a non-empty host) and passes the URL to the platform opener (`open`/`xdg-open`/`explorer`) as a single argv argument, refusing every other scheme before the opener is reached. The `rdp://`/`goble://desktop` handoff path is untouched. Tests: `ui::chat::link_action_tests::{a_link_click_reaches_the_app_opener_with_its_url,a_non_http_scheme_click_is_refused_before_the_opener}`, `state::external_url_tests::{web_scheme_accepts_only_http_and_https,open_external_url_refuses_non_http_schemes}`
- [x] **Agent UI follow-up R8 — the pane's executed-command block reads its status:** `executed_command_block` (`app/src/ui/terminal.rs:90-106`) still ends in `TerminalData::new("terminal", lines).with_status(TerminalStatus::Success)`, so a pane command that is running or that failed is drawn as a finished success; `block_status` (`:113`), which reads `block.state`/`block.exit_code`, is the helper that already does this and is used by the agent view only. Called from `app/src/ui/chat.rs:38` (`agent-parser.md` §11). Fixed: `executed_command_block` takes the newest visible block that carries a command (a finished command leaves an empty prompt block behind it) and maps it through `block_status`, so a command still running draws `Running` and a finished non-zero exit draws `Error`; test `ui::terminal::tests::the_executed_command_block_reads_the_blocks_status`
- [x] **Agent UI follow-up R9 — the four surfaces that are still bordered cards:** `AskUserCard` (`crates/goble-ui/src/elements/ask_user.rs:297`, `Border::all(1.0)` + `corner_radius(8.0)`, drawn inline by `crates/goble-ui/src/views/chat_view.rs:690`) — an agent interaction, while the sibling A5 proposal is a borderless band; the queued-prompt card (`chat_view.rs:755`); and the two handoff cards (`:806`, `:851`). Rebuild each as rows in the pager idiom and flip any test that asserts the border (`agent-tui.md` §10). **Done:** all four now draw as borderless `SurfaceRaised` full-width bands with no corner radius — `AskUserCard`'s root (`crates/goble-ui/src/elements/ask_user.rs`) and the queued-prompt, inline-desktop and screen-link blocks (`crates/goble-ui/src/views/chat_view.rs`) drop `Border::all(1.0)`/`corner_radius(8.0)`, matching A5's proposal band. The absence is asserted, not claimed: `elements::ask_user::tests::the_ask_user_surface_paints_a_band_not_a_bordered_card` checks no `StrokeRect` traces the surface's full bounds (the quick-reply checkboxes still stroke their boxes), and `views::chat_view::tests::chat_view_renders_the_queued_prompt_as_a_borderless_band` plus the extended `chat_view_renders_inline_screen_as_image` / `chat_view_renders_screen_link_card_without_frame` assert `stroke_rect == 0`
- [x] **Agent UI follow-up R10 — the two R1 loose ends:** `resume_command_chat_turn` (`crates/goble-desktop-service/src/state.rs:2733`) issued the resume before subscribing to the bus, a race the R1 verifier could not trigger in five runs but that is real by inspection; and the acceptance clause "a turn containing a command tool never leaves the session busy" was an assertion inside the approve/reject tests rather than a test of its own (`agent-tui.md` §10). **Done:** the listener now subscribes to the daemon bus before `resume_command` is issued, so a broadcast receiver exists before the resumed turn can emit `TraceFinished` and the event cannot be lost; a new named test `state::tests::a_command_turn_frees_the_session_on_every_exit_path` drives the proposal through approved, rejected and approved-but-failing, asserting the daemon holds the session while suspended and drops it once the joined listener sees the terminal event. Verified with `cargo test -p goble-desktop-service` (34 passed; 0 failed)
- [x] **Agent transcript H1 — syntax highlighting infrastructure:** there is no highlighting anywhere in the workspace (`grep -rn "syntect\|tree-sitter\|two-face\|highlight" --include=Cargo.toml .` finds nothing; `crates/goble-ui/Cargo.toml` has `pulldown-cmark` and nothing for code), so `Code` (`crates/goble-ui/src/elements/code.rs`) draws a fenced block in one colour with the language label above it. Add `syntect` (default-features off, pure-Rust regex backend) and `two-face` (bat's grammar set), author **our own** `.tmTheme` in grok-build's visual idiom under a new asset directory — do not copy theme or grammar files out of `~/Projects/grok-build` or `~/Projects/warp-new` — and expose one entry point that turns a text + a language into styled spans; the language comes from a file extension, a markdown fence info string, or the `bash` token for a command. Wire it into markdown fences and the tool-call bodies; keep syntect's foreground colours, drop its backgrounds, and do not regress `Code`'s existing mono/no-wrap contract (`agent-parser.md` §11). **Done:** `crates/goble-ui/Cargo.toml` adds `syntect` 5.3.0 (`default-features = false`, features `parsing`/`dump-load`/`plist-load`/`regex-fancy` — the pure-Rust `fancy-regex` backend, no Oniguruma) and `two-face` 0.4.5 (`syntect-fancy`), so the grammar set is bat's 900 KiB `syntaxes-fancy-newlines.bin`; the theme is **ours**, `crates/goble-ui/assets/themes/goble-night.tmTheme`, loaded from that asset. `crates/goble-ui/src/syntax.rs` is the one entry point, `highlight(text, language) -> Option<Vec<Vec<TextSpan>>>`, reusing the renderer's existing run type and keeping only the foreground plus bold/italic (syntect's background is dropped); `language` resolves a fence info string, a token, an extension or a path, and an unresolved language returns `None` so the plain rendering stays. `Code` carries the runs (`with_highlight`/`with_highlighted_lines`) and keeps its mono family, language label and no-wrap contract; `ChatMessageBubble` wires the fence path (`render_block`) and the read tool-call body (`path_body`, by the path's extension) to it.
- [x] **Agent transcript H2 — a file read is an IDE excerpt, not a text blob:** `path_body` (`crates/goble-ui/src/elements/chat_message_bubble.rs:223-236`) draws the path and then the raw result string as one `Muted` row. A read must draw the file's lines with a right-aligned line-number gutter and H1's highlighting, on its own background band, collapsed to the path alone by default (A4's fold). Depends on A4 and H1 (`agent-parser.md` §11). **Done:** `read_file` is now its own registry shape (`ToolPresentation::Read`; `write_file`/`delete_file` keep `Path`, so a write/delete confirmation is never numbered) and `read_body` draws the path then, while open, an editor excerpt: a right-aligned gutter `format!("{:>w$}  ", n, w = width)` (width from the largest number), each line's content through H1's `syntax::highlight`, every line on a `ColorToken::Bg` band; truncated draws first 5 + `…` + last 3. The base line comes from the call's own data — an explicit `offset`/`start_line` argument, else a consistent `cat -n`-style gutter kept verbatim, else 1 (the harness's `read_file` declares no offset and returns raw content, so the base is 1) — and a failed/empty result, or a gutter that parses only partly, degrades to today's plain `result_rows` rather than wrong numbers (`agent-parser.md` §11)
- [x] **Agent transcript H3 — the diff bands, and the code inside them:** the band exists but is a 10% tint (`crates/goble-ui/src/elements/diff.rs:15` `TINT = 0.10`, painted at `:461` as `surface.mix(&color, TINT)`) and each changed line's text is one flat `Success`/`Error` colour (`:354-358`), where grok-build fills the content area with real insert/delete backgrounds (`diff_insert_bg`, `diff_delete_bg`) and highlights the code inside the band. Saturate our own insert/delete background tokens enough to read as green/red in this dark palette, and run each changed line through H1's highlighter so the code is coloured inside the band. Depends on H1 (`agent-parser.md` §11). **Done:** `theme.rs` gains `ColorScheme.diff_insert_bg`/`diff_delete_bg` and `ColorToken::{DiffInsertBg, DiffDeleteBg}`; dark and midnight are `#0f3d20` (green) and `#4d1116` (red), light `#d6f5e0`/`#fbdada`. `Diff` paints each changed row's full width with the token (context rows band-free) instead of the 10% surface tint, and `Diff::with_language` runs every changed line through `crate::syntax::highlight`, painting the runs inside the band; an absent or unresolved language degrades to the flat `Success`/`Error` colour. Gutters, rail, hunk headers and gap rows are unchanged. `edit_file`'s `diff_body` passes the edited `path` as the language. Tests: `changed_rows_paint_the_theme_band_at_full_width`, `changed_lines_are_highlighted_inside_the_band`, `an_unknown_language_keeps_the_flat_change_colour`, and `chat_message_bubble::tests::edit_call_paints_the_diff_band_and_highlights_the_code`
- [x] **Agent transcript H4 — the command line and its output keep their colours:** `TerminalData::for_command` (`crates/goble-ui/src/elements/terminal_block.rs:151-163`) splits the tool result on newlines into plain `TerminalLine::output` rows and nothing in `goble-ui` parses ANSI (`grep -rn "ansi\|SGR\|escape"` over the terminal-block and chat-content files finds nothing), so an agent's command output loses the colours it actually emitted — while the pane path shows them, because the real emulator draws it. Highlight the command line as shell and reproduce the output's own ANSI colours, reusing the vendored emulator in `crates/goble-terminal` if its parser takes a plain byte string, otherwise a bounded SGR parser. Depends on H1 (`agent-parser.md` §11). **Done:** `crates/goble-terminal/src/ansi.rs` runs the same vendored `vte::ansi::Processor` the screen runs through a line-oriented sink (`render_lines(&[u8]) -> Vec<ScreenLine>`), so the parser is reused, not re-implemented: printable characters keep the colour/weight/emphasis in force, CR/BS/tab are honoured, and every other sequence (cursor addressing, erases, modes, unknown finals) is consumed with no text lost. `TerminalData::for_command` highlights the command line with H1 (`syntax::highlight(command, "bash")`, falling back to plain on an unresolved/multi-line command) and parses the output through that renderer, carrying each line's `runs: Vec<TerminalRun>`; a plain line yields no runs and is still drawn as one `Text` run, and the block paints runs through `InlineText` (colour + bold + italic + underline, default colours left to the theme). `ScreenColor::is_default()` was added so the terminal defaults resolve to the block's theme colour. Tests: `terminal_block::tests::{command_line_is_highlighted_as_shell, ansi_output_keeps_its_colours, an_unrecognised_escape_sequence_loses_no_text}` (`agent-parser.md` §11)
- [x] **Agent UI follow-up R11 — the clear paths must clear the approval state (regression):** `on_clear_transcript` (`app/src/actions.rs:633-644`) clears `rt.messages`, `rt.pending_ask`, `rt.queued_prompt` but not the approval fields A5/R1 added, while `finish_turn` (`app/src/state.rs:1523-1524`) does clear `rt.pending_command`/`rt.command_selection`; `pane_chat_snapshot` reads them back and `app/src/ui/chat.rs:211` renders them, so a stale approval card and its selected candidate survive "Clear transcript". Same omission in `app/src/state.rs:1623-1625` and in `on_stop` (`app/src/actions.rs:645-654`), which only sets `busy = false`. The existing test only asserts the message list is empty, so it misses this (`agent-tui.md` §10). **Done:** all three clear paths now drop `rt.pending_command` and `rt.command_selection` alongside the rest of the pane runtime — `on_clear_transcript` and `on_stop` in `app/src/actions.rs`, and the store-less branch of `bind_active_pane_conversation` in `app/src/state.rs`. Each path has a test that fails if either field survives: the existing `clear_transcript_clears_active_pane_messages` is strengthened, plus `stop_drops_the_pending_approval` and `rebinding_without_a_store_drops_the_pending_approval`. Verified with `cargo test -p goble-app` (176 passed, 0 failed)
- [x] **Agent UI follow-up R12 — the model must be able to see that a call failed (regression R5 introduced):** R5 removed the `ERROR: ` prefix so the UI reads the status from the record, but `crates/goble-core/src/reasoning.rs:728` and `:924-925` now write the tool row as `<call_id>\n<message>`, and `build_history` (`:966-969`) puts that remainder straight into the LLM-facing `Message.content`. The structured replacement does not reach it: the assistant row's `tool_calls` is deserialized into `LlmToolCall` (`crates/goble-core/src/llm.rs:8-12`), which has `id`/`name`/`arguments` and **no status**. So the model, which used to read `ERROR: <message>`, now sees the message alone with no failure signal — the premise in `agent-parser.md` §10 that "nothing reads the result text" is false for the history builder. Either carry the outcome into the LLM-facing model or restore an explicit, machine-readable marker; do not fix it by putting the prefix back for the UI (`agent-parser.md` §11). **Done:** `llm::Message` (the LLM-facing message) carries `tool_status: Option<ToolCallStatus>`, and `reasoning::build_history` resolves each `Role::Tool` message's status from the assistant row's persisted call records (a `call_id -> status` map over the rows' `tool_calls` columns) and stamps it on the message; the persisted tool row is unchanged (`<call_id>\n<body>`, no marker). Since no provider protocol has a status slot on a tool message, the three wire builders (`into_openai_messages`, `into_ollama_messages`, the Anthropic mapping) send a failed tool message as `[tool call failed]\n<body>` and a successful one as its bare body, so the model reads the failure. Tests: `reasoning::tests::test_failed_tool_call_is_failed_in_the_llm_history` (a real failing `run_turn`, then `build_history` yields a `Role::Tool` message with `tool_call_id == tc_bad` and `tool_status == Error`), `llm::tests::{failed_tool_message_carries_a_failure_marker_on_the_wire,a_successful_tool_message_is_sent_as_its_body}`, with R5's `test_failed_tool_result_row_does_not_encode_status_as_text` still green (`agent-parser.md` §11)
- [x] **Agent UI follow-up R13 — the error notice is still a bordered card:** `build_agent_error` (`app/src/ui/chat.rs:408-413`) wraps the agent's error in `Container::with_background(Surface).with_border(ColorToken::Error).with_corner_radius(...)`, painted inline in the agent transcript through `build_agent_chat`'s `with_notice` (`app/src/ui/chat.rs:160` → `crates/goble-ui/src/views/chat_view.rs:216`, drawn at `:694-695`). R9 fixed four surfaces and this is the fifth; the rule has no exception for it (`agent-tui.md` §10). **Done:** `build_agent_error` drops `with_border(ColorToken::Error)` and `with_corner_radius(...)`; the notice is now a full-width `SurfaceRaised` band of rows with padding only — the pager idiom A5's proposal and R9's four bands use. The absence is asserted, not claimed: `ui::chat::error_notice_tests::the_error_notice_is_a_band_not_a_bordered_card` renders the builder headlessly and checks `stroke_rect == 0` and that its band fill carries `corner_radius == 0.0`. After the change `grep -rn 'with_border\|Border::all'` over the agent-surface files (`app/src/ui/chat.rs`, `views/chat_view.rs`, `elements/{chat_message_bubble,chat_composer,chat_content,conversation_card,ask_user,group_chat_message}.rs`) finds no hit; the sole remaining one is the sanctioned terminal block (`elements/terminal_block.rs:672`). Verified with `cargo test -p goble-ui -p goble-app` (goble-app lib 177 passed, 0 failed; goble-ui lib 277 passed, 0 failed; every integration suite ok)
- [x] **Agent UI follow-up R14 — one command, one rendering:** the persisted tool-result path builds its block with `TerminalData::new` + `TerminalLine::output` (`app/src/state.rs:278-301`), while the assistant row's tool call and the pane both use `TerminalData::for_command` (`crates/goble-ui/src/elements/chat_message_bubble.rs:382`, `app/src/ui/terminal.rs:150`). So H4's shell highlighting and ANSI-coloured output reach two of the three paths and not the third, and the same command can appear highlighted and flat in one transcript (`agent-parser.md` §11). **Done:** the result row's command is resolved from the call record on the assistant row's persisted `tool_calls` column (the `command` argument of a command-shaped call, found and parsed by the same `tool_presentation_for`/`arguments` the renderer uses) and `tool_terminal_data` now builds the block through `TerminalData::for_command`, so the command line is shell-highlighted and the output keeps its ANSI colours; a non-command result (or one whose call has no command) has no command line to invent and keeps the plain block. Test `state::pane_session_tests::a_persisted_tool_result_builds_the_same_block_as_the_tool_call` asserts the persisted block equals `TerminalData::for_command(command, output, status)` — the block the tool-call path's `command_body` builds — and that its command line carries shell runs and its output an ANSI colour.
- [x] **Agent UI follow-up R15 — the agent surface is pill-free, locked by tests:** the agent's transcript draws no pill anywhere — not for the agent itself and not for a tool call, which is the row grok-build draws. `crates/goble-ui/src/test_util.rs` owns the predicate (`is_pill`, `pills`, `pill_rects`, `assert_pill_free`, `assert_no_row_border`, `bordered_control`): a rounded fill that wraps a row, and a border with no control under it, are pills; two shapes pass by design — a **bordered control** (a border and a fill over the same rect: the shared terminal command block's card, or a button such as the ask card's option) and a **text highlight** (a rounded fill no taller than the line of text it backs: an inline code span). `chat_message_bubble::tests::pill` sweeps prose and markdown in both roles, a reasoning step collapsed and expanded, every tool family in every fold and in `Pending`/`Running`/`Error`, every sub-agent status, the ask card and the still-running footer; `the_lock_catches_a_row_wrapped_in_a_card` proves the lock is not vacuous (the same row wrapped in a rounded card is reported, unwrapped is not); `views::chat_view::tests::the_agent_transcript_paints_no_pill` paints the whole pane in one frame with a call in flight, an interrupted ask, a command proposal, a queued prompt and the footer; and `ui::chat::agent::agent_pill_tests::the_agent_pane_paints_no_pill_around_its_rows` renders the `RootView` over a real store and asserts no pill covers any row's own run, excluding only the pane's square split frame (window chrome), whose squareness it also asserts. No pixels changed: the pixels were already pill-free. Verified with `cargo test -p goble-core -p goble-ui -p goble-app` (`goble-ui` lib 345 passed, `goble-app` lib 215 passed, `goble-core` lib 259 passed, every integration suite ok) (`agent-parser.md` §12)
- [x] **Agent parser H5 — the markdown boxes are boxes:** a fenced code block and a blockquote are wrapped in a rounded `SurfaceRaised` container (`crates/goble-ui/src/elements/chat_message_bubble.rs:991-1007`, and in the group view `crates/goble-ui/src/elements/group_chat_message.rs:234-243`, `:326-330`, `:404-413`). They carry no border, but the rule names "bordered card or box"; grok-build draws both as full-width bands instead. Convert them to bands so the agent's markdown is rows, not rounded panels (`agent-tui.md` §10). **Done:** `ChatBlock::CodeBlock` is a full-width `SurfaceRaised` band (the code element stretched in a cross-axis-stretched column) and `ChatBlock::BlockQuote` is a new `QuoteRail` element — a muted 2 px rail spanning the content's height with the content indented past it; the group view's `render_code_block` drops its corner radius and `render_block_quote` uses the same `QuoteRail` (`QuoteRail` is `pub(crate)` in `chat_message_bubble.rs`). The fence's language label and the H1 highlight are untouched. Tests: bubble `fenced_code_block_is_a_band_not_a_rounded_box` / `block_quote_is_an_indent_and_a_rail`, group `code_block_draws_a_band_not_a_rounded_box` / `block_quote_draws_an_indent_and_a_rail` — each asserts no rounded `SurfaceRaised` fill and the flat band / square muted rail, plus the surviving fence label and the highlighted fence body. Inline code deliberately keeps its small rounded chip: it matches the bubble's `resolve_span` idiom (a 3 px rounded chip), not a markdown box.
- [x] **Agent UI chrome C1 — drain live agent activity into app state:** `RootView::drain_events` (`app/src/root_view.rs:120-308`) matches a fixed list of names, so the `agent:log` / `agent:started` / `agent:finished` / `agent:state_update` / `agent:tool_result` events the desktop service already emits (`crates/goble-desktop-service/src/state.rs:1553`, `:1584`, `:1613`, `:1623`, `:1705`) are dropped, and nothing exposes what is running as live state (`pane_in_flight_tool_count` is documented test-only, `app/src/state.rs:1549`). Handle those names and add one accessor the chrome reads: the running executions, the pane's in-flight tool calls and busy flag, a pending approval or question, and a turn start time when one can really be observed. This item also delivers the `07 - Observability` drain row below (`agent-tui.md` §11). **Done:** `drain_events` now handles all five `agent:*` names; the one accessor is `UiState::live_work()` returning `LiveWork { executions: Vec<LiveExecution>, tool_calls, turn_busy, pending_approval, pending_question, turn_started_at }`. A worker run is tracked from `agent:started` (which now carries the service's own `started_at` on the event) until `agent:finished`; `agent:state_update` stores the run's `RuntimeState` and `agent:tool_result`/`agent:log` its last activity, so none is dropped. `turn_started_at` is recorded by the new `UiState::begin_turn` where the app issues a turn (it observes its own start) and cleared by `finish_turn`; a turn whose start was not observed reports `None` rather than a guessed time. `pane_in_flight_tool_count` is **deleted** in favour of `live_work()`. Verified: `cargo test -p goble-app` — lib `181 passed; 0 failed` (3 new), every integration suite ok, doc-tests 0; the acceptance test `root_view::tests::drain_events_leaves_the_live_accessor_reporting_what_is_in_flight` scripts `chat:tool`, `chat:turn_finished`, `agent:started`×2, `agent:finished`, `agent:state_update`, `agent:tool_result` and `executions:updated` and asserts the accessor reports exactly one in-flight tool and one running execution and nothing else. `cargo test -p goble-desktop-service` → 34 passed.
- [x] **Agent UI chrome C2 — the live turn-status footer:** no footer exists anywhere; the chat column ends at the divider + composer (`crates/goble-ui/src/views/chat_view.rs:1058-1059`) and `RunningIndicator` is dead code that paints nothing (`crates/goble-ui/src/elements/running_indicator.rs:47-49`). Add a one-row band above the composer drawn in the pager idiom: while busy the activity (a running tool / waiting on approval / waiting on a question / thinking) plus the elapsed time, while idle with work in flight a `{n} {kind} still running` line naming each kind as one hit target, and zero height when nothing is in flight; revive or delete `RunningIndicator`. Depends on C1 (`agent-tui.md` §11). **Done:** the new `TurnStatusFooter` element (`crates/goble-ui/src/elements/turn_status.rs`, `TurnStatus`/`TurnActivity`/`WorkKind`) draws the three states in the pager idiom (borderless full-width row, a `◆` bullet, zero height when nothing is in flight) and is placed in `ChatView` directly above the composer's divider via `with_turn_status`/`with_on_open_work` (added only while in flight, so an idle pane costs neither height nor column spacing). `RunningIndicator` is revived: its `paint` now draws `◐ ◓ ◑ ◒` frames, phase driven by the real observed turn start the app passes (the element layer has no clock or frame counter), and no token count is drawn because no live source carries one. The busy activity is resolved in `app/src/state.rs::pane_turn_status` from the pane's own live runtime (pending approval → `Waiting on approval…`, pending question → `Waiting on a question…`, an in-flight call → its command/name, else `Thinking…`/`Responding…`) and the still-running line counts `WorkKind::{Command, Execution}` for the pane's in-flight tools plus `live_executions` (C1's `live_work` data; `pane_in_flight_tool_count` no longer exists). The cue fires `UiActions::on_open_work` → `UiState::open_work_overlay`, which today only flips `work_overlay_open`: the overlay is S7, so the action has no visible surface yet. The composer keeps the only Stop control (a busy footer is not a hit target). Verified with `cargo test -p goble-ui -p goble-app` (`agent-tui.md` §11)
- [x] **GUI polish R1 — Chat geometry:** the transcript's rows are full-width bands now, not pills. The agent's own reply (`ChatMessageBubble`, `crates/goble-ui/src/elements/chat_message_bubble.rs`) paints no box at all — no background fill, no corner radius, no border — and the width cap is gone (`BUBBLE_MAX_WIDTH_RATIO` deleted, the `ConstrainedBox` around the bubble and the role-based `MainAxisAlignment` row removed, `rebuild` losing its `max_width`), so the reply is laid out at the transcript's own width: the pane's. The user's own message is the one surface — a `SurfaceRaised` band with square corners (`with_corner_radius` gone) — and it spans the pane too. Both keep the transcript's padding at both edges (`EdgeInsets::uniform(spacing_px(SpacingToken::Md))`: 12 px at the default density, 10.2–13.8 px across `Density`), so no line of text touches the pane's border. Everything else is untouched: tool-call folds, reasoning folds, code/diff/terminal blocks, sub-agent rows, the streaming caret, every click target and the markdown rendering; `ChatRole::Tool` (the pane's inline executed-command block) loses only the same redundant bubble fill, because `terminal_block` carries its own band and border. Tests: `elements::chat_message_bubble::tests::transcript_rows_are_full_width_bands_not_pills` (each row measures the pane's full width; the reply paints zero fills and zero strokes; the user's message paints exactly one square band spanning `0..width`; both inset their text by 10–15 px), `views::chat_view::tests::the_transcript_rows_span_the_pane_with_ten_to_fifteen_px_of_padding` (a real `ChatView` transcript: no strokes, one square band across the pane, both rows sharing the pane's left edge inside the padding) and `ui::chat::chat_geometry_tests::the_panes_transcript_paints_full_width_rows` (the app's own pane surface over a real in-memory `DesktopState`: the user's band runs from `SIDEBAR_WIDTH` to the window's right edge with square corners, and the reply's text starts at the same x). Each of the three was confirmed red with the old cap and alignment temporarily restored, then green without. No existing test encoded the old geometry — all 296 `goble-ui` and 199 `goble-app` lib tests passed unchanged, so none was rewritten or weakened. Verified with `cargo test -p goble-ui -p goble-app` → exit 0 (`goble_ui` lib `298 passed; 0 failed`, `goble_app` lib `200 passed; 0 failed`, every integration suite ok, doc-tests 0) (`06-renderer/agent-tui.md` §11, `06-renderer/ui-spec/05-chat-view.md`)
- [x] **GUI polish R2 — the topbar tabs:** the workspace/space tabs (`build_workspace_strip` / `build_workspace_chip` plus the `WorkspaceChip` element in `app/src/ui/shell.rs`) were rounded chips inset in the bar; they now read as browser-style tabs. Each tab's own `layout` returns `TOPBAR_HEIGHT` and its `paint` fills that whole rect with `fill_rect` — square corners, no stroke — so a tab runs the full height of the topbar. Adjacent tabs are separated by exactly one `Divider::vertical` child per boundary (`build_workspace_strip` inserts it before every tab after the first, and the row's spacing is `0.0`), so N tabs draw N-1 rules: one line per boundary and no gap that would read as two. The active tab stays distinct with the existing `SurfaceRaised` fill and `Text` label colour; inactive tabs keep no idle background and take a `Hover` fill only under the pointer. The bar itself no longer pads vertically: the strip is pinned to `TOPBAR_HEIGHT` (`ConstrainedBox::with_height` around a `CrossAxisAlignment::Stretch` row), so the shorter controls center against the tabs exactly where they were — the +/▾ control and the Settings icon keep their place and size (asserted at the bar's mid-height). The label still swaps for the inline rename field while `space_rename_editing` is set (now centred in the same full height via `build_rename_tab`, so the bar's height is unchanged while editing), and the trailing "x" still closes that space. Tests: `ui::shell::tests::{a_workspace_tab_fills_the_bar_with_square_corners, only_the_active_tab_is_filled, a_click_on_the_tab_selects_its_space, a_click_on_the_close_control_closes_its_space}` and `ui_render::{workspace_tabs_fill_the_toolbar_and_draw_one_rule_per_boundary, the_active_workspace_tab_becomes_the_rename_field}`. Verified with `cargo test -p goble-ui -p goble-app` → exit 0 (`goble_app` lib `199 passed; 0 failed`, `ui_render` 14 passed incl. both R2 tests, `goble_ui` lib `296 passed; 0 failed`; re-verified 2026-09-12) (`06-renderer/agent-tui.md` §11)
- [x] **GUI polish R5 — Shell agent surface:** two halves, both about the shell/terminal space. **(1) The shared input bar.** The terminal pane had no input bar at all — only the PTY grid — so `app/src/ui/terminal.rs` now pins the chat surface's own composer element to the bottom of the shell pane. `chat::build_terminal_composer` (`app/src/ui/chat.rs`) builds it from the very `ChatComposer` the chat surface (`ChatView`) mounts, wired to the pane's own state: the value is this pane's draft, focus is `active && state.composer_focused`, the model label is this pane's `PaneControls::model`, the path label is this pane's session path, `Stop` shows while this pane's turn is busy, and the change/focus/send/cmd-enter/slash/attach/select-model/stop/profile callbacks, both footer menus and the harness/dir/branch pills all route to this pane's own controls (`state.composer_context.get(&pane_id)`). Enter sends the draft as this pane's agent turn and opens the harness on it; Cmd/Ctrl+Enter starts a new conversation there — the same gestures the chat composer carries. **One control is deliberately omitted: A5's command-proposal card.** A suspended command only exists while the harness owns a pane's input — Esc at a proposal is the proposal's Reject, not the way out — and a pane whose harness is open draws the shared agent view (which contains that card) instead of this bar, so a card here could never be live and would be a dead button. The deleted bespoke editor also treated a `!`-prefixed draft as a shell command; the shared bar's Enter is the agent turn and does not, because the pane's grid directly above the bar is where a shell command is typed and runs (and the app exposes no action that writes a command to a pane's pty from a UI callback). `TerminalView` gained `bar`/`composer_focused`, forwards MouseDown/MouseUp/MouseMove (and KeyDown while the bar holds the keyboard) to its rebuilt subtree before treating the event as the grid's, and the bespoke `❯` input line plus its editor (`handle_harness_key`) are deleted. **(2) One agent view.** The pty-only degraded view (`TerminalView::build_agent_view`, a bare list of `terminal_block`s) is deleted; `terminal::build_terminal` returns `chat::build_agent_chat(...)` for `BlockView::Agent` — the single function both a `PaneKind::Chat` leaf (`app/src/ui/panes.rs::build_leaf`) and an open shell harness call — with its Escape hook wired to close the harness (`on_set_pane_harness_mode(pane_id, false)` plus unfocusing the bar), and `chat::build_agent_chat` threads that hook into `ChatView::with_on_escape` (a chat pane passes `None`), so the shell's harness is the same agent view element Space 1 mounts, not a second copy. Tests (both over a real `RootView` on a single terminal space with a detached session, so the pane's own events, the app's real actions and the per-frame rebuild are what they drive): `ui::terminal::tests::the_shells_bottom_bar_is_the_shared_composer` (the shell pane draws the `ChatComposer` placeholder the chat pane draws, and the bespoke `❯` line is gone) and `ui::terminal::tests::the_shells_bar_sends_an_agent_turn_and_opens_the_shared_agent_view` (the focused bar takes typing and edits the pane's own draft; Enter opens the harness on the pane's bound conversation and the turn is drawn in the shared transcript with the shared composer still at the bottom; Esc returns the pane to the shell with the conversation's card left in its block list; the grid takes the keys back and the bar is not edited; a click on the card reopens the agent view). Two tests were **rewritten, not deleted**, because their premises were the two things R5 removes: `harness_mode_owns_the_keys_and_esc_returns_to_the_shell` and `cmd_enter_esc_and_a_card_click_round_trip_the_agent_view` are replaced by the two tests above, and `the_agent_view_never_draws_a_running_block_as_success` becomes `a_command_block_is_drawn_from_its_lifecycle_not_a_guess`, which keeps every one of its assertions (Executing/Background → Running, DoneWithExecution 0 → Success, non-zero → Error, and the drawn title colour for each) on the surviving `block_status` → `terminal_block` path. No assertion was deleted or narrowed and the lib test count is unchanged (`goble-app` 200). Verified with `cargo test -p goble-ui -p goble-app` → exit 0 (`goble_app` lib `200 passed; 0 failed`, `goble_ui` lib `298 passed; 0 failed`, every integration suite ok, doc-tests 0) (`06-renderer/agent-tui.md` §11)
- [x] **Agent UI chrome C3 — the topbar live indicator:** the app's `build_topbar` (`app/src/ui/shell.rs:56-123`) is a two-child `SpaceBetween` row with no slot for status. Add a live indicator between the workspace strip and the settings button - a spinner plus `* N` from C1's state when work is in flight, inert at zero - without changing `TOPBAR_HEIGHT` or the workspace controls, and without switching the app to the unused `goble-ui::Topbar`. Depends on C1 (`agent-tui.md` §11). **Done:** `build_topbar` gains a third child between `left` (the workspace controls) and the settings icon — `build_live_indicator` (`app/src/ui/shell.rs`) draws `RunningIndicator` + `◆ N` where N is C1's `LiveWork::work_count()` = the active pane's in-flight tool calls plus every running execution; it is a `LiveWorkCue` hover/click hit target whose click fires `UiActions::on_open_work` → `UiState::open_work_overlay`. At zero it is a zero-size `Empty` that draws nothing and cannot be clicked, so the bar keeps its current look. `UiSnapshot` gains `live_work_count`/`live_work_phase`, read once from `live_work()` in `RootView::rebuild`; `TOPBAR_HEIGHT` (36/40) and the workspace chips and `+ ▾` control are untouched, and the app keeps its own topbar. No context/token indicator is drawn: no live event carries a token count. The work overlay is S7, so the click currently fires an action with **no visible surface yet** — stated in code and here. Verified by `cargo test -p goble-app` (`ui::shell::tests::{the_cue_shows_the_live_count_and_a_spinner,the_cue_is_inert_at_zero,the_cue_click_fires_the_work_overlay_action,the_topbar_height_is_unchanged}`, `root_view::tests::the_topbar_cue_reads_the_live_count_and_opens_the_work_overlay`)
- [ ] Replace or re-license the 42 icon assets in `crates/goble-ui/assets/icons/` that are byte-identical to files in the AGPL side of `~/Projects/warp-new` (`terminal-emulator.md`)
- [ ] Fix the `POST /pair` hash check (it stores any hash and always answers `paired: true`) and set the pairing hash at provision time (`remote-terminal-renderer.md`)
- [ ] Set `GOBLIN_TLS_BUNDLE` in the Helm chart and `install-goblin.sh` — the chart mounts the bundle but never points the worker at it, so deployed workers start plaintext (`remote-terminal-renderer.md`)
- [ ] Define the remote terminal session + stream protocol (metadata, frames, input, resize) (`remote-terminal-renderer.md`)
- [ ] Render the **session chip** (host badge) above the composer, per warp-new (`remote-terminal-renderer.md`)
- [ ] Render remote PTY output in the local chat (`remote-terminal-renderer.md`)
- [ ] Add a `RemoteTransport`-style trait + bounded reconnect with backoff; keep mTLS over WebSocket rather than switching to SSH (`remote-terminal-renderer.md`)
- [ ] (Later) optional SSH transport backend and optional renderer-on-remote frame streaming (`remote-terminal-renderer.md`)
- [~] Add overlay + form primitives to `goble-ui` — `Dialog` (backdrop + centered panel) in; `FormField`/`SecretField`/`Toggle` pending (`form-components.md`)
- [ ] Port the provider/API-endpoint connector logic from grok-build model config (`form-components.md`)
- [~] Build the provider-settings overlay form (`app/src/ui/model_form`) done; remote-bootstrap composer pending (`form-components.md`)
- [ ] Let skills/plugins request structured input through the overlay (`form-components.md`)
- [x] **Agent UI follow-up R3 — remove the sidebar footers:** the left conversation sidebar's two bottom rows are gone — the `Plugins` label with its `plus` `TopbarButton` (which opened the MCP connectors panel) and the `Screen` label with its `monitor` `TopbarButton` (which opened the screen sheet), plus the trailing `Divider` that opened the footer block. `app/src/ui/sidebar.rs` dropped the `TopbarButton`/`AiActions`/`ScreenActions` imports and `build_sidebar`'s `ai_actions`/`screen_actions` parameters (its one caller, `build_ui`, was updated); the search field, New conversation row, conversation list, `View all`/`Show less` control and the `Scrollable` list state are unchanged. Both panels stay reachable from the `Cmd+K` palette (`Open connectors`, `Open screen`) — those entries are untouched. No test asserted either row, so none was removed. Verified with `cargo test -p goble-app` (lib `195 passed; 0 failed`) (`06-renderer/ui-spec/04-conversation-sidebar.md`; that doc's "Plugins footer" section and `09-testing-checklist.md`'s "Plugins footer" visual line still describe the removed row and are stale)

## 07 — Observability
- [x] Drain `executions:updated`/`agent:*` events into app state - **delivered by the chrome item C1** (`agent-tui.md` §11), which is what marks this row: `drain_events` handles the five `agent:*` names into `UiState`'s live execution records and `executions:updated` still refreshes the pages; the accessor is `UiState::live_work()` (`executions-and-trace.md`)
- [ ] Add an executions list + per-execution trace view in the renderer (`executions-and-trace.md`)
- [ ] Add an **agent-level** observability view (live status, sub-agent tree, trace) (`07-observability/README.md`)
- [ ] Make local harness runs feed the same trace path as remote (`executions-and-trace.md`)
- [ ] Add a logs view (filter by execution/worker/level) (`logs.md`)
- [ ] Route the `agent:log` events into app state (`logs.md`)
- [ ] Run all log lines through the secret sanitizer before display/emit (`logs.md`)
- [~] Build our own crash reporting with local-always capture and consent-gated upload — `crates/goble-telemetry` is written and tested (config, redaction, breadcrumbs, fingerprint, durable queue, upload worker, panic hook); no collector is deployed and the endpoint is `https://telemetry.goble.dev` (`crash-reporting-and-updates.md`)
- [x] Honour `DO_NOT_TRACK` — precedence is `DO_NOT_TRACK` > `GOBLE_TELEMETRY` > the stored user choice > the channel default; the reference implementation has no `DO_NOT_TRACK` support at all (`crash-reporting-and-updates.md`)
- [~] Stand up the collector — `crates/goble-telemetry-server` serves `/health`, `POST /v1/crashes`, `POST /v1/events`, `GET /v1/summary` with an optional shared token and a bounded body; it has never been deployed (`crash-reporting-and-updates.md`)
- [~] Deliver updates from a signed manifest — `crates/goble-update` validates the manifest, verifies an ed25519 signature over a fixed text payload, checks the sha256 while downloading, and produces pure install plans; no release signing key exists yet (`crash-reporting-and-updates.md`)
- [ ] Surface an available update in the UI and wire `plan_install().apply()` + the relaunch handoff (`crash-reporting-and-updates.md`)
- [ ] Add a first-run telemetry notice (`TelemetryState.notice_shown`) and a settings toggle writing `~/.goble/telemetry.json` (`crash-reporting-and-updates.md`)
- [ ] Add minidump capture on Linux and Windows — a Rust panic hook catches panics only (`crash-reporting-and-updates.md`)
- [ ] Split debug info + upload symbols per release so CI backtraces resolve (`crash-reporting-and-updates.md`)
- [~] Ship installers: macOS DMG with a custom background image, Linux AppImage + `.deb` + tarball, Windows per-user Inno Setup with an EULA page — scripts exist and are shell/YAML-checked, but none has been executed (`crash-reporting-and-updates.md`)
- [ ] Generate the release signing key pair and make `--manifest` sign in CI (`crash-reporting-and-updates.md`)
- [x] Make official builds signed instead of signed-if-convenient — `GOBLE_REQUIRE_SIGNING=1` turns a missing credential into a build failure, `scripts/release.sh` sets it for every channel except `dev`, and `--allow-unsigned` is the explicit local escape hatch (`crash-reporting-and-updates.md`)
- [x] Complete the macOS signing path — hardened runtime with a deliberately empty `packaging/macos/entitlements.plist` that says why, no `--deep`, `codesign --verify --strict` after signing, and `spctl --assess` on the DMG and the app after notarisation, which is the check that answers whether Gatekeeper accepts the download (`crash-reporting-and-updates.md`)
- [x] Sign the Windows application executable, not just the installer — the exe is signed from a staged copy before packaging (never `target/release/` in place) and both it and the setup exe are checked with `signtool verify /pa` (`crash-reporting-and-updates.md`)
- [ ] **Obtain the certificates** — Apple Developer Program + Developer ID Application (99 USD/year), and a Windows OV/EV code-signing certificate (~200–500 USD/year) — and add them as repository secrets; no release can be produced until then by design (`crash-reporting-and-updates.md`)
- [ ] Run one release with the real certificates and confirm the notarisation round trip, `spctl`, and `signtool verify` on a real artifact (`crash-reporting-and-updates.md`)
- [ ] Extend packaging to RPM, Flatpak, Homebrew cask and winget, and add arm64 Linux/Windows runners (`crash-reporting-and-updates.md`)
- [x] Set a Windows version floor in the installer — `MinVersion=10.0.18362` in `packaging/windows/goble.iss`, because portable-pty is ConPTY on Windows and Inno's default would install on Windows 7, where the terminal cannot start at all (`crash-reporting-and-updates.md`)
- [ ] Generate `THIRD-PARTY-NOTICES.md` from the dependency graph (`cargo about` + template, as the reference does) instead of maintaining the table by hand (`crash-reporting-and-updates.md`)
- [ ] Decide whether to ship our own ConPTY (`conpty.dll` + `OpenConsole.exe`) so Windows 10 1809 is supported (`crash-reporting-and-updates.md`)

## 08 — Threads (deferred)
- [ ] Define the thread-messaging server hosted on the worker (participants = humans + agents) (`../08-threads-deferred/README.md`)
- [ ] Reuse `goble-core::thread` types + existing thread store (`../08-threads-deferred/README.md`)
- [ ] Add the threads UI to the native shell (`../08-threads-deferred/README.md`)

## 09 — Mobile (deferred)
- [ ] Wire the local machine as a Tailscale-exposed worker (self-as-worker) (`../09-mobile-deferred/README.md`)
- [ ] Confirm client/workspace + harness event contracts are mobile-friendly (`../09-mobile-deferred/README.md`)
- [ ] Scaffold the Kotlin client against the same workspace/routing contract (`../09-mobile-deferred/README.md`)

## 10 — Platform & performance
- [ ] Confirm all three wgpu backends + DPI resize are covered (`wgpu-renderer-platform.md`)
- [ ] Profile the atlas construction (text/icon) path (`wgpu-renderer-platform.md`)
- [ ] Add a frame-time debug overlay + histogram (`performance.md`)
- [ ] Virtualize long message lists / transcripts (`performance.md`)
- [ ] Move streaming decode off the main thread; confirm frame budget holds (`performance.md`)
