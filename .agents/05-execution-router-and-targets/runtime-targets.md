# 05 — Runtime targets

**Status:** `[~]` partial (local unsupported, self-as-worker absent)
**Owns:** the enumerable set of places the harness can run
**Depends on:** [`README.md`](README.md)

## The targets

```rust
enum RuntimeTarget {
    Local,                             // in-process / local worker
    Worker { worker_id: WorkerId },    // remote goblin worker
    SelfAsWorker,                      // this machine as a worker (Tailscale) for mobile
}
```

| Target | Where the harness runs | Connection | Renderer |
| --- | --- | --- | --- |
| `Local` | this machine, in-app or local worker | none / loopback | in-process |
| `Worker { .. }` | a remote goblin worker | WebSocket over mTLS | streamed events (remote-terminal-renderer) |
| `SelfAsWorker` | this machine, exposed via Tailscale | Tailscale | served to a mobile client |

## Addressability

- Targets are resolved from a **workspace** (see [`../03-workspace-model/workspace.md`](../03-workspace-model/workspace.md)): a workspace's `kind` maps to a target.
- A `Worker` target needs a **worker id** and mTLS bundle (existing `WorkerConfig` + `worker_bundle`).

## Selection rules

- If the workspace is `Local` and the machine has the harness → `Local`.
- If the workspace is `Remote` → `Worker { .. }` (resolved via `WorkerPool`, round-robin or tag-first).
- The **local machine as worker** is not a normal path for the desktop UI; it is the future mobile path (self-as-worker via Tailscale), deferred to [`../09-mobile-deferred/README.md`](../09-mobile-deferred/README.md).

## A `Worker` target is a viewer session, not a terminal

What a remote workspace *is*, in the client: a conversation whose shell **does not exist locally**. Warp-new calls this "cloud agent" and gives it its own tab rather than making it a toggle on a terminal pane — a pane with no local shell, `TerminalManager::new_deferred` plus a connect, and the transcript fed entirely by the worker's streamed events. Our equivalent needs no new transport (`WorkerClient::connect` + `handle_worker_message` already stream), but it does need the session shape to be honest about it:

- Nothing local backs the pane. No local PTY, no local CWD, no local shell to fall back to; if the connection drops the pane shows that, it does not become a shell.
- The **run outlives the client**. The worker drives its own daemon (`goblin-worker/src/runner.rs`: it registers the harness, waits for `TraceFinished`, answers `AskUser`), so closing the laptop or putting it to sleep does not stop a turn. On reconnect the client re-attaches and replays what it missed rather than starting over — the machinery for this is already in the daemon protocol (`Snapshot`, `Replay`, `Checkpoints`, `Rewind`, `Fork`).
- **The environment and the model are chosen at submit time**, in the composer, next to the send control: the environment is a `MediumKind` ([`../04-agent-runtime/computer-use.md`](../04-agent-runtime/computer-use.md)), the model is a passthrough of the local choice. Warp-new remembers the last pair per session; the choice is per conversation, not global.

## Tasks

- [ ] Add `Local` and `SelfAsWorker` to the runtime-target set (and remove the `local` bail).
- [ ] Represent each target's connection params (local handle / mTLS address / tailscale addr).
- [ ] Map a workspace `kind` → the default target, but allow per-conversation override.
- [x] Build the `Worker` pane as a viewer session with no local shell, so a dropped connection reports itself instead of degrading into a local terminal — `PaneKind::Worker` + `UiState::adopt_routing` (`app/src/state/ui_state/worker.rs`, resolving the routing through `resolve_worker_for_target("any", …)` and opening `PaneAttach::Connecting` with the pane's shell dropped), built by `app/src/ui/worker.rs` and never through `terminal::build_terminal`; `worker:status` moves `Connecting → Attached` and `Attached → Detached`, and a viewer pane never becomes a terminal (`run_terminal_command` / `open_pane_harness` refuse it). Verified `cargo test -p goble-app` → exit 0, lib 447 passed / 0 failed with the 4 new cases in `app/src/root_view/tests/worker_pane.rs` (see [`RESOLVER.md`](../RESOLVER.md)). Re-attach after a drop is the next task below, not this one.
- [x] **Re-attach to a running session instead of starting over (A2).** The client asks for the session's own state and replays what it missed instead of starting a turn or drawing an empty transcript: `DesktopState::attach_chat_session` (`crates/goble-desktop-service/src/state/attach.rs`) reads the daemon's snapshot (is a turn on it now?), its checkpoints (the recorded turns) and its `Replay`, and answers with the session's own id, the span replayed, the turns it has, and the session's own transcript — folded from the recorded turns and the events they produced, never from this machine's rows about the conversation. A conversation with no session comes back as `SessionAttach::NoSession`, and nothing is started. The app re-attaches on the open (`UiState::adopt_routing` → `attach_worker_session`) and on a serving `worker:status` report after a drop (`UiState::worker_status_serving`), and the two rules hold there: once a pane has re-attached, its transcript is re-resolved from the session's own rows (`PaneRuntime::session_rows` in `refresh_pane`, so the store is not consulted for it), and a conversation with no session records `WorkerPaneSession::NoSession`, which the pane's own line says ("… · no session to rejoin"). Verified `cargo test -p goble-desktop-service -p goble-app` → exit 0 (see [`RESOLVER.md`](../RESOLVER.md)).
- [x] **Carry the environment (medium) in the composer for a `Worker` conversation, chosen at submit time and remembered per conversation (A3, environment half).** The rich input's context row leads with an environment pill (`crates/goble-ui/src/elements/chat_composer/composer.rs::context_children`; glyph `computer` → `agentmode`, the same `PopupMenu` every other pill opens, and its host's rows are the mediums the app has), drawn only for a host that names an environment — and the host that names one is exactly the conversation with an environment to choose: `RootView::rebuild` fills `ComposerContext::harness_label/ids/items` from `UiState::pane_environment(pane_id)` (`app/src/state/ui_state/worker.rs`) for a viewer pane and leaves every other pane label-less, so a local conversation draws no environment control at all. The choice is the conversation's: `UiState::adopt_environment` reads `chats.medium_id` (`DesktopState::get_chat_medium`, the `workspace_routing` per-conversation idiom) and falls back to the medium its routing implies (`media::medium_for_routing` → `remote-xrdp`), and the selection action persists the new one (`DesktopState::set_chat_medium`), so two conversations never share an environment. The submit carries it: `send_agent_prompt` (`app/src/actions/prompt.rs`) reads `pane_environment` for the turn's medium and scopes the project to it, and `DaemonModel::run_turn`'s remote route puts it on the `HarnessTurn` — the medium the daemon runs the turn on and streams (`TraceStarted { medium_id }`, `ExecutionRecord::medium_id`). Verified: `cargo test -p goble-ui -p goble-app -p goble-desktop-service` → **exit 0** with the three new cases in `app/src/root_view/tests/environment.rs` (a remote conversation draws the control and a local one beside it draws none; the choice is written on the conversation, redrawn from a fresh client and not written on a sibling conversation; the submitted turn's environment is the chosen one, and a remote submit with no transport fails loudly instead of running locally) — see [`RESOLVER.md`](../RESOLVER.md).
- [~] **Carry the model in the composer for a `Worker` conversation (A3, model half — blocked on the worker path).** A per-turn model chosen in the client has no path to the worker: `HarnessTurn` (`crates/goble-harness-types/src/lib.rs`) has no model field, so `build_remote_turn` (`app/src/daemon/state.rs`) can carry none; on the far side the worker builds its own harness with `WorkerState::config.llm_model` (env `LLM_MODEL`) in `goblin-worker/src/runner.rs::prepare_run` and its provider from `llm_factory.rs::default_provider_factory` (the worker's own secrets and `LLM_PROVIDER`), so the model is resolved there, not per turn. What is missing is a model on the turn request (protocol + daemon + per-turn provider/harness selection) — a protocol change, not a UI one — so no model control is drawn for a `Worker` conversation rather than shipping a field the worker would ignore.

- [x] **A viewer pane that is gone leaves no session behind.** Nothing but `UiState::close_worker_pane` reclaimed a `pane_workers` entry (`app/src/state/ui_state/worker.rs`), reached only from `adopt_routing`'s local branch, while both pane teardown sites — the pane close in `app/src/actions/make_actions.rs` and `close_space_at` in `app/src/actions/pane_ops.rs` (closing a tab / Close Others) — removed `pane_sessions`, `pane_runtime`, `pane_hover`, `sub_agent_views` and the pty and left the worker session in place, so `worker_status_serving` and `worker_connection_dropped` kept re-attaching and re-detaching pane ids whose panes and tabs were gone, on every worker status event. **Resolved:** the cleanup is one rule — `UiState::forget_pane` (`app/src/state/ui_state/panes.rs`: session, runtime, hover flag, child view, viewer session, terminal+pty) — and both teardown sites call it instead of repeating the list, so a pane that is destroyed while its session is still attached leaves nothing behind for the worker's status path to find (`worker_panes` reads `pane_workers` alone). Proven by `app/src/root_view/tests/worker_pane.rs::closing_a_tab_holding_a_viewer_pane_leaves_no_session_behind` and `::closing_a_viewer_pane_leaves_no_session_behind`: each opens viewer panes on a paired worker beside a live one, closes the tab / the pane, asserts no `pane_workers` entry, no runtime, no drawn pane for the closed id and that the `worker:status` reports which follow (over the app's event bus) move the live pane and touch no dead id — both red with the `pane_workers` removal taken out of `forget_pane` (`the closed tab's viewer pane kept no session: [1, 2]`, and `and touched no dead pane id: [2, 1]` once the map assertions are lifted). `cargo test -p goble-app` → exit 0, lib 459 passed / 0 failed (see [`RESOLVER.md`](../RESOLVER.md)).
