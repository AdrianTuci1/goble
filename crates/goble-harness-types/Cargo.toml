[package]
name = "goble-harness-types"
version = "0.1.0"
edition = "2021"
description = "Model-facing types for a harness turn: ids, grants, interaction hint, schedule."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
